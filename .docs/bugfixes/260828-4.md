Split out of `.docs/bugfixes/260828-1.md` (branch `fix-cwd-matters-cleanup-deletes-parent`, the `--cwd_matters` cleanup data-loss fix) at the user's request, so the data-loss fix is not held up behind unrelated work. Every item below was FOUND while doing that work but is a separate defect in a different subsystem. Item text, mechanisms and evidence are verbatim from that checklist; `LAYER n` there is `BUG n` here.

- [x] BUG 1 — **MOVED TO PR #558 at the user's direction and fixed there** (see LAYER 17 of `.docs/bugfixes/260828-3.md`). It belongs with the cleanup deletion path that PR rewrites, and #557 merges first, so keeping it there avoids two PRs editing the same functions. The deferral rationale recorded below was also wrong on the facts: it said the fix needed `os.Root`/`openat2` from "Go 1.26", but `go.mod` already says `go 1.26.3`, so it was available all along. Original entry: (future hardening, raised by the LAYER 5 implementor — NOT attempted): every deletion in the cleanup path is guard-then-delete on a path string, so a TOCTOU window exists in which a verified ancestor component could be swapped for a symlink. Exposure is bounded: the walk uses `os.Remove`, which neither follows a final-element symlink nor recurses, so the worst case is unlinking one empty dir or one symlink; and the single destructive `RemoveAll(workSpace)` has an identical guard->remove window before and after LAYERs 2/5. Closing it properly needs the deletions done through relative directory handles (`os.Root` / `openat2`, Go 1.26), which is a larger redesign and would not help speed. Decide whether wr wants it.
- [x] BUG 2 (flaky test found during LAYER 6's verification runs — recorded, not fixed, not skipped): `TestSchedulerSubmitJobsDefaultsMissingRequirements` failed 1 of 10 full `make test` runs: `client/client_test.go:204 Expected: nil / Actual: 'receive time out'`, in context "Given a running manager that schedules runners", with `lvl=warn msg="reply to client failed" err="object closed"` logged alongside; `--- FAIL: TestSchedulerSubmitJobsDefaultsMissingRequirements (2.75s)`. **Worth treating as a possible product bug rather than test noise**: this is the same "spurious receive time out" family that `requestTimeout`/`ClientMinRequestTimeout` (jobqueue/client.go:3193-3208) was introduced to eliminate, so it may be a remaining gap in that work.
  - Reproduced, and **not** an ENOSPC artifact: 3 failures in 30 runs of `client.test -test.run TestSchedulerSubmitJobsDefaultsMissingRequirements` with 16G free on `/`, under 14 busy loops plus two `dd conv=fsync` loops plus a concurrent jobqueue test loop (load average ~25, 0% idle). Verbatim symptom each time: `client_test.go:204 Expected: nil / Actual: 'receive time out'`, `--- FAIL ... (1.81s / 1.94s / 2.34s)`, two of the three with `lvl=warn msg="reply to client failed" err="object closed"` alongside. 0 in 30 at load ~15-20, so it takes a saturated box; disk was never implicated.
  - Verdict: **test noise, not the product gap it was flagged as.** This is the only test in client/client_test.go that assigns the package global `jobqueue.ClientMinRequestTimeout = time.Second` — the very floor `requestTimeout` exists to impose so a busy-but-alive manager's slow reply is not mistaken for a dead one — and then asserts a real `SubmitJobs` round-trip (a DB write plus scheduler processing) inside it. With the production floor the same request has 60s. The warn line is a consequence, not a cause: the Convey aborts at :204, the deferred `Disconnect` and `Stop` run, and the reply the manager was still preparing has nowhere to go. Every sibling test in the file uses `Timeout: 10 * time.Second` and no override.
  - Fix (test-only): drop the override and match the siblings' `Timeout: 10 * time.Second`, so the request sits on the production floor. Nothing in the test asserts a timeout, so nothing it proves is weakened. Green: 0 occurrences of the symptom in 30 runs under the same saturation that produced the 3. (That green run had one unrelated failure, `bind: address already in use` — the load generator's own manager taking the port, not this symptom.)
- [x] BUG 3 (flaky test found during LAYER 6's verification runs — recorded, not fixed, not skipped): `TestJobqueueBasics` failed 1 of 10 full `make test` runs (`--- FAIL: TestJobqueueBasics (10.83s)`). The failing assertion was truncated out of the capture, and it did not reproduce in 8 direct `-count=8` iterations under load nor in 6 later full runs, so the first job is to capture which assertion fails.
  - Reproduced, and the truncated assertion captured: `jobqueue_test.go:2406 Expected: true / Actual: false` — `So(<-ok, ShouldBeTrue)` in "Adding one while waiting on a Reserve will return the new job". 1 failure in 35 runs under 14 busy loops plus two `dd conv=fsync` loops, with 16G free on `/`. **Not** an ENOSPC artifact.
  - Root cause: a wall-clock race, BUG 5's family. The Convey started `ReserveScheduled(1000ms, "1024:300:1:0")` in one goroutine and, from a second, added the matching job on the 2nd tick of a 100ms ticker, then required `w && ticks <= 8` — the whole Connect plus Add plus reserve delivery had to land within ~800ms of the reserve starting. A contended box misses that, the reserve runs out its own 1s wait and returns nil, and `worked` reports false.
  - Fix (test-only, deterministic): the reserve now asks for `reserveWaitingForAddTimeout` (30s), and only the Add can make it return a job at all — running the wait out returns nil — so the clock can no longer decide the outcome. The Add moved onto the test's own goroutine, which also removes an unsynchronised append to the shared `jobs` slice. The one remaining clock check is `reserveStillWaitingCheck` (200ms): the reserve must NOT have returned before the Add, and since nothing of that scheduler group exists yet, load can only lengthen that wait, never end it. The old ticker goroutine also sent twice on an unbuffered channel on its success path and leaked; that is gone.
  - Non-vacuous: pointing the reserve at a scheduler group the Add cannot satisfy makes it red at `jobqueue_test.go:2385 Expected '<nil>' to NOT be nil`. Green: 0 in 40 runs under the load that produced the red.
- [x] BUG 4 (PRODUCT bug, found and reproduced during LAYER 6's review — the flaky-test fix models around it, so it must not be forgotten): a client reconnecting to a manager that is mid-`Stop` can hang for up to 60s, far past its own retry budget. Two causes. (1) `shutdown` (jobqueue/server.go:6052) calls `closeClientSubscriptions()` at :6102 BEFORE `close(s.stopClientHandling)` (:6103), `waitForClientHandling` (:6104), `time.Sleep(ShutdownSocketWait)` (:6105, default 50ms) and `s.sock.Close()` (:6107) — so for >=50ms every client subscription has been killed while the command port still accepts connections. `handleSubscribe` (serverCLI.go:690) / `registerClientSubscription` (server_subscription.go:444) have NO shutdown guard, so a subscribe arriving after the :6102 sweep repopulates `s.clientSubscriptions` with a `serverSubscription` nothing will ever `close()`, and the client long-polls a manager that is gone. (2) `reconnectOnce` -> `Client.reconnect(timeout)` -> `Connect` succeeds against the still-listening port, and `Connect` then widens the recv deadline to `requestTimeout(timeout)` >= `ClientMinRequestTimeout` = 60s (client.go:518, 3203-3208), discarding the cap `subscriptionReconnectTimeoutFor` (subscription.go:381) deliberately computed; since `reconnect` only checks `time.Now().After(retryEnd)` BETWEEN attempts (subscription.go:368), a single attempt can overrun the whole retry budget by up to 60s. User-visible: `wr add -w` / `AddAndWait` against a manager being stopped can hang far past `retryTime`. Likely the same family as item 10's "receive time out". Reviewer's evidence bounds it at >=3s against a 200ms budget (15x); the mechanism points at 60s.
  - Red evidence (at commit b0b9180 + the LAYER 6 fix, with only the dead-port redirect removed, 8 busy loops, `-count=6`): `--- FAIL: TestSubscriptionReconnectResync`, `subscription_test.go:1587 Expected: true / Actual: false` on `So(closed, ShouldBeTrue)` — the subscription did not close within 3 seconds. The LAYER 6 implementor independently measured 4 failures in 12 loaded runs.
  - Red command (deterministic, no load needed, built on this branch): `cp $SP/zz_red_bug4_test.go jobqueue/zz_red_bug4_test.go && CGO_ENABLED=0 go test ./jobqueue/ -run 'TestRedBug4ShutdownWindowHang' -count=1 -v` where `SP=/tmp/claude-1000/-home-ubuntu-wr-clone2/5d7020f8-4042-4274-8df6-eefdd9469e3c/scratchpad`. It sets `serverConfig.Timings.ShutdownSocketWait = 2s` to widen the window in which subscriptions are dead but the command socket still listens, gives the subscription a 200ms retry budget, stops the manager, and asserts the subscription gives up within 1s.
  - **CORRECTION — that red command is NOT a valid discriminator, and must not be used as one.** It calls `server.Stop(ctx, true)` SYNCHRONOUSLY and then measures `time.Since(wentAway)`, so the elapsed figure includes `Stop`'s own duration, which the test itself inflates by setting `ShutdownSocketWait = 2s`. Verified by the orchestrator: it fails identically with and without the fix (`2.062253677s` at base vs `2.063484377s` with the fix applied). Instrumenting the poll loop showed the subscription itself had already given up at ~204ms in that run — the test was measuring the manager, not the client. The real 60s form was reproduced instead by instrumenting a baseline run, where a real client's budget runs while the manager finishes shutting down: `DBG client.reconnect ok after 7.46ms` / `DBG dialSub ok after 11.56ms` / `DBG subscribe req err after 1m0.012723686s: receive time out` / `EXP closed=true after 1m0.064282419s (retry budget 200ms; Stop returned at 2.05s)`. The permanent test runs `Stop` in a goroutine for exactly this reason. Original (misleading) evidence, kept for the record: `closed=true after 2.061261913s (retry budget 200ms)`, `Line 61: Expected '2.061261913s' to be less than '1s' (but it wasn't)!`, `--- FAIL: TestRedBug4ShutdownWindowHang (2.82s)`. So the subscription does eventually give up — but only when the manager's socket finally closes, i.e. it waits on the manager's shutdown schedule instead of its own retry budget, overshooting it by 10x here and by however long `ShutdownSocketWait` plus the rest of shutdown takes in reality. The reviewer that found this separately measured >=3s against a 200ms budget under load with default timings, and the `ClientMinRequestTimeout` mechanism (client.go:518, 3203-3208) bounds the worst case at 60s.
  - Note the fix must respect DEVELOPERS.md section 1: a submitted command must still run once and have its outcome recorded, and connection errors/`ErrRecovering` must keep retrying (crash recovery) — only a live manager's authoritative `ErrBadJob` makes a runner give up. Do not make reconnect give up on transient errors in the name of honouring the budget.
  - Fix, server half: the `up == false` guard already existed at `validateRequest` (serverCLI.go:477-487, from #551); what was missing was ATOMICITY, since `handleRequest` snapshots `up` at the top of the dispatched goroutine, so a subscribe whose snapshot predates `beginShutdown` could still reach `registerClientSubscription` after the sweep and leave an entry nothing would ever close, plus its `deliverQueuedUpdates` goroutine. `storeClientSubscription` now checks a `subsClosed` flag set by `closeClientSubscriptions` under the same `csmutex`, so there is no window; refused subs are closed outside the lock and `handleSubscribe` maps the refusal to `ErrClosedStop`, matching what `validateRequest` already returns.
  - Shutdown ordering deliberately NOT changed, and the reason is worth keeping: `closeClientSubscriptions()` runs first because subscribers park in `sub.wait(min(clientTimeout, 25s))` inside handlers tracked by `s.wg`, and closing them while the readers and socket still work is what delivers a prompt, clean "subscription closed" reply. Moving it after `s.sock.Close()` would make each client instead wait out its own 30s receive deadline AND make `s.wg.Wait(ServerShutdownWaitTime)` (5s) time out with a warning on every shutdown.
  - Fix, client half — and this took two attempts. The first version computed `subscriptionBudgetTimeout(retryEnd, requestTimeout(s.client.timeout))`, but `s.client.timeout` is the client's original CONNECT timeout, which `wr add -w` defaults to 120s (cmd/add.go:70, cmd/root.go:366), while the post-reconnect socket sits on the 60s `ClientMinRequestTimeout` floor. With the production 24h budget the cap therefore moved the deadline UP: measured 1m59s blocked with the fix vs 59s without — **it doubled the very hang it was meant to remove**, and the permanent tests structurally could not catch it, because a 200ms test budget masks any limit >= 60s.
  - The fix that shipped makes "only ever narrower" structural instead of a computation each call site must get right: `Client.requestWithin` reads the socket's own deadline via `GetOption(mangos.OptionRecvDeadline)`, applies the requested value only if strictly smaller, and restores the value it READ (deferred, so it survives error and panic). `requestTimeout(c.timeout)` no longer appears on this path, which also fixes the second-order bug where a shared client's socket was left at 120s for every subsequent `request()` after any subscription reconnect. `subscription.go:399` is now just `requestWithin(s.subscribeRequest(), time.Until(retryEnd))`. The `timeout <= 0` guard is load-bearing, not padding: mangos treats a non-positive `receiveExpire` as "wait forever" (verified in mangos v3.4.2 `protocol/req/req.go:375,423` and `RecvMsg`'s `if c.receiveExpire > 0`). End-to-end: 1m58.06s -> 58.06s.
  - Tests: `TestSubscriptionReconnectDuringManagerShutdown`, three Conveys — the subscription gives up on its own budget while a stopping manager's socket lingers (reviewer measured the neutered form at 61.31s, the exact 60s mechanism); a subscribe arriving after the sweep is refused, not registered; and a production-shaped arithmetic guard (120s client timeout, socket on the 60s floor, 24h budget) asserting the cap can only narrow, which fails `Expected 60000000000 / Actual 120000000000` against the broken version. The whole test costs ~3.1s, not the ~62s first reported — that figure came from a failing neutered run.
  - DEVELOPERS.md sections 1 and 2 verified directly by the reviewer: `ClientRetryTime` 24h, `retryEnd`, the retry loop and the give-up set unchanged; connection errors, `ErrClosedStop` and `ErrRecovering` still fail one attempt and retry; every runner path stays on the untouched `request()`; `ClientMinRequestTimeout` not lowered globally; no lock added across a slow call; `sub.close()` outside `csmutex`; `-race` clean including `-count=5` on the new test.
  - Gates: `make test` `353 passed / 9 skipped / 2m56s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator (reviewer got two clean runs plus `-race`).
- [x] BUG 5 (flaky test, found during the LAYER 8 review in the sibling branch; wall-clock race, unrelated to either fix): `TestWaitForLiveManagerStartup` (cmd/manager_test.go:740) failed one full `make test` with `Expected '0' to be greater than '1'` — the `reports` slice was empty. Passed 3/3 in isolation and on re-run; disk was not implicated (81%/20G). Diagnosis from the reviewer: `readyAt := preStart.Add(70ms)` against a 20ms poll, so under load the first poll already lands past `readyAt` and no "still starting" line is ever logged. Same family as BUG 2/BUG 3 — a test asserting an observation within a wall-clock window it does not control. Fix deterministically (control the clock or observe the transition), do NOT widen the window or drop the assertion.
  - Red (natural, not a demonstration): CPU load alone would NOT reproduce it — instrumentation showed the Convey's setup completing in ~200us, 350x inside the 70ms window. **Disk** load did, immediately, because the stall is in the setup's file syscalls not its CPU: 3 concurrent `dd conv=fsync` loops alongside 16 busy loops gave 6 failures in 20 runs, `DBG setup=335ms reports=0`, `setup=371ms reports=0`, etc., and un-instrumented reproduced the reported message verbatim (`manager_test.go:740 Expected '0' to be greater than '1'`). Reading the loop confirms that is the ONLY path to `len(reports)==0`, since `reportWaiting` fires only after a failed attempt.
  - Fix (test-only): the fake connector counts calls instead of reading a clock — `attempts++; if attempts > startupNilPolls { return client }` with `startupNilPolls = 12`. Setup duration can no longer consume the wait, because the wait's length is now a poll count. `time.Sleep` is a guaranteed lower bound, so 12 nil polls cost >= 60ms of polling and the window spans >= 2 report intervals for ANY setup duration; load can only lengthen it, never shorten it — the exact inversion of the old failure mode. The sibling elapsed assertion is now written as `startupNilPolls*managerStartupPollInterval` and is still non-vacuous (deleting the `time.Sleep` from `waitForLiveManagerStartupWith` makes it red).
  - All four siblings classified by injecting a 200ms stall at the point each one's arithmetic depends on: **:354 races** (deadline `preStart+80ms` with `preStart` captured before `MkdirAll`/`WriteFile`/`Chtimes`) — fixed; **:457 races** in its fixture self-check only — fixed with a pure-arithmetic guard; **:215 and :514 cannot race** (their report comes from `extendDeadline`, which runs before the first connect on every iteration) — converted anyway for uniformity. Two further `readyAt` sites (:568, :637) also converted, so no connector in cmd/manager_test.go now decides readiness from a wall clock. :637's `readyAt` was dead code, and asserting `attempts == 0` there makes `jq ShouldBeNil` non-vacuous instead of trivially true.
  - Can-still-fail: with the connector made to succeed immediately, red on the elapsed assertion; neutralising that too (GoConvey's FailureHalts masks what follows) reproduces the original flake's exact message deterministically — `Line 771: Expected '0' to be greater than '1'`. Mutating the three new `attempts` expectations proved they observe real values (1, 1, 0). 30/30 green against the disk load that had produced 6/20 failures.
  - Gates: `make test` `356 passed / 9 skipped / 2m47s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator.
- [x] BUG 6 (same bug CLASS as BUG 4, found by its review; all pre-existing, none fixed): "a computed ceiling that can exceed the real one". `Client.Ping(timeout)` (jobqueue/client.go:767-772) sends `Timeout` to the server as advisory only and never bounds its own receive, and `ShutdownServer` (client.go:826-840) pings at `ClientSuggestedPingTimeout` (10ms) inside a `ClientShutdownTimeout` (120s) `select` that is only re-checked between ticks — so one ping into exactly BUG 4's shutdown window blocks on the 60s floor and can overrun the 120s ceiling. This is the strongest instance, and BUG 4's new `requestWithin` is precisely the primitive that would fix it. Also: `Client.Reserve`/`ReserveScheduled` (client.go:981-1030) have the same shape in the other direction (a `timeout` above 60s trips the socket floor before the server replies); and `quickReconnect` (client.go:2260) hardcodes a 1s connect timeout so its new socket carries the 60s floor while `reportFinalState`'s `retryEnd` is only checked between attempts (harmless at the 24h budget, and section 1 mandates the retry). NOT defects, checked: `restTimeout()` (client_rest.go:161-167) is only-narrower; `waitForSubscriptionUpdates` (server_subscription.go:508-511) clamps to a 25s constant; `Connect`'s `requestTimeout(timeout)` widening (client.go:602) is the deliberate floor that BUG 4's fix now reads rather than recomputes.
  - Red (deterministic, no load needed): widen the shutdown window with `ShutdownSocketWait = 3s`, `setNumRPCReaders(1)` and `InterruptTime = 5s`, so the single reader admits one last request after client handling stops and then exits, leaving everything after it unread. Evidence at base: `ping 3 took 1m0.001s err=receive time out` against a 10ms request, and `ShutdownServer returned true after 1m0.201s` against its own 120s ceiling.
  - Mechanism detail worth keeping: `beginShutdown` sets `drain = true` as well as `up = false`, so `validateRequest`'s `(!up && !drain)` gate never fires during shutdown — pings keep being answered normally right up until the readers stop. That is why `ShutdownServer` reaches the window at all instead of exiting early on `ErrClosedStop`.
  - Fix: `Ping` now uses `c.requestWithin(cr, timeout)` (BUG 4's primitive) so its `timeout`, previously advisory to the server only, also bounds the client's own receive. Because `requestWithin` can only narrow, `Connect`'s readiness ping is byte-identical — `setConnectSocketOptions` has already put the socket on `timeout`, so it falls straight through to `requestLocked`. The only call site whose behaviour changes is `ShutdownServer`'s, where the socket sits on the 60s floor and the request asks for 10ms. Result: `--- PASS ... (3.67s)`.
  - Second half, **flagged as unproven**: `ShutdownServer`'s wait loop is now deadline-driven rather than a `select` racing `time.After` against the ticker, removing the "ceiling only re-checked between ticks" shape and fixing a ticker leak on the give-up path. With the bounded ping the residual overrun was only ~10ms, and a red for this half needs a ping blocking near the 120s mark, which is not cheaply testable. It is exercised but NOT discriminated by the new test. Kept because it is small, strictly safer and removes the shape rather than the symptom — but recorded here as not proven by a failing test, which is the honest status.
  - `Reserve`/`ReserveScheduled` deliberately left alone: they have the mirror shape (a `timeout` above the socket deadline means the client abandons before the server replies), but `requestWithin` cannot fix it — it only narrows, and this needs WIDENING, the exact direction BUG 4 records as having doubled the hang it was meant to remove. The only in-tree caller already enforces the invariant: `cmd/runner.go:150-151` raises the connect timeout to `reserveint + 5s`, with a comment saying why. Reaching it needs an out-of-tree caller doing `Reserve(>60s)` on a shorter-timeout client. No reproduction, no caller, and it is the critical path.
  - DEVELOPERS.md sections 1 and 2: `Ping` is called only from `establishServerInfo` (inside `Connect`, where the narrowing is a no-op) and `ShutdownServer`, so no retry loop calls it and crash recovery is structurally unaffected; runner paths stay on the untouched `request()`; `ClientMinRequestTimeout` unchanged globally; the client mutex is now held for at most 10ms on this path instead of up to 60s.
  - Gates: `make test` `356 passed / 9 skipped / 3m47s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator; `-race` clean including `-count 5` on the new test.
- [x] BUG 7 (non-blocking findings from BUG 4's review, recorded so they are not lost): (1) **FIXED** (was: jobqueue/subscription.go:399 + client.go:414) — if the budget is exhausted between `retryEnd` being set and the resubscribe (`time.Until(retryEnd) <= 0`), `requestWithin`'s `<= 0` guard hands the request the full 60s floor, the very overrun the fix exists to prevent. Unreachable in production (`RetryTime` has no CLI/config surface, always 24h) and it would fail the new test loudly rather than silently, but a `if time.Until(retryEnd) <= 0 { return ... }` guard in `reconnectOnce` would close it. (2) subscription.go:413-416 — `subscriptionBudgetTimeout`'s doc overstates the guarantee: `unsubscribeRejectedReplacement` (:427-435) and `unsubscribeAfterDialFailure` (:850-859) are steps of a reconnect attempt still on plain `request()` and the 60s floor. (3) client.go:433-441 — `recvDeadline`'s error return and `errRecvDeadlineType` are unreachable with mangos v3.4.2 and therefore untested; defensible as a guard on an `interface{}` API.
  - Item (2) is FIXED on branch `fix-shutdown-hang-and-flakes` (Copilot raised the same point on PR #557): `subscriptionBudgetTimeout`'s doc (subscription.go:414-423) now claims only that it bounds the step it is given (the reconnect's `Connect` and readiness Ping), names `requestWithin` as what separately caps the resubscribe, and admits the steps it does not bound. Two corrections to the wording above, checked at the call sites: `unsubscribeAfterDialFailure` (:856) is reached ONLY from `subscribe` (:608), the initial subscribe, never from `reconnectOnce`, so it is not a step of a reconnect attempt and has no retry budget to overrun; and the other unbounded step is the replacement `dialSubscriptionSocket` (:388), whose `timeout` argument is the new socket's receive deadline for later long-polls, not a bound on the dial itself (mangos's first `Dial` attempt is synchronous and carries no timeout of its own). **Follow-up worth doing, deliberately not done here:** `unsubscribeRejectedReplacement` (:432, called from `reconnectOnce` at :407) genuinely is a reconnect step on plain `request()` and the 60s floor, so a reconnect whose replacement socket is rejected can hold the poll goroutine ~60s past its budget before the subscription finishes closing — BUG 4's family. `requestWithin(..., time.Until(retryEnd))` would close it.
  - That follow-up is now done, but the one-liner was not the whole answer, so the question it begs was settled first: what should happen when the budget is already spent? Skipping is **wrong**. This step is reached only when `replaceSock` refuses because the subscription is stopping, and the replacement's id is known nowhere else — `Unsubscribe` sends the id the subscription is still holding, not the replacement's. The manager has no reaper for a registration (`unregisterClientSubscription` is called only from `handleUnsubscribe`, the websocket teardown, and a failed catch-up), so skipping would strand a `serverSubscription`, its `deliverQueuedUpdates` goroutine and its two channels for the manager's lifetime, and leave `hasAnyClientSubscriptions()` permanently true — costing every job transition the zero-subscriber early-out DEVELOPERS.md rule 2 depends on.
  - Fix: `unsubscribeRejectedReplacement(subscriptionID, retryEnd)` goes through `requestWithin` with `rejectedReplacementUnsubscribeTimeout(retryEnd)` — whatever is left of the budget, or `subscriptionReconnectTimeout` when it is spent, so the request is always sent and never on the 60s floor. Production behaviour is byte-identical: `requestWithin` only narrows and `ClientRetryTime` is 24h, far above the floor. `subscriptionBudgetRemaining`'s doc no longer claims the unsubscribe is unbounded.
  - Red (deterministic enough to be a real guard): with the step on plain `request()`, the new Convey "The unsubscribe cleaning up a rejected replacement is bounded by the retry budget" fails **10 out of 10** runs under load at `subscription_test.go` `So(returned, ShouldBeTrue)` — the unsubscribe never came back inside the 2s limit against a 200ms budget. A second Convey, "A rejected replacement is unsubscribed even when the retry budget is spent", pins the no-skip decision: the skip-when-spent alternative leaves `serverClientSubscriptionCount` at 2 instead of 1.
  - `dialSubscriptionSocket` (:388) checked as asked and **deliberately not fixed**. The unbounded dial is real but is not the subscription's: `internal/mangostlstcp.NewDialer` builds `&net.Dialer{}` with no `Timeout` and dials with `context.Background()` (tlstcp.go:362-372, :689-705), so `dialClientSocket` — every `Connect` in wr — has the identical exposure, as does the `hs.Wait()` handshake after it. The dialer plumbs only `OptionKeepAliveTime` and `OptionNoDelay` into the `net.Dialer`; mangos v3.4.2 has no connect-timeout option, so a fix means a new transport option threaded through both dial sites, i.e. changing the connect path of every wr client for a failure mode nothing has reported. `OptionDialAsynch` is not a substitute: it also makes the dialer redial forever, so a genuinely unreachable manager would stop being detected at dial time and would surface later as a poll error instead. And the subscription path is the narrowest exposure in the tree — `reconnectOnce` has just completed a full `Connect` (dial, TLS and readiness Ping) to the same host inside `min(remaining, subscriptionReconnectTimeout)`, so a black hole would have to open in the microseconds between.
- [x] BUG 8 (PRE-EXISTING `wr mod` bug; found while implementing LAYER 9 on the sibling branch, then independently confirmed by Copilot on PR #558, review 5056905598): `mergeBehaviours` (jobqueue/job.go, ~:186-203) keeps only the LAST modifier entry per trigger. Each new behaviour replaces the FIRST existing entry with a matching `When`, so a second new entry for the same trigger overwrites the first NEW one. Measured: `wr mod --on_exit '[{"run":"x"},{"cleanup":true}]'` on a non-cwd_matters job stores `{"on_exit":[{"cleanup":true}]}` — `run x` is silently lost, even though `Behaviours.String()` supports arrays per trigger and `wr add` handles the same input correctly. Copilot's wording: "loses user-requested behaviours during `wr mod`". Routed here rather than to PR #558 because it has nothing to do with `--cwd_matters` and that PR is already large.
  - Red command: `jm.SetBehaviours(Behaviours{{When: OnExit, Do: Run, Arg: "x"}, {When: OnExit, Do: CleanupAll}})` applied via `applyTo` to a non-cwd_matters job; assert BOTH survive (`Behaviours.String()` contains the run and the cleanup_all). Also cover the existing-job side: a job that already stores two on_exit entries, modified with one new on_exit entry.
  - Red evidence (at 34530f8): RED-8a `{"on_exit":[{"cleanup_all":true}]}` — the modifier's own first entry (`echo x`) was overwritten by its second. RED-8b `{"on_exit":[{"run":"echo new"},{"run":"echo old2"}]}` — **worse than Copilot described**: replacing a trigger on a job that already had two entries for it replaces only the first, so the job ends up with a MIXTURE of the new behaviour and a stale old one, not merely a loss.
  - Care needed: decide what "replace the behaviours for this trigger" should mean when the modifier supplies several for one trigger — almost certainly "replace the whole set for that trigger with the whole supplied set", not "merge entry by entry". Check `Behaviours.String()`/`BehavioursViaJSON` round-tripping and the REST PATCH path, and make sure the sibling branch's cwd_matters filter (which runs after the merge) still behaves.
  - Fix: `applyBehaviours` is now a single `job.Behaviours = mergeBehaviours(job.Behaviours, j.Behaviours)`, with `mergeBehaviours` a self-contained free function. Semantics: for every distinct `When` the modifier mentions, drop ALL the job's existing entries for that trigger and keep the modifier's entries for it in supplied order; leave unmentioned triggers untouched; append a trigger the job does not yet have; an empty modifier set returns `existing` unchanged (byte-identical to the old no-op).
  - **The fix exposed a second, worse bug that nobody had reported: the `Nothing` convention was itself broken.** `wr mod --on_exit ""` maps to `[{"nothing":true}]` and means "turn this trigger off", but on a job holding more than one entry for that trigger the old first-match replacement produced `{"on_exit":[{"nothing":true},{"run":"echo old"}]}` — the old behaviour still ran. `rest_test.go:222` passed only because its job held exactly one `on_exit` entry, which first-match happened to hit. Now correct, and that test still passes untouched.
  - Reviewer's verdict on trigger identity (the part the orchestrator was unsure of): exact `When` equality is right. `OnFailure|OnSuccess` is a distinct trigger with its own JSON key in `bvjMapping`, it is unreachable from both user-facing paths (`cmd/mod.go` and serverREST.go:1193-1224 only ever emit single-bit `When`s; the only construction in the tree is a test), and the identity rule is unchanged from the old code. Bitmask-overlap identity would be strictly WORSE: a modifier supplying only `OnFailure` would then delete a stored `OnFailure|OnSuccess` entry, silently destroying its `OnSuccess` half.
  - Tests: `TestJobModifierBehaviours` (6 Conveys, exact-JSON assertions so order is proven, not just membership) plus a Convey in `TestRESTJobModificationEndpoint` asserting both the PATCH reply and a re-fetch. Reviewer re-verified redness independently: 4 of 6 Conveys plus the REST one go red on revert, with the two always-green ones shown non-vacuous. It also caught that GoConvey's `FailureHalts` masked the REST re-fetch assertion in the first neutered run, so it neutralised the reply assertion and proved the re-fetch fails on its own.
  - Gates: `make test` `355 passed / 9 skipped / 2m45s` PASSED, `make lint` `0 issues.`
  - **MERGE HAZARD with PR #558 — read before merging either.** #558 also extracted the OLD BUGGY loop into a METHOD `func (j *JobModifier) mergeBehaviours(job *Job)` at the identical position in jobqueue/job.go. A method and a free function of the same name coexist legally in Go, so a "keep both sides" conflict resolution **compiles** while #558's `applyBehaviours` keeps calling the buggy method — silently reintroducing BUG 8. This branch's tests would catch it, but the resolution is not self-evident. Separately, both branches add `func TestJobModifierBehaviours` to jobqueue/job_test.go, which is a loud duplicate-symbol compile error rather than a silent one.
- [x] BUG 9 (PRE-EXISTING inconsistency, found by BUG 8's review; unrelated to BUG 8 and unchanged by it): the CLI and REST disagree on the empty-array form of a behaviour trigger. `wr mod --on_exit '[]'` is a no-op for that trigger, whereas a REST PATCH of `{"on_exit":[]}` maps to `Nothing` and turns it off (serverREST.go:1219-1224). The documented off-switch, `--on_exit ""`, works correctly on both. Decide which reading is right and make them agree.
  - User's decision: an explicitly supplied empty array means "turn this trigger off", so the CLI matches REST. Consistent with BUG 8's per-trigger replace semantics, and `--on_exit ""` already meant the same thing.
  - Reference checked rather than assumed: a REST PATCH of `{"on_exit":[]}` stores `{"on_exit":[{"nothing":true}]}` and leaves other triggers alone, now pinned by a Convey in `TestRESTJobModificationEndpoint`.
  - Fix: the "an explicitly empty set means Nothing" rule now has ONE authoritative home, `jobqueue.ModifyBehaviours` (the private `modifyBehaviours` moved out of serverREST.go and was exported). `cmd/mod.go`'s three near-identical inline flag blocks became `modBehaviours`/`modTriggerBehaviours`, which call it, so `--on_exit '[]'` takes exactly the same route as `--on_exit ""`. `cmd/add.go` deliberately untouched: at add time an empty array means "no behaviours", and add has no per-trigger replace semantics.
  - The distinction that had to survive, verified both ways in a new `cmd/mod_test.go`: a flag NOT passed leaves `set == false`, so the trigger is absent from the returned `Behaviours`, `SetBehaviours` is never called and `mergeBehaviours` never mentions it; a flag passed with `[]` or `""` yields exactly `{"on_exit":[{"nothing":true}]}`. Red at `mod_test.go:47`/`:67` (`Actual: ""`), with the two "not passed"/"supplied normally" Conveys green before AND after.
- [x] BUG 10 (flaky test in BUG 6's OWN test, found during BUG 5's gate runs): `TestPingBoundedDuringManagerShutdown` (jobqueue/client_connect_test.go:171) failed one full `make test` on `So(errors.Is(err, mangos.ErrRecvTimeout), ShouldBeTrue)` — `Expected: true / Actual: false`. The request came back inside its budget but with a DIFFERENT error, so the bound held and only the error's identity was wrong. Passed 10/10 in isolation (`-count=10`), so it is a full-suite-load flake. Cannot have been caused by BUG 5's change, which touches only `cmd/manager_test.go`.
  - Likely cause to check first: under load the manager's shutdown can progress far enough that the request fails with a connection/closed error rather than a receive timeout, i.e. the test asserts one specific way of failing when what it actually cares about is that the call returned within its budget. If so, assert the bound (which is the behaviour under test) and accept either error, rather than widening a timing window.
  - Reproduced, and the guessed cause was only half right. Under 14 busy loops plus two `dd conv=fsync` loops plus a concurrent jobqueue test loop, 1 run in 25 failed with `took=264us returned=true err=<nil> isRecvTimeout=false`: the request did not fail with a different error, it **succeeded**. The Convey had not been exercising anything. (The other half of the guess is real too: forcing the shutdown further along, past `ShutdownSocketWait`, makes the same request fail with `send time out` after 1.5s, which would fail the timing assertion as well.)
  - Root cause: `pingUntilUnread` declares the manager unread as soon as one `Ping(ClientSuggestedPingTimeout)` misses its 10ms bound — and on a loaded box a manager that is still perfectly able to answer misses 10ms. The Convey then measured a request that manager answered. So the flake was not an over-specific error assertion on a correct measurement; it was a measurement that could silently not happen, whose only visible trace was the error's identity.
  - Fix: the leaf moved out of the shutdown race entirely, into `TestRequestBoundedOnDeadlinelessSocket`. It keeps exactly what the old one meant to prove — mangos reads a non-positive receive deadline as "wait forever", so that is the socket state `requestWithin` most needs to narrow — but gets its unanswerable reply from a healthy manager that is deliberately slow: a `Reserve` against an empty queue, which the manager holds open for `heldReplyWait` (2s) before replying "nothing ready", exactly as `TestClientRequestTimeoutDecoupledFromConnect` already relies on. Against a 200ms budget the outcome is deterministic, `mangos.ErrRecvTimeout` included, and success is impossible to mistake for a bounded failure because success takes 2s. The two Conveys that really are about shutdown stay in `TestPingBoundedDuringManagerShutdown`.
  - Non-vacuous: making `requestWithin` never narrow makes it red at `client_connect_test.go` `Expected '2.000491867s' to be less than '1s'` — it waited for the manager's reply instead of its own deadline. Green: 0 failures in 40 runs under the load that produced the 1 in 25.
  - BUG 7 item (1) fixed: `subscriptionBudgetTimeout(retryEnd, limit)` became `subscriptionBudgetRemaining(retryEnd) (time.Duration, error)`, returning a new unexported `errRetryBudgetSpent` when nothing is left, and `reconnectOnce` guards BOTH budget-consuming steps — once at the top for the connect, and again in `resubscribeWithinBudget` immediately before `requestWithin`. Re-reading the budget before the resubscribe is what actually closes the hole: the connect can take up to `subscriptionReconnectTimeout` (1s) and the dial takes more, so a budget alive at function entry can be spent by the time the resubscribe is reached. `requestWithin`'s `timeout <= 0` arm is untouched — it correctly means "caller wants no bound"; the fix is at the call site.
  - **A second hole found in the same shape**, not in the original report: the old `remaining <= 0 -> return limit` arm handed a SPENT budget the full `subscriptionReconnectTimeout` for the connect step too. Only a 1s overrun rather than 60s (Connect narrows its own Ping to that 1s), but the same defect, and it now takes the same route: no budget, no request.
  - DEVELOPERS.md section 1 preserved: the retry loop's only give-up is still `time.Now().After(retryEnd)`; `errRetryBudgetSpent` fails one attempt like any other error and is never inspected; connection errors, `ErrClosedStop` and `ErrRecovering` still retry. No infinite retry and no lost job — `remaining <= 0` implies `now >= retryEnd`, so the loop gives up on the same clock check, and the exact-tie case goes through `waitBeforeReconnect` (>=10ms) and gives up next pass.
  - Red: `subscription_test.go:1763 Expected: true / Actual: false` (subscription did not close within a second) and `:1799 Expected: 1 / Actual: 2` (a resubscribe was registered on a healthy manager), `--- FAIL (65.37s)` — the 65s IS the 60s `ClientMinRequestTimeout` floor. The second Convey proves the stronger claim: with a spent budget, nothing is sent at all.
- [x] BUG 11 (CI-only failure that turned out to be a PRODUCTION defect, and NOT the regression the orchestrator first blamed): CI's `test` job failed on `87ca23b` and `fdb420f` while `b6059d4` and `eb7ddc0` passed, with two port-starved packages — `internal` fatal on `available localhost ports couldn't be checked / listen tcp 127.0.0.1:0: bind: address already in use`, and `network/port`'s `TestPort`. It reproduced on a re-run, so it was not a one-off.
  - **The orchestrator's hypothesis (our new manager-starting tests exhausted CI's port budget) was REFUTED on causality, confirmed only on mechanism.** Manager counts before vs after `87ca23b`: `TestPingBoundedDuringManagerShutdown` had 3 Convey leaves and GoConvey already gave each its own `serve()`, so moving one leaf into a new test is 3 managers before, 3 after; `subscription_test.go`'s +2 are sequential and land in the `jq_default` lane, which has priority 0 and is dispatched LAST, minutes after `other` (priority 92) has finished; `TestJobqueueBasics`'s rewrite is manager-neutral; `cmd/manager_test.go`'s changes use fake connectors with no sockets. Nothing in the suspect window touches the `other` lane's packages, the runner lanes, or the port checker. The variable is WHEN `other` is scheduled relative to the suite's own churn — at `b6059d4` it got its slot at t~16s with ambient `eph` peaking at 2257; at HEAD it got it at t~58s, inside the ambient peak. **Newly visible, not newly caused.**
  - **Root cause, a production defect**: `port.Checker.AvailableRange(size)` (network/port/port.go) found a contiguous range by binding OS-offered ephemeral ports and HOLDING ALL OF THEM until some it happened to hold turned out adjacent. Measured on an idle box: **7120 simultaneous listeners for `size=4`** — a quarter of the 28232-port ephemeral range — stable at 7117-7155 across runs. The suite fires that five times from two packages `go test -p 4` runs concurrently in the same `other` lane (`internal`'s config Conveys twice via `findPorts`, and `TestPort` at sizes 2, 4, 4). A user running `wr` on a busy machine gets "The default ports couldn't be used for you" (internal/config.go:616) for no good reason.
  - Reproduced locally only by matching CI's `WR_TESTSUITE_MAX_PARALLEL=4`; the local default is 24, which is why plain `make test` never showed it. `ss` sampling caught the failing window verbatim: ambient `eph=15522` when the lane started, the two checkers adding `ephlisten=12210` on top, taking the range to **98% of 28232**, then collapse and `--- FAIL: TestPort (9.86s)`.
  - Isolated calibration in a `--network none` container with a helper holding N foreign ports — old vs new: at 12000 held, old passes but takes **71.8s**; at 22000 old **FAILS** `address already in use`; at 25000, 27000 and 28000 (99%) old **FAILS**; new passes at every level, 322-800ms.
  - Fix: `AvailableRange` takes one free port from the OS (proving the host is listenable and giving a per-call start), releases it, then walks upward BY PORT NUMBER testing candidate ranges, wrapping to `minSearchPort` (10000). It holds at most `size` ports at once and releases a failed candidate before the next. Dead state removed with it (`ports` map, `checkRange`, `portsBefore`, `portsAfter`, `firstAndLast`). Effect in the real suite: `ephlisten` peaks at **14** across a whole run versus 12210, and `TestPort` runs in **0.01s** instead of 0.8-21.9s.
  - No leak in the tests added by BUGs 2-10: `Client.Disconnect`/`server.Stop` run on every leaf including failing ones, verified individually.
  - Test shapes changed, each stated: DELETED the white-box Conveys for `checkRange`/`portsBefore`/`portsAfter` (the functions no longer exist; the behaviour they supported is still asserted by `checkAvailableRange`). ADDED "AvailableRange never holds more ports than the range asked for", which injects a counting listener at a new `Checker.listen` seam and asserts `peak == 4`, `live == 0` — this is the assertion that fails on the old code at 7120. ADDED a case for the new explicit `errNoContiguousRange` path. RESHAPED the release-error Convey to build its own listener rather than re-claim the just-offered port, which raced the very TOCTOU its doc comment warns about. CHANGED "fails when tcp listening fails" from `localhost:1` (bindable as root, so it passed by privilege accident) to `203.0.113.1:0`. REMOVED `checkAvailableRange`'s `too many open files` tolerance and its `SkipConvey` — that `So` was the literal CI failure line, and the search now needs four fds rather than thousands. Skip count unchanged at 9.
  - Gates: `make test` `358 passed / 9 skipped / 3m10s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator; plus a CI-shaped `WR_TESTSUITE_MAX_PARALLEL=4 make test` PASSED with ambient `eph` at 14000-15600 during the `other` lane — the exact conditions that failed twice before the fix.

- [ ] BUG 12 (PRE-EXISTING landmine found while investigating BUG 11; not fixed, not caused by this PR): during the runner lanes, a `--runnermode` child of `TestJobqueueRunnerScheduling` (a copy of `jobqueue.test` under `/tmp/TestJobqueueRunnerScheduling*/001/runner`) is **both LISTENing on `*:5023` and connecting to it from within the same process**, at roughly 250 connections/second for about a minute — 15465 sockets, over half the ephemeral range. 5023 is this user's DEFAULT PRODUCTION manager port, and the runner's `--rserver` is the lane's real manager address, so falling back to the default config should not be happening at all. Harmless to the port checker now that BUG 11 is fixed, but half the ephemeral range vanishing during the runner lanes is a landmine for anything else, and a test runner talking to the default production port is wrong on its own terms.
  - Red command: pending — run the suite and `ss -tanp | grep 5023` during the runner lanes to catch the owner.
