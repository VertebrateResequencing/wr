- [x] After doing `source ~/.openstack-theta-hgi-dev` I can `wr cloud deploy` and teardown successfully, but `make test` is failing on cloud-related tests. Use the bugfix skill workflow to fix. `make race` should also work.

  - Added bounded OpenStack retries for transient server creation/status/port visibility races, preserving cleanup of failed spawns.
  - Added a robust floating-IP port lookup fallback: after device-id and Nova interface lookups, derive the server fixed IP from named addresses, all server addresses, or server details, then locate the Neutron port by fixed IP without guessing on ambiguous results.
  - Added local behavioural regressions for delayed port visibility, compute-interface fallback, fixed-IP fallback, server-details fallback, ambiguous fixed-IP ports, context cancellation, transient create retries, and initial server 404 polling.
  - Gates passed: focused OpenStack spawn tests, `make lint`, `source ~/.openstack-theta-hgi-dev && make test`, and `source ~/.openstack-theta-hgi-dev && make race`.
