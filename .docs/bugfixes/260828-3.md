- [x] LAYER 1 (root cause): `Job.ActualCwd` gets poisoned with `Job.Cwd` on `--cwd_matters` jobs. `ActualCwd` must stay "" whenever `CwdMatters` is true (documented invariant on `JobEndState.Cwd`, jobqueue/client.go:2586-2593: "The cwd you supply should be the actual working directory used, which may be different to the Job's Cwd property; if not, supply empty string"). Three breaches: (a) jobqueue/client.go:1466 `newExecuteLiveState(cmd.Dir, ...)` — `resolveWorkingDir` sets `cmd.Dir = job.Cwd` for a CwdMatters job (client.go:1187-1216) and returns `actualCwd == ""`, so every live-introspection touch ships `Cwd == job.Cwd`; (b) jobqueue/serverCLI.go:355-362 `applyLiveSnapshot` does `job.ActualCwd = jes.Cwd`, poisoning the server-side job every touch interval; (c) jobqueue/job.go:1829-1831 `JobModifier.applyCmdCwd` sets `job.ActualCwd = job.Cwd` outright on `wr mod --cwd_matters`. Introduced by 5993460 (#530), shipped in v0.37.0 and v0.37.1.
  - Red command: `cp /tmp/claude-1000/-home-ubuntu-wr-clone2/5d7020f8-4042-4274-8df6-eefdd9469e3c/scratchpad/zz_red_test.go jobqueue/zz_red_test.go && CGO_ENABLED=0 go test ./jobqueue/ -run 'TestRedLayer1ServerAndModifier|TestRedLayer1ExecuteSnapshot' -count=1` (exit 1)
  - Red evidence (develop): `RED-1a` Execute of a CwdMatters job sent 12 live snapshots carrying a non-empty `Cwd`; expected none. `RED-1b` `applyLiveSnapshot` on a CwdMatters job: `Expected '/some/cwd' to be blank (but it wasn't)!` (zz_red_test.go:80). `RED-1c` `JobModifier.SetCwdMatters(true)`: `Expected '/some/cwd' to be blank (but it wasn't)!` (zz_red_test.go:92).
  - Fix: the invariant now lives in one place. New `Job.setActualCwd(cwd)` (`jobqueue/job.go`) is the sole writer of `ActualCwd` from a `JobEndState`; it ignores a blank cwd (as before) and any cwd on a `CwdMatters` job, so the mistake cannot be re-made by copying a neighbouring assignment. All four `JobEndState`-derived writers route through it — `applyLiveSnapshot` (serverCLI.go:359), `applySuccessfulEndStateLocked` (serverCLI.go:1112), `updateAfterExit` (job.go:1049), `Client.ended` (client.go:2652) — plus `resolveWorkingDir` (client.go:1214); that deleted four copies of `if jes.Cwd != "" { ... }`. `newExecuteLiveState(cmd.Dir, ...)` -> `newExecuteLiveState(actualCwd, ...)` (fix a). `JobModifier.applyCmdCwd` now clears `ActualCwd` instead of setting it to `Cwd` (fix c). New `Job.workingDir()` returns `ActualCwd`, else `Cwd` **only when `CwdMatters`**, and feeds `sshCommandForRunningJob` from both `buildJStatus` and `liveJobUpdateDataFromJob`. `grep 'ActualCwd = '` over non-test Go now yields only the setter and two deliberate clears (job.go:1863 `applyCmdCwd`, serverWebI.go:795 `resetJobExecutionFields`).
  - Tests: `client_payload_test.go` "Execute of a cwd_matters job sends no cwd in its live snapshots" (paired with `ShouldNotBeEmpty` on stdout snapshots so it cannot pass vacuously) and its positive counterpart "Execute of a job with a wr-created working dir sends it in live snapshots" (asserts the hashed cwd is still shipped, so an "always send empty" false fix fails); `live_jtouch_test.go` "A live jtouch of a cwd_matters job stores no ActualCwd" (real authenticated jtouch path, then the web-UI projection); `job_test.go` `TestJobModifierCwdMatters` + an `ToStatus()` SSHCommand fallback case.
  - Existing tests corrected (all encoded the bug; none weakened): `client_payload_test.go` two `So(states[N].Cwd, ShouldEqual, cwd)` deleted and a Convey renamed (`liveExecuteJob` sets `CwdMatters: true`, so they asserted the violation); `jobqueue_test.go:6871` `So(job.ActualCwd, ShouldEqual, tmp)` -> `ShouldBeBlank` (the surrounding `execute` check still proves the job runs in `Cwd`, so the behavioural claim is intact); `job_test.go` a `"/tmp/wr"` literal extracted to a const (`goconst`, byte-identical).
  - Display/ssh: no regression. `status.html:576` renders `CwdBase`+`Cwd`, so a cwd_matters job shows its full working dir from `CwdBase` with a blank leaf; `cmd/status.go:439-455` and `behaviours.go:326-328` already fell back to `Cwd`. ssh for a running `--cwd_matters` job now works (it was `""` pre-v0.37.0). The `workingDir()` asymmetry is deliberate: a non-CwdMatters job with no reported `ActualCwd` runs in a hashed dir that does not exist yet, so `Cwd` would be a lie — `job_test.go:384` still asserts `""` for that shape.
  - Also found: `applyCmdCwd` has poisoned `ActualCwd` since `wr mod --cwd_matters` first existed (commit 7b1f002), predating 5993460 — #530 widened an older bug rather than creating breach (c).
  - Also found and closed by this fix: `mountBaseDirs` (job.go:749-764) branches on `ActualCwd != ""`, not on `CwdMatters`, so under the poison it returned `defaultMount = Cwd` and `defaultCacheBase = filepath.Dir(Cwd)` — **mount cache dirs written into the parent directory**, the same wrong-parent family as the reported deletion. It now returns the documented `Cwd/mnt` and `cacheBase = Cwd` (job.go:715-719, job.go:267-272, cmd/add.go:196-198). RELEASE NOTE: for a `wr mod --cwd_matters` job, or a `--cwd_matters` job on a re-attempt under v0.37.0/v0.37.1, an undefined mount point moves from `Cwd` back to `Cwd/mnt` and an unspecified `CacheBase` from the parent of `Cwd` back to `Cwd`. That restores pre-v0.37.0 documented behaviour, but tooling built against v0.37.x paths will notice.
  - Also found and closed by this fix: a SECOND parent-destruction route independent of `Behaviour.cleanup`. `Job.Unmount` -> `rmEmptyMountDirs` (job.go:986-999) is gated on `ActualCwd != ""`; under the poison it called `rmEmptyDirs(Cwd, Cwd)`, and `rmEmptyParentDirs` (utils.go:850-860) starts at `filepath.Dir(current)` and walks UPWARD past `baseDir` when leaf == base. Hardening this walk is folded into LAYER 2 below.
  - Gates: `make test` `353 passed / 9 skipped / 29 packages / 2m52s` PASSED and `make lint` `0 issues.`, both re-run independently by the orchestrator. LAYER 2's red (RED-2a, RED-2b) deliberately still fails at this commit.
- [x] LAYER 2 (defence in depth): `Behaviour.cleanup` (jobqueue/behaviours.go:238-256) deletes `filepath.Dir(j.ActualCwd)` recursively. With `ActualCwd == Cwd` that is the PARENT of the user's own working directory, so the default `on_exit` `[{"cleanup":true}]` recursively deleted a real user's entire scripts directory (`/lustre/.../scripts_ciseqtl`: every `.R`, `.sh`, `.README`, `.rds` and a sibling results dir), then `ensureCwdExists` (client.go:1340-1346) recreated the bare cwd. Triggered server-side on the FIRST attempt via `killLostJobAndTriggerBehaviours` (jobqueue/server.go:4594-4623) running behaviours against the poisoned server-side job, and runner-side on any re-attempt because `copyJobForClient` (serverCLI.go:1827) ships `ActualCwd` to the client. cleanup must (i) bail out when `j.CwdMatters` is true — cmd/add.go:219-224 documents cleanup/cleanup_all as having "no effect when cwd_matters is true" — and (ii) refuse to treat any path that is not a proper descendant of `j.Cwd` as a deletable workspace, since `rmEmptyDirs`/`rmEmptyParentDirs` (jobqueue/utils.go:832-860) otherwise walk UPWARD past `Cwd`.
  - Red command: `cp /tmp/claude-1000/-home-ubuntu-wr-clone2/5d7020f8-4042-4274-8df6-eefdd9469e3c/scratchpad/zz_red_test.go jobqueue/zz_red_test.go && CGO_ENABLED=0 go test ./jobqueue/ -run 'TestRedLayer2CleanupCwdMatters' -count=1` (exit 1)
  - Red evidence (develop): `RED-2a` CwdMatters job with `ActualCwd == Cwd`: `stat .../scripts_ciseqtl/05_RunCisEQTL.R: no such file or directory` (zz_red_test.go:49) — the parent dir and its contents were deleted. `RED-2b` non-CwdMatters job whose `ActualCwd` is not below `Cwd`: same deletion (zz_red_test.go:64).
  - Red command (scope extension): `cp /tmp/claude-1000/-home-ubuntu-wr-clone2/5d7020f8-4042-4274-8df6-eefdd9469e3c/scratchpad/zz_red2_test.go jobqueue/zz_red2_test.go && CGO_ENABLED=0 go test ./jobqueue/ -run 'TestRedLayer2RmEmptyDirsWalk' -count=1` (exit 1)
  - Red evidence (at commit 1902949): `RED-2c` (final form asserts an error return, not silence — see the note below) `rmEmptyDirs(base, base)` deleted the PARENT of baseDir — `stat .../outer: no such file or directory` (zz_red2_test.go:30). `RED-2d` `rmEmptyDirs(leaf, base)` with leaf not below base deleted `.../other` (zz_red2_test.go:46).
  - Scope extension (found during LAYER 1 review): also harden `rmEmptyDirs`/`rmEmptyParentDirs` (jobqueue/utils.go:832-860) so the walk can never reach or pass `baseDir`. `rmEmptyParentDirs` starts at `filepath.Dir(current)`, so when `leafDir == baseDir` its first candidate is already the PARENT of `baseDir`. That is a second parent-destruction route, reached via `Job.Unmount` -> `rmEmptyMountDirs` (job.go:986-999) rather than via `Behaviour.cleanup`. LAYER 1 closed the poison that triggered it; the walk itself must also become incapable of it.
  - Scope extension: `wr mod --cwd` (modifying only `Cwd`) leaves a stale `ActualCwd` that is no longer below the new `Cwd` — exactly the RED-2b shape. LAYER 2's descendant guard must neutralise it; the stale field itself is fixed in item 4.
  - Fix: `Behaviour.cleanup` (jobqueue/behaviours.go) now bails on `j.CwdMatters || j.ActualCwd == ""`, returning nil — this is the documented no-op that every cwd_matters job with the default `on_exit` hits, so an error would be noise, not signal. It then refuses unless `dirIsBelow(filepath.Dir(j.ActualCwd), j.Cwd)`, returning a wrapped `errNotBelowBaseDir`: that state is always an anomaly (poisoned or stale `ActualCwd`), never routine, so a user with a v0.37.x-poisoned job in their BoltDB gets a named reason rather than a silently skipped cleanup.
  - Fix: `rmEmptyDirs` refuses up front unless `dirIsBelow(leafDir, baseDir)`, and `rmEmptyParentDirs` (jobqueue/utils.go) now checks each candidate parent with the same predicate instead of the old `parent != baseDir` string comparison — which also walked straight past an unclean or symlinked `baseDir`, so the guard was weaker than it looked even before the poison. New `dirIsBelow`/`relIsBelow`/`resolvedAbsPath` make both paths absolute, cleaned and symlink-resolved (resolving the deepest existing ancestor and re-appending the missing tail, since the leaf has usually already been deleted) before comparing with `filepath.Rel`; `.`, `..`, `../...`, blank and unresolvable paths all fail closed. A `baseDir` of `/` needs no special case.
  - Error contract: `rmEmptyDirs(leaf, base)` with `leaf == base` now returns an error rather than nil, so RED-2c's `ShouldBeNil` became `ShouldNotBeNil` (disclosed by the implementor, original kept as `$SP/zz_red2_test.go.orig`; every data-safety assertion untouched, and the change is strictly stricter). Justified: `leaf == base` asks the function to delete the one directory it promises to leave. Reviewer confirmed no in-tree caller can produce it legitimately — `mkHashedDir` puts `ActualCwd` six components below `Cwd`, so `workSpace` is five — and traced both propagation routes: a behaviour error reaches only `Execute`'s "Behaviour problems" stderr block (`myerr` is overwritten by `outcome.myerr` at client.go:2065 before `reportFinalState`, and `classifyExecOutcome` never sees it) and server-side is a `clog.Warn` in `killLostJobAndTriggerBehaviours`. An `Unmount` error can only flip `dorelease` when the message contains "failed to upload", which this one does not. No bury/release change, so DEVELOPERS.md section 1 (a command runs once, its outcome is recorded) is intact.
  - Tests: `jobqueue/behaviours_test.go` `TestBehaviourCleanupSafety` (fixture mirrors the incident: a `scripts` parent holding a precious `.R` file, `Cwd` a child) covers CwdMatters+poisoned for both `Cleanup` and `CleanupAll`, non-CwdMatters poisoned, the stale-`ActualCwd` sibling-tree shape from `wr mod --cwd`, and an `ActualCwd` that escapes `Cwd` through a symlink. `jobqueue/utils_test.go` `TestRmEmptyDirs` covers the positive walk, stop-at-non-empty-parent, an unclean `baseDir` with a trailing separator, and refusals for `leaf == base`, a sibling tree, and symlink escape. Positive coverage already existed and was not duplicated: `TestBehaviours` "Individual Behaviour Trigger() correctly" (behaviours_test.go:126-140) asserts the hashed workspace and `Cwd/a` are deleted while `Cwd` survives; "in order b4,b2" (:184-194) covers stop-at-non-empty.
  - Reviewer re-verified redness by neutering each guard in turn and restoring byte-exactly: dropping `j.CwdMatters ||` fails the cwd_matters case; dropping both cleanup guards destroys the precious file (`stat .../05_RunCisEQTL.R: no such file or directory`) and the stale-ActualCwd workspace; restoring the old `parent != baseDir` loop deletes `base` via the unclean-baseDir case; replacing `resolvedAbsPath` with plain `filepath.Abs` fails exactly the two symlink cases, so symlink resolution is not decorative.
  - Narrowing accepted: the workspace must now be *strictly* inside `Cwd`, so a hypothetical `ActualCwd == Cwd/cwd` would be refused. Safe because `mkHashedDir` (utils.go:690, `mkHashedLevels = 4`) always builds `Cwd/wr_cwd/h0/h1/h2/<uniqueXXXX>/cwd`. The upward walk still removes up to and including `Cwd/wr_cwd` and then stops, matching the old loop exactly.
  - Gates: `make test` `355 passed / 9 skipped / 29 packages / 2m45s` PASSED and `make lint` `0 issues.`, both re-run independently by the orchestrator (+2 tests vs LAYER 1's 353).
- [x] LAYER 3 (don't arm the gun): `wr add` attaches the default `on_exit` behaviour `[{"cleanup":true}]` (cmd/add.go:685) even to `--cwd_matters` jobs, where cmd/add.go:219-224 documents cleanup/cleanup_all as having "no effect when cwd_matters is true". So every cwd_matters job carries a behaviour that is documented as a no-op but was in fact the loaded gun that deleted the user's scripts dir, and `wr status` advertises it (`Behaviours: {"on_exit":[{"cleanup":true}]}`). Fix: when a job's effective `CwdMatters` is true, do not attach `cleanup`/`cleanup_all` to its resolved behaviours. Do it per-job in `JobViaJSON.buildJob`/`resolveBehaviours` (jobqueue/serverREST.go:455-712), where `CwdMatters` (`jvj.CwdMatters || jd.CwdMatters`, line 461) and the behaviours (line 709) are resolved together — that covers `wr add --cwd_matters`, per-job `"cwd_matters":true` in the JSON input, and the REST API in one place, rather than only the CLI flag. Keep non-cleanup behaviours (`run`, `remove`, `copy_to_manager`) untouched; keep the default cleanup for normal (non-cwd_matters) jobs. `Job.Key()` (jobqueue/job.go:1064-1084) is built from Cwd/Cmd/mounts/image only, so behaviours are not part of the dedup key and this cannot change duplicate detection.
  - Red command: `cp /tmp/claude-1000/-home-ubuntu-wr-clone2/5d7020f8-4042-4274-8df6-eefdd9469e3c/scratchpad/zz_red3_test.go jobqueue/zz_red3_test.go && CGO_ENABLED=0 go test ./jobqueue/ -run 'TestRedLayer3NoDefaultCleanupUnderCwdMatters' -count=1`
  - Red evidence (at commit 17a4dd4): all three intended cases fail — RED-3a per-job `"cwd_matters":true` (zz_red3_test.go:35), RED-3b global `--cwd_matters` default (:45), RED-3c explicit on_exit mixing `run` + `cleanup` (:58), each `Expected: false / Actual: true` on "job's resolved behaviours contain no cleanup". RED-3d — a normal non-cwd_matters job STILL gets the default cleanup — passes, so it guards against an over-broad fix.
  - Fix: `buildJob` (jobqueue/serverREST.go) computes `cwdMatters := jvj.CwdMatters || jd.CwdMatters` once and passes it to `resolveBehaviours`, which after resolving defaults-vs-explicit calls a new unexported `Behaviours.withoutCleanups()` (jobqueue/behaviours.go): clones, `slices.DeleteFunc`s every `Cleanup`/`CleanupAll` regardless of trigger, returns nil if none remain. One filter covers all three routes, confirmed by the reviewer: `wr add`/`wr add -f` (cmd/add.go:1171), REST `POST /rest/v1/jobs` (serverREST.go:1624), and `client.Scheduler.NewJobFromJSON` (client/client.go:921, whose defaults set `CwdMatters: true`). `grep 'Behaviours:'` over non-test Go shows `serverREST.go:477` is the only place user input becomes `Job.Behaviours`, so there is no bypass: `cmd/lsf.go:143` (bsub emulation) and `client.Scheduler.NewJob` build jobs with `CwdMatters: true` but never set behaviours, and the web-UI rerun path reuses stored behaviours while clearing `ActualCwd` so LAYER 2 bails.
  - Tests: `jobqueue/rest_test.go` `TestJobViaJSONBehaviours` (at `Convert` + `Behaviours.String()`, the exact string cmd/status.go:498 prints) covers default-sourced cleanup dropped for both per-job and via-defaults `CwdMatters`, explicit cleanup dropped, `cleanup_all` dropped, all three triggers filtered, `run`+`remove` surviving, and a normal job still advertising `{"on_exit":[{"cleanup":true}]}`. Plus a Convey in `TestREST` driving the real REST server, asserting `JStatus.Behaviours` over the wire *and* the stored job via `GetByEssence`, so the fix is proven at the boundary the incident transcript showed (`Behaviours: {"on_exit":[{"cleanup":true}]}`). Reviewer re-verified redness by neutering the guard to `if false && cwdMatters` and restoring byte-exactly (md5-verified).
  - Verified, not assumed: `Job.Key()` (job.go:1064-1110) is `[Cwd "."] Cmd "." mountKey ["." image "." ContainerMounts]` with no behaviours, so dedup is unchanged across versions. `withoutCleanups` cannot mutate a caller's slice (two layers: `resolveBehaviours` starts from nil so `append` always allocates and never shares the batch-wide `jd.On*` backing arrays, then `slices.Clone`). Returning nil rather than an empty slice is safe: the reviewer round-tripped four job shapes (never-set, explicit nil, empty slice, filtered-to-nil) through a real BoltDB file and confirmed all four come back identical through `Trigger`, `String`, `RemovalRequested` and the JStatus projection — a filtered job is indistinguishable from one that never had behaviours. `cmd/status.go:497`, `status.html:1213` and `modify-job.js:262` all handle the empty case already.
  - Help text (cmd/add.go:218-232) rewritten from the promise the code broke ("no effect when cwd_matters is true") to the truth. On the reviewer's recommendation the claim is attributed to `wr add` specifically — "`wr add` discards them instead of storing them on the job, ie. they won't appear in its status" — because the `wr mod` path still stores them (item 9) and cmd/mod.go:88 points users at add's option docs. That wording is true both before and after item 9, so it needs no second edit. Reviewer's other note also applied: `withoutCleanups` moved from mid-const-block down to its siblings (now `Trigger`, `RemovalRequested`, `withoutCleanups`, `String`).
  - Scope note: cleanups are dropped whether they came from the defaults or the job's own explicit JSON. Slightly broader than the user's literal "don't automatically set the default" ask; the orchestrator flagged this and offered to narrow it to `flags.Changed("on_exit")` semantics, with no objection received. Nothing changes functionally either way (the docs already promised a no-op); it only stops `wr status` advertising a deletion that will never happen.
  - Gates: `make test` `356 passed / 9 skipped / 2m45s` PASSED (+1 vs LAYER 5: `TestJobViaJSONBehaviours`) and `make lint` `0 issues.`, re-run independently by the orchestrator. No existing test modified (`git diff -- '*_test.go'` is purely additive, +107/-0).
  - Requested by the user as a third layer on top of the root-cause fix (LAYER 1) and the cleanup guard (LAYER 2).
- [x] LAYER 4 (found during LAYER 1 review): `JobModifier.applyCmdCwd` (jobqueue/job.go:1849-1866) leaves a stale `ActualCwd` when only `Cwd` is modified (`wr mod --cwd`). The job's recorded working directory then points into the OLD `Cwd` tree, so it is not below the new `Cwd`: `cwdLeaf`'s `filepath.Rel` yields a `../..` leaf for the web UI, `mountBaseDirs` picks the wrong cache base, and `Behaviour.cleanup` would target `filepath.Dir` of a directory unrelated to the job — the RED-2b shape, arrived at through supported CLI input rather than a poisoned field. Fix: clear `ActualCwd` whenever `Cwd` changes (a modified job has not yet run in any wr-created dir below the new `Cwd`). Riders raised by the LAYER 1 reviewer as non-blocking: (i) `sshCommandForRunningJob`'s parameter is still named `actualCwd` (jobqueue/job.go:133) though both callers now pass `workingDir()`, which may be `Cwd` — rename it; (ii) add a one-line comment at `behaviours.go:326-328` explaining why `Behaviour.run`'s `ActualCwd -> Cwd` fallback is deliberately unconditional and must NOT be unified with `workingDir()`, so a future refactor does not put an unconditional `Cwd` fallback back into the display path. Also close the coverage gap the reviewer found: `mountBaseDirs` has no test at all (`grep 'mountBaseDirs|CacheBase'` over `jobqueue/*_test.go` returns nothing), which is why the parent-directory cache base went unnoticed; add a table test locking in the documented `Cwd/mnt` mount point and `cacheBase = Cwd` for the CwdMatters case.
  - Red command: `cp $SP/zz_red4_test.go jobqueue/zz_red4_test.go && CGO_ENABLED=0 go test ./jobqueue/ -run 'TestRedLayer4StaleActualCwd' -count=1` (exit 1)
  - Red evidence (at commit 1cfe07c): RED-4a `JobModifier.SetCwd("/new/cwd")` on a job with `ActualCwd = /old/cwd/wr_cwd/a/b/c/uniq/cwd` leaves it untouched — `Expected '/old/cwd/wr_cwd/a/b/c/uniq/cwd' to be blank (but it wasn't)!` (zz_red4_test.go:25). RED-4b (`mountBaseDirs` for a CwdMatters job returns `Cwd/mnt` + `cacheBase = Cwd`) **already passed**: LAYER 1 fixed that behaviour by keeping `ActualCwd` blank. So (d) below is not a red — it is a characterisation test closing the coverage gap the LAYER 1 reviewer found, worth adding despite not being TDD-red because a silent wrong value on that path caused real harm.
  - Fix (a): `JobModifier.applyCmdCwd` (jobqueue/job.go) now clears `ActualCwd` in the `if j.Cwd != ""` branch as well as the `CwdMatters` one, the two clearing sites made adjacent under one comment explaining the shared reason. (b) `sshCommandForRunningJob`'s parameter renamed `actualCwd` -> `workingDir` (job.go:133), matching what both callers now pass. (c) Comment added above `Behaviour.run`'s fallback recording that it is deliberately unconditional (`cmd.Dir` must name a directory that exists, and `""` would silently mean the *manager's* cwd) and must not be unified with `workingDir()`. (d) New `TestJobMountBaseDirs` closing a total coverage gap.
  - Reviewer settled the one real judgement call — is clearing `ActualCwd` on any `Cwd` change safe for a job that is already running? **Yes, and it is blocked rather than merely unlikely**: `collectModifiableJobs` (serverCLI.go:1338-1341) skips `queue.ItemStateRun` and `handleModify` pauses the server first; the REST path uses an allowlist, `restEditableItemState` (serverREST.go:1259-1266) = delay/ready/dependent/bury. A reserved job is `ItemStateRun`, and a lost job stays in the run sub-queue until buried, so neither is reachable. Nothing else loses information: the runner keeps its own `ActualCwd` on its local `*Job` (client.go:1214) and a server-side clear never propagates back; disk-space tracking uses Execute's local variable, not the field; `Job.Unmount`/`rmEmptyMountDirs` are runner-side only; the web UI reads `Cwd` and `ActualCwd` together so the pair stays coherent; no recovery path reads it; and `resetJobExecutionFields` (serverWebI.go:795) already set the "about to run again => no workspace" precedent. No new orphan either: LAYER 2 already refused to delete the old workspace, and the retry path overwrote the field before any cleanup ran.
  - Tests: an assertion block inside `TestJobqueueModify`'s "You can modify the cwd of a job" (jobqueue_test.go:6534-6541) — client API against a real server, placed in exactly the window the stale value occupied, verified red with the old workspace path; `TestJobModifierCwd` (job_test.go) covering the field and the `ToStatus()` display boundary, verified red at both the field and the garbage `/../wr/job1` leaf a user would have seen in `wr status`; a Convey in `TestBehaviourCleanupSafety` asserting the filesystem consequence, verified red with LAYER 2's `refusing to clean up .../old_cwd/wr_cwd/unique`; and `TestJobMountBaseDirs`' five-row table over all three branches.
  - Why that cleanup test asserts nil rather than LAYER 2's error (reviewer's judgement, agreed): after LAYER 4 the job genuinely has no wr-created dir, which is the same contract as "never ran", so nil is right; `errNotBelowBaseDir` should stay reserved for a genuinely anomalous field (a v0.37.x-poisoned BoltDB job), or every retry of every `wr mod --cwd` job would emit a warning naming no real problem. `soPathsExist` still asserts the safety property directly, so a wrong fix that cleared the field but deleted anyway would still fail.
  - Also confirmed: `TestJobqueueModify`'s pre-existing `So(job.ActualCwd, ShouldStartWith, tmp)` assertions protect real behaviour and were correctly left alone — they are taken after `kick` re-runs the job, so they observe a workspace created *after* the mod, under the *new* Cwd. Test diff is +127/-0.
  - Gates: `make lint` `0 issues.`; `make test` `358 passed / 9 skipped / 2m46s` PASSED (+2 test funcs vs LAYER 3's 356) — but only on a clean run: `TestSubscriptionReconnectResync` (item 6) flaked in 2 of the orchestrator's 3 runs and 1 of the reviewer's 2, which is why item 6 moved to the front of the remaining queue.
  - Red command: pending — to be built and recorded when this item's cycle starts.
- [x] LAYER 5 (speed regression introduced by LAYER 2, found by benchmarking on request): the default cleanup behaviour for a normal non-cwd_matters job got ~69% slower and allocates 6.4x more. `dirIsBelow` resolves BOTH of its arguments through `resolvedAbsPath`/`filepath.EvalSymlinks`, which lstats every path component, and `rmEmptyParentDirs` calls `dirIsBelow` once per parent level — so a workspace six components below `Cwd` re-resolves the whole path ~12 times, making the syscall count O(depth^2). Harmless on local disk (0.1ms/job) but wr's jobs run on Lustre, where metadata ops are orders of magnitude dearer, and the reported incident involved 19632 jobs. Fix while keeping LAYER 2's safety exactly: do the pure-lexical `filepath.Clean`+`Rel` check FIRST and refuse immediately on failure (no syscalls, fails closed); then resolve `baseDir` and the leaf ONCE per `rmEmptyDirs`/`cleanup` call and have the per-level walk do lexical containment against the already-resolved base, instead of re-resolving per level. Every path actually removed must still be proven strictly inside the symlink-resolved base. Target: ns/op and allocs/op back within ~10% of the 1902949 baseline, with all LAYER 2 tests (including both symlink cases) still passing.
  - Red command: `CGO_ENABLED=0 go -C <tree> test ./jobqueue/ -run='^$' -bench=BenchmarkJobCleanup -benchmem -benchtime=3000x -count=5`, with `cp $SP/zz_bench_test.go <tree>/jobqueue/`. Compare the working tree against the pre-LAYER-2 baseline worktree at `$SP/before` (commit 1902949). NOTE: `cd` persists between commands in one shell — use `go -C <dir>` or the measurements silently compare a tree against itself.
  - Red evidence: baseline 1902949 = 149726 / 147414 / 147989 / 149172 / 144372 ns/op, 1891-1924 B/op, **51 allocs/op** (mean 147.7us). At 34a14e2 = 255059 / 248995 / 250931 / 245361 / 246543 ns/op, 25594-25638 B/op, **328 allocs/op** (mean 249.4us). Same machine, same benchmark, back to back.
  - Not affected: the add/archive/state-change persistence paths (`make bench`) — LAYER 2's diff touches only `Behaviour.cleanup` and the `rmEmptyDirs` helpers, nothing on the add path.
  - Fix: `dirIsBelow` is replaced by `deletableDirBelow` (jobqueue/utils.go), which proves containment ONCE per cleanup and returns the proven, normalised leaf and base for the deletion to use. It checks lexically first (`filepath.Abs` + `filepath.Rel` + `relIsBelow`: no syscalls, fails closed), then `componentsAreRealDirs` lstats each component of the leaf *below* base — base itself deliberately unchecked, so a symlinked `Cwd` still works — which proves the leaf can only be reached by descending inside the real base whatever base resolves to. The old full resolution survives as `resolvedDirBelow`, taken only when a component turns out to be a symlink or a file, so a symlink staying inside base is still deletable and one that escapes is still refused. `rmEmptyDirs` splits into the guard plus `rmCheckedEmptyDirs`; `rmEmptyParentDirs` does lexical containment per level against the already-proven base; `Behaviour.cleanup` proves once and passes the result down instead of proving it seven times.
  - Result (orchestrator's own interleaved runs, confirmed independently by the reviewer over 2 rounds x `-count=5`): baseline `1902949` 147.7us / 51 allocs; `34a14e2` 249.4us / 328 allocs (+69% / 6.4x); with LAYER 5 154.8us / 61 allocs (**+4.3% median, +5.9% mean**), inside the ~10% target. The residual +10 allocs is exactly the 5 `os.Lstat` calls: stubbing `componentsAreRealDirs` to `return true` returns the numbers to baseline precisely (51 allocs, 147.2us median), so the remaining cost is entirely the guard and is irreducible without weakening it.
  - What actually matters at scale: the guard is now O(depth of the workspace below `Cwd`) and independent of the depth of `Cwd` itself. For a real `/lustre/scratch124/pam/projects/.../wr_RunCisEQTL` cwd, LAYER 2 did ~14 full-path `EvalSymlinks` walks per cleanup (~150 metadata operations); it is now 5 `lstat`s however long the Lustre path is.
  - Correction to the implementor's report: it claimed LAYER 5 "closed a small hole" where LAYER 2 resolved the leaf but removed the unresolved path. The reviewer tested that against a `34a14e2` worktree and found it overstated — the behaviour does differ for a symlink resolving *inside* base (LAYER 2 unlinks the user's symlink and leaves the real empty dir; LAYER 5 removes the real dirs and leaves the now-dangling symlink), and LAYER 5's outcome is the more correct one, but both stay inside base, so no escape existed and this is not a safety fix.
  - Reviewer re-verified the guard is load-bearing: stubbing `componentsAreRealDirs` makes an empty directory outside base actually get deleted; stubbing `lexicalDirIsBelow` destroys the incident's precious file. Both restored byte-exactly (md5-verified) and re-tested green. A 30-case probe covering symlinked components in and out of base, a symlinked base, `..` escapes, `leaf == base`, a prefix-sharing sibling (`baseevil` vs `base`, the `baseLen` off-by-one bait), multi-byte component names, dangling symlinks, files-as-leaf, blanks and `/` all refused or handled correctly.
  - Permanent guard: new `jobqueue/behaviours_bench_test.go` `BenchmarkJobCleanup`, in the `make bench` lane (deliberately outside `make test`, matching `db_bench_test.go`'s precedent of guarding a per-job hot-path cost). Its doc comment records the O(depth) requirement and the measured 69% / 6.4x cost of getting it wrong. No behavioural test was added: LAYER 5 changes no supported behaviour, and LAYER 2's tests already own the property and pass untouched.
  - Gates: `make test` `355 passed / 9 skipped / 2m49s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator. No test file modified (`git diff -- '*_test.go'` empty).
- [x] LAYER 6 (flaky `TestSubscriptionReconnectResync`) — **MOVED OUT of this PR to
  its own branch and PR #560.** It was found during this PR's gate runs but touches
  no cleanup or path code, and kept 105 lines of unrelated test diff in front of
  anyone reviewing a data-loss fix. Its full mechanism, both causes and the
  proven-able-to-fail evidence live in `.docs/bugfixes/260829-3.md` on that branch.
- [x] LAYER 7 (fixed as LAYER 17 below; original entry) (future hardening, raised by the LAYER 5 implementor — NOT attempted): every deletion in the cleanup path is guard-then-delete on a path string, so a TOCTOU window exists in which a verified ancestor component could be swapped for a symlink. Exposure is bounded: the walk uses `os.Remove`, which neither follows a final-element symlink nor recurses, so the worst case is unlinking one empty dir or one symlink; and the single destructive `RemoveAll(workSpace)` has an identical guard->remove window before and after LAYERs 2/5. Closing it properly needs the deletions done through relative directory handles (`os.Root` / `openat2`, Go 1.26), which is a larger redesign and would not help speed. Decide whether wr wants it.
- [x] LAYER 8 (hardening raised by the LAYER 2 and LAYER 5 reviewers; behaviour today is correct and proven, so this is structure not repair): (0) `Job.Unmount` (jobqueue/job.go:948-951) gates on `ActualCwd != ""` rather than `!CwdMatters`, so a cwd_matters job persisted with a poisoned `ActualCwd` by v0.37.0/v0.37.1 takes an `errNotBelowBaseDir` return from `Unmount` where `Behaviour.cleanup` returns a silent nil — the same asymmetry the `CwdMatters` check fixed in cleanup. Harmless today (the error is discarded at client.go:2065) but worth aligning; moved here from item 4, whose implementor brief deliberately scoped it out. (1) `rmCheckedEmptyDirs` (jobqueue/utils.go:854) is an unguarded entry point that accepts any two strings and relies on a doc comment ("Both paths must have come from `deletableDirBelow`") to keep the proof attached — exactly the shape of mistake that caused this whole bug, where a wrong assignment was copied from a neighbour. Per implementation-principles *Encode Lessons In Structure*, have `deletableDirBelow` return an unexported `provenDirs{leaf, base string}` and make `rmCheckedEmptyDirs` accept only that, so an in-package caller cannot reconstruct the pre-LAYER-2 hazard. (2) `deletableDirBelow` returns three unnamed results whose order is only documented in prose — name them or fold them into that type. (3) `componentsAreRealDirs(absDir, len(absBase))` takes a byte length rather than the base path; that is the one place a boundary error would be silent and catastrophic, so pass `absBase` and take `len()` inside. (4) `Behaviour.cleanup` (jobqueue/behaviours.go:266) still hands the *unproven* `workSpace` to `cleanupWorkSpace` while the walk below gets the proven one — provably equivalent today, but it leaves the single destructive `RemoveAll` as the only deletion not on the proven path, inviting a future divergence; pass the proven value. (5) `mountBaseDirs` (jobqueue/job.go:750-763) branches on `ActualCwd != ""` rather than `!CwdMatters`, the same asymmetry as rider (0): a job persisted with a poisoned `ActualCwd == Cwd` by v0.37.0/v0.37.1 still gets `defaultCacheBase = filepath.Dir(Cwd)`, i.e. mount cache dirs in the PARENT of the user's cwd — the same wrong-parent family as the deletion, though writing is not deleting and LAYER 1 closed the poisoning. When fixed, add the `&Job{Cwd: cwd, CwdMatters: true, ActualCwd: cwd}` row to `TestJobMountBaseDirs`. (6) `cwdLeaf` (job.go:1357) and `liveJobCwdLeaf` (serverCLI.go:373) are two implementations of the same projection and only the live one guards `..`; LAYER 4 removed the last supported input that distinguished them, so behaviour is fine today, but the divergence remains. (7) Trivia: `applyCmdCwd`'s new comment says "Moving the job to a new Cwd" though the guard is `j.Cwd != ""`, so a no-op `wr mod --cwd $PWD` also clears a valid `ActualCwd` — the reviewer recommends NOT adding `&& j.Cwd != job.Cwd` (cmd/mod.go:175 passes the flag through unnormalised, so the comparison would be unreliable), just wording the comment to match the guard; and `job_test.go:466`'s Convey title is imprecise since the fallback is CwdMatters-only (pre-existing from LAYER 1). (8) Test gap: LAYER 2's symlink-escape cases detect a neutered guard only through the returned error, because their outward targets are non-empty so the walk would fail on them anyway. Add an *empty* directory outside base to `TestRmEmptyDirs`' symlink case and assert it survives, making "nothing outside the workspace is deleted" — the incident's actual danger — a directly asserted property rather than one inferred from an error value.
  - **This item stopped being hardening: its reviews found three real data-destroying escapes in the cleanup path, two pre-existing and one introduced here.** Three fix/review cycles.
  - Escape 1 (pre-existing, found by review 1, NO poisoning or attacker needed): `deletableDirBelow` proved `filepath.Dir(ActualCwd)` but never `ActualCwd`'s own last component. For a job with a relative `MountConfig.Mount` (documented, supported) whose own `Cmd` replaced its working dir with a symlink (`cd .. && rm -r cwd && ln -s ~/data cwd`), `removeActualCwd` -> `removeAllExcept` -> `os.ReadDir` FOLLOWS it and deleted the target's contents, returning nil.
  - Escape 2 (pre-existing, found by review 1): an `ActualCwd` ending in `..` proved clean via `filepath.Dir`, but the deletion helpers `filepath.Clean` the raw string back down to `Cwd` and wiped it — the incident's exact damage, reachable from a rogue/buggy client's `JobEndState.Cwd` (which `applyLiveSnapshot` copies straight in) or a corrupt BoltDB value.
  - Escape 3 (INTRODUCED by the fix for 1 and 2, found by review 2): `resolvedDirBelow` returns the RESOLVED path, which was harmless while it only fed `rmEmptyDirs`'s `os.Remove`-on-empty-dirs walk, but the fix handed it to the recursive delete. An `ActualCwd` symlinked to somewhere *inside* `Cwd` retargeted the `RemoveAll`: `Cwd/userdata` recursively deleted, error nil, real workspace leaked.
  - Fix: the proof is now split. `realDirBelow` (jobqueue/utils.go) does lexical containment plus `componentsAreRealDirs` with **no symlink fallback**, so it proves the path you asked about; `deletableDirBelow` keeps the fallback and is now reachable only from `rmEmptyDirs`, preserving `Job.Unmount`'s `os.Remove`-only walk unchanged. `Behaviour.cleanup` uses `realDirBelow(j.ActualCwd, j.Cwd)` and, failing that, deletes nothing and returns `errNotBelowBaseDir`. Refusing rather than substituting is deliberate: with `ActualCwd` no longer the real dir wr created, wr cannot know which dir it is entitled to delete, and the two deletion helpers disagree about symlinks (verified empirically: `removeAllExcept(link, ["mnt"])` deletes THROUGH a symlink, `removeAllManaged(link)` only unlinks it). A leaked workspace is recoverable and now loudly reported; a deleted `Cwd/userdata` is not.
  - Riders (0)-(8) all delivered: `Job.Unmount` and `mountBaseDirs` gates aligned with `cleanup`'s (`!j.CwdMatters && j.ActualCwd != ""`, the exact complement — the `ActualCwd != ""` half is still needed or a never-run job errors); `provenDirs` with unexported fields, so `rmCheckedEmptyDirs`/`rmEmptyParentDirs`/`cleanupWorkSpace`/`removeActualCwd` accept only it and no loose-string path into the deletion helpers survives; `componentsAreRealDirs(absDir, absBase)` takes the path not a byte length; `cwdLeaf` and `liveJobCwdLeaf` unified into one guarded projection (`JStatus.Cwd` gains `"."`->`"/"` and full-path-instead-of-`/../wr/job1`, display-only, both pinned by tests); comment and title corrections.
  - Verification (review 3): a from-scratch 29-case probe — symlinked `ActualCwd` pointing outside and inside `Cwd`, each with no mounts / relative `Mount` / absolute `Mount` / `MountConfig{}`; `..` shapes; `ActualCwd == Cwd`; a symlinked `Cwd` PREFIX (still cleans, so the refusal is not over-broad); symlinks deeper inside the workspace; a symlink named `mnt`; `Cwd/wr_cwd` itself symlinked; an already-deleted workspace; blank paths; trailing separators; a prefix-sibling `Cwd` (`cwdevil` vs `cwd`, the `baseLen` off-by-one bait). **Zero escapes, every legitimate workspace still cleaned.** No fourth escape found after tracing every deletion reachable from `cleanup`. Neuter tests confirm the new Conveys fail on ACTUAL DELETION inside the survival assertion, before the error assertion.
  - The one newly-refused case, judged correct: a user pre-creating `Cwd/wr_cwd` (or a level below) as a symlink to elsewhere *inside* `Cwd` — `os.MkdirAll` follows a pre-existing symlink, so wr would have used it. It needs deliberate setup, has no purpose, an outward symlink was already refused before this change, and the cost is leaked disk rather than lost data.
  - Gates: `make test` `359 passed / 9 skipped / 2m54s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator. `BenchmarkJobCleanup` 63 allocs/op (the +2 from lstat-ing one more component, unchanged across the three cycles), ns/op inside noise, guard still O(depth below `Cwd`).
  - Follow-up recorded rather than done (review 3, non-blocking): `provenDirs` now carries two different proof strengths — `realDirBelow`'s (the path you asked about) and `deletableDirBelow`'s (possibly the resolved target) — distinguished only by doc comments. A future caller could take a `deletableDirBelow` result and hand `.leaf` to `removeAllExcept`, reinstating escape 3. Today's only `deletableDirBelow` caller is `rmEmptyDirs`, so this is design feedback, but a distinct type (or a checked `real bool`) would finish what rider (1) set out to do.
- [x] LAYER 9 (found while implementing LAYER 3): LAYER 3's filter covers the add and REST conversion path (`JobViaJSON.buildJob`), but NOT the `JobModifier` path — `applyBehaviours` (jobqueue/job.go:1953) merges behaviours by trigger with no `CwdMatters` filter, so `wr mod --on_exit '[{"cleanup":true}]'`, or `wr mod --cwd_matters` on a job that already stores a cleanup, still leaves a cleanup on a cwd_matters job (also via the REST PATCH path, serverREST.go:156). Not a data-safety hole — LAYER 2's `Behaviour.cleanup` bails on `j.CwdMatters`, so it can only be a silent no-op — but `wr status` still advertises it, so LAYER 3's invariant is not global. **It also makes cmd/add.go's help text, rewritten by LAYER 3, not strictly true**: it now states cleanups "are discarded instead of being stored on the job, ie. they won't appear in its status", which holds for add/REST but not for mod. So this item is required to make the shipped documentation honest, not merely tidy. The reason LAYER 3 deliberately stopped short (a judgement worth making explicitly rather than quietly): mod's semantics are "replace the behaviours for this trigger", so simply filtering the cleanup out of the modifier list would leave nothing for that trigger and `applyBehaviours` would KEEP the job's old value instead of replacing it — a surprising outcome. Doing it properly means mapping "all cleanups dropped for a trigger" onto mod's existing `nothingBehaviour` convention (cmd/mod.go:362-405). Note `jobqueue/rest_test.go:222` already locks in that a REST PATCH with `cwd_matters:true` + `on_exit:[{"nothing":true}]` stores `{"on_exit":[{"nothing":true}]}`, and is unaffected either way. Note also that `wr mod`'s own `--on_exit` default of `[{"cleanup":true}]` (cmd/mod.go:525) is inert, because mod only acts inside `if cobraCmd.Flags().Changed("on_exit")`.
  - Red evidence (at commit 1bb1b2f): RED-9a (`wr mod --on_exit cleanup` on a cwd_matters job), RED-9b (`wr mod --cwd_matters` dropping an already-stored cleanup) and RED-9d (a cleanup-only modifier entry must not resurrect the job's old behaviour for that trigger) all fail `Expected: false / Actual: true`; RED-9c (a non-cwd_matters job still gets the cleanup mod asks for) passes and must keep passing.
  - Fix: `applyBehaviours` (jobqueue/job.go) is now `if j.BehavioursSet { j.mergeBehaviours(job) }` then `if job.CwdMatters { job.Behaviours = job.Behaviours.withoutCleanups() }`, reusing LAYER 3's helper. The old merge loop moved verbatim into `mergeBehaviours`.
  - **The implementor rejected the orchestrator's suggested design and was right to.** The brief specified mapping a cleanup-only trigger onto mod's existing `Nothing` convention; filtering the MERGED RESULT instead sidesteps the problem the `Nothing` mapping existed to solve, because the replacement has already happened by then so a trigger can never fall back to the job's old value. Decisive argument: `wr mod --cwd_matters` with no `--on_exit` never consults the modifier's list at all (`BehavioursSet` is false), so a modifier-list filter could not close the legacy half of the hole — the half that matters. It also makes mod agree with add (both store nothing) rather than inventing a second answer of `{"on_exit":[{"nothing":true}]}`. `Nothing` was verified to work before being discarded.
  - Ordering verified: `applyTo` runs `applyCmdCwd` before `applyBehaviours`, so the filter sees the POST-modification `CwdMatters`. Reviewer confirmed all six combinations by running code, including `--unset_cwd_matters --on_exit cleanup` (cleanup correctly stored) and `--on_exit ""` on a cwd_matters job (`{"nothing":true}` survives, since `Nothing != Cleanup`, matching rest_test.go:222).
  - Reviewer's verdict on the one debatable consequence — an unrelated `wr mod --priority 5` on a legacy v0.37.x cwd_matters job silently drops its stored cleanup: correct, not surprising. LAYER 2 already hard-guards `Behaviour.cleanup` on `CwdMatters`, so the dropped behaviour is a proven no-op and nothing the job DOES changes — only the misleading `wr status` line goes. It is also consistent with mod's existing normalisation of adjacent state (LAYER 4's `applyCmdCwd` clears `ActualCwd` on a bare `--cwd`).
  - Help text: `cmd/add.go:222` is now unconditional (`wr discards them`), which LAYER 3 could not say. Verified exhaustively rather than assumed — the only non-test conversions of user input into `Job.Behaviours` are `serverREST.go:477` (LAYER 3) and `job.go`'s `applyBehaviours` (LAYER 9), so every CLI and REST path is covered.
  - Tests: `TestJobModifierBehaviours` (jobqueue/job_test.go, 4 Conveys at `Behaviours.String()`/`JStatus.Behaviours`, the string `wr status` prints) including two positive controls that must pass before AND after; plus a Convey in `TestRESTJobModificationEndpoint` driving a real server, asserting persisted state via a follow-up GET and a second PATCH. Reviewer re-verified by two independent mutations rather than trusting the implementor: `if false && job.CwdMatters` reddens both job_test Conveys and the REST test; `if !j.BehavioursSet && job.CwdMatters` reddens the REST test at its SECOND PATCH, proving that assertion discriminates on its own.
  - Gates: `make test` `360 passed / 9 skipped / 2m41s` PASSED and `make lint` `0 issues.`
- [ ] LAYER 10 (possible ENOSPC artifact — see the note under item 11 before investigating; flaky test found during LAYER 6's verification runs — recorded, not fixed, not skipped): `TestSchedulerSubmitJobsDefaultsMissingRequirements` failed 1 of 10 full `make test` runs: `client/client_test.go:204 Expected: nil / Actual: 'receive time out'`, in context "Given a running manager that schedules runners", with `lvl=warn msg="reply to client failed" err="object closed"` logged alongside; `--- FAIL: TestSchedulerSubmitJobsDefaultsMissingRequirements (2.75s)`. **Worth treating as a possible product bug rather than test noise**: this is the same "spurious receive time out" family that `requestTimeout`/`ClientMinRequestTimeout` (jobqueue/client.go:3193-3208) was introduced to eliminate, so it may be a remaining gap in that work.
  - Red command: pending — repeat `make test` (roughly 1 in 10) saving full output, or drive the same shape under load.
- [ ] LAYER 11 (flaky test found during LAYER 6's verification runs — recorded, not fixed, not skipped): `TestJobqueueBasics` failed 1 of 10 full `make test` runs (`--- FAIL: TestJobqueueBasics (10.83s)`). The failing assertion was truncated out of the capture, and it did not reproduce in 8 direct `-count=8` iterations under load nor in 6 later full runs, so the first job is to capture which assertion fails.
  - Red command: pending — repeat `make test` (roughly 1 in 10) with full output saved.
  - **Check this first**: during LAYER 6 the box was found to be OUT OF DISK (`/` at 100%, 373M free), with 7977 stale `/tmp/wrtest*` dirs totalling 30G left behind by previous suite runs. ENOSPC made `make test` fail with unrelated-looking errors (`TestJobqueueRunnerResourceLearning`, `TestWaitForLiveManagerStartup`, a truncated `jq_default`) whose root cause was `write /tmp/...: no space left on device` on line 2 of the log. After cleaning, those did not recur, and items 10 and 11 did not reproduce in any subsequent run. **Items 10 and 11 may well have been ENOSPC artifacts rather than genuine flakes** — confirm free disk before spending time on either. Separately: the suite does not clean up `/tmp/wrtest*` after itself, which is worth fixing in its own right, and `make test` can exceed a 10-minute command ceiling when it is failing, so it should be run backgrounded or with a generous timeout.
- [x] LAYER 12 (PRODUCT bug, found and reproduced during LAYER 6's review — the flaky-test fix models around it, so it must not be forgotten): a client reconnecting to a manager that is mid-`Stop` can hang for up to 60s, far past its own retry budget. Two causes. (1) `shutdown` (jobqueue/server.go:6052) calls `closeClientSubscriptions()` at :6102 BEFORE `close(s.stopClientHandling)` (:6103), `waitForClientHandling` (:6104), `time.Sleep(ShutdownSocketWait)` (:6105, default 50ms) and `s.sock.Close()` (:6107) — so for >=50ms every client subscription has been killed while the command port still accepts connections. `handleSubscribe` (serverCLI.go:690) / `registerClientSubscription` (server_subscription.go:444) have NO shutdown guard, so a subscribe arriving after the :6102 sweep repopulates `s.clientSubscriptions` with a `serverSubscription` nothing will ever `close()`, and the client long-polls a manager that is gone. (2) `reconnectOnce` -> `Client.reconnect(timeout)` -> `Connect` succeeds against the still-listening port, and `Connect` then widens the recv deadline to `requestTimeout(timeout)` >= `ClientMinRequestTimeout` = 60s (client.go:518, 3203-3208), discarding the cap `subscriptionReconnectTimeoutFor` (subscription.go:381) deliberately computed; since `reconnect` only checks `time.Now().After(retryEnd)` BETWEEN attempts (subscription.go:368), a single attempt can overrun the whole retry budget by up to 60s. User-visible: `wr add -w` / `AddAndWait` against a manager being stopped can hang far past `retryTime`. Likely the same family as item 10's "receive time out". Reviewer's evidence bounds it at >=3s against a 200ms budget (15x); the mechanism points at 60s.
  - Red evidence (at commit b0b9180 + the LAYER 6 fix, with only the dead-port redirect removed, 8 busy loops, `-count=6`): `--- FAIL: TestSubscriptionReconnectResync`, `subscription_test.go:1587 Expected: true / Actual: false` on `So(closed, ShouldBeTrue)` — the subscription did not close within 3 seconds. The LAYER 6 implementor independently measured 4 failures in 12 loaded runs.
  - Red command: remove the `jq.args[0] = "localhost:" + strconv.Itoa(deadPort)` line from the LAYER 6 Convey and run it under load, or drive a subscribe against a manager inside its shutdown window directly.
- [x] LAYER 13 (PRE-EXISTING infinite loop, found during LAYER 8's second review — reproduced at HEAD, out of scope there, fixed here): `removeAllExcept` (jobqueue/utils.go) looped `for parent != path`, which never terminates when an exception dir is not below `path`. `mountDirsToKeep` (jobqueue/behaviours.go) passes any non-absolute `mc.Mount` straight through, so a job with `Mount: "../evil"`, `".."` or `"."` made its `on_exit` cleanup **hang forever**. Fixed rather than left recorded because it is a permanent hang, not a leaked dir: the maps are built before any deletion, so nothing is destroyed, but `Behaviour.cleanup` runs on the runner and, server-side, inside `killLostJobAndTriggerBehaviours` (jobqueue/server.go:4588), so the hang wedges a manager goroutine for the life of the process. Found independently three times (a LAYER 8 reviewer's probe, this checklist, and Copilot on PR #558), and LAYER 12's sibling work already edited `removeAllExcept` five lines above the loop.
  - Red command: `unset $(compgen -v | grep '^OS_'); CGO_ENABLED=0 timeout 300 go test -tags netgo --count 1 ./jobqueue/ -run TestBehaviourCleanupSafety`
  - Red evidence: all three new leaves failed `So(returned, ShouldBeTrue)` at behaviours_test.go:595, each after its own 5s bound expired — `--- FAIL: TestBehaviourCleanupSafety (15.02s)` against `--- PASS: TestBehaviourCleanupSafety (0.01s)` fixed. Confirmed again by neutering the fix and restoring jobqueue/utils.go byte-exactly (md5 `2fdf4e7723609c5c0302c43e8949e1c0`).
  - Fix: `removeAllExcept` skips an exception whose `filepath.Join(path, dir)` is not `lexicalDirIsBelow` `path` (LAYER 5's helper, reused rather than rewritten), and its upward walk now also stops at the filesystem root (`parent != filepath.Dir(parent)`) so no future caller can make it unbounded again. Skipping rather than erroring is deliberate and protects exactly as much as before: `removeAllExcept` only ever deletes strict descendants of `path`, so an exception outside `path` names nothing it could delete, and the `keepDirs`/`checkDirs` entries built for it were never consulted. Erroring would instead abandon the job's whole workspace, and skip the empty-parent tidy after it, for a case that is a no-op. The 29-case probe's conclusion is untouched: nothing outside a job's own workspace is deleted.
  - Tests: three new leaves in `TestBehaviourCleanupSafety` (jobqueue/behaviours_test.go) for Mounts `../evil`, `..` and `.`, each paired with a real relative Mount that must still be kept, so termination and the keep semantics are asserted together. They call cleanup through a new `triggerWithin` helper (goroutine plus `select` on `time.After`), so a hang fails one assertion instead of hanging the suite, and assert termination and the surviving mount dir BEFORE the error, since a Convey leaf stops at its first failed `So`.
  - Follow-up recorded rather than done: an escaping relative `Mount` is still accepted by `wr add --mounts`/`--mount_json` and then silently does nothing useful. Rejecting it at add time would tell the user, and belongs with the rest of `MountConfig` validation rather than in cleanup.
  - Gates: `BenchmarkJobCleanup` held at 63 allocs/op (155135/155771/158175 ns/op over 3x3000); `make test` `361 passed / 9 skipped / 2m45s` PASSED and `make lint` `0 issues.` (an earlier run of the same tree showed `360 passed / 10 skipped` because `container/docker` skipped itself on a docker.io TLS handshake timeout; that package passes on its own and on the re-run).
- [x] LAYER 14 (PRE-EXISTING `wr mod` bug, found during LAYER 9 and confirmed by its review; unrelated to cwd_matters): `mergeBehaviours` (jobqueue/job.go:186-203) keeps only the LAST modifier entry per trigger. Each new behaviour replaces the first existing entry with a matching `When`, so a second new entry for the same trigger overwrites the first NEW one. Measured: `wr mod --on_exit '[{"run":"x"},{"cleanup":true}]'` on a non-cwd_matters job stores `{"on_exit":[{"cleanup":true}]}` — `run x` is silently lost. The same input through `wr add` correctly stores both (well, stores `run x` once LAYER 3 drops the cleanup). Entirely pre-existing and unchanged by LAYER 9, which does not touch the non-cwd_matters path. Note this is the one case where LAYER 9's "mod now agrees with add exactly" justification does not hold, and the one case the rejected `Nothing`-mapping design would have got right.
  - Red command: pending — `jm.SetBehaviours(Behaviours{{When: OnExit, Do: Run, Arg: "x"}, {When: OnExit, Do: CleanupAll}})` applied to a non-cwd_matters job; assert both survive.
- [x] LAYER 15 (gap in LAYERs 3 and 9's invariant, raised by the LAYER 9 reviewer): a Go-SDK caller constructing `&Job{CwdMatters: true, Behaviours: Behaviours{{Do: Cleanup}}}` by hand and passing it to `Client.Add` bypasses BOTH filters, since neither `resolveBehaviours` nor `applyBehaviours` is on that path (`jobqueue/rest_test.go`'s own `addJob` helper does exactly this to seed fixtures). Harmless today — LAYER 2's `Behaviour.cleanup` bails on `CwdMatters`, so it is a stored no-op, never a deletion — and it is not reachable from the CLI or REST, so the help text stays honest. But per implementation-principles *Encode Lessons In Structure* the invariant should eventually be enforced at the server's job-store choke point rather than at two input-conversion sites.
  - Fix: one canonical helper, `Job.dropImpossibleCleanups()` (jobqueue/job.go), wrapping the existing `Behaviours.withoutCleanups()`, called from TWO choke points so every route is covered by construction: (1) `Server.prepareInputJobs` (jobqueue/server.go), the single loop every added job passes through inside `createJobs`, before `db.storeNewJobs` and before the queue itemdefs are built — chosen over the individual add handlers because all four entry points (`serverCLI.handleAdd`, REST POST, `serverWebI`, and jobs added by a running job) converge there; and (2) a new `db.decodeJob` (jobqueue/db.go), through which every read of a stored job now decodes — `grep NewDecoderBytes jobqueue/db.go` returns exactly one line, inside it, so a future decode site has to go through it. Six call sites collapsed into it. LAYER 9's `applyBehaviours` now calls the same helper instead of inlining the rule.
  - Decision, silently drop rather than reject: cmd/add.go documents the contract as "wr discards them instead of storing them on the job", unconditional after LAYER 9. Rejecting from the SDK would make the Go API contradict the documented CLI/REST behaviour for identical input, and would turn a documented no-op into a hard error. It must be drop on the DB path regardless, and one invariant enforced two different ways is exactly how a weak guard becomes the next template. `Client.Add`'s errors stay reserved for malformed input.
  - Decision, yes apply on DB load: v0.37.0/v0.37.1 persisted these, so real DBs hold them. Without it a restart re-queues the poisoned job and `wr status` keeps advertising a deletion that will never happen. Sanitising on decode removes the no-op the first time the job is touched rather than relying on LAYER 2's guard forever; that guard stays as the last line of defence, untouched. Cost is one `!CwdMatters` bool per decoded job.
  - `Job.Key()` does not include `Behaviours`, so dropping one cannot change job identity or dedup — verified, not assumed.
  - Red: add path `Expected "{\"on_failure\":[...]}"` / `Actual "...,\"on_exit\":[{\"cleanup\":true}]}"` and a `cleanup_all` case; DB path a job written with a poisoned cleanup and read back after a close/reopen. In BOTH tests the third leaf — a non-cwd_matters job whose cleanup must SURVIVE — passed in the red run, so neither is satisfiable by an over-broad filter.
  - Neuter proves the two choke points are independent: removing the `prepareInputJobs` call reddens only the add test (which also proves `GetByRepGroup` on a live job does not read through `db.decodeJob`, so the DB fix cannot mask the add fix); removing the `decodeJob` call reddens only the DB test. Both restored, md5-verified.
  - Corrections to the orchestrator's brief, found by checking rather than accepting: `rest_test.go`'s `addJob` fixture seeds a **non**-cwd_matters job carrying a cleanup, so the premise that this change would defeat it was wrong — it is untouched and still works. The saved partial patch from the earlier interrupted attempt also had three real defects, all fixed: an assertion order that left the over-broad guard unreachable in the red run, a DB test that used one open handle and so never proved a decode from disk, and a call to a non-existent `server.SetItemTTR`.
  - Gates: `make test` `363 passed / 9 skipped / 2m45s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator. `BenchmarkJobCleanup` unchanged at 106 allocs/op (nothing on the cleanup path changed); the add/archive persistence benchmarks also run clean, with `bolt_pages/job` and `bolt_writes/job` identical across counts.
- [x] LAYER 16 (DATA-DESTROYING, found by the user challenging an unsubstantiated claim of mine): `cmd/add.go` documents that "the `cleanup` and `cleanup_all` behaviours will ignore your mounted directories and any mount cache directories, so that nothing on your remote file systems gets deleted", and `mountDirsToKeep` exists to deliver it. That promise was broken for a mount point landing INSIDE the job's workspace but OUTSIDE its `ActualCwd` — e.g. a relative `MountConfig.Mount` of `"../shared"`, resolving to `<workSpace>/shared`. `removeWorkSpaceExtras` deleted every workspace entry except `cwd` and `.muxfys*` **without consulting keepDirs at all**. And `Behaviour.cleanup` runs BEFORE `Job.Unmount()` (client.go:1998 vs :2005), so the FUSE mount is still live and `os.RemoveAll`, which has no mount awareness, recurses through the mount point into the user's remote filesystem.
  - Sharing one mount across many jobs by specifying it outside the cwd is a legitimate, desired pattern, so the fix protects such mounts rather than rejecting them. The orchestrator initially proposed rejecting them at add time; the user pushed back and was right. Two facts confirm the design always intended protection: an ABSOLUTE mount outside the workspace is already safe (cleanup only deletes inside the workspace), and the `.muxfys` cache dirs, which legitimately live in the workspace, were already skipped by name. `removeWorkSpaceExtras` simply never learned about mount points.
  - Red (at 5a3e68c; a plain directory stands in for the mount, so this proves the protection bypass and the deletion — traversal into a live remote filesystem follows from `os.RemoveAll` having no mount awareness and was not measured): `cleanup err: <nil>`, `../shared point false content false`, `../a/b a false content false`, while a normal `mnt` mount survived.
  - Fix: `removeWorkSpaceExtras` now also skips workspace entries that lead to one of the job's mount points. The protected path is derived from the REAL resolution rather than a reimplemented join — `resolveMountPoint(mcMount, cwd, defaultMount)` was extracted as the pure part of `resolveMount` (which keeps its `mountedDirs` recording side effect), and `Job.mountPoints()` maps every `MountConfig` through it using the same `mountBaseDirs(nil)` that `Job.Mount` uses. `entryLeadingTo` then projects each mount to its first workspace path component via `filepath.Rel` + the existing `relIsBelow`, so `"../a/b"` protects `a` as well (deleting `a` would take `b` with it). A mount at or above the workspace yields no name, so `".."`/`"."` behave exactly as before. Purely subtractive: it removes deletions, never adds any.
  - Neuter-and-restore proved each piece load-bearing, each restored byte-exactly: ignoring `keepEntries` reddens both escaping-mount cases; protecting only direct children reddens ONLY the nested `"../a/b"` case; resolving against `j.Cwd` instead of the mount's real base reddens both.
  - Confirmed needing no ceremony: `"../../shared"`, `"../../../shared"` and absolute mounts outside the tree all survive untouched with `err == nil`, cleanup terminates, and the workspace is still fully cleaned.
  - Gates: `make test` `361 passed / 9 skipped / 2m49s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator. `BenchmarkJobCleanup` 63 allocs/op, identical to the stashed baseline (the benchmark job has no mounts, so it never enters the new path).
- [x] LAYER 17 (the TOCTOU window of LAYER 7, closed at the user's direction; `.docs/bugfixes/260828-4.md` BUG 1 moved here). The earlier deferral said it needed `os.Root`/`openat2` from "Go 1.26" — but `go.mod` says `go 1.26.3`, so it was available all along. That deferral was wrong on the facts, not on judgement.
  - **Key discovery: `os.Root` alone would NOT have fixed this.** It refuses to traverse out of the root, but it FOLLOWS relative symlinks that stay inside it — which is exactly LAYER 8's escape 3, the `Cwd/userdata` case. Verified by probe, not assumed. So containment needed a second, independent layer.
  - Design, two layers closing two different holes: (1) **containment, kernel-enforced** — `os.OpenRoot(j.Cwd)` pins Cwd's fd and every deletion below is a name relative to that handle, so a component swapped for a symlink after the proof cannot redirect a deletion out of Cwd (it also pins the dir across a rename of Cwd); (2) **identity** — the one destructive step opens the workspace as its own `*os.Root` and requires `os.SameFile` against the `FileInfo` `componentsAreRealDirs` already lstat'ed, so whatever path the open took, the inode must be the one the proof saw. That costs ZERO extra lstats because the `FileInfo` was already in hand.
  - Consequences: the proof is now taken on the workspace (parent of `ActualCwd`) rather than on `ActualCwd`, and `ActualCwd` is proven as a single named entry of the verified handle (`wsRoot.Lstat("cwd")`) — one component, no path for a swap to redirect. `cleanupWorkSpace`'s no-mounts branch became "remove every entry through `wsRoot`" because `Root` cannot remove `"."`; the workspace dir itself is then removed by the existing upward walk. End state identical.
  - The lexical+lstat proof was KEPT, and each layer buys something the other cannot: `realDirBelow` proves "this is the real dir we asked about, not a symlink's target" (which `os.Root` does not give, since it follows inside-root symlinks) and is the syscall-free front door for blank/`..`/`leaf == base`/prefix-sibling; `os.Root` proves "whatever happens after the check, nothing outside Cwd is touched", which no amount of lstat-ing can give because lstat describes a path and the next syscall re-resolves it. `deletableDirBelow`/`resolvedDirBelow` kept unchanged for `rmEmptyDirs`, so `Job.Unmount`'s contract is untouched. `lexicalDirIsBelow` and `removeAllManaged` became genuinely dead and were deleted; LAYER 13's escaping-exception guard survives in a cleaner relative form (`relIsBelow(rel)`, with the parent walk terminating naturally at `"."`).
  - `os.Root` API as actually found in Go 1.26.3 (probed, not assumed): `(*Root).RemoveAll` EXISTS, unlinks a final symlink rather than following it, and returns nil for a missing path; there is NO `(*Root).ReadDir` (`Root.Open` + `(*File).ReadDir(-1)` works); absolute names and absolute symlinks anywhere in the path are refused; **relative symlinks staying inside the root ARE followed**; `O_NOFOLLOW` is defeated because Go's `doInRoot` catches the `ELOOP` and follows the link itself; implementation is component-wise `openat(O_NOFOLLOW)`, NOT `openat2`.
  - Red: a deterministic seam. New `cleanupProvenHook` (nil in production, precedent `Client.liveTouchHook`) fires in the exact window between the proof and the first deletion. Against HEAD with ONLY the hook added, two Conveys swapping `Cwd/wr_cwd` for a symlink in that window both destroyed data — `stat .../decoy/unique/cwd: no such file or directory` (outside Cwd) and `stat .../wr_RunCisEQTL/userdata/unique/cwd` (inside Cwd). Green on the new code. Neuter proves the layers are independent: dropping `SameFile` loses only the inside-Cwd case; opening by absolute path loses only the outside case; dropping both loses both.
  - A 38-case safety probe written from scratch: **0 escapes on the new code, 2 on HEAD+seam** (the TOCTOU rows). Every legitimate workspace still fully cleaned; LAYER 13's termination cases and LAYER 16's mount cases all hold.
  - Residual, stated plainly: the upward empty-dir walk is not inode-verified. Worst case is what LAYER 7 already accepted — unlinking one empty dir or one symlink — and it is now bounded to inside the job's own Cwd. Everything recursive or destructive is fully closed.
  - Cost: **+19% ns/op and +43 allocs/op (63 -> 106)**, because Go's `os.Root` is component-wise `openat` (not `openat2`) and the upward walk re-walks from the root each time, making it O(depth^2) openats at the fixed depth of 5 that `mkHashedLevels` pins. LAYER 5's actual requirement is intact: the guard is still 5 lstats, O(depth below Cwd), independent of Cwd's depth — checked directly with a deep-Cwd benchmark, where the new code's bytes/op FALL as Cwd deepens (5.38kB vs HEAD's 5.60kB) because the deletions name relative paths, which is the Lustre-relevant direction.
  - Measured but NOT taken: keeping the per-component handles from the descent instead of re-walking gives ~145us / ~115 allocs — faster than HEAD's 155us, and exactly one metadata lookup per level, the minimum possible on Lustre. Recorded as LAYER 18.
  - Gates: `make test` `361 passed / 9 skipped / 2m49s` PASSED and `make lint` `0 issues.`, re-run by the orchestrator on a quiet box (an earlier failing run at 09:19 was inside a window when the other checkout was deliberately saturating the machine to reproduce flakes — `TestRESTJobModificationEndpoint` on a lost-job timing race and `TestReliable2FastStartupNoHistoryScan`, a wall-clock performance assertion that cannot hold at 0% idle). `-race` clean; `GOOS=darwin` builds; `GOOS=windows` still fails only on the pre-existing go-fuse/`Setpgid` errors.
- [x] LAYER 18 (the walk optimisation LAYER 17 measured but did not take). `provenDirs.openChain()` descends from the `Cwd` root handle to the workspace ONCE, keeping one `*os.Root` per component with the invariant "`roots[i]` is the handle that `names[i]` is an entry of". The leaf is then opened as one component off a pinned parent (still `os.SameFile`-checked), and the upward walk removes `names[i]` through `roots[i]` — **zero openats in the walk**, replacing the shrinking-relative-path form that re-walked from the root each level. `provenDirs.parent()`, `.remove()`, `.openProven()` and `rmEmptyParentDirs` are gone, along with the now-dead `base` field.
  - **Safety is strictly stronger, not merely preserved**: the chain PINS each level, so the parent walk can no longer be redirected by a mid-walk swap — HEAD re-resolved every level. The base boundary also becomes structural rather than a check: no handle above the base exists, so the walk cannot name it (LAYER 5/13's `relIsBelow` still guards the entry proof in `realDirBelow`/`resolvedDirBelow`/`exceptionDirs`, untouched). The error contract is preserved level-for-level, including leaf `ENOENT` -> continue to parents, `ENOTEMPTY`/`EEXIST` -> nil, and any parent error -> stop silently.
  - **The ~145us/~115 allocs projection LAYER 17 recorded is RETRACTED and was never achievable.** Measured interleaved over 6 rounds on a 93%-idle box: HEAD 188.6k ns/op / 106 allocs, new **173.6k ns/op / 103 allocs — -8.0% and -3 allocs**. A pre-TOCTOU build (`7ac3320`) measured ~166us on the same box against HEAD ~197us and new ~183us, so the chain recovers roughly HALF the TOCTOU cost, not all of it. Two reasons the projection was optimistic: `BenchmarkJobCleanup` toggles the timer per iteration and each toggle calls `runtime.ReadMemStats` (stop-the-world), putting a ~43us floor under every figure; and the walk's O(depth^2) penalty is smaller than assumed because removals stop at the first non-empty parent.
  - **The Lustre-relevant number, measured by `strace` rather than the clock** (deterministic; unlinkat/fstatat/getdents counts identical): full-depth cleanup deletion phase **18 -> 8 directory `openat`s**, with the upward walk itself going **10 -> 0**. For 500 jobs sharing a hash prefix (the benchmark's shape, where the walk stops after 2 levels): **20.03 -> 13.02 openat per cleanup**, exactly 7.006 saved. That is the figure that matters at 19632 jobs on Lustre, and it is independent of wall-clock noise.
  - Deep-`Cwd` property re-checked: allocs/op identical between shallow and 20-levels-deeper for both builds, and the deep-minus-shallow ns delta is the same (+10.3us HEAD, +11.2us new), so the guard is still O(depth below `Cwd`). Caveat recorded: the new build's deep-`Cwd` B/op is higher (8369 vs 7131) because each nested `os.Root` stores its own full path string — heap bytes, not metadata operations.
  - 44-case probe written from scratch (every action time-boxed at 5s so a LAYER 13 hang shows as HANG): 10 legitimate workspaces all cleaned, 5 mount cases, 3 LAYER 13 termination cases, 16 refusals, 3 TOCTOU cases, 7 `rmEmptyDirs`/`Job.Unmount` cases. **0 escapes, 0 not-cleaned, and HEAD scores identically** — which is the point: behaviour is unchanged.
  - Neuter matrix, each restored byte-exact: dropping the leaf `SameFile` check reproduces LAYER 17's exact inside-`Cwd` red; descending by absolute `os.OpenRoot` instead of `parent.OpenRoot(name)` passes the existing Conveys but fails the probe with `ESCAPE /victim/unique`; dropping the chain-completeness guard panics loudly rather than removing the leaf's name from the wrong directory; dropping `errIsDirNotEmpty` reddens 7 cases.
  - **Found a coverage gap in LAYER 17's own tests**, closed here: when the workspace is already gone at proof time there is no inode to check the open against, so containment is the only remaining layer, and nothing in the suite covered it. The new Convey passes on HEAD (so it is a gap, not a regression) and reds under the absolute-descent neuter.
  - Gates: `make test` `363 passed / 9 skipped / 2m59s` PASSED and `make lint` `0 issues.`, re-run independently by the orchestrator (176.8k ns/op / 103 allocs on the orchestrator's own run). `-race` clean; `GOOS=darwin` builds.
- [x] LAYER 19 (doc accuracy, from Copilot's review of head 907082e — small, but the same failure in miniature as the bug this PR fixes): `Job.mountPoints`'s comment claimed its mount points were "resolved exactly as `Mount()` resolves them", while passing `nil` for `onCwd`, so it could not reflect that option. Behaviour was correct; the promise was false. Worth fixing rather than waving through, because the original data loss was enabled by exactly this — `cmd/add.go` promised cleanup had "no effect when cwd_matters is true", and that was believed for two releases.
  - **The orchestrator's brief got the unreachability reasoning wrong and the implementor corrected it.** The `onCwd` branch is not gated on `ActualCwd == ""` alone: `mountBaseDirs` is `if !j.CwdMatters && j.ActualCwd != "" {...} else if len(onCwd) == 1 && onCwd[0] {...}`, so the `onCwd` branch is reached iff `CwdMatters || ActualCwd == ""` — which is the EXACT complement of `Behaviour.cleanup`'s bail condition `j.CwdMatters || j.ActualCwd == ""`. So the guarantee is stronger than "unreachable via the current caller": whenever cleanup proceeds at all, `mountBaseDirs` resolves without ever consulting `onCwd`, making `nil` not merely safe but indistinguishable from any argument. Independently, client.go:1542 sets `onCwd = job.CwdMatters`, so `Mount(true)` only happens for jobs cleanup skips. Proved empirically with a 4-row matrix over `CwdMatters` x `ActualCwd`: the single row where cleanup deletes anything is the single row where `onCwd` cannot change the answer.
  - Decision, comment rather than parameter, with the argument made rather than defaulted: adding `mountPoints(onCwd []bool)` would NOT make the mistake impossible by construction, which is the only thing that would justify it on this branch's own lesson. The hypothetical future caller writes `mountPoints(nil)` by copying the one call site — exactly the "a weak guard becomes the next template" failure — and gets the identical silently-wrong dirs. It relocates the judgement instead of removing it, and costs a live-but-never-exercised code path. The genuinely structural alternative (passing already-resolved base dirs) is worse: it pushes `mountBaseDirs` knowledge into behaviours.go and lifts the same `nil`-shaped decision one frame up. What settles it is that the invariant IS already encoded in structure — `Behaviour.cleanup`'s early return, which exists for its own load-bearing data-safety reason and is independently defended by tests. What was missing was an accurate statement of which mechanism does the work.
  - Also checked rather than asserted: `unparam` does NOT fire on the parameter version, so "the linter would reject it" was not available as an argument — the implementor built that version and lint-checked it before discarding it.
  - New comment names the concrete wrong answer ("a MountConfig with no Mount of its own would be reported at Cwd/mnt when a Mount(true) had put it on Cwd itself") and bounds it accurately: `cwd` is `j.Cwd` in both branches, so relative and absolute `Mount` values resolve identically either way; only a blank `Mount`, which takes `defaultMount`, can differ.
  - No test added, deliberately: the obvious pin ("`mountPoints` agrees with `Mount()` when `ActualCwd` is set") passes today AND would keep passing under precisely the misuse the comment warns about, because it pins the one case where `onCwd` is provably irrelevant. A test that cannot go red for the defect is not coverage. The supported behaviour — cleanup leaving mount points alone — is already covered by LAYER 16's Conveys, including the blank-`Mount` default case that the new comment's last sentence is about.
  - Gates: `make test` `363 passed / 9 skipped / 2m53s` PASSED and `make lint` `0 issues.` Comment-only; nothing executable changed.

## Rebase onto develop (after #557 merged as 1b2d839)

- LAYER 12 and LAYER 14 are marked done above because they were MOVED to #557 and
  fixed there, as BUG 4 and BUG 8 respectively. They are in develop now, so this
  branch no longer carries them. Left unchecked they made this PR read as shipping
  with two known open bugs.
- LAYER 10 (`TestSchedulerSubmitJobsDefaultsMissingRequirements`) and LAYER 11
  (`TestJobqueueBasics`) stay open, and move to the runnermode-hardening branch at
  the user's direction, so they do not hold up the data-loss fix. Both were found
  BEFORE #557's BUG 12 fix removed a very large load source (every --runnermode
  child was running most of the suite); 14 clean full-suite runs since, across
  #557's head and the merged tree, so their original 1-in-10 rate has not
  reproduced. That is consistent with them being fixed but is not proof - an
  unchanged 1-in-10 rate would still give 14 clean runs about 23% of the time.
- Conflict resolution, for the record. Only two files conflicted in substance:
  - `jobqueue/job.go`: this branch had extracted the OLD buggy behaviour-merge loop
    into a METHOD, while #557 replaced it with a corrected FREE FUNCTION at the same
    position. The call site `j.mergeBehaviours(job)` sits inside the
    `if j.BehavioursSet` block and does NOT surface as a conflict hunk, so git
    silently keeps the method and the whole thing still compiles and passes tests
    with the buggy merge in place. Resolved by hand: the method is deleted, the free
    function kept, and `applyBehaviours` calls it inside the `BehavioursSet` guard
    with the cleanup filter unconditional after it (so `wr mod --cwd_matters` still
    drops a cleanup the job already stored).
  - `jobqueue/job_test.go`: both branches added consts and a function at the same
    position, and both defined `TestJobModifierBehaviours`. Both consts kept, both
    functions kept whole, and this branch's version renamed to
    `TestJobModifierBehavioursCwdMatters` - the two cover different things (#557's
    the trigger-replacement merge semantics, this branch's the cwd_matters cleanup
    drop), so merging them would have hidden that.
  - `.docs/bugfixes/260828-1.md`: this branch originally used that filename, which
    develop had since taken for an unrelated fix. Develop's file is restored intact
    and this branch's checklist is 260828-3.md.
- Verified independently of the rebase: the resulting tree is content-identical to a
  separately resolved merge of the two tips (`git diff` shows only the two test
  functions swapping position), and `go vet ./...` is clean.

- [x] LAYER 19 (found by Copilot's review of the rebased head 6ad8558; verified, then
  fixed): `mountDirsToKeep` read the RAW `MountConfig.Mount` strings, so it protected
  only the mounts written as relative paths. `MountConfig.Mount` may legitimately be
  absolute - the docs allow "any directory you're able to write to" - and an absolute
  path can still land inside the job's own `ActualCwd`. Such a mount went into neither
  `keepDirs` nor `keepActualCwd`, so `removeActualCwd` took its empty-keepDirs path,
  `wsRoot.RemoveAll(actualCwdName)`, and deleted the whole `ActualCwd` **through a live
  mount** - the same route into a user's remote filesystem that LAYER 16 closed for
  relative mounts. Copilot's report was checked against the code and then reproduced
  before anything was changed; it was correct.
  - Red: new Convey "Cleanup of a Job with an absolute mount point inside its ActualCwd
    keeps the mount" in `TestBehaviourCleanupSafety` - the mounted file is deleted.
    (First run of it reported PASS because it was invoked with `-run TestBehaviours`,
    which does not match `TestBehaviourCleanupSafety`; a deliberate always-fail probe
    proved the block was never executing. Recorded because a test that passes without
    running looks exactly like a test that passes.)
  - Fix: `mountDirsToKeep` becomes a method on `*Job` and works from `j.mountPoints()`,
    which already resolves every Mount - empty, relative or absolute - to an absolute
    path. Each resolved point is taken relative to `ActualCwd`: `"."` means the mount
    lands on `ActualCwd` itself, so it is kept whole; anything strictly below goes in
    `keepDirs`; anything outside is left to `removeWorkSpaceExtras`, which was already
    protecting it. All the machinery existed - `mountDirsToKeep` was simply the one
    place still reading the unresolved string.
  - Behaviour change this exposes, deliberate: a `Mount` of `"."` now behaves exactly
    like a `Mount` of `""`, since both name `ActualCwd`. The raw-string reading made
    them mean opposite things - `""` kept `ActualCwd` whole, `"."` deleted its contents
    through the live mount. The escape-hardening Convey that asserted the old `"."`
    behaviour is split: `"../evil"` and `".."` remain genuine escapes and still get the
    workspace swept, while `"."` gets its own Convey asserting it is kept, with the
    reasoning recorded there. Changing that prior expectation is justified because it
    was asserting the defect.
  - Neuter-and-restore: reverting `behaviours.go` alone reddens both new Conveys
    (`Line 854, Expected: nil` twice); restored byte-exact, md5-verified.
  - Gates: `make test` `369 passed / 9 skipped`, `make race` `369 passed / 9 skipped`,
    `make lint` `0 issues.` **`make race` was added to the local gate set here**: CI
    runs `make test` AND `make race` (.github/workflows/tests.yml), and a build-tag
    error on a sibling branch got past `make test` + `make lint` because of that gap.

## Second rebase onto develop (after #559 merged as 45dae26)

- Only one file conflicted, and it was a filename collision, not a code one:
  this branch's review-comment checklist was `260829-1.md`, which #559 had since
  taken on develop. Develop's file is restored byte-identical and this branch's is
  now `260829-2.md`. Same class as the `260828-1.md` collision in the first rebase;
  a dated-checklist name is only free until another branch merges.
- No code conflicts at all: #559's changes were confined to the test-subprocess
  files, which this branch does not touch.
- `TestMain` appears twice in the package by design (#559's `race`/`!race` pair);
  the build tags are mutually exclusive, so that is not a duplicate symbol.

- [x] LAYER 20 (found by Copilot on the rebased head 1886900; verified, then fixed, and
  it was broader than reported): `Job.workingDir()` checked `ActualCwd` BEFORE
  `CwdMatters`, so a cwd_matters Job carrying the v0.37.0|1 poison reported the
  poisoned value rather than Cwd - contradicting the function's own doc comment and
  the rest of this PR's model, in which a cwd_matters Job's ActualCwd is never to be
  trusted. This is what wr displays and what `ssh` is offered for.
  - Broader than the report: `ToStatus` (job.go) and `liveJobCwdLeaf` (serverCLI.go)
    passed `j.ActualCwd` to `cwdLeaf` raw, so the displayed leaf had the same defect,
    and `mountBaseDirs` and `Unmount` each open-coded `!j.CwdMatters && j.ActualCwd
    != ""` to avoid it. Four sites, one idea, two of them wrong.
  - Fix, which SHRINKS the diff rather than adding to it: one named method
    `Job.createdCwd()` - "the directory wr created for this Job, or "" if it created
    none, which is always the case when CwdMatters" - now used by all four, with
    `workingDir()` expressed in terms of it. The open-coded idiom is gone.
  - Red: `TestJob`'s poisoned-ActualCwd Convey, strengthened. **It passed vacuously
    before**: its fixture set `ActualCwd = Cwd`, so consulting ActualCwd and ignoring
    it give the same string, exactly the shape of un-discriminating test that let the
    original bug through review. It now also sets a poisoned value that DIFFERS from
    Cwd. Neuter (`createdCwd` returning `ActualCwd` unconditionally) reddens it.
  - Deliberate display change: a cwd_matters Job now reports a blank status Cwd
    whether or not it carries the poison. It previously showed "/" when poisoned and
    blank when clean, for two Jobs that run in the same place.

- [x] LAYER 21 (found by an adversarial probe, not by review): `Job.Unmount` ->
  `rmEmptyDirs` used `deletableDirBelow`, whose documented job is to fall back to
  proving *the target a symlink leads to* instead of the symlink. A Job's own Cmd can
  replace the mount dir wr made with a symlink to a directory of the user's INSIDE
  their own Cwd; containment then says yes, and the empty-dir walk deleted that
  directory and its parent on the way up. Empty dirs only, so no file content, but
  they are the user's directories and the Job can trigger it.
  - Red: new Convey in `TestRmEmptyDirs` - a symlink inside baseDir leading to an
    empty user dir. With the fallback in place `soPathsExist` fails, i.e. the user's
    directory really is deleted.
  - Fix: `rmEmptyDirs` uses `realDirBelow`, which refuses to follow the link. The
    existing outside-baseDir test still passes (containment catches that one); this
    is the inside-baseDir case that only the symlink refusal catches.
  - **The fallback then had no callers at all**, so `deletableDirBelow`,
    `resolvedDirBelow` and `resolvedAbsPath` are deleted: -80 lines, and the whole
    resolve-a-symlink concept leaves the PR. `provenDirs`'s contract gets STRONGER as
    a result - rel is now always the path the caller asked about, never something a
    link leads to - so its doc comment shrinks too.

- [x] LAYER 22 (found by the adversarial probe; the probe's headline finding): the
  containment proof proves WHERE a directory is, never WHOSE it is. `ActualCwd`
  arrives from the runner, and cleanup treats its PARENT as a disposable workspace,
  so a value naming a real directory of the USER's strictly inside Cwd - the probe
  used `Cwd/userdata/results` - passes containment and had wr sweep `Cwd/userdata`.
  Also reproducible via `Cwd/userdata/results/cwd/..` and, because `filepath.Abs`
  resolves against the runner process's cwd, via a relative `ActualCwd`.
  - Red: new Convey in `TestBehaviourCleanupSafety` driving the real reporting route
    (`applyLiveSnapshot` with a client-supplied `JobEndState.Cwd`), asserting the
    user's file and directory survive. Neuter of the guard reddens it.
  - Fix: after the containment proof, cleanup requires the reported directory's own
    name to be `createdCwdName` - "cwd" - which is what `mkCwdAndTmp` calls every
    working directory wr has ever made. Anything else did not come from wr and
    licenses no deletion. A new `errNotACreatedCwd` is returned rather than a silent
    no-op, because a runner sending one is a bug and a quietly skipped cleanup is how
    a workspace leaks unnoticed.
  - Placed AFTER containment, not before: put first, it preempted the two existing
    Conveys that require `errNotBelowBaseDir` for an `ActualCwd` equal to Cwd and for
    one ending `/..`, turning their errors into silent nils. Order matters here.
  - Two stronger designs were tried and REJECTED, recorded so they are not retried:
    (a) checking provenance in `setActualCwd` - it broke four unrelated display tests
    whose fixtures use synthetic paths like `/tmp/wr/job1`, and rewriting five test
    files' expected strings would have grown this PR rather than shrunk it;
    (b) requiring the `<AppName>_cwd` path segment that `mkHashedDir` creates -
    `AppName` is a mutable package var, "jobqueue" under test and "wr" only once
    `cmd/runner.go` sets it, so deletion would have depended on a global that differs
    between test and production. `createdCwdName` avoids both: it is a fixed name,
    and `keptWorkSpaceEntry` already depended on it implicitly, so this makes an
    existing assumption explicit rather than adding a new coupling.
  - Honest limit, not fixed: a user directory literally named `cwd` inside Cwd would
    still pass. `keptWorkSpaceEntry` has the same hole and always did.
  - Gates: `make test` `369 passed / 9 skipped`, `make race` `369 passed / 9 skipped`,
    `make lint` `0 issues.` **One `make race` run failed first and I re-ran it before
    capturing which test failed, losing the evidence** - the re-run and a third run
    were clean, but that was a mistake worth recording rather than a clean result.

- [x] LAYER 23 (found by a hostile-reviewer agent, reproduced independently before
  fixing; the most serious finding of the polish rounds): the containment proof
  lstat'ed every path component, but the DESCENT that follows opened them with a
  bare `OpenRoot` and verified nothing. `os.Root` refuses a component that leaves
  the root, but permits a relative symlink that stays INSIDE it - which is exactly
  the case `os.Root` alone does not solve. Worse, `componentsAreRealDirs` discarded
  the infos it had gathered when a component did not exist, so when the workspace
  had ALREADY GONE (a normal outcome: another cleanup, or a re-run) the proof
  returned "ok, nothing there", the leaf check bailed out, and `removeUpward` still
  walked up through completely unverified handles.
  - Red: new Convey "Given a workspace that had already gone when cleanup checked
    it". With `wr_cwd` swapped for a relative symlink to `userdata` after the proof,
    cleanup returned **nil** and deleted `Cwd/userdata/unique` - a directory of the
    user's, inside their own Cwd, with cleanup reporting success.
  - Fix: `componentsAreRealDirs` returns an info per component and KEEPS the ones it
    gathered when it runs out of existing path; `provenDirs` carries them; and
    `openChain` opens every level with `openVerifiedDir`, the same `os.SameFile`
    check the leaf already got. The descent now proves each level at the moment it
    opens it, so there is no window at any depth rather than only at the leaf.
  - This also makes three comments TRUE that were previously overclaims: `provenDirs`
    said "no component of it is a symlink, so deleting rel and walking up from it can
    neither leave root nor delete something a link merely points at", which was only
    half-earned by `os.Root` before.

- [x] LAYER 24 (same agent; a "deletes too little" regression this PR introduced):
  `rmEmptyMountDirs` switched on `mc.Mount == ""` / `!filepath.IsAbs(mc.Mount)` and
  so SKIPPED absolute mounts entirely. Since LAYER 16 makes cleanup deliberately
  leave a live mount point alone, this was the only thing that came back for it
  afterwards - so a job with an absolute mount inside its workspace leaked that
  workspace for good. Before this PR the workspace was removed, by deleting through
  the live mount, which is the bug LAYER 16 fixed; the leak is the regression.
  - Fix: iterate `j.mountPoints()`, which already resolves all three spellings of
    Mount, and treat `errNotBelowBaseDir` as "not ours to tidy" rather than a
    failure. This also deletes a FOURTH copy of the mount-resolution rule.

- [ ] Known, recorded rather than fixed (from the same review):
  - `provenActualCwd` runs before the no-mounts branch that never reads its result,
    so a Job whose own Cmd replaced its `cwd` entry with a symlink gets an error and
    a leaked workspace, where `wsRoot.RemoveAll` would have unlinked the symlink
    without following it. Deliberately NOT changed here: it would reverse an existing
    safety assertion, and "RemoveAll does not follow a final symlink" is a claim to
    verify on a deletion path before relying on it, not at the end of a long session.
  - A lost job never calls `Job.Unmount`, so its mount dirs and workspace leak even
    for relative mounts. Pre-existing, not introduced here.
  - A user directory literally named `cwd` inside Cwd still passes LAYER 22's name
    check. `keptWorkSpaceEntry` has the same hole and always did.

## Adversarial round 2 - both findings were defects in round 1's OWN fixes

- [x] LAYER 25: **LAYER 22's guard was cosmetic.** It checked only the last component
  of the reported path, so the round-1 payload became lethal again by appending one
  element: `ActualCwd = Cwd/userdata/cwd`. Worse, the named directory did not have to
  EXIST - `provenActualCwd`'s `IsNotExist` was tolerated - so `Cwd/scripts/cwd` with
  no such dir had `Cwd/scripts` swept and every empty ancestor removed, returning nil.
  - Red: new Convey "Cleanup deletes nothing when the reported cwd does not exist",
    driving the real `applyLiveSnapshot` route. Neuter (restoring the `IsNotExist`
    tolerance) reddens it.
  - Fix: the reported working directory must now exist and be a real dir. The cost is
    that a workspace whose cwd was already removed by a cleanup that failed partway is
    left alone rather than swept - a leaked directory rather than a deleted one, which
    is the right way round.
  - **Why there is no proper provenance check, established rather than assumed:** a
    genuine ActualCwd is `Cwd/<AppName>_cwd/<hash>/<hash>/<leaf>/cwd`, so keying on
    the `<AppName>_cwd` segment would be exact. `AppName` is a package var set to "wr"
    in `cmd/runner.go` and NOWHERE else, so the manager keeps the default "jobqueue".
    Cleanup runs server-side too (`killLostJobAndTriggerBehaviours`, the very path that
    fired in the incident), and there it would refuse every real workspace - silently
    disabling the cleanup it was meant to harden. Requiring the `tmp` sibling that
    `mkCwdAndTmp` always makes was also considered and rejected: 13 of the 17 existing
    workspace fixtures do not create one, so it would have rewritten the test suite.
  - Residual, stated plainly rather than papered over: a user directory literally named
    `cwd`, that exists, inside their own Cwd, still passes. This is a shape check, not
    a proof of origin.

- [x] LAYER 26: **LAYER 24's fix introduced a new deletion.** Feeding every resolved
  mount point to `rmEmptyDirs` meant an absolute `Mount` naming an existing EMPTY
  directory of the user's inside their Cwd was removed, and its empty parents with it
  - `wr add --mounts` documents "any directory you're able to write to", so that is a
  legitimate thing for a job to name. It fired even when nothing had ever been mounted
  there, and even when the mount point never existed.
  - Red: new `TestJobUnmountEmptyDirTidyUp`. Neuter reddens it with
    `stat .../userdata/incoming: no such file or directory`.
  - Fix: the tidy-up is confined to mount points inside the workspace wr made. Being
    inside Cwd is not enough - the workspace is the only tree wr created, so it is the
    only one with anything of ours to tidy in. The LAYER 24 leak fix is kept for
    mounts that really are in the workspace.
  - **Neuter methodology bug, worth recording:** my first neuter of this deleted the
    guard, which left an unused variable, so the package did not build and the run
    reported ZERO failing assertions. That looks exactly like "the test does not bite".
    A neuter must still compile; check that before believing its verdict.

- [ ] Recorded, not fixed (round 2, and none of these are weaponisable against
  pre-existing user data as far as the probe could establish):
  - `os.SameFile` is not an identity check across delete+recreate: ext4 reuses a freed
    directory inode immediately, so the descent's per-level proof can be satisfied by a
    different directory. Steering the inode requires creating the target after the
    proof, so the probe could redirect the descent into an attacker-made dir but could
    not get pre-existing user data into it. The doc claim that a swapped symlink
    "cannot redirect the deletions to a different directory, even one inside base" is
    therefore too strong and should be softened before merge.
  - `rmEmptyMountDirs` returns nil for three different situations, including "could not
    even open Cwd" (chmod 000), so a real failure is indistinguishable from "not ours".
  - `provenWorkSpace` calls `filepath.Abs(j.ActualCwd)`, so a relative ActualCwd would
    resolve against the runner process's cwd. Nothing sets one today.

## Adversarial round 3 - it broke both of round 2's fixes

- [x] LAYER 27: **round 2's "the working dir must exist" check was never reached when
  the WORKSPACE was missing too.** `emptyWorkSpace` returns nil on `IsNotExist` before
  `cleanupWorkSpace` (and so before that check) ever runs, while `cleanupBelowCwd`
  still called `removeUpward` - which tolerates a missing leaf and then unlinks every
  empty parent up to Cwd. One extra non-existent path element defeated it, silently.
  Blast: any depth of empty user directories inside Cwd. Also a TOCTOU: the check
  lstats once, so `cwd` need only exist for an instant - a plain racing goroutine won
  66 of 300 runs.
- [x] LAYER 28: **round 2's mount-tidy-up guard was derived from an UNPROVEN
  ActualCwd.** `workSpace := filepath.Dir(j.ActualCwd)` applied none of the proofs
  cleanup uses, so with ActualCwd poisoned to Cwd - the v0.37.0|1 shape - the
  "workspace" became the PARENT of Cwd and every mount inside Cwd passed the guard.
  Round 2's fix was a total no-op in exactly the case this PR exists for.
- [x] LAYER 29: **the `createdCwdName` residual was far worse than recorded.** I wrote
  it up as "a user directory literally named cwd would still pass". Measured: with
  `Cwd/analysis/` owning a subdirectory called `cwd`, the whole of `Cwd/analysis` was
  destroyed - `run.R`, `README`, `results/final/out.tsv`, everything, not just empty
  dirs - and `analysis` itself unlinked, returning nil. That is the original incident
  verbatim, and `cwd/` is exactly what wr names things and a common staging
  convention. Recording a residual without measuring its blast radius understated it.

  **One fix for all three, replacing the guesswork with the one property that is
  actually checkable.** `mkHashedDir` always builds the working directory at exactly
  `mkHashedLevels + 2` components below Cwd - `<AppName>_cwd/c0/c1/c2/<MkdirTemp>/cwd`
  - so `relIsCreatedCwd` now requires that depth AND the `cwd` name, and both cleanup
  and `Job.Unmount`'s tidy-up go through it. Every attack path above is 2 to 4 deep
  and is refused. `Job.createdWorkSpace()` gives Unmount a workspace that has passed
  the same test, instead of a bare `filepath.Dir` of whatever was reported.
  - Depth is derived from `mkHashedLevels`, never written out, and
    `TestCreatedCwdDepthMatchesMkHashedDir` pins it against what `mkHashedDir` really
    produces. That test is the whole safety of this approach: a wrong depth would
    refuse every real workspace and silently stop all cleanup, which is worse than
    what it fixes. It passes, so the depth is right rather than merely plausible.
  - Still NOT a proof of origin, and the comment says so: a user directory at exactly
    that depth ending in `cwd` would pass. Proof of origin needs the `<AppName>_cwd`
    segment, and `AppName` is a package var set to "wr" only in `cmd/runner.go` - the
    manager keeps "jobqueue", and cleanup runs server-side too, so keying on it would
    refuse every real workspace exactly where the incident's cleanup ran.
  - Fixture cost, and why it is worth it: 17 workspace fixtures were 2-3 dirs deep,
    i.e. shapes wr cannot produce. They now go through `testWorkSpace()`, which builds
    the real depth. Fixtures that could not occur in production are a large part of
    why these holes survived three rounds of review.
  - Neuter (reduced to containment only, checked to COMPILE first) reddens 3
    assertions. Gates: `make test` `371 passed / 9 skipped`, `make race` `371 passed`,
    `make lint` `0 issues.`

- [ ] Recorded from round 3, not fixed:
  - A wr-created mount dir ABOVE the workspace (`Mount: "../../shared"`) is no longer
    tidied by either path, so it leaks. A leak, not a deletion.
  - `removeUpward`/`removeEmptyParents` use `Root.Remove`, which unlinks a plain file
    as readily as an empty dir. Only reachable in the post-`openChain` window, which
    the test seam sits before; the probe could not drive it through `Trigger`.
  - `Unmount` now returns an error where it used to swallow `errNotBelowBaseDir`, and
    the client folds that into the job's error. A job that sabotaged its own mount dir
    is reported as a job failure.

## Adversarial round 4

- [x] LAYER 30 (**a live-mount deletion, and the oldest bug found in any round**):
  `mountDirsToKeep` compared `filepath.Rel(j.ActualCwd, mount)` on RAW strings.
  `Job.Cwd` is stored exactly as the user typed it - `cmd/add.go` never normalises it,
  and it cannot, because Cwd feeds `Job.Key()` and so job identity - so a relative Cwd
  makes the ActualCwd built from it relative too, while an absolute
  `MountConfig.Mount` stays absolute. `filepath.Rel` fails when given one of each, the
  mount went unrecognised, `keepDirs` stayed empty, and `removeActualCwd` ran
  `RemoveAll("cwd")` straight through a mount that is still live - `Job.Unmount` runs
  AFTER cleanup. Measured: absolute Cwd keeps `[mnt]` and the mounted file survives;
  relative Cwd keeps `[]` and it is deleted.
  - The tell was in the file already: `entryLeadingTo`, which protects mounts OUTSIDE
    ActualCwd, does call `filepath.Abs` on both sides. The two mount protections
    disagreed, and the one guarding the more dangerous deletion was the one that did
    not normalise.
  - Fix: one shared `relBelowDir` that normalises both sides, used by both. Red is
    `TestCleanupWithRelativeCwd`, built with the REAL `mkHashedDir` rather than a
    hand-made path; the neuter (restoring the raw `filepath.Rel`, checked to compile)
    reddens it.
- [x] LAYER 31 (a defect in round 3's fix, again): `Job.createdWorkSpace()` applied the
  depth and name check and NOTHING else - no normalisation, no proof that the
  components are real dirs rather than symlinks, no requirement that the working
  directory is actually there. `rmEmptyMountDirs` therefore walked trees wr never
  created and removed the user's empty directories from them. It now makes the same
  proofs cleanup makes, via `isRealDirBelow`.
  - That stat is taken THROUGH the proven root handle rather than by absolute path -
    gosec's taint analysis flagged the absolute form (G703), and it was right to: the
    path comes from the runner, and a name resolved relative to a root cannot leave it.

- [x] Round 4 independently verified `createdCwdDepth` against a real `mkHashedDir`
  call, at three different `AppName` values: 6 every time. It also confirmed a wrong
  constant fails LOUDLY - mutating it reddens both the pin test and the
  workspace-is-cleaned regressions - which was the risk that made the depth approach
  worth checking before trusting.

- [ ] Recorded from round 4, not fixed:
  - `Cwd == "/"` is accepted, making the whole filesystem the containment boundary.
    Nothing validates that Cwd is absolute, non-root or non-empty. Reaching anything
    needs `/a/b/c/d/e/cwd` to exist.
  - The depth-6 residual, measured: a user tree at exactly that depth
    (`Cwd/projects/2024/batch/run/out/cwd`) loses 13 of 13 files, and the sweep is
    recursive and unbounded downward. The reported path is client-supplied, so
    anything that can speak to the manager as a runner chooses the target. Depth is a
    coincidence filter, not an origin proof, and the code says so.
  - `removeAllExcept` re-resolves multi-component relative paths without a per-level
    handle, unlike the `dirChain` descent. Everything inside ActualCwd is doomed
    anyway except the keep-dirs, so the reachable damage is "delete through a live
    mount you meant to keep", needing a nested mount plus a race won by the job's Cmd.
  - `AppName` is a mutable package global feeding the created path; one containing a
    separator would push every real workspace to depth 7 and silently disable cleanup.
  - Gates: `make test` `372 passed / 9 skipped`, `make race` `372 passed / 9 skipped`,
    `make lint` `0 issues.` One `make race` run failed and the re-run passed; the
    grep-captured re-run showed no failing test, so it was a flake, but the evidence
    was again not captured at the moment it happened.

## Adversarial round 5

- [x] LAYER 32 (**the worst finding of any round: deletion in a completely different
  directory tree**): nothing ever absolutises `Job.Cwd` - `cmd/add.go` stores `--cwd`
  verbatim - while `openBaseRoot` and `relBelowDir` resolve it with `filepath.Abs`,
  i.e. against THE CURRENT PROCESS's working directory. Cleanup runs in two
  processes: the runner, and the manager via `killLostJobAndTriggerBehaviours`. With
  a relative Cwd every containment proof still passes, but against the MANAGER's
  directory, so deletion lands on whatever sits at the same relative path beside it.
  The probe deleted a user's `proj/results/2026/runA/sampleX/align/scripts` next to
  the manager while the job's real workspace was somewhere else entirely, err nil.
  - Fix: cleanup and `createdWorkSpace` refuse unless BOTH `Cwd` and `ActualCwd` are
    absolute. `Job.Cwd` cannot be normalised at the source - it feeds `Job.Key()` and
    so job identity - so refusing is the available move, and it turns a deletion in
    the wrong tree into a leaked workspace and a loud error.
  - This also closes round 5's F5: a relative `ActualCwd` on an absolute-Cwd job is
    definitionally not one wr produced, and was being resolved against the process cwd
    and accepted.
- [x] LAYER 33 (round 4's fix, third call site): `rmEmptyMountDirs` still compared raw
  strings with `filepath.Rel(workSpace, mount)` while the other two call sites were
  converted to the normalising `relBelowDir`. Same defect, same commit, opposite
  direction: an absolute Mount with a relative Cwd was silently skipped and its dir
  leaked. Now uses `relBelowDir` like the others.
- [x] LAYER 34: **two guards had NO test coverage at all.** Round 5 mutated each one
  and the entire suite stayed green while a user's directory was deleted:
  `createdWorkSpace`'s `isRealDirBelow` (round 4) and `provenActualCwd`'s must-exist
  (round 3). `TestCleanupGuardsWithNoOtherCoverage` now covers both, and each was
  confirmed to redden under a COMPILING mutation. Adding a guard without a test that
  fails without it is how a later "simplification" removes it silently.

- [ ] Recorded from round 5, NOT fixed - all three are real, and two are data loss:
  - **An explicit mount cache inside the workspace is deleted before `Unmount` can
    upload it.** `keptWorkSpaceEntry` protects only names prefixed `.muxfys`, which
    covers muxfys's DEFAULT cache dir but not `MountTarget.CacheDir` (lands at
    `<workspace>/<name>`) or `MountConfig.CacheBase` (lands at
    `<ActualCwd>/<name>/.muxfys_cache*`). Cleanup runs BEFORE `Unmount`
    (client.go:2093 then :2100), so for a `Write: true, Cache: true` target this
    destroys the job's own not-yet-uploaded output. Both confirmed by the probe. NOT
    fixed here because the correct keep-set has to be derived from `resolveCacheDir`/
    `resolveCacheBase`, and getting that subtly wrong either leaks or keeps losing the
    data; it deserves tracing properly rather than a guess at the end of a long
    session. **This is the highest-priority item remaining.**
  - **A data race on `Job.ActualCwd`**, confirmed under `-race`: `Behaviour.cleanup`
    reads it unlocked while `applyLiveSnapshot` writes it under `j.Lock()`, and both
    are live in the manager at once. `createdWorkSpace`'s own doc says "must be called
    with at least an RLock held"; the cleanup path does not. Not fixed because the
    obvious fix - taking the lock across cleanup - would hold a lock across slow
    filesystem work, which DEVELOPERS.md section 2 forbids. The right shape is to
    snapshot the fields under the lock and have cleanup work from the snapshot, which
    is a real refactor of the whole path.
  - **Cleanup is not idempotent since round 3's must-exist check**: a `Cmd` that
    removes its own working directory, or the documented runner-then-server double
    cleanup with a sibling mount, now aborts the sweep and leaks the workspace
    including `tmp`. A leak, not a deletion, but a regression this PR introduced.
  - Gates: `make test` `373 passed / 9 skipped`, `make race` `373 passed / 9 skipped`,
    `make lint` `0 issues.`

## Round 5 holistic refactor - one resolution point instead of eight derivations

- [x] LAYER 35 (**the structural fix; this is why rounds 2, 3, 4 and 5 each found
  their bug inside the previous round's fix**). Safety was re-derived from the raw
  `j.Cwd` / `j.ActualCwd` strings at eight independent sites - `Behaviour.cleanup`,
  `provenWorkSpace`, `mountDirsToKeep`, `entryLeadingTo`, `Job.createdWorkSpace`,
  `Job.rmEmptyMountDirs`, `Job.mountPoints` and `mountState.resolveCacheDir` - and
  each derived them slightly differently. Some absolutised and some did not (LAYERs
  30, 33); some proved the components were real dirs and some did not (LAYER 31);
  some required the working dir to exist and some did not (LAYERs 27, 34). Patching
  one site at a time is what kept producing the next bug.
  - Fix: `Job.resolveWorkSpace()` in the new `jobqueue/workspace.go` is the ONLY
    place either field is read for a destructive purpose. It snapshots the Job's
    fields under its `RLock` and releases it; refuses a `CwdMatters` Job, a blank
    `ActualCwd`, and either path unless it is already absolute; cleans both and
    computes the relative path once; applies `relIsCreatedCwd`; opens `Cwd` as an
    `os.Root` and proves the workspace and the working dir through it with
    `realDirBelow`; and resolves and classifies every mount point AND cache
    location in the same breath. It returns a `*jobWorkSpace` carrying the root
    handle, the proven dirs and the keep-sets; `Behaviour.cleanup` and
    `Job.rmEmptyMountDirs` consume nothing else, so they cannot disagree.
  - Deleted, rather than left as alternative routes: `provenWorkSpace`,
    `cleanupBelowCwd`, `emptyWorkSpace`, `cleanupWorkSpace`, `provenActualCwd`,
    `mountDirsToKeep`, `mountedWorkSpaceEntries`, `keptWorkSpaceEntry`,
    `removeWorkSpaceExtras`, `Job.mountPoints`, `Job.createdWorkSpace`,
    `isRealDirBelow`, and `Job.Unmount`'s own `createdCwd() != ""` gate.
    `resolveCacheDir` was split so that `Job.Mount` and cleanup share one
    derivation of where a cache lands instead of having one each.
  - `keptEntry` no longer has a rule of its own for the working directory. It
    survives exactly when something at or inside it must survive, and that same
    thing names it as the workspace entry leading there - one rule, not two.
    Mutating the old clause reddened nothing, which is what a second way of saying
    the same thing looks like.
- [x] LAYER 36 (**data loss; round 5's highest-priority open item**): an explicit
  mount cache inside the workspace was deleted before `Unmount` could upload it.
  `keptWorkSpaceEntry` protected only names prefixed `.muxfys`, which is muxfys's
  DEFAULT cache dir, not `MountTarget.CacheDir` (lands at `<workspace>/<name>`) or
  `MountConfig.CacheBase` (lands at `<ActualCwd>/<name>`, with muxfys's own
  `.muxfys_cache*` inside it). Cleanup runs BEFORE `Unmount` (client.go:2093 then
  :2100) and muxfys only uploads at `Unmount`, so a `Write: true, Cache: true`
  target lost the job's own output. muxfys also deletes a cache dir it chose
  itself but NOT one it was given (its remote.go:52-58), which is why the explicit
  ones are the dangerous half.
  - Fix: the keep-set is derived in the resolution from `resolveCacheBase` and
    `resolveCacheDir` - the same functions `Job.Mount` uses to put the caches
    there - and each resolved location is classified as inside `ActualCwd`, an
    entry of the workspace, or outside. A `CacheBase` that resolves to the
    workspace is covered by the `.muxfys` name rule, which is kept for exactly
    that case; one that resolves to `ActualCwd` itself keeps `ActualCwd` whole,
    since there is then no way to delete the job's output without deleting the
    cache with it.
  - Proven both ways, as asked: `TestCleanupKeepsMountCaches` has a survival case
    for each of the four cache shapes AND three "keep too much" cases - a
    configured-nothing mount whose `<workspace>/mycache` is still deleted, a
    lookalike `mycache2` beside a kept `mycache`, and a `.muxfys_decoy` deleted for
    a Job that mounts nothing.
- [x] LAYER 37 (**the confirmed data race**): `Behaviour.cleanup` read `ActualCwd`
  unlocked while `applyLiveSnapshot` wrote it under `j.Lock()`, both live in the
  manager at once. Fixed by `Job.workSpaceSnapshot()`, which copies the five fields
  the resolution needs under `RLock` and releases it before any filesystem work -
  the shape DEVELOPERS.md section 2 requires, since the alternative would hold a
  lock across a directory walk. `Behaviour.run`'s read of the same field moved
  under the lock too (`Job.behaviourRunDir`). `TestCleanupActualCwdRace` races 50
  cleanups against 50 snapshots; removing the `RLock` makes it report a DATA RACE
  under `make race` and pass without it, so the guard is only visible there.
- [x] LAYER 38 (**idempotence, the regression round 3 introduced**): requiring the
  working dir to exist made a second cleanup abort and leak the workspace including
  `tmp`, for a `Cmd` that removed its own working directory or for the documented
  runner-then-server double cleanup with a sibling mount.
  - The rule is now stated once and applied consistently. A workspace wr can
    identify may be swept. One it cannot identify is refused, loudly. One that is
    not there at all is nothing to sweep, and only the empty-parent walk - which
    can delete no data - proceeds.
  - What makes "identify" possible without the working dir is `relIsJobCreatedCwd`:
    the path is compared against what `calculateHashedDir` + `os.MkdirTemp` would
    build from this Job's own `Key()`, which is proof of origin where depth and
    name are only a coincidence filter. The `<AppName>_cwd` component is
    deliberately exempt, since `AppName` is `"wr"` in the runner and `"jobqueue"`
    in the manager and keying on it would refuse every real workspace server-side.
  - It only ever ADDS tolerance; it never licenses deleting more than the
    depth-and-name check already allowed, so a future change to how `MkdirTemp`
    names things can leak a workspace but cannot destroy one.
  - `TestCleanupIsIdempotent` covers both reported cases with workspaces built by
    the real `mkHashedDir`, and `TestRelIsJobCreatedCwd` pins the recogniser
    against it the way `TestCreatedCwdDepthMatchesMkHashedDir` pins the depth.
- [x] Guard mutation check, run over every guard in the final code (27 mutations,
  each verified to still COMPILE with `go vet` before the suite was run, because a
  build failure reports zero failing assertions and looks exactly like a guard that
  does nothing). All 27 redden a test. Two mutations that were GREEN on the first
  pass were dealt with rather than waved through: `keptEntry`'s working-dir clause
  was genuinely dead and was deleted, and the `len(mounts) == 0` fast path now has
  the `.muxfys_decoy` test above. Two more were green only because the pair masked
  each other (`proveActualCwd`'s and `actualCwdNow`'s "must be a real dir"); both
  now have a case that reaches only one of them.
- [x] Existing regression tests: none weakened or deleted. One fixture was made
  DEEPER: `TestBehaviourCleanupSafety`'s "ActualCwd that leaves Cwd via a symlink"
  used `<Cwd>/escape/cwd`, which is refused on depth before the symlink is ever
  looked at, so it was not testing the symlink guard at all. It now uses a path at
  the depth wr really creates, and the symlinked component is what stops it.

- [ ] Recorded from the refactor, NOT fixed:
  - The origin proof is deliberately NOT mandatory. Making it so would close the
    recorded depth-6 residual outright, but `Job.Key()` is built from Cmd, mounts
    and image, so a `wr mod` between the run and the cleanup would stop every real
    workspace matching - and a mandatory check would then either silently disable
    cleanup or error on legitimate jobs. As an additional acceptor it can only
    tolerate more, never delete more.
  - Cleanup still runs BEFORE `Job.Unmount` (client.go:2093 then :2100), which is
    the whole reason the mount points and caches have to be protected at all.
    Reordering would remove the hazard class, but `run` behaviours are documented
    to run in the working dir with the mounts live, so it is a user-visible change
    and out of scope here.
  - `Job.mountBaseDirs` still reads `Cwd`/`ActualCwd`, but it is the mounting path,
    not a deletion path. It now shares `resolveMountPoint`, `resolveCacheBase` and
    `resolveCacheDir` with the resolution, so the two agree on WHERE by
    construction and differ only in `Mount`'s extra `onCwd` option.
  - `Job.Unmount`'s tidy-up still swallows a resolution refusal instead of
    reporting it, because an error returned there is folded into the job's own
    outcome and can bury it. It makes the same decision cleanup does; it just does
    not say why.
