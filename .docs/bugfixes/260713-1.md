- [x] Using the client pkg Scheduler SubmitJobs() method results in this in the manager logs: `t=2026-07-13T10:08:50+0100 lvl=crit msg="jobqueue server client handling panic" err="runtime error: invalid memory address or nil pointer dereference" stack="[clog.go:350 utils.go:258]"`
  - Updated `client/client.go` so all three Scheduler submission APIs apply the existing default requirements, override, queue, and avoid semantics to jobs whose requirements are nil.
  - Updated `jobqueue/serverCLI.go` to reject raw missing-requirements add payloads before persistence/scheduling, with public and encoded-request regressions in `client/client_test.go` and `jobqueue/client_payload_test.go` proving successful defaulting and typed `add: bad request` rejection.
- [x] The public Scheduler submission methods and manager request handling panic on nil job entries instead of rejecting malformed input.
  - Discovered while reproducing the reported nil-Requirements submission panic across public and encoded manager requests.
  - Updated `client/client.go` to validate complete batches before defaulting or forwarding, returning method- and index-specific typed bad-request errors without mutating or partially submitting mixed batches.
  - Updated `jobqueue/serverCLI.go` to reject raw nil entries before environment persistence/queue work, with public and encoded-request regressions in `client/client_test.go` and `jobqueue/client_payload_test.go`.
- [x] The manager panics on a submitted job containing a nil dependency entry instead of rejecting malformed input.
  - Reproduced after the nil-Requirements fix with `Dependencies{nil}`: the manager emitted the same critical panic and the client timed out; immediate dereferences occur in dependency preparation and lookup paths.
  - Updated `client/client.go` to reject exact `jobs[i].Dependencies[j]` paths before defaulting or forwarding, preserving atomic batches, valid nil/empty collections, and zero-valued dependency objects.
  - Updated `jobqueue/serverCLI.go` to reject raw malformed dependencies before persistence, with regressions covering all Scheduler methods, all six lower add APIs, manager availability, and typed bad-request diagnostics.
- [x] `TestServerWebISuspendedStatus` intermittently fails because suspended current counts/details do not arrive through the websocket within three seconds.
  - Discovered when `timeout 30m make test` failed in lane `jq_rest_extra` at `jobqueue/serverWebI_test.go:254`: `readJStateCounts(..., 3*time.Second)` returned false.
  - Updated `queue/queue.go` to enqueue transition callbacks at each mutation's lock-protected linearization point and drain them asynchronously in FIFO order, with safe worker restart after `runtime.Goexit`.
  - Added deterministic callback-order, concurrent Reserve/Bury, nonblocking, reentrant, and abnormal-worker-exit regressions in `queue/queue_test.go`; websocket stress, race checks, lint, and the full suite pass.
- [x] Is there an issue that makes it more likely for runners to enter zombie state and be considered "lost contact" by the manager? Especially when many thousands of runners are running at once, running cmds that only last a few seconds each.
  - No regression was found versus `v0.36.5`: touch remains 15s against a 60s TTR, lease renewal precedes optional live processing, only TTR expiry initiates Lost, zombie-process classification runs afterward, and child processes are still reaped with `Wait`.
  - Independent stress completed 2,000 simultaneous TLS runner lifecycles with live snapshots and a subscription without errors or Lost updates; a race variant, targeted lifecycle/classifier tests, lint, and the full suite pass. No code change or persistent test was appropriate because supported behavior is unchanged.
  - No bug was reproduced or fixed, so this item remains open under the bugfix checklist rules.
  - Re-investigated on 2026-07-13 on the HPC cluster with a real LSF manager using isolated state under `.tmp/agent/`, custom ports `49127`/`49128`, and 10,000 two-second jobs in repgroup `lsf_contact_stress_10000_20260713143157`. Evidence in `.tmp/agent/lsf-lost-contact-evidence.log` shows jobs added at `14:31:57`, LSF peaking at `6333` running `wrd_` jobs by `14:32:29`, and final status at `14:35:29` of `complete=10000`, `lost contact=0`, `buried=0`; current `bjobs -w` then reported no unfinished jobs after cleanup.
  - The first LSF run finished before the user checked `bjobs -w`, so a longer externally observable rerun was started, but the subagent failed immediately because the workspace hit its spend cap. That attempted longer rerun produced no evidence because it never started; no code change or regression test was made from that attempt.
  - Re-investigated again on 2026-07-14 on the HPC cluster with a real LSF manager using isolated state under `.tmp/agent/lsf-live-20260714093213/`, custom ports `49327`/`49328`, and two bounded 15,000-job variants. The `sleep 8` run peaked at `15000` LSF `wrd_` jobs and completed with `complete=15000`, `lost contact=0`, `buried=0`; the higher-churn `true` run peaked at `11991` LSF jobs and showed `lost contact=0` throughout. Manager logs and 12,159 runner logs were grep-scanned for lost/zombie/contact/error terms with no matches, the manager stopped cleanly, and final `bjobs -w` was empty. Evidence is in `.tmp/agent/lsf-live-20260714093213/evidence.log`. One `true` job buried before attempt because its working directory path was missing, which is separate from lost-contact/zombie behaviour. No code change or regression test was made.
  - Re-investigated on 2026-07-15 using the manager log clue `bad job (not in queue or correct sub-queue)` and read-only `results_frontend` reference. The reference generator batches many portal-style `Scheduler.NewJob`/`SubmitJobs` jobs under repgroups such as `compress`/`dedupe`, matching the high-pressure short-success workload. Root cause found: `handleArchive` applied `JobEndState` with `job.updateAfterExit()` before completion validation, so a TTR/lost callback could observe `Exited=true` and move/mark the item before `markJobComplete`; in the exact two-minute symptom case, the lost confirmer can also see the short-lived runner pid is gone and reclaim the job to ready/delay/bury before the delayed successful `jarchive` arrives, making that terminal update fail permanently with `ErrBadJob`.
  - Updated `jobqueue/serverCLI.go` so successful archive validation and terminal-state application happen together under the job lock, completion clears `Lost`, and the original successful terminal archive can override a lost-contact reclaim before another runner reserves the job; ordinary non-running jobs still reject. Added locked limit-group decrement reuse in `jobqueue/job.go`, updated the WebI archive helper in `jobqueue/serverWebI_test.go`, and added `TestMarkJobCompleteUsesEndStateAtomically` plus `TestSuccessfulArchiveOverridesLostReclaimBeforeRerun` in `jobqueue/server_test.go` to cover the atomic-completion path, the production `jarchive` lost-reclaim path, ordinary ready/delayed/buried rejection, and the post-rerun stale archive rejection. Reviewer narrowed `handleTouch` back to running-only lookup because recoverable lost touches remain in the run queue and `queue.Touch` rejects non-running items.
  - Validation: the new regressions failed first at the old `markJobComplete`/non-running contract, then passed after the fix; reviewer passing gates were `CGO_ENABLED=1 timeout 2m go test -tags netgo --count 1 ./jobqueue -run 'Test(MarkJobCompleteUsesEndStateAtomically|SuccessfulArchiveOverridesLostReclaimBeforeRerun)' -v`, the bounded prior-regression jobqueue set covering AddAndWait, subscription, Web UI high-pressure refresh, raw add validation, and modify validation, the public Scheduler malformed-input/defaulting tests in `./client`, `timeout 2m golangci-lint run ./jobqueue/...`, `gofmt`/`cleanorder` on edited Go files, and `git --no-pager diff --check`. Full `make test`, `make race`, `make lint`, and `make browser-test` were not run in review because the bounded checks targeted the touched package and prior checklist regressions under the requested short validation budget.
- [x] Is there any new path that could cause jobs to be automatically removed, without a user pressing a button in the status web UI or manually using the `wr remove` command?
  - No new unintended persisted-deletion path exists versus `v0.36.5`: deletion still requires an explicit API/CLI/REST/Web UI/LSF action or the longstanding configured `Behaviour{Do: Remove}` after burial; successful archive transactionally moves live jobs to complete.
  - Independent real-TLS stress archived 2,000 jobs and restarted the manager with all 2,000 completions intact and zero live/deleted jobs; targeted race tests, lint, and the full suite pass. The reported transient Web UI Deleted state is therefore a separate read-only projection/count regression.
  - Marked invalid on 2026-07-14: the jobs are not actually removed; CLI/backend status remains correct, and the remaining symptom is only the Web UI projection/count issue.
- [x] `TestServerWebI` intermittently closes the database while a websocket handler is still active, causing a `database not open` retrieval failure followed by a nil-pointer panic.
  - Discovered when `timeout 30m make test` failed in the `server_webi` lane during nil-Requirements review; a focused rerun passed, confirming teardown-lifetime intermittency rather than a Scheduler patch failure.
  - Updated `jobqueue/server.go` and `jobqueue/serverWebI.go` to register all five upgraded-websocket workers atomically with shutdown state and join them before closing BoltDB or resetting queue state.
  - Added a deterministic active-request shutdown regression in `jobqueue/serverWebI_test.go`; lifecycle stress, race checks, lint, and the full suite pass.
- [x] Another bug for the checklist, possibly related to and negating an earlier one: the web UI can show jobs as becoming deleted, and on refresh of the page those disappear, leaving a low count of completed while some jobs still run for a repgrp. But `wr status -i therepgrp -o s` shows (correctly) that they actually completed successfully. Sometimes the web UI can briefly show them as deleted again, then the deleted jobs disappear, still leaving a wrong completed count.
  - Reopened on 2026-07-14: latest code still reproduces under high update pressure, with Web UI-only Deleted/count symptoms while CLI/backend status remains correct.
  - Confirmed the root was the `v0.37.1` out-of-order queue callback projection already fixed by `69df5de`, not persisted deletion; successful archive marks Complete before queue removal.
  - Added a real server/WebSocket/CLI regression in `jobqueue/serverWebI_test.go` that fails on `v0.37.1` and proves stable 4→5 completed counts with one running and zero deleted across live update, refresh, and reconnect.
  - Extended and documented the completed-RepGroup browser fixture, wired it into `make browser-test`, and captured before/after traces showing four Deleted replaced by five Complete with no collateral visual regression.
  - Re-investigated on 2026-07-14 after the repeatable high-pressure report and read-only `results_frontend` reference. The real pattern is many successful jobs under portal-style RepGroups; the server aggregate/archive path stayed correct under 3001 concurrent completions, but the fresh Web UI seed still deliberately dropped known complete-only RepGroups from the previous removed-jobs-refresh fix. That made successful exact RepGroups disappear on browser refresh while CLI/backend summaries remained correct.
  - Updated `jobqueue/statusstate.go` so fresh Web UI subscribers retain known complete-only RepGroups, while still stripping `deleted` and omitting deleted-only or complete+deleted-only terminal RepGroups so removed jobs do not reappear. Updated `jobqueue/statusstate_test.go` seed semantics, extended `jobqueue/serverWebI_test.go` with a 3001-completion WebSocket refresh regression through the archive/remove path, and refreshed the removed-jobs browser fixture model/docs in `jobqueue/testdata/removed-jobs-refresh/screenshot.mjs` and `jobqueue/testdata/README.md`.
  - Focused gates pass: seed semantics and high-pressure Web UI regressions, neighboring status-state/WebI invariants, `cleanorder`, `golangci-lint run --fix ./jobqueue/...`, `node --check` for the touched browser fixture, reviewer reruns of the focused Go tests/lint/diff checks, and reviewer PASS.
- [x] Submitted jobs containing a nil behaviour entry are accepted and persisted, then panic when runner, status, or removal paths evaluate the behaviour.
  - Discovered while auditing the job's other exported slice-of-pointer collection during the nil-dependency fix; `Behaviours{nil}` is dereferenced by behaviour trigger, removal, and string paths.
  - Updated `client/client.go` and `jobqueue/serverCLI.go` to reject exact `jobs[i].Behaviours[j]` paths before defaulting, mutation, environment persistence, or queueing, returning typed bad-request errors atomically.
  - Added public and encoded-request regressions covering all Scheduler/lower add variants, downstream trigger/removal/string safety, manager availability, and preservation of nil/empty/zero/configured-Remove behaviour semantics.
- [x] Job modification accepts nil nested behaviour or dependency entries and panics the manager before persisting the modification.
  - Discovered while auditing downstream trust-boundary bypasses during the nil-behaviour submission fix; public `jobqueue.Client.Modify` reaches unchecked dereferences through `JobModifier.SetBehaviours(Behaviours{nil})` and `JobModifier.SetDependencies(Dependencies{nil})`.
  - Added one authoritative `JobModifier` validator used before public send, raw-manager/REST pause or lookup, and direct in-memory modification, returning typed `jmod` bad-request errors with exact nested paths.
  - Added `jobqueue/modify_validation_test.go` to reproduce both former panic sites and prove queue, Bolt/index, pause-state, manager-availability, valid-empty/zero-value, and subsequent-modification semantics remain atomic.
