- [x] `perl -e 'for (1..10000) { print "echo $_\n" }' | wr add -i echo`, viewed in the status website, has a weird “twitching” behaviour, where the bar the the status of echo keeps dropping to 0 and then back to correct count as jobs complete. It seems like the broadcast system replacement introduced in commit ac24a01a08622832793275f08c632d8acd317c19 is not suited to hight speed reliable updates. Consider an overhaul that is more like the 3rd party dependency used before? Or otherwise fix/find a solution that is fast and reliable, doesn’t block the server and doesn’t leave clients with lost messages. Or if they’re going to get lost, somehow know this and accumulate changes for the next broadcast. Strange this is even `seq 1 15000 | sed 's/^/echo /' | wr add -i bigmod -d neverappears` twitches, and that’s steady-state because the jobs don’t change state; there shouldn’t even be any broadcasts to make the web UI twitch. Is it a “catch up” firing on a timer in case messages were lost? That can’t be the solution to possibility of missed messages. Remove that and find a better solution.
  - Removed blind periodic status catch-up polling from the browser and replaced status overflow recovery with explicit `StatusResync` messages.
  - Serialized per-client status broadcast coalescing so a required resync marker cannot be silently lost under concurrent high-speed sends.
  - Staged current snapshots atomically until `SnapshotDone`, preventing visible repgroup rows from dropping to zero during partial snapshots, and added browser coverage for the 15,000-job steady-state twitch case.
- [x] Likely related to the previous point, completed repgrps dissapear from the web UI, when they should remain on the page.
  - Preserved already-observed completed repgroups across current snapshot pruning so completed rows remain visible without loading all historical completed-only groups.
  - Added a browser regression that drives the real status page through an empty resync snapshot and verifies the completed repgroup remains visible.
  - Verified `make browser-test`, `make test`, and `make lint`; a full `make race` run hit an unrelated live-jtouch timing assertion that passed on focused rerun.
- [x] The `wr status -h` text for the FIELD list is too wide, going beyond 80 cols.
  - Wrapped generated `WR_STATUS_FORMAT` FIELD aliases across multiple help lines while keeping them sourced from the shared field registry.
  - Added a rendered-help regression that checks FIELD-list lines stay at or under 80 columns.
  - Verified focused `cmd` tests, `make lint`, `make test`, and `make race`; transient unrelated jobqueue failures passed on rerun.
