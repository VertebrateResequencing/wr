- [x] `printf 'echo a\necho b\n' | wr add -i tabletest`Resulted in the status webpage showing 1 complete and 1 running job, with the running state stuck forever even though it really completes ~instantly. Refreshing the webUI and then searching for tabletest finds the 2 jobs as complete. Similar webUI issues happen when eg. removing a large number of jobs. Some show removed, the rest get stuck in their previous state.
  - Added authoritative websocket current-state snapshot markers and client-side snapshot pruning so dropped deltas cannot leave stale running or removed counts visible forever.
  - Added a focused websocket regression test plus real browser status-page repro/screenshot scripts under `jobqueue/testdata/status-page-stale-counts`.
  - Added `make browser-test`/`make webui-test` as an explicit browser UI gate, documented `jobqueue/testdata` conventions, and kept normal `make test`/`make race` separate from browser tests.
- [ ] `wr status -h` doesn’t list valid FIELD for WR_STATUS_FORMAT.
- [ ] `seq 1 15000 | sed 's/^/echo /' | wr add -i bigmod -d neverappears` resulted in `Added 14995 new commands (5 were duplicates)` instead of 15000 new commands with no duplicates. Reapeating the same add says `Added 0 new commands (15000 were duplicates)`, but `wr status -i bigmod -o c` says `dependent: 14995` and everything else 0. On a fresh manager this can all work as expected, so it’s a flake. The web UI also seems to mess up showing details of these dependent jobs, only showing Attempts 0 on the Status line without showing a status related to waiting on dep group or similar.
- [ ] ```
  printf '%s\n' 'perl -e '\''my $x = "A" x (200*1024*1024); exit 1'\''' > $TDIR/memcmd.txt
  wr add -f $TDIR/memcmd.txt -i memtest -m 10M
  wr status -i memtest -o d
  ```
  Shows `Previous problem: command exited non-zero` instead of the expected `command exited non-zero; note: command used too much RAM`.
- [ ] Suspended jobs in the web UI say “suspended - use wr resume to make it schedulable again”. Instead they should just say “suspended” under state and have a “Resume” button like for other buttons (all or 1, if it’s possible to resume a single job in a repgrp).
- [ ] Stopping the manager accumulates `WebSocket error: Unknown error` lines in the web UI. Starting the manager again (same token, ie. the manager was stopped by `kill -9` on its pid) does not clear the `Connection to the manager has been lost!` and websocket warnings, but does show new activity.
- [ ] ```
  printf '%s\n' 'bash -c '\''for i in $(seq 1 60); do echo "progress $i"; head -c 5000000 /dev/zero | wc -c >/dev/null; sleep 1; done'\''' > $TDIR/livecmd.txt
  wr add -f $TDIR/livecmd.txt -i livetest -t 5m -m 1G
  ```
  When watched on the web UI, after the first heartbeat stdout is displayed, but RAM does not update to show peak usage at the heartbeat, and `wr status` does not show the peak RAM or the STDOUT/ERR.
