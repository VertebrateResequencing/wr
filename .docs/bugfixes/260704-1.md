- [x] We're now on a new branch based on the merged-to-develop openstackfix branch. It also tried to fix some openstack issues. Use the same techniques as before to run all Openstack-related tests individually without skips and then run all tests overall to ensure everything still works, and fix any issues with the bugfix skill.

  Cause found: every OpenStack-named test function passed individually without skips, but an OpenStack-enabled `make test` still failed because the generated suite plan ran the three live OpenStack test functions concurrently across package lanes. Those tests share real cloud quota, security groups, keys, networks, and remote job execution, so parallel full-suite execution could interfere even though targeted runs were green.

  Fix:
  - When the live OpenStack environment is configured, serialize the three live tests: `TestOpenStack`, `TestOpenstack`, and `TestJobqueueWithOpenStack`.
  - Exclude those exact tests from their normal parallel lanes while keeping package coverage intact.
  - Teach `go-test` lanes to honor `RunPattern` and `SkipPattern`, so split `go test` lanes can run or skip exact test functions.
  - Add planner/runner regression tests for the serialized OpenStack plan and `go test` run/skip arguments.

  Verification before the final full-suite run:
  - All 18 OpenStack-related test functions passed individually without skips.
  - `go test -tags netgo --count 1 ./internal/testsuite` passed with dummy live OpenStack env.
  - `go test -tags netgo --count 1 ./internal/testsuite` passed with live OpenStack env vars unset.
  - `make lint GOLANGCI_LINT_ARGS=--new-from-rev=origin/master` passed.
  - `git diff --check -- internal/testsuite/planner.go internal/testsuite/runner.go internal/testsuite/planner_test.go` passed.

- [x] `TestJobqueueWithMounts` fails in an OpenStack-sourced full `make test` when stale `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY` variables are present.

  Cause found: `TestJobqueueWithMounts` documents `JOBQUEUE_REMOTES3_PATH` plus `~/.s3cfg` as its precondition, but the S3 config helper gives exported AWS credential variables precedence over `~/.s3cfg`. The OpenStack rc supplies the S3 target/path needed by the test but also exports stale AWS credentials, so the test talks to the expected object store with the wrong credentials.

  Targeted confirmation:
  - `source ~/.openstack.rc; unset AWS_ACCESS_KEY_ID AWS_SECRET_ACCESS_KEY; go test -p 1 -tags netgo -timeout 35m --count 1 -v ./jobqueue -run '^TestJobqueueWithMounts$'` passed.

  Fix:
  - Test-side only: `TestJobqueueWithMounts` now unsets `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` while it runs, restores their previous state afterwards, and filters those two variables from the saved job environment passed to S3 mount jobs so the documented `~/.s3cfg` credentials are used.

  Verification:
  - `go test -p 1 -tags netgo -timeout 5m --count 1 -v ./jobqueue -run '^TestEnvWithoutAWSCredentials$'` passed.
  - `source ~/.openstack.rc; timeout 35m go test -p 1 -tags netgo -timeout 35m --count 1 -v ./jobqueue -run '^TestJobqueueWithMounts$'` passed.

- [x] `TestJobqueueWithMounts` passes on its own, but stalls in the OpenStack-sourced full `make test` when run in the normal parallel `jq_default` lane with live S3/muxfys configuration.

  Cause found: the live S3/muxfys mount test was still running in the parallel `jq_default` lane whenever `JOBQUEUE_REMOTES3_PATH` and `~/.s3cfg` were available. In the full OpenStack-sourced suite it could leave a `fuse.MuxFys` mount pinned while sibling jobqueue lanes were active, even though the same test passed when run alone.

  Fix:
  - When the live S3 mount-test prerequisites are present, move `TestJobqueueWithMounts` to its own serial `jobqueue_mounts` lane.
  - Exclude that exact test from `jq_default` only in that live-S3 configuration, preserving the normal plan when S3 prerequisites are absent.
  - Add planner coverage proving the S3 mount lane stays covered and the normal plan is unchanged without live S3 setup.

  Verification before the final full-suite run:
  - `go test -tags netgo --count 1 ./internal/testsuite` passed.
  - `go test -tags netgo --count 1 ./internal/testsuite -run TestPlannerSerializesLiveS3MountTestsWhenConfigured -v` passed.
  - Reviewer reran `go test -tags netgo --count 1 ./internal/testsuite`, `golangci-lint run --new-from-rev=origin/master ./internal/testsuite`, and diff checks successfully.
