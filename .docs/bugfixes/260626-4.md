- [x] CI subprocess-heavy tests flake under contention on PR #533 (after 260626-3's recv-deadline fix cleared the RPC `'receive time out'` class). On the 4-core ubuntu-latest runner the suite's ~7 concurrent lanes oversubscribe CPU and starve the runner subprocesses these tests spawn, so a different contention-sensitive test fails each run: `TestJobqueueSignal` (jobqueue_test.go:1699, the all-cores job's 60s `waitUntilFileExists` marker wait returns false) and `TestJobqueueRunnerKillRequests` (runner_lifecycle_test.go:461; the runner subprocess exits status 1 under its short `--rtimeout`). develop is green, so this branch's heavier suite tipped the balance. Per maintainer decision, HARDEN the affected tests to tolerate contention (the 260625-2 approach: generous bounded marker-wait/polling and runner timeouts that are free on the success path), WITHOUT changing suite parallelism and WITHOUT changing what any test proves. Audit the whole subprocess-test family up front (don't whack-a-mole one CI run at a time).
  - Introduced a shared `runnerStartWait = 3 * time.Minute` (free on the success path: every waiter polls and returns the instant its condition holds, so a generous bound only helps the contended case) and converted the whole server-spawned-runner wait family to it: `waitUntilFileExists` (signature simplified to use it), the `pollUntil`->`pollUntilFor(runnerStartWait, ...)` runner-completion waits in `runner_lifecycle_test.go`, `waitUntilNoRunners`, the `waitUntilJobState(...)` recover/lost/cmd3 waits in `TestJobqueueSignal`, and the `runner_scheduling_test.go` batch waits.
  - Fixed the real blocker a first pass missed: `runServer`'s hard 20s daemon self-stop watchdog was killing the test server (and its runners) before a contended test could finish; raised it to `daemonBackstopWait = 3 * runnerStartWait`, preserving the leaked-daemon backstop (the normal path stops the server itself first).
  - Set `runServer`'s `serverConfig.Timings.CheckRunnerTime = 1s` so the test server re-spawns a runner that missed its 1s reserve within ~1s instead of the default ~1 minute (production default unchanged). Removed a temporary `WR_DEBUG_SERVER_LOG` debug aid.
  - Confirmed via diff that the emission-chokepoint commit (6d104da) does NOT touch `SetReadyAddedCallback`/the RAC layer/runner-spawning, so the remaining `TestJobqueueSignal` failure is contention sensitivity of its `Cores: NumCPU()` all-cores job (it can't start unless the host has every core free), NOT a code regression on this branch. Verified by implementor + reviewer: `make lint` 0 issues, `go build`/`go vet` clean, `TestJobqueueRunnerKillRequests`/`TestJobqueueRunnerLostJobs` pass locally.
  - Does NOT by itself make CI green: `TestJobqueueSignal` additionally needs decoupling from real host load (its all-cores job is unverifiable under any parallel-test contention) — tracked separately in `260626-5.md`.
