- [x] `make race` on CI (PR #533) flakes on `TestClientAddAndWait` ("AddAndWait ignores a lost update and waits for the later terminal event", jobqueue/subscription_test.go:362) with `'jstart(...): bad job (not in queue or correct sub-queue)'` — the 260625-4 signature: under the race detector + the contended 4-core CI runner, the server reclaims a job (tight TTR) while a runner-client is mid `reserve -> start (jstart) -> archive`. The subscription tests use `applySubscriptionTimings` with `ItemTTR = 50ms`; the lost-update test intentionally lets that TTR expire during a 200ms sleep to simulate a lost update, but the same 50ms TTR also races the `reserve -> jstart` setup under contention so jstart fails before the intended sleep. (This surfaced only now that 260626-3/4/5 cleared the earlier make-test failures and CI reached make race.) Per the maintainer's harden-the-tests choice and the 260625-4 precedent: make the tight-TTR job-lifecycle tests contention-robust while PRESERVING each test's intent (lost-update induction, deadline behaviour, etc.) — e.g. raise the TTR to a value that can't race the reserve->jstart setup and scale the dependent sleeps/deadlines so the lost/deadline behaviour is still induced. Audit the whole tight-timing job-lifecycle test family up front (subscription tests + any other tests running real reserve/start/archive/touch under a tight ItemTTR/TouchInterval), not just this one.
  - Root cause confirmed: a job's TTR countdown starts at Reserve (not at jstart/Started), so the whole reserve-response + Started round-trip must fit inside the TTR; the subscription tests' 50-200ms TTRs raced that setup under the race detector on the contended CI runner, so the server reclaimed the job mid-setup and jstart failed with "bad job".
  - Fix (test-side only): added named constants `subscriptionSafeItemTTR = 5s` (lifecycle tests) and `subscriptionLostItemTTR = 1s` (tests that intentionally induce a lost/reclaimed state), and raised every sub-second `applySubscriptionTimings`/`SetItemTTR` site (in subscription_test.go, statusstate_invariant_test.go, rest_test.go) to them, scaling dependent sleeps/deadlines. The lost-update test now uses `1s TTR < lostStallSleep 1.5s < waitCtx 5s` (self-documented) so it STILL induces the lost update before the terminal archive; the deadline test is driven by its 2s wait deadline (the 5s TTR never fires within it), unchanged.
  - Audited all 15 `applySubscriptionTimings` sites (no sub-second literal remains) plus the invariant + REST lost paths; the two remaining sub-second TTRs (jobqueue_test.go:4711, the signal daemon at :1242) are a different class (real `Execute` auto-touch, where the TTR/touch interplay is the subject) and correctly left unchanged.
  - No assertions/test-names/skips/logic changed (mechanically confirmed). Verified by implementor + reviewer: `go build`/`go vet`/`make lint` clean; `TestClientAddAndWait` passes 3/3 under `-race` (~9s); broader `-race` subscription suite + REST/invariant pass.
