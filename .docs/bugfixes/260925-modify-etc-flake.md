- [x] Flake: TestJobqueueModify (lane modify_a) failed in GitHub CI on PR #618 (head 9a62c2f1, 2026-09-25) at jobqueue/jobqueue_test.go:6924: `Expected: time.Duration(1000000000) Actual: time.Duration(0)`, for `So(stats.ETC, ShouldEqual, job.Requirements.Time)` straight after `jq.Started`.
  - Cause: GetServerStats (jobqueue/server.go:~4939) computes `ETC = etc.Truncate(time.Second).Sub(now.Truncate(time.Second))`, with etc = StartTime + Requirements.Time (1s). If a whole-second boundary falls between StartTime and the stats call, which is likely on a loaded runner and possible even 0.1s apart (a start at x.95s, stats at x+1.02s), both truncate to the same second and ETC is 0. The product's rounding is fine for a display figure; the test assumes no boundary is crossed.
  - Red: needs a deterministic form, for example controlling the times or asserting against the value computed from job.StartTime and the stats call's own now.
  - Red (before): a temporary seam in the test waited until the clock was past x.9s, called `jq.Started`, then slept until x+1.02s before `GetServerStats`. It failed 3 of 3 with `Expected: time.Duration(1000000000) Actual: time.Duration(0)`. With the fix and the same seam it passed 3 of 3. The seam is not committed.
  - Fixed in the test only; the product's whole-second figure is fine for status and web output. The assertion now reads the server's own copy of the job (`server.q.Get`), takes its StartTime + Requirements.Time truncated to the second, and brackets `GetServerStats` with two clock reads. ETC must equal the value computed from one of them, so a boundary crossed during the call is allowed and a boundary between Started and the call is computed exactly. With GetServerStats mutated to return an ETC of 0, the test fails (`Expected '0s' to be in the container`).
  - Other uses: the only other `stats.ETC` assertion (jobqueue_test.go:6915) expects 0 with no running job, where ETC is always 0.
  - Gates: `make lint` 0 issues. `CGO_ENABLED=0 go test -tags netgo -count=1 ./jobqueue/ -run TestJobqueueModify` passed. The first `make test` failed only TestJobqueueRunnerKillRequests (runner_lifecycle_test.go:557), the known kill-before-start product bug tracked on fix-kill-request-lost; the re-run passed (703 passed, 20 skipped). `CGO_ENABLED=1 make race` passed (703 passed, 19 skipped).
