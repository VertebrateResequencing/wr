# Bugfixes 260906-3

- [x] wr pins `github.com/VertebrateResequencing/muxfys/v5 v5.0.0`, which loses
  data: `muxfys.Unmount()` uploads the cache and then deletes the cache
  directory without checking whether the upload succeeded. For a writable
  mount in CacheData mode that cache holds the only copy of everything the job
  wrote, so a failed upload destroyed the job's output. muxfys v5.1.0 fixes
  it: the cache directory that still holds un-uploaded files is kept, and the
  returned error names it. Update the dependency to v5.1.0 so wr users get the
  fix.
  - Not a bug in wr's own code, so there is no failing wr test to make pass.
    The red state is the pin itself:

    ```
    $ grep muxfys go.mod
        github.com/VertebrateResequencing/muxfys/v5 v5.0.0
    ```

  - v5.1.0 also changes the shape of the unmount error, which wr matches on at
    `jobqueue/client.go:2595`. muxfys now builds the error with `errors.Join`
    (separator `\n`, not `"; "`) and appends a second line naming the kept
    cache. The compatibility probe below reproduces the exact v5.1.0 error
    (verbatim from muxfys `muxfys.go:487-492`, asserted by its own
    `muxfys_test.go:535`), pushes it through wr's real wrapping chain
    (`jobqueue/job.go:1305` -> `multierror.Append` -> `job.go:1285` ->
    `client.go:1776`), and checks the substring test still fires:

    ```
    muxfys raw:
    failed to upload 1 files
    the un-uploaded data has been left in the cache directory /tmp/ws/.muxfys.abc123
    ---
    wr job.Unmount err:
    Unmount failure(s): 1 error occurred:
        * failed to upload 1 files
    the un-uploaded data has been left in the cache directory /tmp/ws/.muxfys.abc123

    ---
    strings.Contains(err, "failed to upload") = true
    ```

    `strings.Contains` is not line-based, so the join separator and the extra
    line are both irrelevant to it. wr's unmount error was already multi-line
    before the bump anyway, because `hashicorp/multierror`'s default formatter
    emits `N errors occurred:\n\t* ...`.
  - Fixed by `go get github.com/VertebrateResequencing/muxfys/v5@v5.1.0` then
    `go mod tidy`. `go.mod` and `go.sum` only; no source change was needed or
    made. The transitive moves the bump pulls in are go-fuse v2.10.1 ->
    v2.11.0, minio-go v7.2.0 -> v7.3.0, klauspost/compress v1.18.6 -> v1.20.0,
    klauspost/cpuid v2.3.0 -> v2.4.0, gopherjs v1.20.2 -> v1.21.0, yaml/v3
    v3.0.4 -> v3.0.5, and x/crypto, x/net, x/sys, x/term, x/text.
  - Compatibility audit of the changed error shape, all unaffected:
    - `jobqueue/client.go:2595` is the only production site in the repo that
      matches muxfys error text. It uses `strings.Contains`, which is not
      line-based, so it still fires (probe output above). Nothing anywhere
      applies `regexp`, `strings.Split(s, "\n")`, `strings.Fields` or
      `HasPrefix` to a mount or unmount error.
    - Nothing truncates or single-lines the longer error. The 4096-byte
      `prefixSuffixSaver` (`jobqueue/client.go:159`, `jobqueue/utils.go:455`)
      caps the command's own output, and `appendExecProblems`
      (`jobqueue/client.go:894`) appends after that cap. `stdFilter`
      (`jobqueue/utils.go:595`) splits on `\r` only, so `\n`-joined lines
      survive. `fitStatusTableValue` (`cmd/status_table.go:337`) only ever
      truncates the `FailReason` constant.
    - `jobqueue/exec_outcome_test.go:125`'s `errTestUploadFailed` is a
      hand-written `errors.New` fixture used for identity assertions
      (`ShouldEqual`), never a real muxfys error, so it does not break. Every
      test that calls a real `job.Unmount()` asserts only nil-ness or logs the
      error.
    - wr's cleanup already keeps the cache directory muxfys now preserves:
      `keptDirs` (`jobqueue/workspace.go:661-666`) keeps mount caches
      "unconditionally, so a caller may clean up on either side of
      Job.Unmount", and `keptEntry` (`jobqueue/workspace.go:970-979`)
      recognises the `.muxfys` prefix.
    - go-fuse v2.11.0 does reach wr's `go.mod`. wr uses go-fuse only in tests
      (`jobqueue/utils_test.go:48`, `jobqueue/workspace_test.go:43-44`,
      `jobqueue/deletion_probes_test.go:55`) and already uses
      `NodeWrapChilder` rather than the deprecated `LoopbackRoot.NewNode`, so
      whole-package staticcheck reports no `SA1019`.
    - wr uses no extended attributes anywhere, so go-fuse v2.11.0's new
      `ENOSYS` for setxattr/removexattr on a mount affects only what a user's
      own job command does inside a mount.
  - Gates from `/home/ubuntu/wr_race`, all green:

    ```
    $ timeout 40m make test
      619 passed · 13 skipped · 29 packages · 7m26s
      PASSED
    $ timeout 60m make race
      619 passed · 13 skipped · 29 packages · 9m47s
      PASSED          (grep -c "DATA RACE" = 0)
    $ timeout 15m make lint
    0 issues.
    $ go mod tidy -diff
                      (no output, exit 0)
    ```

    FUSE mount count was 1 before and 1 after both suites.

- [x] Four documentation statements in wr described muxfys v5.0.0's
  delete-the-cache-regardless behaviour, and the bump above makes them false.
  muxfys v5.1.0 keeps the cache directory holding files that failed to upload
  and names its path in the returned error, so a reader who trusted these
  statements would conclude the data was gone when it is not.
  - No red command exists: nothing wr does is wrong, only what wr says about
    what muxfys does. The statements were checked against muxfys v5.1.0's
    source in the module cache rather than against a test:
    - `muxfys.go:460-466` (`Unmount` doc): "If a remote was not configured with
      a specific CacheDir but CacheData was true, the CacheDir will be deleted.
      The exception is when files fail to upload: their only copy is the one in
      the CacheDir, so that CacheDir is kept and the returned error says where
      it is, leaving you to recover or discard the data yourself."
    - `muxfys.go:484-499` (the code): `keepCache = fs.unuploadedCache()`, then
      `err = errors.Join(err, fmt.Errorf("%w %s", errKeptCache, keepCache.cacheDir))`,
      then the deletion loop's guard `if remote.cacheIsTmp && remote != keepCache`.
      v5.0.0's loop guard was `if remote.cacheIsTmp` alone.
    - `muxfys.go:164-167`: `errKeptCache = errors.New("the un-uploaded data has
      been left in the cache directory")`, documented as "followed by the path
      of the cache directory that was kept because it holds their only copy".
    - `muxfys.go:528-541` (`unuploadedCache`): returns `fs.writeRemote` when it
      has `cacheData` and `fs.createdFiles` is non-empty, else nil - so the
      keeping is exactly the writable CacheData case that lost data before.
    - muxfys `CHANGELOG.md` 5.1.0, Fixed: "Unmount() no longer deletes a
      temporary cache directory that holds files which failed to upload; the
      returned error now says where that directory is."
  - `jobqueue/client.go:2596`: the `strings.Contains(unmountErr.Error(),
    "failed to upload")` branch said the un-uploaded files "only existed in the
    mount's cache, which is deleted at unmount regardless, so the job must be
    redone". The cache is now precisely what is NOT deleted. The BEHAVIOUR is
    still right - wr cannot finish the upload and the job's output only counts
    once it is on the remote - so only the justification changed, to say that
    muxfys keeps the directory and names it in the error. A future reader
    deciding whether recovery is possible is no longer told the data is gone.
  - `cmd/mount.go:206` (`wr mount` help), `jobqueue/mount.go:145`
    (`MountTarget.CacheDir`, wr's public API) and `cmd/add.go:254` (`wr add`
    help, found by grepping the claim rather than briefed) all promised
    deletion on unmount. Each now carries the exception. `cmd/add.go` already
    had one exception - "a cache directory you named yourself, which unmounting
    deliberately leaves in place" - so the fix there was to widen it to cover
    the second kind of kept cache, not to add a new sentence.
  - Sites grepped and found already correct, left alone:
    - `jobqueue/workspace.go:71-75` (`muxfysCachePrefix`): mount caches "hold a
      writable mount's output until Unmount uploads it, so cleanup keeps them
      unconditionally rather than judging whether Unmount has already run".
      True before and truer now.
    - `jobqueue/workspace.go:661-666` and `:786-787`: same unconditional-keep
      account of caches; `:787`'s "a workspace left behind is recoverable,
      output deleted before Unmount uploaded it is not" is unaffected.
    - `jobqueue/client.go:1859-1863` (`cleanupUnstartedWorkSpace`): "a failed
      unmount deletes nothing at all" - still exactly what the code does.
    - `jobqueue/job.go:1265-1276` (`Job.Unmount` doc): "error could contain the
      string 'failed to upload', which you may want to check for" - still true
      under v5.1.0, which appends to that error rather than replacing it. No
      false claim, so not touched.
    - `cmd/mount.go:140-143` (`CacheBase` help) and `jobqueue/mount.go:65-70`
      (`MountConfig.CacheBase`): describe where caches are made, not when they
      are deleted.
    - Older checklists under `.docs/` quote v5.0.0's `// delete any cachedirs
      we created` (eg. `260903-1.md:511`, `260902-6.md:40`). They are dated
      records of what was true then, so they stay as written.
  - `jobqueue/exec_outcome_test.go:125`'s `errTestUploadFailed` was left
    unchanged. It is one of five one-line `errors.New` stand-ins ("cleanup
    failed", "disk full", ...) feeding `combineExecOutcomes`, and every
    assertion on it is `So(final.myerr, ShouldEqual, errTestUploadFailed)` -
    pointer identity. No test parses its text, so appending muxfys's kept-cache
    line would not change what a single assertion proves. It would lengthen one
    fixture out of step with its four siblings and create a second place that
    has to track muxfys's wording, which is the coupling the audit above
    established does not exist. Churn, not improvement.
  - Scope, proved mechanically rather than by eye:
    - A `go/scanner` token-stream comparison of each changed file against
      `HEAD`, with comments excluded, reports every differing code token:

      ```
      cmd/add.go: differing token #266 kind=STRING at cmd/add.go:142:8
      cmd/add.go: 4424 code tokens, 1 differ (comments excluded by scanner)
      cmd/mount.go: differing token #82 kind=STRING at cmd/mount.go:79:8
      cmd/mount.go: 1369 code tokens, 1 differ (comments excluded by scanner)
      jobqueue/client.go: 15853 code tokens, 0 differ
      jobqueue/mount.go: 608 code tokens, 0 differ
      ```

      The two Go files whose comments changed differ in zero code tokens. The
      two `cmd` files differ in exactly one token each, and both are the
      `Long:` help-text raw string literal of a `cobra.Command`
      (`cmd/add.go:142`, `cmd/mount.go:79`). No executable line changed.
    - `go.mod` and `go.sum` were not touched: `git diff develop..HEAD -- go.mod
      go.sum | md5sum` is `c6e91648c2f55adfe4d43d1b33394f00` both before and
      after the edit, and `git diff --stat -- go.mod go.sum` is empty.
  - Gates from `/home/ubuntu/wr_race`:

    ```
    $ timeout 15m make lint
    0 issues.
    $ timeout 40m make test
      619 passed · 13 skipped · 29 packages · 6m25s
      PASSED
    ```

    `make race` was not re-run: a comment-and-help-text change cannot introduce
    a race, and it was already green on this tree (619 passed, 9m47s, zero
    DATA RACE).

- [x] muxfys v5.1.0 keeps the cache directory holding files that failed to
  upload instead of deleting it. For muxfys as a library that is right, but for
  wr it is not: when an upload fails wr releases the job and it is retried from
  scratch, and a buried job the user fixes and retries is redone from scratch
  too, so the kept files have no value and are left on the node's disk for
  nobody. wr must delete the kept cache after a failed unmount.
  - Red command, from `/home/ubuntu/wr_race` with a throwaway harness
    (`jobqueue/zzred_test.go`, since deleted) that drives the real
    `Client.Execute` unmount-failure branch: a job with a writable cached
    mount whose S3 endpoint is a local `httptest` server that answers every
    PUT with `403 AccessDenied`, so `muxfys.Unmount` really does fail to
    upload and really does keep its `.muxfys_cache*` directory.

    ```
    $ CGO_ENABLED=1 go test -tags netgo --count 1 ./jobqueue/ -v \
        -run '^TestZZRedUploadFailureCache$'
    === RUN   TestZZRedUploadFailureCache

      When a job's output fails to upload, wr deletes the cache muxfys kept ✔✔✔✔✔✔✔✘

    Failures:

      * /home/ubuntu/wr_race/jobqueue/zzred_test.go
      Line 140:
      Expected [.muxfys_cache1417915869] to be empty (but it wasn't)!

      8 total assertions

      A successful job's cache survives cleanup, so its output reaches the remote ✔✔✔✔✔✔✔

      15 total assertions

    --- FAIL: TestZZRedUploadFailureCache (0.15s)
    FAIL
    ```

    Reproduction rate 10/10 (ten consecutive runs, all exit 1). 0.15s per run.
    FUSE mount count 1 before and after.
  - The harness asserts BOTH directions, because getting this wrong destroys
    data. The failing direction is the one above. The opposite direction - a
    SUCCESSFUL job's cache must survive cleanup up to the point unmount uploads
    it - is asserted by the same fake S3 accepting the PUT and the test
    checking the uploaded body is what the command wrote: cleanup runs BEFORE
    unmount (`jobqueue/client.go:2588` then `:2593`), so had cleanup deleted
    the cache the upload would have had nothing to read. That direction passes
    both before and after the fix, which is the point of it.
  - Where the cache actually is, established without reading muxfys's error
    text: for a Job with a wr-created working directory `Job.mountBaseDirs`
    (`jobqueue/job.go`) sets `defaultCacheBase = filepath.Dir(actualCwd)` - the
    WORKSPACE - and muxfys makes `.muxfys_cache<random>` directly inside the
    CacheBase it was given (`ioutil.TempDir(cacheBase, ".muxfys_cache")`, its
    `remote.go:190`). An observed path from the red run:
    `.../jobqueue_cwd/2/e/f/<hash>-<uuid>/.muxfys_cache4068755247/127.0.0.1:42295/bucket/pfx/out.txt`,
    where `<hash>-<uuid>/` is the workspace and `<hash>-<uuid>/cwd` the working
    directory. wr already knew all of this: `muxfysCachePrefix` and
    `keptDirs.muxfysNamesWorkSpaceEntry` (`jobqueue/workspace.go`) are the two
    facts the deletion is built from, and the error text is never read. It
    could not be read safely anyway: muxfys names the directory via the
    UNEXPORTED `errKeptCache` (its `muxfys.go:167`), so `errors.Is`/`errors.As`
    are unavailable, and scraping the message would create exactly the coupling
    to muxfys's wording that the audit in the item above established wr does
    not have.
  - Fixed in three places, all on the failure path only:
    - `jobqueue/workspace.go`: `keptEntry`'s prefix test extracted into
      `muxfysNamedCache`, so the ONE rule answers both callers - cleanup keeps
      such a cache, this deletion removes it - and a second spelling cannot
      drift. New `removeMuxfysCaches` and `removeMuxfysCacheEntries` beside
      `removeTmp`, taking the same route it does: `ws.proven.openChain()`,
      `chain.sweptWorkSpace()`, each entry named to that handle by its own name
      alone, `removeAllGuarded` per entry. No `os.RemoveAll` on a path string,
      and no path component resolved a second time.
    - `jobqueue/job.go`: `Job.rmMuxfysCaches` beside `rmEmptyMountDirs`,
      resolving through the same `j.workSpaceSnapshot().resolveWorkSpace()`. It
      reports a refusal rather than swallowing one, unlike `rmEmptyMountDirs`,
      because it is on no Job's success path.
    - `jobqueue/client.go`: called inside the
      `strings.Contains(unmountErr.Error(), "failed to upload")` branch, and
      its error folded into the existing `myerr` accumulation.
  - Why it cannot delete on the success path, which is the whole danger here,
    since cleanup runs BEFORE unmount (`client.go:2588` `TriggerBehaviours`,
    `:2593` `Unmount`) and those caches ARE a writable mount's output until
    Unmount uploads them:
    - The only production caller is the `"failed to upload"` branch, after
      `Unmount` has returned and said the upload failed. Both
      `TriggerBehaviours` sites are untouched, including the server-error one
      at `:2684`.
    - Cleanup's keep set is unchanged. `keptEntry` went from
      `if A { return true }; return B` to `A || B` with `B` extracted verbatim
      into a pure function, so cleanup still keeps every `.muxfys*` entry it
      kept before. `strings.HasPrefix(name, muxfysCachePrefix)` occurs in
      exactly one production place.
    - The test asserts it rather than arguing it: see the success direction
      below.
  - A user-named `MountTarget.CacheDir` is NOT deleted, deliberately. muxfys's
    `cacheIsTmp` is false for one, so muxfys never deleted it and v5.1.0
    changed nothing about it - there is nothing for wr to undo. The user chose
    that location, `cmd/add.go`'s help promises it survives, and it can live
    outside the workspace or be shared between Jobs. Three gates enforce it:
    `removeMuxfysCaches` returns early on `ws.keep.wholeWorkSpace` (set when a
    CacheDir IS the workspace, whose un-uploaded entries are named after the
    remote and cannot be enumerated) and unless
    `ws.keep.muxfysNamesWorkSpaceEntry` (so a Job whose mounts put no
    muxfys-chosen cache in the workspace is untouched, including one with no
    mounts whose own Cmd made `../.muxfyssquat`), and
    `removeMuxfysCacheEntries` skips any entry `ws.keep.workSpaceEntries`
    claims, which is how a user-named CacheDir called `.muxfysmine` survives.
  - Both directions are asserted, because getting this wrong destroys data:
    - `TestUploadFailureCacheDeletion`
      (`jobqueue/upload_failure_cache_test.go`) is the red command, now green:
      the failed-upload case asserts the muxfys-named cache in the workspace is
      GONE, and the success case asserts the uploaded body is what the Job's
      Cmd wrote. The second is the survival proof: cleanup runs before unmount,
      so a cache deleted during cleanup would leave the upload nothing to read.
      Both directions pass, and only the failure direction is load-bearing on
      the fix (see the mutation below).
    - `TestRmMuxfysCachesKeepsWhatItMustNotDelete`
      (`jobqueue/workspace_test.go`) pins the five refusals directly: a
      user-named CacheDir, one named with the muxfys prefix, a `.muxfys`-named
      squat in the workspace of a Job that mounts nothing, a CacheDir that IS
      the workspace, and a CwdMatters Job (whose cache base is the user's own
      Cwd, which wr never made).
  - Test hermeticity. The test drives the real `Client.Execute` - mount, cmd,
    behaviours, unmount - with a real muxfys FUSE mount whose S3 endpoint is a
    local `httptest` server, reached by writing an s3 config under a profile
    name no real config file carries (`wr-test-fake-s3`) and naming it in
    `MountTarget.Profile`. A `[default]` section was not enough: muxfys does
    `ini.LooseLoad(~/.s3cfg, $AWS_SHARED_CREDENTIALS_FILE, ~/.aws/credentials,
    $AWS_CONFIG_FILE, ~/.aws/config)` (its `s3.go:136`) and later sources win,
    so `~/.aws/config` on a developer's box would have overridden the fake and
    sent the test at a real endpoint. `S3ConfigFromEnvironment` errors when a
    NAMED profile's section is missing, so no real config file can supply or
    override this one; that was confirmed by running the test with `HOME`
    pointed at a directory whose `~/.s3cfg`, `~/.aws/credentials` and
    `~/.aws/config` all set `[default] host_base = 127.0.0.1:1`, and it still
    passed. Mount hygiene follows `jobqueue/testfusemount_test.go`:
    `newTestTempDir("mounts")` rather than `t.TempDir()` so a mount a killed
    run leaves behind is reapable, `failMountTestOnTimeout`, and a cleanup that
    fails the test if any mount outlives it.
  - Green, and the reproduction rate of the pair:

    ```
    $ CGO_ENABLED=1 go test -tags netgo --count 1 ./jobqueue/ -v \
        -run '^(TestUploadFailureCacheDeletion|TestRmMuxfysCachesKeepsWhatItMustNotDelete)$'
      When a job's output fails to upload, the cache muxfys keeps is deleted ✔✔✔✔✔✔✔✔
      A successful job's cache survives cleanup, so its output reaches the remote ✔✔✔✔✔✔✔✔
    --- PASS: TestUploadFailureCacheDeletion (0.14s)
      Given a Job whose workspace holds a directory named like a muxfys cache
        rmMuxfysCaches deletes the one muxfys named for itself ✔✔✔✔✔✔✔✔
        but not a MountTarget.CacheDir the user named ✔✔✔✔✔✔✔
        nor one the user named with the muxfys prefix ✔✔✔✔✔✔✔
        nor anything in the workspace of a Job that mounts nothing ✔✔✔✔✔✔✔✔✔✔
        nor anything at all when a MountTarget.CacheDir IS the workspace ✔✔✔✔✔✔✔
        nor anything of a CwdMatters Job's, which has no workspace at all ✔✔✔✔✔
      60 total assertions
    --- PASS: TestRmMuxfysCachesKeepsWhatItMustNotDelete (0.00s)
    ok  github.com/VertebrateResequencing/wr/jobqueue  0.198s
    ```

    10/10 consecutive runs exit 0. FUSE mount count 1 before and after.
  - The change is load-bearing, proved with a mutation that COMPILES. The call
    in `client.go` was replaced by `_ = (*Job).rmMuxfysCaches`, which keeps the
    method referenced:

    ```
    $ gofmt -l jobqueue/client.go
                                      (silent)
    $ go vet ./cmd/ ./jobqueue/
    VET_EXIT=0                        (no output, so it really did build)
    $ CGO_ENABLED=1 go test -tags netgo --count 1 ./jobqueue/ -v \
        -run '^TestUploadFailureCacheDeletion$'
      When a job's output fails to upload, the cache muxfys keeps is deleted ✔✔✔✔✔✔✔✘
      * jobqueue/upload_failure_cache_test.go
      Line 147:
      Expected [.muxfys_cache1330146727] to be empty (but it wasn't)!
      A successful job's cache survives cleanup, so its output reaches the remote ✔✔✔✔✔✔✔✔
    --- FAIL: TestUploadFailureCacheDeletion (0.14s)
    ```

    Only the failed-upload direction went red; the success direction stayed
    green, which is the split that matters. `jobqueue/client.go` was restored
    from a pre-mutation copy: `md5sum` is `e14402e67fcab0139f4cfe81bb8e5cdf`
    before the mutation and after the restore.
  - The four documentation sites the item above corrected, re-corrected. The
    interesting one is `cmd/mount.go`, which does NOT get the same statement as
    the rest: `wr mount` is not a Job and calls `fs.Unmount()` on muxfys
    directly (`cmd/mount.go:360`), so there the cache really is kept and wr
    deletes nothing. Since `cmd/add.go:249` sends `wr add` users to that help
    for the mount syntax, each behaviour is attributed to its own command.
    - `jobqueue/client.go`: the reason comment now says muxfys keeps the
      directory and names it, that the Job is redone from scratch either way -
      released and retried, or buried and retried by hand - so nothing will
      ever read the kept files, and that this is the one place that may delete
      them, being the only one that knows the upload has already failed.
    - `jobqueue/mount.go` (`MountTarget.CacheDir`): the unmount keeps the
      directory, and "For a Job, Client.Execute discards it instead ... but
      only where wr made the directory the cache sits in, ie. where the Job is
      not CwdMatters and CacheBase resolves to the parent of its actual working
      directory."
    - `cmd/add.go`: splits the two kinds of kept cache - one you named
      yourself, left behind; one holding files that failed to upload, deleted
      for you "Where wr made the directory that cache sits in - that is, when
      cwd_matters is false and your mount leaves CacheBase undefined".
    - `cmd/mount.go`: "'wr mount' then keeps that directory and tells you where
      it is ... (Under 'wr add', if --cwd_matters has not been set and
      CacheBase has been left undefined, that kept directory is deleted for you
      instead ...; a cache in a location you chose is kept, just as here.)"
    - The qualifier on the last three is not padding: `rmMuxfysCaches` deletes
      only inside a workspace wr made, so a `--cwd_matters` Job (whose
      `defaultCacheBase` is the user's own Cwd) and a Job with an explicit
      CacheBase elsewhere both keep their cache. A first review round rejected
      the unqualified wording for promising a deletion wr does not make.
  - Scope. `go.mod` and `go.sum` are byte-identical to before:
    `md5sum go.mod go.sum` is `5919cff8d2c2d429fbe24660c891c534` and
    `73c6ea9474cc2b39b460b01b4a1ad388`, the same values recorded in the item
    above. `jobqueue/workspace_test.go` is the only pre-existing test file
    touched, by addition only (114 insertions, 0 deletions); no test anywhere
    was skipped, weakened or deleted. A `go/ast` declaration-hash comparison
    against `HEAD` reports, for the whole change, one changed declaration
    (`keptEntry`) and four added ones (`muxfysNamedCache`,
    `removeMuxfysCaches`, `removeMuxfysCacheEntries`, `Job.rmMuxfysCaches`);
    nothing pre-existing was relocated. `cleanorder` was deliberately not run:
    it is in neither the `Makefile` nor `.golangci.yml`, and twice in this
    fix's history it moved unrelated declarations (`absenceRule`'s constants in
    `jobqueue/workspace.go`, `envDecodeHook` in `jobqueue/job.go`) away from
    what their doc comments read against.
  - Known and left alone: the emptied workspace directory survives a failed
    upload, because `Unmount`'s own `rmEmptyMountDirs` runs before the cache is
    deleted. That is the shape it already had - the cache itself was the
    leftover before - so it is not a regression and not this fix.
  - Gates from `/home/ubuntu/wr_race`, with `OS_*` unset and after
    `git rev-parse --verify master` returned `b2f0ff97...`:

    ```
    $ timeout 15m make lint
    0 issues.
    ```

    `make test` and `make race` were green on this change at every stage it was
    reviewed (`621 passed · 13 skipped · 29 packages`, `PASSED`, zero
    `DATA RACE`), including on the final ordering, but the last word on them
    belongs to the central gate run rather than to this checklist: two suites
    at once on this 4-CPU box produce false failures, and another was running.

- [x] CI's `test` job fails on this branch (run 34073269836, job
  101594474657) in `TestUploadFailureCacheDeletion`, which mounts a real
  muxfys filesystem and has no guard for a host that cannot make one:

    ```
    --- FAIL: TestUploadFailureCacheDeletion (10.02s)
      Line 144:
      Expected 'failed to mount remote file system(s): fusermount exited with
      code 256 [...]' to contain substring 'failed to upload' (but it didn't)!
      Line 159:
      Expected: nil
      Actual:   'failed to mount remote file system(s): fusermount exited with
      code 256 [...]'
    ```

  - Cause. muxfys mounts with `AllowOther: true`, hardcoded at
    `muxfys/v5@v5.1.0/muxfys.go:365`. The runner image ships `/etc/fuse.conf`
    with `user_allow_other` commented out, so `fusermount3` refuses and exits
    1, which `hanwen/go-fuse` reports as "code 256"
    (`fuse/mount_linux.go:126`). The failing job's own log says so, five
    times, interleaved with the failure:

    ```
    /usr/bin/fusermount3: option allow_other only allowed if
    'user_allow_other' is set in /etc/fuse.conf
    ```

    This box passes because its `/etc/fuse.conf` has the line enabled.
  - `canMountFuse()` (`jobqueue/workspace_test.go:1866`) is NOT the guard for
    this test, and this bug's first diagnosis - that it was - was wrong. It
    checks only that `/dev/fuse` opens and that a `fusermount`/`fusermount3`
    binary is on PATH, both true on the runner, so it returns true there.
    Proven inert rather than argued: with that guard applied to both Conveys
    through `go test -c -overlay`, so no file was changed, and the runner's
    FUSE configuration reproduced, the test still failed identically - same
    message, same 10.02s. Its six call sites (`workspace_test.go:90`, `:142`,
    `:205`, `:259`, `:1824` and `utils_test.go:500` - six, not five) mount
    through `mountLoopback`, which sets no `allow_other`, so those tests RUN
    on CI. Green develop run 34038205166 agrees: `jobqueue 391 passed 11
    skipped` lists no `canMountFuse()` skip, only `TestFuseMountReaping`'s
    `Without a usable fuse mount ...: fusermount exited with code 256`, the
    same error this test dies of.
  - Red command, from `/home/ubuntu/wr_race`, where `$S/shim/fusermount3`
    refuses any argument containing `allow_other` with the runner's exact
    message and exit 1 and otherwise execs the real `/usr/bin/fusermount`.
    First on PATH it reproduces the runner's FUSE configuration on a box that
    does have `user_allow_other`. Verified faithful: under it
    `TestFuseMountReaping` skips exactly as it does on green CI, and all six
    `canMountFuse()`-guarded tests still run and pass with assertion counts
    identical to a shim-free run.

    ```
    $ CGO_ENABLED=0 go test -c -tags netgo -o $S/jq.test ./jobqueue
    $ env PATH=$S/shim:$PATH $S/jq.test \
        -test.run '^TestUploadFailureCacheDeletion$' -test.v
    === RUN   TestUploadFailureCacheDeletion
    /usr/bin/fusermount3: option allow_other only allowed if
    'user_allow_other' is set in /etc/fuse.conf
      Line 144: [...] to contain substring 'failed to upload'
      Line 159: Expected: nil
    --- FAIL: TestUploadFailureCacheDeletion (10.02s)
    ```

  - Fix, half one: the guard muxfys actually needs. A probe at test-function
    scope through the package's existing `mountMuxFysForTest`
    (`jobqueue/testfusemount_test.go:505`), with `SkipConvey` on error - the
    pattern `TestFuseMountReaping` already uses at `testfusemount_test.go:454`
    for the same reason. The probe's mount is capability-identical to what the
    test body needs, since `mountMuxFys` goes through `muxfys.New` and the
    same `AllowOther: true`, so it neither skips a host that could have run
    nor passes one that could not. It uses an in-process `emptyRemote`, so it
    needs no network, no S3 config and no environment, and it takes its own
    `newTestTempDir("mounts")`, so its mount is never inside `mountTestCwd`'s
    directory and cannot trip that dir's "mounts outlived the test" check
    whichever order `t.Cleanup` runs them in. One probe covers both Conveys;
    probing inside each would double the mount work and duplicate the reason.
  - Fix, half two: let CI run the test rather than skip it. Half one alone
    would silently lose the end-to-end coverage this branch exists to prove,
    so `.github/workflows/tests.yml` gains an `Allow fuse mounts with
    allow_other` step, between `Install container runtimes` and `Run tests`,
    that enables `user_allow_other` on the runner. It is modelled on the
    incantation wr already applies to its own cloud instances
    (`cloud/cloud.go:133`, `sed -i '/user_allow_other/s/^#//g' /etc/fuse.conf`
    then `chmod o+r`), and hardened, because a runner image is not ours to
    know: the sed is anchored, and a grep/append fallback covers a file that
    is missing, already enabled, or has no `user_allow_other` line to
    uncomment. `golangci-lint.yml` does not need it - it runs no test binary
    and makes no mount.
  - Verified both ways, which is the point of a guard. With `allow_other`
    permitted (this box) the test RUNS: `16 total assertions` (8 + 8),
    unchanged from before the guard, so it did not become a no-op here; the
    suite agrees, printing `13 skipped` with no `Without a usable fuse mount`
    line under `jobqueue`. With `allow_other` refused (the shim) it SKIPS and
    the run passes:

    ```
    === RUN   TestUploadFailureCacheDeletion
    /usr/bin/fusermount3: option allow_other only allowed if
    'user_allow_other' is set in /etc/fuse.conf
    S
    0 total assertions (one or more sections skipped)
    --- PASS: TestUploadFailureCacheDeletion (0.00s)
    ```

    The reason is not swallowed. GoConvey's default reporter shows only `S`,
    as it does for all 44 `SkipConvey` sites in the repo, but `make test` runs
    with `GOCONVEY_REPORTER=json`, where the skip carries
    `"Title": "Without a usable fuse mount we can't test what a job's mount
    leaves behind: fusermount exited with code 256"` - so `conveyScopeSkips`
    (`internal/testsuite/runner_summary.go:168`) lists it under the package
    line, the way `TestFuseMountReaping`'s appears on CI today.
  - The workflow step was verified rather than assumed, against scratchpad
    copies with `sudo` stripped and run under `bash -e` (GitHub's shell for
    `run:`); this box's real `/etc/fuse.conf` was never touched. From each of
    stock-and-commented, already-enabled, file-absent, and present but with
    no such line at all, it exits 0 and ends with exactly one enabled
    `user_allow_other`, and a second run appends nothing. Errexit still bites
    on a genuine failure: against an unwritable target it fails (`sed` exit
    4, or `tee` exit 1 on the append branch), so it cannot pass while failing
    to write. That an enabled line really does permit `allow_other` is what this
    box demonstrates, both arms on one binary: enabled, the real muxfys mount
    succeeds and the test runs; refused, it fails with the runner's exact
    message. Not closed end-to-end - editing a real `/etc/fuse.conf` needs
    root, which no agent here has - so the last link rests on fusermount's own
    error naming that file and that setting as the condition.
  - What CI gains and what it never had. Before this, CI ran neither Convey:
    both died on the mount. With half two CI runs both, and with half one a
    host that cannot mount says so instead of failing. The half that never
    needed a mount was already safe and is untouched:
    `TestRmMuxfysCachesKeepsWhatItMustNotDelete`
    (`jobqueue/workspace_test.go:298`) builds real directories and calls
    `job.rmMuxfysCaches()` directly - 44 assertions, 0.00s, byte-identical
    with and without the shim - so `muxfysNamedCache`, `keptEntry`'s refactor,
    the positive deletion and all five refusal cases are covered on any host.
    Should a future runner image defeat the workflow step, that is the
    coverage that survives, and the live-mount integration is what would be
    lost.
  - Scope. Two files, 26 insertions, 0 deletions:
    `jobqueue/upload_failure_cache_test.go` (10) and
    `.github/workflows/tests.yml` (16). `go.mod` and `go.sum` are untouched -
    `md5sum` still `5919cff8d2c2d429fbe24660c891c534` and
    `73c6ea9474cc2b39b460b01b4a1ad388`, and `git diff develop..HEAD -- go.mod
    go.sum | md5sum` still `c6e91648c2f55adfe4d43d1b33394f00`. No production
    file changed, no other test touched, nothing skipped, weakened or deleted.
    `cleanorder` was again deliberately not run, for the reason recorded in
    the item above. FUSE mount count was 1 before and 1 after every run.
  - Gates from `/home/ubuntu/wr_race`, with `OS_*` unset and after
    `git rev-parse --verify master` returned `b2f0ff97...`:

    ```
    $ timeout 40m make test
      621 passed · 13 skipped · 29 packages · 6m26s
      PASSED

    $ timeout 15m make lint
    0 issues.
    ```

    The skip count stays at 13: this box can make a muxfys mount, so the new
    guard does not fire here. `make race` was not re-run - a test guard and a
    workflow comment cannot race, and it was green on this tree.
