- [x] Thanks to commit bb8ec025fbc082c83c9cb300ebcab088e5bfb92d, make test got to ~30s and make race ~10s slower. With later PRs it slowed massively. Does make test actually run all existing tests?
  - Old `make test` mostly covered all packages via `OTHER_PKGS`, but the guarantee lived in Makefile grep/exclusion logic.
  - Old `make race` did not cover the same package surface; it listed only a subset of non-jobqueue packages and its prefix-based jobqueue skip accidentally hid tests like `TestRESTJobModificationEndpoint`.
  - New `cmd/wr-testsuite` discovers packages with `go list ./...`; special slow packages are split, and everything else is covered automatically in both `test` and `race`.
- [x] Can make test be sped back to ~30s without flakes, preserving test spirit and reimplementing tests if needed?
  - Moved suite orchestration into Go, split additional slow lanes, prioritized long lanes, compiled split binaries concurrently, and capped lane fan-out by effective `GOMAXPROCS`.
  - Added run-specific `WR_TEST_PORT_BASE` selection so lane port ranges are below the ephemeral port range and less likely to collide with stale servers or unrelated outbound sockets.
  - Split `TestJobqueueRunners2` by `WR_TEST_SHARD` while preserving direct `go test` behaviour when the shard env var is unset.
  - Fixed an actual race exposed by exact race coverage: ready-added callback now reads scheduler-group inputs from a locked job snapshot, and `reqForScheduler` clones `Other`.
  - Current timings on this 4-core machine vary: `make test` passed in 35.02s and later 44.49s; `make race` passed in 1:09.85. Getting reliably closer to 30s now needs splitting/reworking the remaining slow scenario groups, especially `medium_a`, `runners`, `signal_b`, `runners2_a`, `server_webi`, and `cmd_resume`.
- [x] Makefile is complex/brittle/duplicated; can the same thing be achieved much simpler, maybe Go-side, so adding a package cannot be forgotten?
  - Makefile `test` and `race` are now thin wrappers around `go run ./cmd/wr-testsuite`.
  - The planner has regression tests for automatic package coverage, race coverage, exact jobqueue test matching, sharded lanes, empty `other`, CPU-aware parallel caps, compile parallelism, lane port range sizing, and long-lane prioritisation.
  - `make lint` now passes cleanly after running `golangci-lint run --fix` and adding narrow legacy-test suppressions.
