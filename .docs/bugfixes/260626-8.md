- [x] `TestJobqueueRunnerScheduling` "fractional CPU requirements" Convey (runner_scheduling_test.go) is timing-fragile and flaked on CI at line 279 `So(simultaneous, ShouldBeGreaterThan, maxCPU)` ("Expected '4' to be greater than '4'") — sibling to the line-310 marker-count flake just fixed in 260626-7. The Convey adds `count = maxCPU*2` jobs at `Cores: 0.5` running `sleep 1`, and a 500ms poll tracks the max concurrent RUNNING count, expecting `> maxCPU` (i.e. fractional packing lets more than maxCPU jobs run at once). Under the runner-spawn ramp on a contended box the short `sleep 1` jobs finish before all `count` runners spawn, so the observed peak only reaches `maxCPU`, not `> maxCPU`. Make the Convey DETERMINISTIC (same approach as TestJobqueueSignal): jobs that BLOCK until the test releases them (touch a started marker, then wait for a release file), so all `count` 0.5-core jobs are guaranteed running simultaneously (the scheduler's canCount for 0.5-core jobs is `maxCPU*2 = count`); poll until `simultaneous` reaches the target (deterministic since the jobs block), assert `> maxCPU`, then create the release file so the jobs complete and archive; keep the `ran == count` and clean-runner-marker checks. This removes the timing fragility for line 279 and also makes the marker count deterministic (blocking jobs => one runner per job => `count` runners, so the relaxed `assertCleanRunnerMarkers(_, count)` still holds). Preserve intent: fractional packing (`> maxCPU` concurrent), completion (`ran == count`), clean runner exits.
  - Fix (runner_scheduling_test.go, test-side only): the `count = maxCPU*2` 0.5-core jobs now run `blockUntilReleasedCmd(startedPath, releaseFile, perlCmd)` — touch a per-job started marker, `while test ! -e <releaseFile> && [ $i -lt 1800 ]; do sleep 0.1; done` (self-timeout aligned to `runnerStartWait`), then the original perl that writes the `>i` output file. Markers live in a separate `os.MkdirTemp` dir (not the job Cwd), so they don't inflate `ran` (which reads `job.ActualCwd`). The poll uses `pollUntilFor(runnerStartWait, …)` and returns only when the monotonic peak `simultaneous` reaches `count` (deterministic — blocking jobs never reduce the running count, so the poll can't exit early); then `So(simultaneous, ShouldBeGreaterThan, maxCPU)` (= `count > maxCPU` = `maxCPU*2 > maxCPU`, true for any `maxCPU>=1`). Only AFTER both assertions is the release file written, after which `waitUntilNoRunners` drains and `ran == count` + `assertCleanRunnerMarkers(runnertmpdir, count)` still hold.
  - Incidental: removed the now-redundant `maxWait` param from `waitUntilNoRunners` (always `runnerStartWait` at all 4 call sites — `unparam` flagged it once the new site was added); helper uses `runnerStartWait` internally, mirroring `waitUntilFileExists`. Behaviour-preserving; updated all 4 call sites (runner_scheduling_test.go + runner_lifecycle_test.go:576).
  - Verified by implementor + reviewer (clean context): `go build`/`go vet`/`make lint` (0 issues) clean; `TestJobqueueRunnerScheduling` passes 5/5 (implementor) + 3/3 (reviewer); `TestJobqueueRunnerAutomaticExecution` passes. Determinism reasoned, not re-run-until-green.
  - Reviewer advisory (NOT fixed here — separate Convey, more robust, has not flaked): the adjacent "add some 0 CPU jobs, which are limited by memory" Convey shares the `sleep 1` + 500ms simultaneous-poll shape, but asserts `ShouldBeBetweenOrEqual(maxCPU, maxCPU*2)` (lower bound `maxCPU` INCLUSIVE), so the strict `>maxCPU` failure mode cannot occur there; it is only theoretically vulnerable (peak landing BELOW `maxCPU` under extreme CPU starvation). Next candidate for the same blocking-job treatment if it ever flakes.
