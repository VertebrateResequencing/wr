- [x] CI flake. TestAddPrintsDuplicateBreakdown (cmd/add_test.go:1342, in completeTestJobs) failed with 'jobqueue Connect(): could not reach the server' on a loaded GitHub runner (PR #609 run, head 93479b6b; passed on re-run). completeTestJobs connects with a 2s timeout.
  - Lane: `make test` (CGO_ENABLED=0, netgo), 4-vCPU runner.
  - Cause (diagnosed): the connect-time ping's reply took over 2s because of a scheduling stall on the shared runner. The server's ping path takes no lock an add could hold (serverCLI.go:597, 704, 1922), so the defect is the cmd tests' 2s connect budget. CPU starvation, stress and SIGSTOP loops never reproduced it locally; the longest connect seen was 103ms in 24,000.
  - Red command: `cd cmd && CGO_ENABLED=0 go test -tags netgo -count=1 -run 'TestZZRedCompleteTestJobsSlowPing$' .` The throwaway harness is cmd/zz_red_connect_stall_test.go and is not committed. A TLS proxy holds back the ping record by PING_DELAY (default 2.5s), and the real completeTestJobs goes through it. It failed 4 of 4:
    ```
      Line 1342:
      Expected: nil
      Actual:   'jobqueue Connect(): could not reach the server'
    --- FAIL: TestZZRedCompleteTestJobsSlowPing (3.23s)
    ```
    Control: PING_DELAY=1.5s passed 3 of 3.
  - Fix: cmd/status_test.go adds `testConnectTimeoutSeconds` (30) and `testConnectTimeout`, next to `testServerPublishTimeout`. Every cmd test `jobqueue.Connect` that passed 2s now passes `testConnectTimeout` (cmd/add_test.go, cmd/status_test.go, cmd/suspend_test.go). Every `timeoutint = 2` in cmd/add_test.go, which the CLI connect() in `wr add` uses, is now `testConnectTimeoutSeconds`. `rtimeoutint = 1` is a job's reserve timeout, not a connect budget, so it is unchanged, as are the existing `timeoutint = 120` resets and the 2s `Reserve` waits. Server code is unchanged.
  - After the fix, the red command passes with the default 2.5s PING_DELAY and with PING_DELAY=5s. No regression test is committed: the change widens a test budget and alters no supported behaviour, and the harness needs a TLS-record proxy that only simulates a runner stall.
- [x] cmd test servers fail to start when their picked manager port is taken first. freeStatusTestPorts (cmd/status_test.go:1240) picks ephemeral ports and releases them. jobqueue.Serve returns before the manager port is bound, since it's bound after prior-state recovery (jobqueue/server.go:1873, listenWithRetries at :1912). So another process can take the port in between. The server then logs `could not listen on the manager port, so exiting` after its 5s retry, and waitForTestServerServing t.Fatals. startStatusTestServer's retry (cmd/status_test.go:1473-1482) only looks at Serve's own error, so it never sees this. Seen twice on 2026-09-25 on the shared host: in a `go test ./cmd/` run, and in a `make test` cmd_default run.
  - Red command: `CGO_ENABLED=0 go test -tags netgo -count=1 -run 'TestZZRedStatusTestServerPortSquatted$' ./cmd/`. The throwaway harness, not committed, is at scratchpad/flake/zz_red_port_squat_test.go. It is startStatusTestServer's body, with a listener taking the picked manager port just before Serve. It failed:
    ```
    EROR could not listen on the manager port, so exiting port=44879 err="listen tcp 0.0.0.0:44879: bind: address already in use"
    FAIL	github.com/VertebrateResequencing/wr/cmd	5.356s
    ```
  - Correction to the cause: publishExit is os.Exit, so the failed publication ends the whole cmd test binary (the `FAIL ... 5.356s` above). waitForTestServerServing never gets to t.Fatal. No retry in the helper can see this unless the exit is replaced.
  - Fix: the publish-exit seam moves from jobqueue's unexported `publishExit` to a new module-internal package, internal/publishexit, with `Exit` (os.Exit by default) and `Set`, which swaps it and returns a restore func. Tests in cmd can now see that publication gave up, but library callers of jobqueue still can't turn the spec E1 exit off. Production behaviour is unchanged. jobqueue/server.go calls `publishexit.Exit(1)`, and the two depgranularity_startup_test.go tests that swapped `publishExit` use `publishexit.Set`. cmd/status_test.go replaces startStatusTestServer's loop with startTestServer (startQueueCommandTestServer in cmd/suspend_test.go had the same pick-release-bind loop and now uses it too). For each attempt, tryStartTestServer installs a `publishexit.Set` replacement that signals a channel, calls Serve, and waits on Serving() or that channel. If publication gave up, it stops the server in the background, since shutdown waits with no deadline for its ports to close, and then retries with fresh ports. Retrying on an in-use error from Serve itself still works as before. statusTestServerConfig now picks ports through a `pickTestServerPorts` var, whose default is freeStatusTestPorts. No other cmd test helper picks ports and then releases them.
  - Regression test: TestStartStatusTestServerRetriesTakenPort in cmd/status_test.go wraps pickTestServerPorts to take the first picked manager port, then calls the real startStatusTestServer. Before the fix, the test binary exited:
    ```
    EROR could not listen on the manager port, so exiting port=37737 err="listen tcp 0.0.0.0:37737: bind: address already in use"
    FAIL	github.com/VertebrateResequencing/wr/cmd	5.162s
    ```
    After the fix, it passes in 6.4s. It serves on a different port, and a client connects. The throwaway harness also failed before the fix and was deleted. A copy is kept in the scratchpad.
- [x] Flake: TestJobqueueRunnerKillRequests (lane runner_kill_requests) failed at jobqueue/runner_lifecycle_test.go:557 in a local `make test` on 2026-09-25, at load average about 6-7. A killed 20s job ran to completion instead of being buried. It passed when the lane binary was re-run, and in the next full run. No red command yet. Logs: scratchpad/flake/item2-test.log.
  - Diagnosed as a product bug: a kill that reaches the runner before Execute installs its kill handler is dropped. It moved to its own branch, fix-kill-request-lost, with checklist .docs/bugfixes/260925-kill-before-start.md.
- [ ] Flake: TestJobqueueModify (lane modify_b) failed at jobqueue/jobqueue_test.go:6921 in a local `make test` on 2026-09-25, at load average about 6-7. It expected `ready` after a kick and a failed re-run, and got `delayed`. The lane binary passed 3 of 3 on re-run. No red command yet. Logs: scratchpad/flake/item2-test2.log.
  - Cause (diagnosed): with ReleaseDelayMin=1ns, queue.Release puts the item in the delay sub-queue, and only the startDelayProcessing goroutine moves it to ready (queue/queue.go:1780-1850). The test reads the state once, straight away. No product impact. Red: `git apply /tmp/claude-11346/-nfs-users-nfs-s-sb10-src-go-github-com-VertebrateResequencing-wr-incidental/7b24332e-a102-43cb-847e-a702b9dca2f3/scratchpad/flake2/red_modify.patch && cd jobqueue && WR_RED_DELAYMOVE_SLEEP=300ms WR_TEST_SHARD=b WR_TEST_LANE=58 go test -count=1 -run '^TestJobqueueModify$' .` gives `Expected: "ready" Actual: "delayed"`. Recommended fix: pollUntil the state is ready.
- [x] A manager stopped while still recovering, with another process listening on its manager port, never exits. Found in review of item 2. Shutdown's waitForPortsClosed (jobqueue/server.go) dialled the configured manager and web ports until nothing answered, with no deadline. Publication binds those ports only after recovery (spec E1), so a manager stopped during recovery holds neither of them, and a foreign listener that accepts connections kept the loop spinning forever. item 2's cmd helper stopped a failed server in the background to avoid this.
  - Red command: `cd jobqueue && CGO_ENABLED=0 go test -tags netgo -count=1 -run 'TestDepGranularityStartupStopIgnoresForeignPort$' .`. This is the committed regression test in jobqueue/depgranularity_startup_test.go. It holds the manager port with a listener that accepts and closes connections, pauses recovery, starts Stop, releases recovery once Serving() closes, and asserts Stop returns within 15s. A listener that never accepts doesn't reproduce the bug, because its backlog fills and the 10ms check dials then time out as if the port were closed. Before the fix it failed:
    ```
      Line 1189:
      Expected: true
      Actual:   false
    --- FAIL: TestDepGranularityStartupStopIgnoresForeignPort (15.20s)
    ```
    After the fix it passes in 0.3s.
  - Fix: publication records each port it actually binds (`noteBound`, guarded by ssmutex): the manager port after listenWithRetries succeeds, and the web port in serveWebInterface. The web port is now bound with net.ListenConfig.Listen and served with ServeTLS instead of ListenAndServeTLS, so the bind is observable. A bind failure logs the same "server web interface had problems" error as before, and runHTTPServer closes the listener if ServeTLS fails before serving. waitForPortsClosed now waits only on the bound ports. cmd's tryStartTestServer now stops a server whose publication gave up with a synchronous `server.Stop(ctx, true)`, and the background-stop comment is gone. Stale comments in TestDepGranularityStartupExitsWhenPortUnavailable about Stop needing the listener closed first are removed. CHANGELOG has a "### Fixed" entry.
- [ ] Flake: TestJobqueueExecutionAndDependencyScenarios (lane jq_execution_details) failed at jobqueue/jobqueue_test.go:5074 in a local `CGO_ENABLED=1 make race` on 2026-09-25, on commit 4794e43. Buried jobs in repgroup should_delete: expected 0, got 1. The lane's race binary passed 2/2 on re-run. No red command yet. Log: scratchpad/flake/item2b-race.log.
  - Cause (diagnosed): deleteJobIfRequested (server.go:6325-6329) deletes in a goroutine, and the test counts buried should_delete jobs once without waiting. No product impact on the final state. Red: `git apply /tmp/claude-11346/-nfs-users-nfs-s-sb10-src-go-github-com-VertebrateResequencing-wr-incidental/7b24332e-a102-43cb-847e-a702b9dca2f3/scratchpad/flake2/red_delete.patch && cd jobqueue && WR_RED_DELETE_SLEEP=20ms WR_TEST_SHARD=c WR_TEST_LANE=76 go test -count=1 -run '^TestJobqueueExecutionAndDependencyScenarios$' .` gives `Expected: 0 Actual: 1`. Recommended fix: poll until 0. The nil-queue panic this exposed at 300ms is a separate product bug, on branch fix-delete-after-stop-panic.
- [x] Flake: TestReliable2FastStartupNoHistoryScan (jobqueue/reliable2_startup_test.go:~112, lane jq_default) failed in GitHub CI on PR #613, head a2deb05, on 2026-09-25: `Expected '6.378874ms' to be less than '6.268444ms'`. The ratio check `largeElapsed < c2HistoryScaleLimit(4) * smallElapsed` compares millisecond-scale startups. A ~1.6ms small startup leaves a 6.3ms budget that a few ms of runner noise exceeds. The absolute bound (5s) is the one that matters for users. No red command yet. Log: scratchpad/ci613.log.
  - Red command: `cd jobqueue && CGO_ENABLED=0 go test -tags netgo -count=1 -run 'TestZZRedC2Noise$' .`. The throwaway harness, not committed, is at scratchpad/flake/zz_red_c2_noise_test.go. It applies the history-scale check to the CI sample (1.567ms small, 6.379ms large). It failed with the old check (`Line 21: Expected: true`). It also adds 25ms of noise to the large startup of a real measurement. That case only fails the old check when the small startup is under about 8ms: the local small startup was 6-11ms, so locally it passed.
  - Fix: jobqueue/reliable2_startup_test.go adds `c2HistoryScaleSlack` (30ms) and `c2StartupScalesWithHistory`. The test now fails only if the large startup takes both over 4x as long as the small one and more than 30ms longer: `large >= max(4*small, small+30ms)`. The 5s absolute bound is unchanged.
  - After the fix, the harness passes both cases, and the real test passes (8.2ms small, 10.1ms large).
  - Mutation check: a temporary scan of the complete bucket was injected into Serve right after initDB. With the new check, the test still fails for both kinds of scan. Decoding every job took 157ms small and 1.53s large. Walking the keys without decoding took 16ms small and 101ms large. The scan was removed afterwards.
- [ ] jobqueue tests have the same pick-release-bind port race as item 2, in jobqueueTestInit/isolateTestConfig port picking, which item 2 (cmd only) didn't cover. A local `go test ./jobqueue/` on 2026-09-25 died at 207s, straight after `could not listen on the manager port, so exiting port=45993`, with no `--- FAIL` line: the real os.Exit ended the binary. Found in the item 2/5 re-review. No red command yet.
