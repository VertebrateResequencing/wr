- [x] On another machine I did `wr cloud deploy --max_local_ram 1`, then `echo "sleep 60" | wr add -i test`. The worker node gets spawned, but then there's a scheduler issue: "OpenStack: Failed to create a usable server: could not look for exe [WR_LOGSMAXSIZEMB=500]: exec: "WR_LOGSMAXSIZEMB=500": executable file not found in $PATH". It seems to me that the most basic thing doesn't work and Openstack mode is broken. How can the tests be passing? Improve the tests properly so this gap can't happen again and fix the problem using the bugfix skill workflow.
  - Cause: the OpenStack scheduler executable check parsed the first shell word as the executable. Production `wr manager start` prefixes runner commands with log-rotation environment assignments, so spawned OpenStack runners tried to resolve `WR_LOGSMAXSIZEMB=500` instead of `wr`.
  - Test gap: focused scheduler tests covered plain commands, quoted executables, absolute paths and missing executables, but not leading shell environment assignments. The live OpenStack jobqueue test used a custom runner command without the production-shaped log-rotation prefix.
  - Fix: skip leading POSIX shell assignment words when identifying the executable to check on spawned OpenStack servers, while still rejecting empty, malformed and assignment-only commands.
  - Coverage: added focused scheduler regressions proving `WR_LOGSMAXSIZEMB=500 WR_LOGSMAXBACKUPS=3 sleep 60` checks `sleep`, not the assignment, and strengthened the live OpenStack jobqueue runner command with a harmless log-rotation-style env prefix.
  - Verification: implementor and reviewer passed focused scheduler/jobqueue tests, `make lint`, `make test`, and `make race`; broad gates skipped live OpenStack/S3 integration because credentials/config were not configured in this environment.
