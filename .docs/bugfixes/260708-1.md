- [x] This still fails for me. Run the test in isolation and confirm and fix if needed.
  - The isolated live OpenStack test did not reproduce before the patch on this runner, but it did take 878.91s and exercised the reported no-Docker leaf.
  - Replaced the separate `docker info` probe with the actual monitored `docker run` job result, so the "when docker is not installed" assertion only runs when that real job buries on the spawned runner.
  - Verified with lint, focused helper coverage, and the isolated live `TestJobqueueWithOpenStack` command sourced from `~/.openstack.rc`.
