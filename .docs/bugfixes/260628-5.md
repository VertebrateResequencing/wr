- [x] `wr cloud deploy --debug` still tears down immediately after `could not talk to wr manager on server ... after 40s`, and leaves behind the ssh port forwarding processes it started.

  - Existing behaviour: there is no flag/path that forces the later manager-connect failure into the interactive "To debug further..." pause; that pause only happens when the remote `wr manager start` command itself returns an error.
  - Fix: added deploy-forwarder cleanup before teardown on manager-connect failure and on partial forwarding setup failures, reusing the same pid-file scoped cleanup for stale reconnect forwarders.
  - Safety: cleanup only kills pids recorded in the specific deploy pid files passed to the helper; tests prove an unrelated pid file/process survives.
  - Also fixed `startForwarding` so a process started successfully but failing to write its pid file is killed and waited for instead of being leaked.
  - Gates: focused `cmd` tests passed, full `cmd` package passed, and `make lint` passed under reviewer-run isolated caches.
