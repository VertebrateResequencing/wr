- [x] wr-status: add filter options for `--pending` and `--dependent` jobs
  - Issue body: Similar to the `--running` and `--buried` options, it would be nice to filter to the jobs that are in the other possible states, i.e. `--pending` and `--dependent`.
  - Updated `cmd/status.go` to add `--pending` and `--dependent` flags that reuse the existing state filter path.
  - Added `cmd/status_test.go` coverage for pending/ready and dependent status filters against an in-process jobqueue server.
  - Verified by implementor and reviewer subagents with focused Go tests, `make lint`, `make test`, and diff checks.
