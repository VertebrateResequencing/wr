- [x] Web UI regression: after all jobs in a repgroup complete, refreshing the web UI (a fresh status subscriber connecting) should make that repgroup disappear — the web UI must NOT show completed-only repgroups on a fresh load, unless the user searches for them or the jobs completed during the current (post-refresh) session. Instead, the completed-only repgroup reappears on refresh: it shows with 0 jobs momentarily, then fills up to the completed count. Root-cause and fix so a fresh status seed excludes repgroups that are complete-only (no incomplete/live jobs), while jobs that complete during an active session still show live. Likely area: jobqueue statusState fresh-subscriber seed / seedRepGroupComplete (statusstate.go) and the maintained-counter seeding (seedStatusStateForItemDefs). IMPORTANT: the existing test jobqueue/.../TestReliableCompletedRepGroupRemovedOnRefresh (in .docs/reliable/harness/reliable_repro_test.go) currently asserts the OPPOSITE (that the completed-only repgroup IS in the fresh seed) — that assertion encodes the wrong behavior per this bug and must be corrected as part of the fix.
  - Root cause: `seedCountCopy` (jobqueue/statusstate.go) had a branch returning `{complete:N}` for complete-only repgroups (no live jobs), so `liveSeedLocked`/`subscribe` put them in a fresh-connect seed and a refresh re-showed them (0 then filling to the completed count). This reversed prior fix 260626-2; commit #548 (4e1b5d9) re-introduced the inclusion and flipped its tests, and the reliable spec/phases added more tests asserting it.
  - Fix: removed the complete-only branch from `seedCountCopy` so complete-only / deleted-only / complete+deleted-only repgroups are omitted from the fresh seed; a repgroup with >=1 live job still seeds its live states + complete count (via `liveCountCopy`). Updated doc comments on `seedCountCopy`/`liveSeedLocked`/`subscribe`. The live-update path is untouched, so a repgroup completing WHILE connected stays visible (260625-6), and CLI search (`GetStatusByRepGroupMatch`) still shows completed jobs.
  - Corrected the tests that encoded the wrong behavior: `TestStatusStateSeedSemantics` and a `TestStatusState` reconnect Convey (jobqueue/statusstate_test.go); `TestHighPressureCompletedRepGroupStatusSurvivesRefresh` Phase-2 fresh-reconnect (jobqueue/serverWebI_test.go, new `readRepGroupAbsentDuring` helper) while keeping its Phase-1 live-job-survives, in-session live, and CLI-search assertions; `TestReliableCompletedRepGroupRemovedOnRefresh` (.docs/reliable/harness/reliable_repro_test.go).
  - Note: this deliberately reverses spec A2 test 3 / "Regression D" (which asserted a complete-only repgroup stays in the fresh seed) — that expectation was demonstrably wrong per this bug and prior fix 260626-2; the counter's purpose (fast-seeding repgroups that HAVE live jobs) is unaffected.
  - Verified: TDD (corrected tests fail pre-fix, pass post-fix); full `make test` (358 passed) and `make race` (358 passed, 0 races) with OpenStack tests skipped; `make lint` 0 issues; Section E guards green (`everLost==0`); browser fixtures `completed-repgroup-visibility` + `removed-jobs-refresh` pass (the unrelated `repgroup-bar-flicker` JS fixture fails pre-existing, exercises no Go code).
