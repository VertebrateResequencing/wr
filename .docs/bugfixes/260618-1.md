- [x] Make minimal fixes to get `make test` to pass, then commit.
  - Updated REST test expectations to compare `JobState` values with typed constants.
  - Verified `make test` passed via implementor and reviewer subagents.
