- [x] Up until recent commit 6f890988444d632ae7d309c460e82c3cfb58cfd3, this codebase was pinned to an old dodgy version of bcast to do broadcasts for status_ws and for some other usage. The system was not reliable, with sometimes missed events when viewing the web UI. Consider and explore via temporary code changes alternative options: Re-use code related to how we do subscriptions to job completion in eg. `wr add --sync`. Though this was recently implemented by LLM and hasn’t been used in the wild so may be buggy. Use the very latest version of bcast. Use an alternative 3rd party broadcast library. Keep the custom replacment implemented by LLM in commit 6f890988444d632ae7d309c460e82c3cfb58cfd3, cognisant that it is likely to be buggy. It would be nice if we were able to use the same well tested and reliable broadcast system as the heart of both `add --sync` and status_ws. If there isn’t a clear and unambiguous winner amongst all options, discuss with me pros and cons of each so I can pick. Otherwise you pick. Then you’ll implement the pick using the bugfix skill workflow.
  - Rejected latest `github.com/grafov/bcast` after exploration reproduced a focused hang, and avoided adding a third-party broadcaster because the maintained alternative still used lossy fan-out semantics for this case.
  - Routed status websocket per-job detail updates through the same queued server subscription machinery used by `AddAndWait`/`wr add --sync`, while keeping aggregate status, bad-server, and scheduler fan-out on the existing best-effort caster.
  - Preserved CLI subscription semantics by allowing normal client subscriptions to receive terminal/lost/resync updates only, and opting status websocket subscriptions into non-terminal state changes.
  - Extracted the package-private server-side subscription queue/fan-out code into `jobqueue/server_subscription.go` instead of creating a public or general-purpose broadcast package.
  - Added regression coverage for details-subscribe/update races, browser timestamp unit conversion, and deterministic websocket subscription cleanup.
