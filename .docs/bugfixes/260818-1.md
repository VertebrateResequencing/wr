# Bugfixes 260818-1 — reliable4 live-prod findings (2026-08-17 profiling run)

Source: `.docs/reliable4/prod-run-20260817.md` (NEXT STEPS section). Priority
order is the doc's, and is deliberate: Bug 1 hands CPU back to the bbolt
committer, which partially relieves Bugs 2 and 6 before they are touched.

Quality gates for every bug: `make lint`, `make test`, `make race` (unset all
`OS_*` vars first), plus the relevant `developers/wrdev.sh` scale mode.

- [x] Bug 1 (Priority 1, Finding 3) — 28-42% of the manager's CPU is spent recomputing an invariant: `Job.schedulerGroupSnapshot` (job.go:1139) runs 2 MD5s + a sort + several allocations for EVERY ready job on EVERY rac cycle, from `Server.snapshotReadyJobs` (server.go:2100). Proven by a 245x A/B on the live production manager: 61,000 limit-0 ready jobs = 19,640ms CPU per 25s (0.79 cores, 41.8% in snapshot) vs 1 ready job = 80ms per 25s, for identical zero real work. Fix by memoising the derived strings (key/group/requirements) on the Job. See "Fix constraints for the implementer" items 1-5 in the doc (invalidate on Requirements/LimitGroups/Cmd/Cwd/MountConfigs/container-field writes; `Job.Key()` is invariant after add so cache unconditionally; keep `priority`/`previousGroup` live; do NOT change `schedulerGroupString`'s output format, it is persisted and parsed elsewhere; `prepareReadyJob` legitimately recomputes requirements and must invalidate; DEVELOPERS.md rule 2 - no new server-wide exclusive lock on the transition path).
  - Memoised the derived scheduler-group strings on the `Job`: new `jobDerived`
    struct (`key`, `requirements`, `group`) in an unexported `Job.derived` field,
    guarded by the Job's existing `RWMutex`. `schedulerGroupSnapshot` is now a
    double-checked memo (RLock fast path; per-job write lock only to compute).
    `priority`/`previousGroup` are still read live; `schedulerGroupString`'s
    output format is byte-identical (asserted in the test).
  - Invalidation: `JobModifier.applyTo` (single exit), `handleUserSpecifiedJobLimitGroups`
    (single exit, all 3 callers hold the write lock), and
    `updateJobRequirementsForRetry` via a **`defer` at the top of the function** —
    the first review cycle caught a tail-placed invalidation being skipped by the
    `jobOverrideAlwaysUseJobReqs` early return, which left a permanently stale memo
    that would have made `ensureGroup` seed `sgroup.req` (what the scheduler
    `bsub`s) from stale requirements. `persistModifiedJobs` now holds the job write
    lock across `handleUserSpecifiedJobLimitGroups` (a pre-existing unlocked
    mutation, contrary to that function's own contract).
  - `Job.Key()` itself is deliberately still uncached: it is called with the job's
    write lock already held (`prepareInputJobs`, `modifyJob` before/after
    `applyTo`, and `derivedLocked`), so a self-locking `Key()` would self-deadlock.
    The hot path reads the key from the memo, so this costs nothing.
  - Files: `jobqueue/job.go`, `jobqueue/server.go`, `jobqueue/serverCLI.go`,
    NEW `jobqueue/reliable4_snapshot_memo_test.go`
    (`TestReliable4SchedulerGroupSnapshotMemoised`,
    `TestReliable4SchedulerGroupSnapshotInvalidated`,
    `BenchmarkReadyBacklogSnapshot`), `developers/wrdev.sh` (new
    `idle-backlog-cpu [jobs] [seconds] [pprofPort]` scale mode).
  - RED/GREEN: derivations over 3 steady rac cycles at a 20k limit-0 backlog
    60,000 -> 0; mallocs per ready job per cycle 18 -> 2;
    `BenchmarkReadyBacklogSnapshot` 125,919,705 ns/op / 18,065,944 B/op /
    340,013 allocs/op -> 4,369,676 ns/op / 0 B/op / **0 allocs/op** (28.8x).
  - Scale gate, real-binary A/B at 50,000 limit-0 jobs, no runners, 13 driven rac
    cycles: pre-fix **4,090ms CPU per 25s** (snapshot 2,670ms, 65%; profile
    reproduces the prod signature snapshotReadyJobs -> schedulerGroupSnapshot ->
    schedulerGroupString -> Requirements.Stringify + 22% GC) FAIL, post-fix
    **800ms per 25s** (snapshot 170ms) PASS => 314ms -> 61ms manager CPU per
    cycle. Prod's pre-fix figure was 19,640ms/25s at a 61k backlog.
  - The gate is hardened against vacuous passes: it FAILs `(NOT MEASURED)` if the
    profile has no `Duration:` line, if total CPU is 0/unparseable, or if no rac
    cycle ran (verified: an unreachable-pprof run that used to print PASS now
    exits 1).
  - Gates: `make lint` 0 issues; `make test` 377 passed; `make race` 377 passed,
    0 data races; `make bench` persistence benchmarks unchanged. Regression scale
    gates: `backlog-rescan-check 2000 50000` still `scanWork == 2000` (260725-2
    invariant intact), `priority-fairness-check` PASS.
- [x] Bug 2 (Priority 2, Finding 2) — the archive path is still serialised on the single bbolt write lock: at only ~660 concurrent runners the archive queue was ~600 deep at ~12/s with a MEAN block of 43s against the 60s ClientMinRequestTimeout floor, which is what makes compress jobs land in `delayed` with Exitcode 0. Fold pending archives into ONE `db.Update` and reply to every waiter individually. Honour every constraint in the doc's Priority 2 "Fix constraints" (per-job error semantics like bbolt.Batch's individual-retry-on-error; keep the archive-vs-change guard at db.go:2425; keep db.wg accounting and the close() drain; keep backupDirty set; follow the shape of bestEffortWriter/drainBestEffort at db.go:1156/1185 but note archives are synchronous request/response, not fire-and-forget). Escalation rule: if per-job reply/error semantics turn out to need restructuring of `handleArchive`/`archiveCompletedJob` rather than a contained writer, STOP that bug, write a `/spec-writer` prompt doc under `.docs/reliable4/` instead, mark this item as escalated-not-fixed, and continue to Bug 3.
  - `archiveJob` no longer calls `db.bolt.Batch` (bbolt detaches its batch the
    instant one starts and arms a fresh `MaxBatchDelay`, so under continuous
    arrival the batches stayed tiny — prod showed 234 concurrent `batch.run`
    goroutines for ~600 pending archives, ~2.5 per batch). It now encodes the job
    (still under `job.RLock`, still outside any transaction), hands an `archiveOp`
    with a buffered(1) reply channel to a single long-lived `archiveWriter`
    goroutine, and blocks on its own reply — the same synchronous contract as
    before. Each wake folds EVERY pending archive into ONE `db.Update` and then
    replies to each waiter individually.
  - Per-job error semantics mirror `bbolt.Batch` exactly: on a failed shared
    transaction the offender is swap-removed, the rest are retried together, and
    each offender is re-run in its own transaction so its caller gets its own
    error. Panic isolation (which `bolt.Update` loses versus `Batch`'s
    `safelyCall`) is restored via `applyArchiveOp` + `errArchivePanic`, so a
    panicking op cannot wedge the writer and every future archive.
  - Shutdown: `stopArchiveWriter()` runs from `finaliseBackup` BEFORE `wg.Wait`
    and the final backup, and `swapArchives(true)` latches the queue shut in the
    same critical section as the last drain, so a late archive gets `errDBClosed`
    instead of hanging. This is strictly stronger than before, where an in-flight
    `Batch` could commit after `wg.Wait`.
  - `arMu` is a strict leaf mutex (append/swap only, never held across the bolt
    write lock); the writer takes no `db.Lock`, `db.wgMutex`, job lock or queue
    mutex, so DEVELOPERS.md rule 2 holds. `db.wg` is untouched (archives never
    used it) and `backupDirty.Store(true)` still happens after the write attempt.
  - The `bucketJobsLive` archive-vs-change guards live in `beBatch.applyChanges`
    and `jobExitData.update` (NOT `archiveJobTx`, as the brief had it) and are
    untouched; bolt serialises write txs so the commit-order reasoning is
    unchanged, and the encode-to-commit window actually shrinks 43s -> 764ms.
  - Files: `jobqueue/db.go`, `jobqueue/db_bench_test.go` (added `bolt_txns/job`,
    new `BenchmarkArchiveSpacedArrivals` for the production arrival regime),
    NEW `jobqueue/reliable4_archive_coalesce_test.go` (5 main-suite tests:
    coalescing/latency, per-job errors, panic isolation, `close()` drain,
    archive-beats-resurrecting-change), NEW
    `jobqueue/reliable4_archive_rate_test.go` (`reliability_repro`), and
    `developers/wrdev.sh` (new `archive-rate [archivers] [seconds] [thinkMs]`).
  - RED/GREEN, same harness: 100 archives cost **100 write transactions**
    (biggest 1, mean latency 9.199s, max 18.183s) -> **11 transactions**
    (biggest 11, mean 303ms, max 404ms).
  - Scale gate A/B on an identical 9.99GiB / 4745MiB-freelist DB, 660 archivers,
    180s, jittered arrivals:
    pre-fix **17.0/s, mean 40,494ms, p99 54,874ms, queue 603 deep** -> FAIL;
    post-fix **144.9/s, mean 764ms, p99 2,752ms, queue mean 110 / max 441** ->
    PASS. The pre-fix side reproduces prod's measured signature (~600 deep,
    ~12/s, mean 43s) almost exactly. Doc targets (mean < 5s, p99 < 60s) met with
    6.5x / 21x headroom.
  - The jitter is load-bearing and documented as such: the gate's FIRST version
    passed pre-fix because un-jittered archivers finish in lockstep and all
    arrivals land inside one 10ms bbolt batch window, coalescing without the fix.
  - Benchmarks: `BenchmarkArchiveSpacedArrivals` (production regime) 12.27s ->
    2.48s/op, `bolt_writes/job` 9.140 -> 3.455, `bolt_txns/job` 0.995 -> 0.200.
    `BenchmarkArchiveJobs` (saturated 64-way, fresh temp DB) 717ms -> 257ms/op
    but `bolt_writes/job` 2.478 -> 2.704 (**+9% worse**) — accepted: that
    regression exists only where a commit is faster than bbolt's fixed 10ms
    `MaxBatchDelay`, i.e. never on the prod shape (~109ms per single-archive
    commit), which is why the production-regime metric moves the other way.
    Tuning `MaxBatchSize`/`MaxBatchDelay` is ruled out by the doc.
  - The batch is deliberately uncapped (matching the proven `beBatch`): every
    archiver blocks on its own reply, so pending is bounded by concurrent archive
    RPCs, and a cap would reintroduce multiple commits, i.e. the bug.
  - `ctx` cancellation deliberately NOT added to the reply wait: the writer would
    still commit while the caller returned `ErrDBError` and skipped `s.q.Remove`,
    leaving a job both archived and live.
  - Gates: `make lint` 0 issues; `make test` 382 passed (was 377, +5 new);
    `make race` 382 passed, 0 data races; `make bench` all 8 PASS. Regression
    scale gates: `writestorm-freeze` PASS (max archive latency 3.26s),
    `idle-backlog-cpu 50000 25` PASS (850ms/25s, Bug 1 intact),
    `backlog-rescan-check 2000 50000` PASS (`scanWork == 2000`).
  - Review also closed a false-PASS path in the new gate: the verdict ignored the
    `errors=` field and the `go test` exit status, so fast-failing archives (all
    returning `errDBClosed`) would have shown low latency and PASSed.
  - Follow-up (gate hygiene, same commit series): `WRDEV_ROOT` was set but never
    exported, so the big-DB work copies of three scale gates landed NEXT TO the
    shared `/nfs/hgi/wr/sb10-bigdb/` fixture instead of under `$WRDEV_ROOT`, and
    the script-side cleanup was a no-op (`archive-rate`) or entirely absent
    (`writestorm-freeze`, `report-storm`) — so a timeout-killed run leaked ~10GB
    into the shared fixture directory. All three now pass `WRDEV_ROOT` on the
    `go test` inline prefix, use a `local work=...` path in both the "copied to"
    message and an `rm -f "$work" "${work}_bk"` cleanup, and capture the pipeline
    status first (`|| rc=$?` ... `return "$rc"`) so appending the `rm` cannot
    swallow a gate's FAIL exit code. Proved with a throwaway tiny DB: copy
    observed under `$WRDEV_ROOT`, and after SIGKILLing the test binary mid-run
    (so no Go `t.Cleanup` could run) the script's own `rm` removed the leftover.
    `pristine10` verified byte-identical throughout.
- [x] Bug 3 (Priority 3, Finding 1) — `wr suspend`/`resume`/`status -i <substr> -z` does an unbounded, uncancellable archived-history scan: the control RPC times out, the operator cannot un-suspend at all, and manager heap went 0.35GB -> 12.1GB. Two small independent parts per the doc: (1) `cmd/suspend.go` `getSelectedJobs` must always send a state filter, not only with `-a` (resume => JobStateSuspended; suspend => the live states it actually affects); (2) `server.go:5413` `getJobsByRepGroup` must fetch complete jobs only when the caller explicitly asks (e.g. an `IncludeComplete` field on `repGroupOptions` set by the status path) rather than "whenever State happens to be empty" (server.go:5471). Record the doc's larger follow-ups (push Limit/Offset into `retrieveCompleteJobsByRepGroup`; request cancellation; slow/heavy-request warning) in the checklist as follow-ups rather than doing them here.
  - Part 1: `cmd/suspend.go` `getSelectedJobs` now ALWAYS sends a state filter,
    not only with `-a`. It sends a new `JobStateIncomplete` (`!complete`) meta
    state rather than the doc's literal per-command filters, because a single
    `JobState` cannot express suspend's three affected states (`queue.suspendItem`
    spans delay|ready|dependent) and the precise filters would have broken two
    deliberately-named existing tests that encode the "matching vs changed"
    distinction (`cmd/resume_test.go` "reports matching non-suspended jobs without
    changing them", `cmd/suspend_test.go` "leaves buried jobs buried"). The set of
    jobs actually suspended/resumed is provably unchanged: queue rows can never be
    `JobStateComplete` because `queue.removeItem` deletes the key, and
    `suspendJobs`/`resumeJobs` resolve every key through `s.q.Get`, so an
    archived-only key could never have contributed to the changed count.
  - Part 2: `repGroupOptions` gains an explicit `IncludeComplete`, so complete
    jobs are fetched only when a caller asks, instead of "whenever `State` happens
    to be empty". All four `getJobsByRepGroup` callers decided deliberately:
    `handleGetByRepGroup` ("getbr", backs `wr status -i`) true; REST
    `restJobsStatusByID` true; web `sendJobDetails` true; REST PATCH
    `restJobsModificationRepGroupTarget` false. The pre-existing
    `State != "" && State != complete` short-circuit was KEPT — removing it would
    make `wr status -i rg --pending` decode the whole history and discard it.
  - `JobStateIncomplete` is deliberately absent from `parseRESTJobState` and
    `restCancelState`, so no REST query-string behaviour changes (rule 10).
  - USER-VISIBLE CHANGES (all confined to the `-i`/`-i -z` path; `-a`, `-f`, `-l`,
    `-y` unaffected):
      1. `(out of N matching)` for suspend/resume now counts live jobs only —
         this is the bug being fixed (`wr suspend` is documented as suspending
         queued commands).
      2. `wr suspend -i <rg>` / `wr resume -i <rg>` on a repgroup whose jobs have
         all finished now exits 1 with `no matching jobs found` instead of exit 0
         printing `Suspended 0 queued commands (out of 4 matching)`. Unavoidable
         without reinstating the unbounded scan, and consistent with `wr remove
         -i`, which already dies for the same reason. Pinned by a test.
      3. NO change to `wr status` (any form), the REST status endpoints, or the web
         status page — all still return archived jobs.
  - REST 409 PRESERVED. The first review cycle caught this being silently changed
    to 404 for an all-complete repgroup, and found the behaviour is specified in
    `.docs/issue-197/spec.md:133-137` (409 `no editable jobs matched` for targets
    resolving only to non-editable states; 404 only for targets resolving to
    nothing). Preserving it costs no scan: `archiveJobTx` unconditionally writes
    `bucketRGEndTime[repGroup]`, so the new `db.repGroupHasHistory` answers "does
    this repgroup have history?" with one O(log n) `Get`, no decode, no write lock,
    and only on the refusal path.
  - Files: `cmd/suspend.go`, `cmd/status.go`, `cmd/status_test.go` +
    `cmd/suspend_test.go` (pre-existing `--recent` flag-pollution resets — proven
    load-bearing: with both removed `./cmd/` fails "-f, -i, -l and -a are mutually
    exclusive"), `jobqueue/job.go`, `jobqueue/server.go`, `jobqueue/serverCLI.go`,
    `jobqueue/serverREST.go`, `jobqueue/serverWebI.go`, `jobqueue/db.go`,
    `jobqueue/rest_test.go`, NEW `cmd/suspend_history_test.go`, NEW
    `jobqueue/reliable4_history_scan_test.go`, NEW
    `jobqueue/reliable4_history_seed_test.go` (`reliability_repro`),
    `developers/wrdev.sh` (new `control-rpc-history` mode), `DEVELOPERS.md`
    (rule 6 extended from "no history cold-scan on startup" to control paths too).
  - RED/GREEN: CLI `(out of 5 matching)` -> `1` and `(out of 10 matching)` -> `2`;
    server-side suspend/resume `-i rg` **5,000 decodes / 84.9ms -> 0 decodes /
    358us**, `-i sub -z` 10,000 -> 0, REST PATCH 5,000 -> 0, while `wr status -i`
    still returns 5,003 jobs / 5,000 decodes (the wrong-direction guard).
  - Scale gate `wrdev.sh control-rpc-history` A/B at 200,000 archived jobs over 20
    repgroups (a tenth of prod's history): `resume -i <substr> -z`
    **10,578ms -> 235ms** and peak-RSS growth **3,137MB -> 51MB** (61x),
    reproducing prod's 12.1GB excursion in miniature. Gate guards: an in-band
    reference scan (`wr status -i`, which legitimately DOES want the history) must
    return exactly the seeded per-group count or the gate FAILs NOT MEASURED, plus
    per-group/total history floors (`control-rpc-history 20 20` correctly FAILs
    NOT MEASURED) and a VmHWM baseline sampled BEFORE the timed commands —
    load-bearing, since pre-fix `suspend` alone (1,651ms) is under the 5s bar, so
    without the RSS metric only `resume -z` would catch the bug.
  - Gates: `make lint` 0 issues; `make test` 385 passed (was 382); `make race` 385
    passed, 0 data races. Regression gates `idle-backlog-cpu 50000 25` and
    `backlog-rescan-check 2000 50000` both PASS (Bugs 1-2 and 260725-2 intact).
  - FOLLOW-UPS recorded, deliberately NOT done here (the doc scopes them out; the
    first two may warrant their own spec):
      a. Push `Limit`/`Offset` down into `retrieveCompleteJobsByRepGroup` (and its
         RTK cursor) so even a legitimate `wr status -i ... -z -l 1` cannot
         materialise millions of jobs — today the limit is applied in `limitJobs`
         AFTER the full decode, so that path can still blow the heap.
      b. Request cancellation, so a disconnected/timed-out client stops the work:
         prod showed two `handleGetByRepGroup` goroutines still CPU-bound 12+
         minutes after the client gave up, one encoding a multi-GB reply for
         nobody. Cancellation plumbing across the RPC layer is plausibly a
         `/spec-writer` item.
      c. Log a warning when a request exceeds a few seconds or allocates heavily —
         prod logged NOTHING for a 12-minute, 12GB request.
      d. `getRepGroupsList` still calls `retrieveRepGroups()` for every non-exact
         match, so `-z` still enumerates every historical repgroup; the expensive
         decode is gone so this is now cheap.
      e. Narrow back-compat gap in the 409 probe: `bucketRGEndTime` arrived in
         v0.36.5 with no rebuild on upgrade, so a repgroup whose last completion
         predates v0.36.5 and which has no live jobs answers 404 where the spec
         says 409, until something new completes in it. Deliberately NOT fixed: a
         rebuild would be a startup history scan (violating the very rule 6 this
         change extends), and a `bucketRTK`-prefix fallback would flip genuine
         404s into 409s. Only affects which 4xx refusal is returned.
  - Follow-up (Bug 1's regression guard made deterministic): the memo test was
    flaky under host load (seen at load ~100 on 8 cores as
    `Expected 20000 / Actual 17392`). Root cause is BIDIRECTIONAL: with no runner
    command configured, the server's own background rac cycles take
    `buildSchedulerGroups`' `rc == ""` branch, which runs `prepareReadyJob` for
    EVERY ready job and therefore INVALIDATES each job's memo. So a background
    cycle racing the test could warm memos a driven cold cycle had not reached yet
    (17,392 instead of 20,000) OR make already-derived jobs cold again (21,580).
    Fixed by pausing the server in `memoBacklogServer` for the test's lifetime, so
    the only rac cycles that run are the ones the tests drive, plus a cumulative
    `memoDerivations == memoBacklog` assertion. Both original assertions stay
    exactly as strict (`cold == memoBacklog`, `steady == 0`) — the flake was fixed
    by making the measurement deterministic, NOT by loosening a threshold.
  - `Server.Pause()` is safe here and does not weaken the test: it only sets
    `drain`/`Mode`/`pauseRequests` (server.go:3755-3774); the one consumer that
    matters is `readyAddedCallback` (server.go:4199), whose early return fires
    before it would call `buildSchedulerGroups` — which itself never consults
    `drain`/`Mode`, so the directly-driven calls the test measures are byte-for-byte
    the same paused or not. `Pause()` does not gate `Add` (no `drain` check on
    `handleAdd` -> `createJobs` -> `queueNewJobItems` -> `enqueueItems`), so the
    backlog still populates fully and `len(AllItems()) == n` still means what the
    helper assumes.
  - Verified: defeating the memo (`derivedLocked` computing without storing) makes
    `steady == 0` fail with **60,000** (3 cycles x 20,000), so the guard still
    catches exactly the regression it exists for; **20 consecutive green
    iterations** at load 86-93; `BenchmarkReadyBacklogSnapshot` 2.06-2.88 ms/op at
    **0 B/op, 0 allocs/op**; `make lint` 0 issues; `make test` 385 passed;
    `make race` 385 passed with the fix at load ~104 (a matched-load controlled
    comparison — an earlier run at load ~118 failed three unrelated tests
    `TestServerWebI`/`TestJobqueueProduction`/`TestJobqueueExecutionAndDependencyScenarios`,
    which a pristine baseline and this load-matched with-fix run both cleared as
    contention, not this change); `idle-backlog-cpu 50000 25` PASS at 660ms/25s.
  - Known trap for whoever next reuses `memoBacklogServer` (pre-existing server
    behaviour, not introduced here): the drain early-return at server.go:4199
    happens before `racRunning = true` and before `defer s.finishRAC()`, so a
    trigger that ran `setRACPending` while paused leaves `racPending` true with
    `waitingReserves` unclosed, and `waitForPendingReserves`
    (serverCLI.go:768-782) would then block forever. These tests never reserve or
    touch, so they are unaffected — but a future test that reuses this helper AND
    reserves a job would HANG rather than fail. Such a test must `Resume()` first
    (the helper also never resumes, and directly-driven `buildSchedulerGroups`
    does not dispatch runners, since `scheduleGroupRunners` is called from
    `readyAddedCallback` at server.go:4229).
- [x] Bug 4 (Priority 4, Finding 5) — a command that can NEVER exec (`fork/exec: argument list too long`, i.e. Cmd over MAX_ARG_STRLEN 128KB) is treated as a transient failure and retried ~31 times. Classify E2BIG/ENOENT/EACCES from fork/exec as permanent: bury on the FIRST attempt with a FailReason naming the real cause, Attempts == 1.
  - `jobqueue/client.go`: every `cmd.Start()` error used to take one path,
    `c.Release(job, nil, FailReasonStart)` = "retry me". Now
    `reportStartFailure` classifies the failure via `permanentStartFailReason`
    and BURIES the permanent ones on the first attempt, via the house
    `buryWrapErr` so the real cause is recorded as the job's stderr.
  - Permanent (bury): `E2BIG` -> new `FailReasonCArgs` ("command line too long"),
    `ENOENT` -> reused `FailReasonCFound`, `EACCES` -> reused `FailReasonCPerm`
    (the same vocabulary `classifyExitStatus` already uses for shell exit 127/126,
    which it likewise treats as permanent). Deliberately LEFT retryable, each
    pinned by a test so a future widening trips it: `ETXTBSY` (produced for real
    with an open write handle, and proven transient in-band — the same job
    completes once it is closed), `ENOEXEC`, `ENOMEM`, `EAGAIN`, `EMFILE`,
    `ENFILE`, `EINTR`, `EISDIR`, `ENOTDIR`, and `exec.ErrNotFound`.
  - Why burying ENOENT/EACCES cannot strand work on a healthy node: `exec.Command`
    pre-resolves via `LookPath` ONLY when `filepath.Base(name) == name`, so a BARE
    name (wr's default `RunnerExecShell` is `bash`) can only ever produce
    `*exec.Error{Err: exec.ErrNotFound}` — never an errno — and stays retryable,
    while a slash-containing path produces the raw errno. The two branches are
    disjoint by construction; both halves are pinned by
    `TestReliable4ForkExecErrnoWrapping`. So a farm node missing `bash` still
    retries; only an operator-configured absolute shell path buries.
  - THE BUG WAS WORSE THAN FINDING 5 RECORDS. The doc says "retried ~31 times";
    the retries are in fact UNBOUNDED. `releaseJobSnapshot`/`finalizeReleasedJob`
    (`server.go:4961`/`:5019`) only decrement `UntilBuried` when
    `!job.StartTime.IsZero()`, and server-side `StartTime` is set ONLY in
    `applyJobStart` (`serverCLI.go:949`), which requires a real pid+host. An exec
    that never started never reports a start, so `UntilBuried` stays at
    `Retries+1` forever and the ceiling is never reached — **even `--retries 0`
    cannot stop it**. Measured: `untilBuried=3` still after 4 releases; the
    pre-fix scale run sat at `delayed: 20, buried: 0` for 90s while the failure
    count climbed 20 -> 47 across 22 runner logs. The doc's "31" came from a
    DIFFERENT job class (a dedupe job with `FailReason "command exited non-zero"`).
  - `Attempts` is 0, not the 1 the doc asked for, and 0 is the correct value:
    `Attempts++` happens only in `applyJobStart`, which needs a real pid, so
    reaching 1 would mean faking a start report. Every existing pre-exec bury
    (`FailReasonCwd`, `FailReasonEnv`, `FailReasonMount`, docker setup) also
    leaves `Attempts == 0`. The property the operator cares about is asserted
    instead: exactly ONE reservation/runner slot consumed (`reserves == 1`),
    `state == buried`, `UntilBuried == 0`, and the real cause in `FailReason`.
  - Log volume: the permanent branch quotes at most 200 bytes of the command line
    plus its total length (`abbreviateCmdLine`), because the E2BIG case is by
    definition a >128KB command and every runner logs this at warn — quoting it
    whole is what produced prod's 1.3MB single log lines (~1.3MB -> ~340 bytes).
    The transient branch's message is byte-identical to before. `Exitcode` still
    stays -1 and `Exited` false, as the old `Release` path left them.
  - Files: `jobqueue/client.go`, NEW `jobqueue/reliable4_execfail_test.go`
    (`TestReliable4ForkExecErrnoWrapping` pins the stdlib wrapping,
    `TestReliable4PermanentStartClassification` pins the errno boundary incl. the
    negative cases, `TestReliable4ExecImpossibleBuriedFirstAttempt` is the
    live-server behavioural reproducer), `developers/wrdev.sh` (new
    `exec-impossible-retries [jobs] [seconds] [cores]` gate, farm-safe `-s local`).
  - RED/GREEN per case (reserves / state / FailReason): E2BIG 4/delayed/"command
    failed to start" -> **1/buried/"command line too long"**; ENOENT and EACCES
    likewise 4/delayed -> 1/buried with "command not found" / "command permission
    problem"; ETXTBSY negative control unchanged at 4/delayed.
  - Scale gate A/B, identical params, pre-fix binary built from a pristine
    worktree: **buried 0/20, 47 exec attempts and still climbing, 22 runner logs,
    FAIL** -> **buried 20/20, exactly 20 exec attempts, 2 runner logs, PASS**.
    The gate measures three independent things (runner-side failure lines,
    manager-side buried count, and the FailReason on every job) and FAILs
    NOT MEASURED on a missing measurement, a short command that can actually
    exec, zero jobs added, a dead manager, empty counts or zero runner logs — all
    verified to fire.
  - `ef_reap_runners` (the gate kills leftover `-s local` runners, on a shared host
    that runs other people's production managers) has three independent safety
    gates: same-user `ps` only, an EXACT argv `--logdir` token match (a substring
    match would have reaped a prefix-sharing logdir — proven with decoys, then
    fixed), and the house `is_ours` re-check requiring `$WRDEV_ROOT/wr`. `ps`
    output is captured before filtering so the filter cannot self-match. Decoy
    test after the fix: exact-logdir-but-not-our-binary SURVIVES, exact match
    KILLED, prefix-logdir SURVIVES.
  - Gates: `make lint` 0 issues; `make test` 388 passed (was 385); `make race` 388
    passed, 0 data races. Regression gates `idle-backlog-cpu 50000 25` and
    `backlog-rescan-check 2000 50000` both PASS.
  - `wr add` deliberately does NOT reject an over-long `Cmd`: with `job.Group`
    set, `buildExecCmd` feeds the command to `newgrp` on STDIN, so an over-long
    `Cmd` execs fine and an add-time reject would refuse a runnable job. The
    effective argv is also longer than `Cmd` (module-load and `set -o pipefail`
    prefixes) and `MAX_ARG_STRLEN` is Linux-specific, so any threshold is a guess.
    The manager now names the real cause on the first attempt instead.
  - FOLLOW-UPS:
      a. **The `UntilBuried`/`StartTime` defect is NOT closed for the transient
         path** this fix deliberately preserves. Any release without a start still
         fails to decrement `UntilBuried`, so a persistent-but-transient start
         failure (a permanently `noexec` mount, a broken loader, a shell path that
         stays busy) retries FOREVER ignoring `--retries` — the ETXTBSY negative
         control demonstrates it (`reserves=4, untilBuried=3`). Fix by either
         decrementing on any owner-reported release regardless of `StartTime`, or
         adding a distinct pre-start attempt counter. Worth its own `/bugfix`.
      b. `cmd/runner.go:246` ("reserved a job" logging `job.Cmd`) and `:304`
         ("will start executing [%s]") still log the full `Cmd`, i.e. ~2 x 130KB
         per job; the doc's `reserved a job` p99 of 24,261 / max 1,345,498 bytes
         comes from line 246 and is untouched. This fix cuts the multiplier from
         N to 1, which is the bulk of the win.
      c. A warning-only (not rejecting) over-long-`Cmd` notice in `cmd/add.go`
         remains possible.
- [ ] Bug 5 (Priority 5, Finding 4) — `killExcessCmds` hands `bkill` an unbounded argv with no timeout, blindly retries the identical failing kill every cycle, and spams 75KB/min of log. Chunk `toKill` to a bounded batch (model the cap on the 260722-1 bsub fix), run each bkill under a context/timeout, use the house `backoff` package (DEVELOPERS.md rule 8) so an identical failing kill is not re-issued every cycle, log a summary (counts + a few sample ids) not the whole id list, and make the outcome distinguishable (how many were actually killed vs already-gone) so "no matching job found" can no longer hide un-reclaimed over-provisioned runners.

Not in this batch: Finding 7 (web status count divergence) needs a live repro
first — see the doc's Priority 6.
