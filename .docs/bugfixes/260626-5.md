- [x] Redesign `TestJobqueueSignal` to decouple from real host CPU load so it is deterministic on CI and locally (PR #533). Today its recover job requires `Cores: runtime.NumCPU()` (ALL cores); under any parallel-test contention (CI's 4-core/7-lane runner, or a loaded dev box) the all-cores scenario can't reliably start/complete, so the start marker never appears and the test fails — a code-correct test that is unverifiable under contention (NOT a code regression; confirmed the emission chokepoint never touches runner-spawning). Maintainer chose: redesign it. Prove the SAME behaviours — (1) killed runners after a hard server crash come up lost, and (2) new runners don't overcommit resources due to existing runners — against a CONSTRAINED scheduler capacity rather than `NumCPU` physical free cores, while keeping real runner subprocesses for the crash-recovery part. The local scheduler schedules against a configurable `MaxCores` (scheduler/local.go: default `runtime.NumCPU()`, capped by `ConfigLocal.MaxCores`; it counts against its own accounting, not real-time host free cores), so a small fixed `MaxCores` + a capacity-holding job sized to it proves no-overcommit independent of real load. Also fix the test's client-driven `Reserve` ordering race in its earlier Convey blocks (tight 50ms reserves that race under load). Success criterion: `TestJobqueueSignal` passes reliably (repeatedly) even on a loaded box.
  - Investigation found the DOMINANT cause was deeper than the all-cores job: a phantom cross-test reserver. The `--servermode`/`--runnermode` subprocesses re-ran the whole test binary UNFILTERED, so unguarded reserving tests — notably the recv-fix's own `TestClientRequestTimeoutDecoupledFromConnect` (a blocking `Reserve(2s)`) — ran inside `TestJobqueueSignal`'s server on the inherited port (servermode skips test-config port isolation) and stole its jobs, making the reserves nondeterministic.
  - Fix (jobqueue/jobqueue_test.go only; no production code): restrict both subprocesses to `-test.run TestJobqueue` (`startServer` args and `runServer`'s `RunnerCmd`), matching the existing `-race` runner-binary path. The two dispatcher entrypoints (`TestJobqueueSignal`, `TestJobqueueRunnerModeEntrypoint`) still run; all unguarded reservers are excluded from the subprocess.
  - Decoupled from host load: set the test daemon's local scheduler `MaxCores = signalTestMaxCores` (1) via the existing `SchedulerConfig` knob, and sized the capacity-holding recover job to that capacity instead of `runtime.NumCPU()`. No-overcommit is now proven against the scheduler's configured-capacity accounting (`floor((maxCores - cores)/req.Cores) = 0`), independent of how many physical cores are free. Production default unchanged.
  - Made the reserves deterministic with a `reserveJobsByCmd` helper (reserves until each expected Cmd is obtained, keyed by Cmd; a genuine non-reservation still fails via `ShouldNotBeNil`).
  - Both behaviours preserved: hard-crash recovery (real `--servermode`/`--runnermode` subprocesses, SIGKILL of server + lost runner, restart with kept DB, runners come up `JobStateLost`) and no-overcommit (`job3.StartTime ShouldHappenOnOrAfter job.EndTime`). Verified by implementor + reviewer: `go build`/`go vet`/`make lint` clean; `TestJobqueueSignal` passes 3/3 (~15-16s, was 182s+/flaky) on a loaded box; sibling runner tests pass; `TestClientRequestTimeoutDecoupledFromConnect` still passes standalone (it just no longer runs inside the subprocess).
