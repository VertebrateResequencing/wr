- [x] That didn't help, see below. Are you actually trying your fixes out on copy of .tmp/db? Fix it properly and verify that it is really fixed.

  ```
  INFO[07-08|21:44:56] wr manager is upgrading its database: writing sorted database job lookup index (3090000 reverse entries written, 3115527 entries processed)
  INFO[07-08|21:44:57] wr manager is upgrading its database: committing database upgrade
  EROR[07-08|21:45:32] wr manager failed to start on port 11465 after 10s
  ```
  - Root cause: after `initDB` finished the Bolt upgrade, `Serve` still had slow pre-token startup work (`createQueue`, `loadPriorState`, web setup, then token persistence), but the upgrade sidecar had already been removed, leaving the parent with no live progress signal.
  - Added a post-upgrade startup sidecar state, `starting manager after database upgrade`, which is written only after an existing/restored DB actually needed migration and is removed when startup returns.
  - Kept brand-new DB startup out of the DB-upgrade timeout path by distinguishing fresh DB initialization from legacy DB migration.
  - Added bounded behavioural regressions for the post-upgrade pre-token window and for brand-new DB startup not reporting an upgrade.
  - Verified directly on a production-mode copy of `.tmp/db` with `--timeout 10`: upgrade reached `committing database upgrade`, then `starting manager after database upgrade`, then started successfully in `real 39.60`; the isolated manager stopped cleanly and the sidecar was absent afterward.
