- [x] When I use this on the example db, I get output like the following. Why is processed always equal to total? That just looks like a bug. If we can't easily know the true total ahead of time, don't output it. If we can get the true total quickly, fix the bug. Use the bugfix skill workflow.
  - Removed the misleading reverse lookup rebuild denominator; the upgrade cannot know the true total without an extra walk over large legacy lookup buckets just for display text.
  - Reworded progress to report the honest cumulative source-entry count and current source bucket, e.g. `10000 source entries processed so far; currently reading repgroupToKey`.
  - Added behavioural regression coverage for the emitted upgrade progress detail across two source buckets, proving the fake `total` wording is gone.
- [x] Another item to add to the checklist: again using .tmp/db on another machine, the first manager start fails like below. Waiting a while longer the process is still running, but the token still hasn't been written. Killing the pid after a minute or 2 and then starting the manager again doesn't trigger the database upgrade and also fails.

  ```
  INFO[07-08|21:09:24] wr manager is upgrading its database: writing sorted database job lookup index (3090000 reverse entries written, 3115527 entries processed)
  INFO[07-08|21:09:25] wr manager is upgrading its database: committing database upgrade
  EROR[07-08|21:10:01] wr manager failed to start on port 11465 after 10s
  ```
  - Fixed the parent startup wait to keep treating a DB-upgrade sidecar from this startup as active while the recorded upgrade process is still alive, even if the final Bolt commit phase is quiet for longer than the freshness window.
  - Added a regression for the quiet commit phase with no token yet: startup now keeps reporting `committing database upgrade` and waits until the manager becomes connectable instead of falling back to the ordinary timeout.
  - Verified the restart-after-kill part does not need a separate migration recovery change: killing before Bolt commits rolls the transaction back so the upgrade is retried, while killing after commit means the DB is already upgraded and no later upgrade should run.
