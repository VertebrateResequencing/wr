# Bugfixes 260827-2

The seven items come from the "SUPERSEDING ANALYSIS (2026-08-27, code-first,
independent)" section of `.docs/reliable4/throughput-architecture.md`, read
against the production measurements in `.docs/reliable4/prod-validation-260827.md`.
They are the cheap, contained part of the write-path work: cut how many bolt
write transactions one `wr add` costs, and close two LSF hazards one call site
away from the ones `df13076` fixed.

`.docs/bugfixes/260827-1.md` (today's other checklist) is done and untouched
by this one. `.docs/bugfixes/260824-1.md` is binding history for the LSF exec
bounds items 5 and 7 touch.

## How write transactions are counted here

bbolt's `DB.Stats().TxN` counts **read** transactions only (it is incremented in
`beginTx`, not `beginRWTx`), so the red commands below do not use it. bbolt
increments the meta txid once per **committed write** transaction and a read
transaction reports the current one, so

```go
db.bolt.View(func(tx *bolt.Tx) error { id = tx.ID(); return nil })
```

is an exact, zero-instrumentation count of committed write transactions.

Two tunings are measured, because they answer different questions:

- **default coalescing** (`ServerDBBatchDelay` 10ms, `ServerDBBatchSize` 10000):
  how many transactions the add path actually commits when it is the only writer.
  Sequential phases cannot coalesce with each other, so this counts phases.
- **coalescing disabled** (`MaxBatchSize` 1, a legitimate configuration via
  `WR_MANAGERDBBATCHSIZE`): how many transactions the add path *asks* for.
  `DB.Batch`'s full-batch check runs under `batchMu` before the append, so with
  `MaxBatchSize` 1 every `Batch` call deterministically gets its own transaction.

## Red measurements at `062d05a`, before any fix

Probe sources kept in the session scratchpad as
`zzred_probe_jobqueue_test.go.txt` and `zzred_probe_scheduler_test.go.txt`; they
build their fixtures from the existing `dgrStartServer` and
`newFakeLSFScheduler` helpers and were run in-tree as
`jobqueue/zzred_probe_test.go` / `jobqueue/scheduler/zzred_probe_test.go`.

```
go test -tags netgo -count=1 -v ./jobqueue/ -run \
  'TestRedProbeAddWriteTransactions|TestRedProbeStoreEnvRepeat|TestRedProbeStoreBatchedEmptyRemainder'
```

Exit status 1:

```
default add 0 (1 jobs): write transactions=2, bucketEnvs KeyN +0
default add 0 cost 2 write transactions, want 1
nocoalesce add 0 (1 jobs): write transactions=4, bucketEnvs KeyN +0
nocoalesce add 0 cost 4 write transactions, want 1
bulk1000 add 0 (1000 jobs): write transactions=6, bucketEnvs KeyN +0
storeLimitGroups(empty): write transactions=1
storeLimitGroups(empty) cost 1 write transactions for zero work, want 0
storeLimitGroups(nil): write transactions=1
num=999: storer calls=1, empty calls=0
num=1000: storer calls=2, empty calls=1
num=1000 made 1 empty storer calls (each a committed empty write
  transaction), want 0
num=1001: storer calls=2, empty calls=0
num=2000: storer calls=3, empty calls=1
re-storeEnv of an unchanged, already-stored env: write transactions=1,
  bucketEnvs KeyN +0
re-storing an unchanged env cost 1 write transactions (a full commit that
  writes nothing new, blocking before createJobs), want 0
```

```
go test -tags netgo -count=1 -v ./jobqueue/scheduler/ -run \
  'TestRedProbeBjobAppearedUnbounded|TestRedProbeCountCmdsPrunesOnError'
```

Exit status 1:

```
waitForBjob took 25.10591168s, ok=true (bjobsAppearTimeout=10s,
  bjobs hangs 25s)
waitForBjob absorbed the whole hanging bjobs round trip: 25.10591168s
  (want a bound near bjobsAppearTimeout=10s)
countCmds err=scheduler(lsf) parseBjobs(): failed to run [bjobs -w]: exit
  status 1, reserved elements left=0
a failed bjobs pruned the reserved set to 0 (want 2): wr may now bkill an
  element it has handed a reservation to (DEVELOPERS.md rule 5)
```

## The read-then-write hazard, and why items 2 and 3 do not widen it

`prepareNewJobs` computes `jobsToQueue`/`jobsToUpdate` from a **read**
transaction taken before the write (`db.go:2984`), so widening a fold widens the
window in which two adds that are each other's dependency-resolution input could
both observe "no live member".

Neither item 2 nor item 3 widens it, for the same reason: both act **entirely
after** that read has closed, and neither folds work from more than one request.

- Item 2 replaces a write transaction with a read one, or skips it. It runs
  before `storeNewJobs`, so it cannot move `prepareNewJobs`' read at all.
- Item 3 merges five write transactions that already all happen after the read,
  into one. The window's start is unchanged and its end arrives no later, since
  one commit of the same puts cannot finish after five sequential commits of
  them. The fold is intra-request: one add's five bucket puts, never several
  adds' puts.

Anything that folded *across* requests would widen it, and is out of scope here.

## Items

- [x] **1. `bucketEnvs` grows by one never-deleted record per add.** Claimed in
  the superseding analysis as "the upstream cause of the DB growth every other
  mechanism feeds on": `managerremotesameaslocal: true` makes `addEnvVars`
  (`cmd/add.go:713`) ship the client's whole `os.Environ()`, which on an LSF exec
  node includes `LSB_JOBID`, so `byteKey` (`jobqueue/utils.go:145`) is unique per
  add; `envCacheSize` is 12 (`db.go:69`) and `bucketEnvs` has only `Put` and
  `Get`, never `Delete` (`db.go:3224`, `:3243`).

  **The growth claim is FALSIFIED. Do not credit any fix here with relieving DB
  growth.** Measured on the read-only production copy
  `/nfs/hgi/wr/sb10-bigdb/prod.db` (7.93 GB, 2026-07-28, taken while
  `managerremotesameaslocal: true` was already set - the config has not changed
  since 2026-07-08):

  | bucket | KeyN | LeafInuse | mean record |
  | --- | --- | --- | --- |
  | `bucketEnvs` | **549** | **1.31 MB** | 2,392 B |
  | `bucketJobsLive` | 118,213 | 0.18 GB | 1,633 B |
  | `bucketJobsComplete` | 2,155,154 | **2.87 GB** | 1,427 B |

  549 env records against 2.27M jobs, 0.04% of the DB's leaf bytes. The code
  reason the env does not vary per add: a `wr add` that runs as a wr job does not
  inherit the runner's LSF environment, because `Client.Execute` sets
  `cmd.Env = env` from `job.Env()` (`client.go:1786-1814`), **replacing** the
  environment with the one stored at the originating add. So jobs that add jobs
  propagate one stable env rather than minting a new one, and `TMPDIR` is
  overridden only when `tmpDir != ""` (mounts).

  **What is real, and is what gets fixed:** on a cache miss `storeEnv` re-`Put`s
  bytes already identical in `bucketEnvs`, paying a full write transaction that
  writes nothing new, **sequentially before `createJobs`**. With 549 distinct
  envs in the DB and a 12-entry cache, that is the ordinary production case.

  - Red command: `go test -tags netgo -count=1 -v ./jobqueue/
    -run TestRedProbeStoreEnvRepeat` - store an env, evict it with
    `envCacheSize+5` others, store it again. Exit status 1:

    ```
    re-storeEnv of an unchanged, already-stored env: write transactions=1,
      bucketEnvs KeyN +0
    re-storing an unchanged env cost 1 write transactions (a full commit that
      writes nothing new, blocking before createJobs), want 0
    ```

  - Not changed, deliberately: shipping per-runner environments. The env volume
    is 1.31 MB, so there is nothing to win, and dropping it would change
    user-visible behaviour. The operator needs no config change either.

  **Fixed (2026-08-27, branch `reliable4`):**

  - `jobqueue/db.go`: on a cache miss `storeEnv` now asks `db.bolt.View` whether
    `bucketEnvs` already holds the key (`Get(...) != nil`, the mmap slice compared
    to nil and never retained, so no ~2.4 KB copy) and calls `db.store` only when
    it does not. The cache is populated either way. The returned `envkey` and the
    error behaviour are byte-for-byte HEAD's, including caching nothing when the
    store fails; the one new error source (`View`) can only fire where `Batch`
    would also have failed, and lands on the same `ErrDBError` in `handleAdd`.
  - Finding the key stored is as conclusive as finding it cached was. Both assume
    no collision of `byteKey`'s 128-bit hash, and nothing deletes from
    `bucketEnvs` - the reviewer enumerated every `Delete`/`DeleteBucket` in
    `jobqueue/` (`bucketStdO`, `bucketStdE`, `bucketJobsLive`,
    `bucketEndTimeToKey`, `bucketLGs`, `bucketJobLookupEntries`, and the reverse
    lookups, which `isIndexedLookupBucket` gates to `bucketRTK`/`bucketDTK`/
    `bucketRDTK`). A dev DB wipe or backup restore goes through the opener that
    builds a fresh `envcache`, so cache and bucket cannot disagree across a swap.
  - **Does not widen the `prepareNewJobs` read-then-write window.** `storeEnv`
    returns before `createJobs` (`serverCLI.go:886` then `:892`), which is where
    both ends of that window live, so neither end moves. The removed write
    transaction never ordered one add's read against another add's job write
    (bolt's writer mutex orders writes only, and the removed write preceded each
    add's own read), so no protective ordering is lost.
  - Regression test `jobqueue/reliable4_add_tx_test.go`
    (`TestReliable4AddEnvNoRewrite`) on the existing `dgrStartServer` fixture: a
    first-time store costs exactly one write transaction, and a store of an env
    already in the db, evicted from the cache with `envCacheSize+5` others, costs
    none. Both go through `addTxStoreCost`, which removes the key from the cache
    and asserts `retrieveEnv` reads the bytes back, so the guarantee cannot be met
    by never writing.
  - `envCacheSize` stays 12. The `View` makes it irrelevant to the write path, so
    raising it would only save a cheap read transaction, at ~1.3 MB of retained
    heap to hold production's 549 envs.

  **Mutation mapping:**

  | Reverted | Assertion that fails |
  | --- | --- |
  | `storeEnv` restored to HEAD's body | `reliable4_add_tx_test.go:76` `So(addTxStoreCost(ctx, d, env), ShouldEqual, 0)` Expected 0, Actual 1 |
  | the `db.store` call dropped ("never write") | `:97` `So(d.server.db.retrieveEnv(ctx, envkey), ShouldResemble, env)` Actual `[]uint8(nil)`, in both leaves |

  **Gates** (host load average 122-123, 8 cores), run by the implementor and
  again by the reviewer: `make lint` 0 issues; `make test` **492 passed - 9
  skipped - 29 packages - 4m54s** (baseline 491 plus the one new test, skips
  unchanged, no flake); `go test -race ./jobqueue/ -run
  TestReliable4AddEnvNoRewrite` ok 1.8s, and `-count 10` ok 7.2s, so the
  transaction-id deltas are not load-sensitive; `go vet -tags netgo` and
  `-tags 'netgo reliability_repro'` clean on `./jobqueue/`; `cleanorder -min-diff
  -dry` on `db.go` shows only the four known-dirty hoists and nothing near
  `storeEnv`, and is a no-op on the new test file.

- [ ] **2. `storeLimitGroups` takes a write transaction unconditionally**
  (`db.go:1230`), including when the map is empty and `storeLimitGroup`
  (`db.go:1274`) returns `limitGroupUnchanged` without a `Put`. `Tx.Commit` has
  no early-out - it always rewrites the freelist page and fsyncs twice - and this
  blocks before `storeNewJobs`.

  - Red command: `go test -tags netgo -count=1 -v ./jobqueue/
    -run TestRedProbeAddWriteTransactions`. Exit status 1:

    ```
    storeLimitGroups(empty): write transactions=1
    storeLimitGroups(empty) cost 1 write transactions for zero work, want 0
    storeLimitGroups(nil): write transactions=1
    storeLimitGroups(nil) cost 1 write transactions for zero work, want 0
    ```

- [ ] **3. `storeNewJobData` spawns five goroutines for five concurrent
  `bolt.Batch` calls** (`db.go:2306-2352` via `launchBatchStore`). Concurrency
  across bolt write transactions can never help - they serialise on one mutex -
  so the goroutines only add transactions.

  - Red command: `go test -tags netgo -count=1 -v ./jobqueue/
    -run TestRedProbeAddWriteTransactions`. Exit status 1:

    ```
    nocoalesce add 0 (1 jobs): write transactions=4, bucketEnvs KeyN +0
    nocoalesce add 0 cost 4 write transactions, want 1
    ```

    With default coalescing the same add commits 2, because bbolt's 10ms window
    happens to merge the five calls; with the window closed the add path's real
    request of one transaction per bucket is visible. Both must reach 1.

- [ ] **4. `storeBatched` empty-remainder bug** (`db.go:3907`):
  `if offset := num - (num % batchSize); offset != 0` fires with `offset == num`
  whenever `num % batchSize == 0`, calling `storer(bucket, data[num:])` on an
  empty slice - one extra fully-committed empty write transaction per store.

  - Red command: `go test -tags netgo -count=1 -v ./jobqueue/
    -run TestRedProbeStoreBatchedEmptyRemainder`. Exit status 1:

    ```
    num=999: storer calls=1, empty calls=0
    num=1000: storer calls=2, empty calls=1
    num=1000 made 1 empty storer calls (each a committed empty write
      transaction), want 0
    num=1001: storer calls=2, empty calls=0
    num=2000: storer calls=3, empty calls=1
    ```

- [ ] **5. `bjobAppeared` is an unbounded `bjobs -w` that can permanently wedge a
  scheduler group** (`jobqueue/scheduler/lsf.go:1816-1823`): no context, no
  timeout, no `WaitDelay`, carrying the `//nolint:gosec,noctx` that `df13076`
  removed from `parseBjobs`. `bjobsAppearTimeout` does not bound it, because the
  `<-limit` arm cannot be selected while `s.bjobAppeared(jobID)` runs in the
  `<-ticker.C` arm (`lsf.go:1792-1808`), so a hung call hangs `waitForBjob` ->
  `submitToQueue` -> `schedule()` forever and wedges `Scheduler.limiter[cmd]`
  (`scheduler.go:585-604`) so that scheduler group is never scheduled again.

  - Red command: `go test -tags netgo -count=1 -v ./jobqueue/scheduler/
    -run TestRedProbeBjobAppearedUnbounded`, with a fake `bjobs` whose
    `bjobs -w <id>` appearance check sleeps 25s. Exit status 1:

    ```
    waitForBjob took 25.10591168s, ok=true (bjobsAppearTimeout=10s,
      bjobs hangs 25s)
    waitForBjob absorbed the whole hanging bjobs round trip: 25.10591168s
      (want a bound near bjobsAppearTimeout=10s)
    ```

    `ok=true` is the point: the timeout arm never won, it just answered 15s late.

- [ ] **6. `countCmds` prunes reserved elements from a possibly-truncated
  snapshot** (`lsf.go:2094-2098`): it calls `pruneReserved(present)` even when
  `parseBjobs` errored, dropping ids wr has handed reservations to - the
  protection DEVELOPERS.md rule 5 exists for. Narrow today (needs `cmd == ""`,
  whose only production caller is shutdown), but `df13076`'s new 30s timeout
  makes mid-list truncation reachable where it previously was not.

  - Red command: `go test -tags netgo -count=1 -v ./jobqueue/scheduler/
    -run TestRedProbeCountCmdsPrunesOnError`, two reserved elements and a
    `bjobs -w` list call that exits 1. Exit status 1:

    ```
    countCmds err=scheduler(lsf) parseBjobs(): failed to run [bjobs -w]: exit
      status 1, reserved elements left=0
    a failed bjobs pruned the reserved set to 0 (want 2): wr may now bkill an
      element it has handed a reservation to (DEVELOPERS.md rule 5)
    ```

- [ ] **7. Cheap correctness and polish.** Three unrelated small things, verified
  present at `062d05a`:

  - `limiter/doc.go` still says "Your callback is only called once per group
    while that group is in use", which `5cfb9b2` made false: `withResolvedGroups`
    resolves with `l.mu` released, so the callback can be called concurrently and
    more than once for the same group. `limiter.go`'s `SetLimitCallback` doc
    already says so; the package doc contradicts it.
  - `limiter/limiter.go`'s copyright reads `2019-2021, 2024-2025`, but `5cfb9b2`
    changed it in 2026.
  - `parseBjobs` (`lsf.go:2190-2208`) sets no `Stderr`, so a `bjobs` failure
    surfaces only `exit status N` where `bsubStderr` surfaces LSF's reason.

  - Red command: none of the three is a behaviour a test can assert - two are
    prose/metadata and the third is an error-message improvement with no current
    caller asserting on it. Item 7 therefore carries the error-message change as
    its only testable part, red-commanded by the regression test added with it.

- [ ] **8. `retrieveEnv` caches a MISS, so the env can never be stored again.**
  Found while fixing item 1, confirmed independently by the reviewer.
  `db.retrieve` leaves its value nil for an absent key (`db.go:3888-3901`) and
  `retrieveEnv` (`db.go:3260`) `envcache.Add`s that nil under the key. `Contains`
  then reports true, so `storeEnv` of those exact bytes takes its cache early-out
  and stores nothing, leaving jobs pointing at an `EnvKey` with no record and
  every later `retrieveEnv` reading the poisoned nil.

  - Red command evidence (throwaway probe, run against both HEAD's `storeEnv` and
    item 1's, byte-identical output, so item 1 neither widens nor narrows it):

    ```
    retrieveEnv of an absent key returned []; cache Contains=true
    storeEnv after the poisoned miss: write transactions=0, bucketEnvs KeyN +0
    retrieveEnv now returns ""
    ```

  - Latent rather than live: every live `EnvKey` comes from a committed
    `storeEnv`, and the obvious caller-side miss is `job.EnvKey == ""`, a key
    `byteKey` can never mint.
