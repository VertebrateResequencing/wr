- [x] Use the bugfix skill workflow to get all tests to pass consistently. Don't just bypass or skip flakey tests. Make them more reliable.
  - Fixed corrupt-DB recovery to close the validated backup BoltDB handle before copying/reopening the live DB.
  - Replaced brittle BoltDB byte-size assertions with behavioral backup validity and live-job assertions.
  - Made runner startup, pending-kill handling, scheduler readiness, and scheduler ordering tests deterministic.
  - Replaced the racy websocket broadcaster with a close-safe internal caster and added close-during-send coverage.
  - Verified with repeated focused tests, `make test`, `make race`, pinned golangci-lint v1.62.0, `go vet`, formatting, and diff checks.
