# Bugfixes 260827-1

Both items come from the static read of the completion path recorded in
`.docs/reliable4/prod-validation-260827.md` ("The completion path, read - and a
reframe"), during the production investigation of tens-of-seconds request
latency. They are independent of, and additional to, the three larger confirmed
mechanisms in that document (add-path lock contention, backup copy I/O, the
mmaplock remap freeze).

- [x] **`decrementGroupCount` runs the LSF scheduler synchronously inside the
  archive RPC, and `bjobs -w` is unbounded.** `archiveCompletedJob` ->
  `decrementGroupCount` (`jobqueue/server.go:7055`) calls `s.scheduleRunners`
  directly, not in a goroutine (`server.go:7081`) - contrast `scheduleGroup`
  (`server.go:5271-5289`), which deliberately spawns one "because the external
  scheduler command (eg. bsub) can be slow". That path reaches `lsf.schedule`
  (`jobqueue/scheduler/lsf.go:1566`) -> `checkCmd` -> `killExcessCmds`, and
  `parseBjobs` (`scheduler/lsf.go:2053-2084`) runs `bjobs -w` over all of the
  user's LSF jobs with no timeout and no context - plain `exec.Command`, `Start`,
  scan, `Wait`. Production's LSF account had 22,500+ foreign pending jobs during
  the incident, so a steady fraction of archive RPCs each absorb one whole LSF
  round trip before replying. `Scheduler.Schedule`'s per-cmd limiter
  (`scheduler/scheduler.go:566`) de-duplicates one-at-a-time, so it does not
  multiply, but it does serialise. `parseBjobs` was missed when
  `bsubExecTimeout`/`bkillExecTimeout` were added (`.docs/bugfixes/260824-1.md`,
  which touched only `submitToQueue`/`runBsub` and `bkillBatch`); its
  `//nolint:gosec,noctx` comment dates from commit `69ab307` "Modernise (#534)",
  not from that bound.

  **Scope, stated plainly so nobody later credits this with more than it
  deserves: this is a latent hazard, NOT production's bottleneck.** A
  2,715-goroutine dump taken under deliberate load at job limit 500 contained
  **no `os/exec`, `bsub`, `bjobs`, `RunCmd` or scheduler frames at all**, and
  99.9% of the archive handler's blocking was the archive writer. So at that
  instant the synchronous LSF call was blocking nothing. It is fixed here because
  an unbounded external command on an RPC's own goroutine is a hazard whatever
  today's profile says - the bound is what stops a slow farm from becoming a
  stalled manager - not because it explains the observed latency. The three
  confirmed mechanisms are handled separately.

  - Red command (a): `go test -tags netgo -run TestRedProbeBjobsUnbounded
    -count=1 -v ./jobqueue/scheduler/`, with a fake `bjobs` that sleeps 8s when
    called without a job id (probe source in the session scratchpad; it builds an
    `*lsf` the same way `newFakeLSFScheduler` does). Exit status 1:

    ```
    === RUN   TestRedProbeBjobsUnbounded
        zzred_probe_test.go:65: checkCmd (killExcessCmds -> parseBjobs) took
          8.006861723s, err=<nil>
        zzred_probe_test.go:68: checkCmd absorbed the whole bjobs round trip:
          8.006861723s (want a bound well under the 8s bjobs takes)
        zzred_probe_test.go:76: schedule took 8.117022781s, err=<nil>
        zzred_probe_test.go:79: schedule absorbed the whole bjobs round trip:
          8.117022781s (want a bound well under the 8s bjobs takes)
    --- FAIL: TestRedProbeBjobsUnbounded (16.12s)
    ```

  - Red command (b): `go test -tags netgo
    -run TestRedProbeDecrementGroupCountBlocks -count=1 -v ./jobqueue/`, a
    minimal `Server` plus the mock scheduler's existing `ConfigMock.ScheduleBlock`
    (the same fixture `TestReliable2ScheduleGroupDeadlock` uses to park a
    `Schedule()` call "in bsub"). Exit status 1:

    ```
    === RUN   TestRedProbeDecrementGroupCountBlocks
        zzred_probe_test.go:70: decrementGroupCount (the archive RPC's
          scheduling step) is still blocked in the external scheduler after
          5.024002499s
    --- FAIL: TestRedProbeDecrementGroupCountBlocks (5.02s)
    ```

  **Fixed (2026-08-27, branch `reliable4`):**

  - `jobqueue/server.go`: new `scheduleRunnersAsync` holds the one goroutine
    shape both callers need (`s.wg.Add`/`Done` so shutdown still waits,
    `internal.LogPanic`, and an exclusively-owned clone/snapshot because
    `scheduleRunners` mutates `failures`/`retryBackoff` without the sgroup lock).
    `scheduleGroup` now calls it instead of inlining that goroutine, and
    `decrementGroupCount` calls it instead of `scheduleRunners` directly. The
    decrement itself stays synchronous, so the count is correct the moment the
    caller returns.
  - Verified before making it async: none of the three callers
    (`archiveCompletedJob` `serverCLI.go:1401`, `releaseJob` `server.go:5955`,
    `finalizeDeletedJobs` `server.go:6130`) reads a result or depends on
    scheduling having happened - each ignores the call and proceeds to unrelated
    work. Concurrent RPC handlers already gave the synchronous call no ordering
    guarantee over which count reached the scheduler last, and
    `Scheduler.Schedule`'s per-cmd limiter returns immediately when a schedule
    for that cmd is in flight, so this defers work without multiplying
    goroutines.
  - `jobqueue/scheduler/lsf.go`: `parseBjobs` now takes a ctx and runs its exec
    under `exec.CommandContext` with a `bjobsExecTimeout` (30s) derived from
    `context.Background()` - not the scheduling ctx, matching `bsubExecTimeout`'s
    stated reasoning - plus a non-zero `WaitDelay` of `bjobsPipeCloseGrace`
    (10s). Both are test-lowerable package vars with defaults documented against
    their siblings. `countCmds` gained the ctx to pass through.
  - The **load-bearing** part of that change: `parseBjobs` hands the exec an
    `io.Writer` Stdout (the new `bjobsLineParser`, which splits chunks into whole
    lines and feeds `parseBjobsLine` as they arrive, keeping the old streaming
    memory profile and the `scanBufferSize` line cap) instead of scanning
    `Cmd.StdoutPipe`. os/exec force-closes only pipes it owns, so with
    `StdoutPipe` a read blocked on a descendant that inherited the pipe is
    interrupted by neither the exec context nor `WaitDelay` - measured below.
  - A `bjobs` that did not deliver its whole list is an error, never a silently
    short callback set: the counts it feeds (`countCmds`, `killExcessCmds`)
    decide whether wr submits more runners or kills some, and `schedule()` turns
    the error into a retryable failure under the group's existing backoff. An
    `exec.ErrWaitDelay` after a complete read and a successful exit is accepted
    as success via the existing `lingeredOnPipes` predicate and warn-logged,
    mirroring 260824-1's decision for bsub.
  - 260824-1 is intact: `bsubPipeCloseGrace`/`bkillPipeCloseGrace`, their
    non-zero `WaitDelay`s and the `lingeredOnPipes` acceptances are untouched,
    and `TestReliable4BsubPipeLinger`/`TestReliable4BkillPipeLinger`/
    `TestLSFSubmitToQueueStderr` still pass. `fakeBsubDelays` was renamed
    `fakeLSFDelays` (it now holds bjobs knobs too) with its call sites updated;
    no assertion changed.
  - Regression tests: `jobqueue/scheduler/reliable4_bjobs_bound_test.go`
    (`TestReliable4BjobsBound`) and `jobqueue/reliable4_schedule_async_test.go`
    (`TestReliable4ArchiveSchedulingAsync`), reusing `newFakeLSFScheduler` and
    `ConfigMock.ScheduleBlock` rather than new harnesses.

  **Mutation mapping** (each names the single assertion that fails when that one
  change is reverted):

  | Reverted | Assertion that fails |
  | --- | --- |
  | `scheduleRunnersAsync` -> direct `scheduleRunners` in `decrementGroupCount` | `reliable4_schedule_async_test.go:133` `closedWithin(done, 3s)` false; test takes 6.02s |
  | `exec.CommandContext` -> `exec.Command` | `reliable4_bjobs_bound_test.go:111` `So(err, ShouldNotBeNil)` got nil; test takes 60.23s |
  | `WaitDelay = bjobsPipeCloseGrace` -> `0` | `:112` elapsed `30.006509455s` not < 10s (and `:156` `20.022642101s`) |
  | `io.Writer` Stdout -> `StdoutPipe` + `bufio.Scanner`, context and WaitDelay kept | `:112` elapsed `30.006569577s` not < 10s (and `:156` `20.008782289s`) - proves the writer is load-bearing, not stylistic |
  | `lingeredOnPipes` acceptance removed | `:155` `So(err, ShouldBeNil)` got `parseBjobs(): failed to run [bjobs -w]: exec: WaitDelay expired before I/O complete` |
  | `parseBjobs` error swallowed (`return nil`) | `:111` err nil, and `:129` `errors.Is(statErr, os.ErrNotExist)` false - wr bsub'd a fleet against a count it never obtained |

  **Gates** (host load average 122, 8 cores): `make lint` 0 issues;
  `make test` **488 passed - 9 skipped - 29 packages - 4m49s** (baseline 486
  passed - 9 skipped; +2 are the two new tests, skips unchanged);
  `go test -race ./jobqueue/scheduler/` ok 30.2s; `go test -race ./jobqueue/
  -run 'TestReliable4|TestReliable3|TestReliable2ScheduleGroupDeadlock'` ok
  174.3s; `go vet -tags netgo` and `-tags 'netgo reliability_repro'` clean over
  `./jobqueue/ ./cmd/ ./queue/ ./client/... ./internal/ ./limiter/` and
  `./jobqueue/scheduler/`; `cleanorder -min-diff -dry` a no-op on all five
  changed files.

- [x] **The limiter holds its exclusive mutex across a bolt read transaction.**
  `markJobComplete` -> `limiter.Decrement` (`limiter/limiter.go:242`) takes
  `l.mu`, and `vivifyGroup` (`limiter/limiter.go:212`) calls the group-getter,
  which reaches `db.retrieveLimitGroup` (`jobqueue/db.go:2232`) -> `db.retrieve`
  -> `db.bolt.View`: a bolt read transaction under the limiter's exclusive mutex.
  A DEVELOPERS.md rule 1 violation on a lock every completion needs. `Decrement`
  deletes a group when its count reaches 0, so a limit group that drains and
  refills re-vivifies - and re-reads - each time; when the DB's read side is
  stalled (as the confirmed mmaplock freeze does for the duration of a backup
  copy), one vivify freezes every completion's `Decrement` and every reservation
  behind it.

  - Red command: `go test -tags netgo
    -run TestRedProbeLimiterDecrementBlockedBySlowLookup -count=1 -v ./limiter/`,
    with a `SetLimitCallback` that stalls on an unrelated group name, standing in
    for a bolt read stalled by the backup copy's mmaplock hold. Exit status 1:

    ```
    === RUN   TestRedProbeLimiterDecrementBlockedBySlowLookup
        zzred_probe_test.go:58: Decrement (every completion's path) is still
          blocked behind the stalled group lookup after 5.023246047s
    --- FAIL: TestRedProbeLimiterDecrementBlockedBySlowLookup (5.02s)
    ```

  **Fixed (2026-08-27, branch `reliable4`):**

  - `limiter/limiter.go`: the callback is now resolved with `l.mu` RELEASED and
    the result handed to `vivifyGroup`, so nothing under `l.mu` calls it. New
    `withResolvedGroups(ctx, groups, fn)` owns the pattern: it loops
    `runWithGroups` (takes `l.mu`; if every name is either resident or already
    resolved it calls `fn` under that one uninterrupted hold and returns nil,
    otherwise it returns the names still needing resolution and calls nothing)
    against `resolveGroups` (calls `l.cb` per unresolved name with the lock
    released). `vivifyGroup` lost its ctx and no longer calls `l.cb` at all.
    `GetLimit`, `attemptIncrement`, `GetLowestLimit` and `GetRemainingCapacity`
    now go through it; `checkGroups`, `incrementGroups` and
    `registerGroupNotifications` take the resolution map instead of a ctx.
    `SetLimitCallback`'s doc comment now states that it is never called under the
    Limiter's lock, so it may be slow, but may therefore be called concurrently
    and more than once for the same group.
  - Constraint by constraint: a resident group is returned as-is and never
    replaced by resolved data (which would reset its count to 0 and break the
    limit); `checkGroups` + `incrementGroups` still run under one hold, as does
    the failed check plus `registerGroupNotifications`, so no `Decrement` can
    slip between them; the resolution map is per-call, never a cross-call cache,
    so a forgotten group's next vivify still observes a changed callback value; a
    name that goes missing again while the lock is released is re-resolved rather
    than treated as unlimited, and the loop terminates because each pass resolves
    at least one name never resolved before and never resolves one twice; the
    -1/invalid "no limit" semantics and the forget-at-zero memory bound are
    unchanged; and no new lock was added, so no ordering inversion is possible.
  - Regression tests `limiter/reliable4_slow_lookup_test.go`:
    `TestReliable4LimiterSlowLookup` (a stalled lookup must not block another
    group's `Decrement`, `Increment` or `GetRemainingCapacity`),
    `TestReliable4LimiterSlowLimit` (under 32 workers contending for a limit-1
    group with 25ms lookups and 8 holders making that group come and go, the
    observed maximum concurrent holders must stay 1) and
    `TestReliable4LimiterWaitRelease` (a waiting `Increment` must not miss a
    `Decrement` that lands in the gap after its failed check).

  **Mutation mapping** (each names the single assertion that fails when that one
  change is reverted):

  | Reverted | Assertion that fails |
  | --- | --- |
  | callback resolved back under `l.mu` | `reliable4_slow_lookup_test.go:301`, `:313`, `:323` all `returnsWithin(5s, ...)` false; test takes 15.05s |
  | resident group overwritten by resolved data | `:249` maxHolders Expected 1, Actual **8**; and pre-existing `limiter_test.go:319` 166 increments not in 100-125 |
  | resolution loop gives up after one pass (missing name treated as unlimited) | `:249` maxHolders Expected 1, Actual **8** |
  | notification registered in its own lock hold, not the failed check's | `:164` `So(<-result, ShouldBeTrue)` false - the waiter misses the gap `Decrement` and times out |

  **Gates** (host load average 131, 8 cores): `make lint` 0 issues; `make test`
  **491 passed - 9 skipped - 29 packages - 5m2s** (488 before this fix; +3 are
  the three new tests, skips unchanged); `go test -race ./limiter/ ./queue/` ok
  3.6s / 2.8s; `go test -race ./jobqueue/ -run` over the limit-group tests
  (`TestJobqueueLimitGroups`, `TestJobqueueSuspendResumeLimitGroups`,
  `TestReliable3LimitSlotStall`, `TestReliable3LimitGroupOverProvision`,
  `TestReliable4RacBound`) ok 12.1s; `go vet -tags netgo` and
  `-tags 'netgo reliability_repro'` clean; `cleanorder -min-diff -dry` a no-op on
  both changed files. The pre-existing 100-125 / 75-100 concurrency split in
  `TestLimiter` holds unchanged and was not retuned.
