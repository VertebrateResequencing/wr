# Bugfix 260628-8

- [x] Doing a normal `wr manager start` gives the web interface with the host name instead of "localhost", which makes it copy/pastable given that I am accessing the deploy host via ssh tunnel. Use the bugfix skill workflow to improve the link given if possible, ideally reusing the same code `wr manager start` uses to present that url.

  - Updated `wr cloud deploy` to display the deploy host FQDN with the locally forwarded web port, then format that through the existing `websiteURL()` helper used by `wr manager start`.
  - Added regression coverage that proves the cloud deploy URL does not use the remote OpenStack manager host when presenting the local forwarded web URL.
  - Implementor passed focused regression tests, `make lint`, and `go test -tags netgo --count 1 ./cmd -v`.
  - Reviewer passed focused `./cmd` tests covering this URL and prior deploy fixes, `make lint`, `git diff --check`, and `gofmt -l`.
