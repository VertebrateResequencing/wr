- [x] After updating to `go.nanomsg.org/mangos/v3`, `TestSubscriptionStateChangeEvents` fails during subscription setup with `invalid or unsupported option`; complete the v3 migration using the v3 docs and preserve wr behavior.
  - Replaced the v1 mangos module/imports with `go.nanomsg.org/mangos/v3 v3.4.2`, ran `go mod tidy`, and switched TLS transport setup to v3's import-driven registration.
  - Switched the server from cooked REP plus `OptionRaw` to the v3 raw `xrep.NewSocket()` constructor, preserving wr's routed raw reply behavior.
  - Kept TLS configuration on `DialOptions`/`ListenOptions`, used v3's synchronous initial dial lifecycle to avoid TLS handshakes racing with shutdown, and mapped failed transport dials back to wr's existing `ErrNoServer` surface.
  - Recognized v3's typed `mangos.ErrClosed` anywhere wr already treated a closed socket as expected/recoverable, while retaining the legacy string fallback.
  - Kept wr's raw `xrep` request loop instead of switching to cooked REP contexts because handlers reply later from per-request goroutines using original routing headers.
  - Verified focused jobqueue/socket tests plus `make test`, `make race`, `make lint`, `git diff --check`, and `go mod tidy -diff`.
