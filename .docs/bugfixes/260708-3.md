- [x] If progress is shown on the terminal and it keeps increasing, and if the poor speed isn't obviously a bug (eg. lack of sorting or batching before insert into bolt, which is slow), that might be fine. If it's a poor implementation, fix it with the bugfix skill workflow.
  - Reproduction note before fix: a fresh copy of `.tmp/db` started with `wr manager start --timeout 2` showed terminal progress and correctly avoided the initial timeout, but after about 5 minutes it was still rebuilding the first reverse lookup bucket at only 267,733 entries processed. The current code inserts reverse-index keys in lookup-key iteration order even though the destination bucket sorts by job key, which is a poor BoltDB insertion pattern.
  - Fixed the reverse lookup upgrade to collect generated reverse-index keys, sort them in destination-bucket order, compact duplicates, and then write the BoltDB bucket sequentially.
  - Added regression coverage proving the rebuild prepares destination-key ordered writes and compacts duplicates while preserving existing upgrade progress behavior.
  - Retime after fix: a fresh copy of the 3.9 GB `.tmp/db` upgraded and started with `wr manager start --timeout 2` in 42.23 seconds; the upgrade reached the commit phase in about 17 seconds.
