- [x] Using `--debug` seems to have brought up a different issue. Use the bugfix skill workflow to fix it:

  ```
  $ wr cloud deploy --debug
  INFO[06-28|18:02:00] created a new key and certificate for TLS
  INFO[06-28|18:02:01] please wait while openstack resources are created...
  INFO[06-28|18:02:06] please wait while a server is spawned on openstack...
  INFO[06-28|18:02:58] please wait while I start 'wr manager' on the openstack server at 172.27.19.38...
  WARN[06-28|18:02:59] failed to start wr manager on the remote server
  Error: unknown flag: --runner_debug
  Usage:
    wr manager start [flags]
  [...]
  EROR[06-28|17:02:59] unknown flag: --runner_debug
  [...]
  ```

  - Root cause: `wr cloud deploy --debug` built the remote `wr manager start` command with stale `--runner_debug`, which is no longer a valid `manager start` flag.
  - Fix: changed the debug deploy command fragment to keep `--debug` for the remote manager and use the supported `--runner_syslog` flag for runner logging.
  - Tests: added local-only GoConvey coverage for `buildDebugArg()` and the full remote manager-start command builder, proving `--runner_debug` is not emitted.
  - Gates: focused `cmd` tests passed, full `cmd` package passed, `make lint` passed, and reviewer reran `make test` successfully.
