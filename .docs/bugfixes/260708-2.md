- [x] Investigate why using .tmp/db (from v0.36.5) just seems to hang indefinitely when trying to start a manager with this branch. Fix issues with the bugfix skill workflow. Improve logging for any legitimate db upgrade process and stop the initial timeout that normally happens during `wr manager start` if it takes too long, but only in the case of a db upgrade, and show the upgrade state on the terminal to explain the slow start.
  - Added a DB-upgrade status sidecar so the daemonized child can report active upgrade phases to the parent before the manager is ready to accept connections.
  - Added upgrade progress logging for the v0.36.5 first-open bucket rebuilds, including phase start/completion, processed counts, durations, and sidecar cleanup.
  - Changed manager startup waiting to extend the normal timeout only while a fresh, live DB-upgrade status is present, and to print the upgrade state on the terminal.
  - Added focused regression coverage for DB upgrade progress/status cleanup and manager startup timeout behavior.
