- [x] `wr cloud deploy` can go down the "could not talk to wr manager on server ... after 40s" debug/teardown path even though the remote manager is started and reachable moments later through the SSH tunnel (`wr manager status` says `started`).
	- Added a behavioural regression test for transient post-forwarding manager connection failure followed by success.
	- Updated cloud deploy readiness to retry manager connection attempts until the real start deadline expires, preserving the existing debug/teardown path when the deadline is exhausted.