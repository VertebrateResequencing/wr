- [x] That fix for the web UI was even worse. Now it flickers so fast it looks like its not there, and the total jobs listed for the reprp keeps rising above the total number of jobs actually added. Fix it properly.
  - Reproduced the regression with a browser fixture under `jobqueue/testdata/repgroup-flicker-overcount`: the row could disappear after an empty current snapshot, and later show `12 complete` for only 6 jobs.
  - First attempted fix preserved seen rows with a short client-side grace window and made live deltas source-aware instead of blindly additive. Review rejected this because it was still timeout-based: a delayed delta after the grace window could flicker, and a legitimate late partial completion could be dropped.
  - Second attempted fix added status sequence numbers and snapshot cutoffs so the client could ignore deltas already covered by an authoritative snapshot while applying later deltas. Review rejected this because the snapshot cutoff was captured before snapshot state collection, so snapshots could include changes that their cutoff did not cover.
  - Third attempted fix moved cutoff capture after snapshot collection and added coverage for the `3 running / 3 complete` delayed-delta case. Review rejected this because sequence assignment happened in the async queue-change callback, after queue mutations were already visible, leaving visible-before-sequenced and transition-after-snapshot-before-cutoff holes.
  - Fourth attempted fix added queue mutation generations assigned under the queue lock and used those generations for status deltas/snapshot cutoffs. Review rejected this because the queue snapshot still retained mutable item pointers after releasing the lock, and because current snapshot counting could double-count a job visible both as live in the queue and completed in the DB during archive/removal.
  - Fifth/final attempted fix froze immutable queue item snapshots under the queue lock and de-duplicated completed DB additions by live job key during websocket current snapshots. Final review rejected this because it introduced a lock-order inversion: status snapshots took `sfsmutex.RLock -> queue.RLock`, while TTR processing held `queue.Lock -> sfsmutex.Lock`, creating a possible deadlock between status snapshots and TTR expiry.
  - Stopped after the fifth failed review cycle per the bugfix workflow. The partial implementation was not committed and was reverted; this remains unresolved.
  - **Resolved** via a structured investigation (`.docs/broadcast/investigate.md`): root-caused the whole class as a payload-semantics defect, not a transport one. The queue change callback fires in a detached goroutine *after* the queue unlocks, so delta messages can never share a consistent cut with the recovery snapshot — which is exactly why all five attempts above failed (more sequencing/cutoff/generation machinery on a delta protocol cannot close that gap).
  - Evaluated five schemes in parallel git worktrees (baseline probe, native absolute state, the `centrifuge` library, SSE+`Last-Event-ID`, a `melody` control, and a native sequenced accumulator). The third-party broadcast/transport libraries were the wrong axis — a `melody` control proved a transport-only swap leaves the bug fully intact, and `centrifuge`/SSE only fix it by also switching to absolute payloads while adding dependency/structural cost. Chose the native absolute-state scheme (zero new deps, net code deletion).
  - Implemented idempotent **absolute per-RepGroup state, server-coalesced**: new `jobqueue/statusstate.go` (authoritative `map[RepGroup]map[JobState]int` + `+all+` aggregate under a strict-leaf mutex, per-client dirty set, throttled sender returning fresh copies), with `applyTransition` called at every former `statusCaster.Send` site (`server.go` change + TTR callbacks, `serverCLI.go`) and `statusState` seeded at startup. Added a new absolute wire message; the client (`websocket-handler.js`) now replaces a RepGroup's counts wholesale. Deleted the delta fan-out, `casterOverflowValue`/`replacePending` overflow→resync, `SnapshotID`/`SnapshotDone` staging, and the client `ignore` map / `StatusResync` / snapshot-staging / prune-restore. Net −434 LOC.
  - New regression `jobqueue/testdata/repgroup-flicker-overcount/` fails on the old delta handler (row dropped to 0 211× while 12000 jobs existed; overcount to 12400/12000) and passes on the new one (0 flicker, 0 overcount, exact convergence while dropping most messages). Added `statusstate_test.go` (idempotency, clamping, dropped-intermediate self-heal, copy-out) and a `-race` lock-order test driving the real `queue.Queue` (TTR-under-lock + concurrent drainers) proving the leaf order `queue.mutex → job → statusState.mu`, avoiding the attempt-5 inversion. Migrated all prior status browser fixtures to the absolute protocol with their behavioural assertions preserved.
  - Verified by implementor and reviewer subagents: `make lint` clean, `make test` pass, focused `-race` lock-order pass, and `make browser-test` pass.
  - Follow-up (user retested): the data was now correct (total fixed at 10000, persists, full green bar once complete) but the per-RepGroup bar was still visually flickering during the storm — a frontend rendering defect the absolute protocol exposed, not a data problem. Root cause: the wholesale apply path (`websocket-handler.js` `setTrackerCounts`→`resetTrackerCounts`) zeroed every per-RepGroup percentage observable on each message, and those pct observables were not rate-limited, so the bound segment widths (`status.html` `style: { width: runPct()+'%' }` etc.) collapsed to 0% ~20×/sec. A stacked bar's segments always sum to ~100% (every job is in some state), so the bar should stay full with only its colour proportions shifting.
  - Fixed frontend-only (no data-layer change, no delta reintroduction): `setTrackerCounts` now sets each count observable directly to its new absolute value (absent states → 0) without a reset-to-0 pass and without writing pct observables (recomputed via `updateProgressBars`); repgroup pct observables are now rate-limited like the inflight ones; `updateProgressBars` clears pcts to 0 when a RepGroup drains to empty instead of leaving them stale. The CSS already animates `.progress-bar` width (`transition: width 0.4s`), so widths now grow/shrink smoothly.
  - New browser regression `jobqueue/testdata/repgroup-bar-flicker/` samples the rendered `.progress-bar` segment widths over a ~10000-job storm: fails on the pre-fix client (873/936 populated frames collapsed, min rendered filled width 0%) and passes after (0 collapse frames, min filled ~97%, no segment DOM recreation, converges to 100% complete). Reviewer independently reproduced the pre-fix failure and the post-fix pass; `make browser-test` (all 11 fixtures) and `make lint` green.
