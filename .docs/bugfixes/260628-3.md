- [x] On master branch I can: `source ~/.openstack-theta-hgi-dev && wr cloud deploy`, but on develop branch (and this branch) it fails with `failed to connect to openstack: You must provide exactly one of DomainID or DomainName to authenticate by Username`. Notably the openstack-related dependencies were upgraded since master. Is it needing something new, or has something just been renamed? Do we already have some kind of rename/backwards compatibility for env vars that just needs to be extended?

  - Root cause: the gophercloud v2 upgrade replaced the old `clientconfig.AuthOptions` path with `openstack.AuthOptionsFromEnv()`, which only reads generic `OS_DOMAIN_ID`/`OS_DOMAIN_NAME` and no longer maps common RC-file vars such as `OS_USER_DOMAIN_NAME` and `OS_PROJECT_DOMAIN_NAME`.
  - Fix: added a local OpenStack auth env wrapper that preserves the old compatibility behaviour without reintroducing `github.com/gophercloud/utils`; user-domain env vars populate authentication, project-domain env vars populate project-name scope, generic/default domain vars remain fallbacks, and `OS_PROJECT_*` still overrides tenant vars.
  - Tests: added local-only auth option regression coverage that fails pre-fix with the reported DomainID/DomainName error and verifies separate user/project domains, project-ID scope, generic domain fallback, and MaybeEnv discoverability.
  - Gates: implementor and reviewer both passed the focused cloud auth tests and `make lint`; reviewer also passed `go test -tags netgo --count 1 ./cloud -v`.
