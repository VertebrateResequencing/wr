- [x] A manager that stops just after burying a job with an OnFailure Remove behaviour can panic instead of shutting down cleanly. deleteJobIfRequested (jobqueue/server.go:~6325-6329) runs `go s.deleteJobs(ctx, []*Job{job})` untracked. resetStateAfterShutdown sets `s.q = nil` under ssmutex (server.go:~7609), but jobHasDependents (server.go:~3027, 3037) and removeDeletableJobs (server.go:~6277) read s.q with no lock and no nil check, so the goroutine dereferences a nil queue. It's also a data race. Found on 2026-09-25 while forcing the jq_execution_details should_delete flake.
  - Red command: `git apply /tmp/claude-11346/-nfs-users-nfs-s-sb10-src-go-github-com-VertebrateResequencing-wr-incidental/7b24332e-a102-43cb-847e-a702b9dca2f3/scratchpad/flake2/red_delete.patch && cd jobqueue && WR_RED_DELETE_SLEEP=300ms WR_TEST_SHARD=c WR_TEST_LANE=76 CGO_ENABLED=0 go test -tags netgo -count=1 -run '^TestJobqueueExecutionAndDependencyScenarios$' .` The patch adds an env-gated sleep before deleteJobs in the goroutine and is not committed. On develop 709f389:
    ```
    panic: runtime error: invalid memory address or nil pointer dereference
    github.com/VertebrateResequencing/wr/queue.(*Queue).HasDependents(0x0, {...})
    github.com/VertebrateResequencing/wr/jobqueue.(*Server).jobHasDependents(...)
    FAIL	github.com/VertebrateResequencing/wr/jobqueue	14.284s
    ```
  - Fixed in `jobqueue/server.go`. `deleteJobIfRequested` now puts its goroutine on `s.wg`, like `scheduleRunnersAsync`, so shutdown's `wg.Wait` (which has no deadline, it only logs) waits for the delete before `s.q.Destroy` and `resetStateAfterShutdown`. It doesn't spawn once `inShutdown` is set, and holds `krmutex` across that check and the `wg.Add`, so every Add is ordered before `wg.Wait` even from callers not on `s.wg` (the lost-job release). It uses `krmutex` rather than `ssmutex`, the `storeWebSocketConnection` pattern, because `beginShutdown` holds `ssmutex` while `unscheduleAllGroups` can reach `buryImpossibleItem`, so `ssmutex` could self-deadlock.
  - `deleteJobs` gets the queue once through `queueIfPresent` and does nothing if it is gone. It passes that queue to `removeDeletableJobs` and to `jobHasDependents`, which is now a plain function. The per-job removal moved to `deletePass.remove` to keep `gocognit` under its limit.
  - Regression test `TestStopWaitsForARemoveOnFailureDelete` (`jobqueue/delete_after_stop_test.go`) holds the delete goroutine at a new `deleteOnFailureHook` seam, stops the manager, waits for the database close, then releases the goroutine. It asserts Stop hadn't finished before the release and did finish after it. On the old code, Stop finished first (`Expected: false, Actual: true`) and the released goroutine panicked in `queue.(*Queue).HasDependents(0x0, ...)`. On the fix it passes, and passes 3/3 under `-race`.
  - Red before: the panic above. Red after, with the env sleep moved onto the new goroutine: no panic. The test still fails at `jobqueue_test.go:5076` (`Expected: 0, Actual: 1`), which is the `should_delete` flake the sleep was built to force (the buried job isn't removed yet), not this bug.
  - Other untracked spawns found but not changed: `confirmServerDeadAfter` (server.go, from `recordBadServerState`) sleeps `autoConfirmDead` and then reads `s.q` without a lock in `getJobsCurrent` and `killJobOnServer`, so it has the same nil-queue exposure if it fires after Stop. Tracking it needs a shutdown-aware wait, so it isn't the trivial pattern. `markJobLost`'s `go s.confirmOrReleaseLostJob` is also untracked, but it reaches the queue only through `queueIfPresent`, so it can't dereference a nil queue. At worst it writes to a closed database, which returns an error.
  - Follow-up after review: the `inShutdown` gate left a remove-on-failure job killed by the shutdown itself buried, where before it was usually removed while runners died. Deletes are now accepted until `stopDeletes`, which `closeServerCommsAndDB` calls under `krmutex` after the socket closes and before the database closes, still ahead of `wg.Wait`. `TestStopStillRemovesAJobBuriedWhileRunnersDie` holds shutdown in `waitForRunnersToDie` (new `shutdownRunnersWaitHook` seam), buries the job there, and asserts it leaves both the queue and the live bucket. On the `inShutdown` gate it failed (the poll timed out, `Expected: true, Actual: false`). It passes now.
  - Gates: `make lint` 0 issues, `make test` pass, `CGO_ENABLED=1 make race` pass, all first run.
- [x] `confirmServerDeadAfter`, spawned from `recordBadServerState` with an untracked `go`, sleeps `autoConfirmDead` and then reads `s.q` with no lock in `getJobsCurrent` (`getAllQueueJobs`) and `killJobOnServer`. If it fires after Stop, it hits the same nil-queue panic and data race. Found while fixing the item above.
  - Red: new test `TestStopWaitsForABadServerConfirmation` (`jobqueue/confirm_dead_after_stop_test.go`) holds the goroutine at a new `confirmServerDeadHook` seam, stops the manager, and releases it with a 1ms `autoConfirmDead`. Before the fix it panicked: `(*Server).getAllQueueJobs` <- `getJobsCurrent` <- `killJobsOnServers` <- `confirmServerDeadAfter`, nil pointer dereference.
  - Fixed in `jobqueue/server.go`. The new `confirmServerDeadLater` puts the goroutine on `s.wg`, and doesn't start it once `killRunners` is set, holding `krmutex` across the check and the Add. The wait now selects on a new `stopping` channel that `beginShutdown` closes, so a pending confirmation ends when shutdown starts instead of holding up `wg.Wait`. `getAllQueueJobs` and `killJobOnServer` read the queue through `queueIfPresent`.
  - After: the test passes, including a second case showing a confirmation still waiting on a 1h `autoConfirmDead` doesn't hold up Stop, and passes 3/3 under `-race`. CHANGELOG "### Fixed" entry added.
  - Gates on ae461b8 (covering both follow-ups): `make lint` 0 issues, `make test` pass. The first `CGO_ENABLED=1 make race` failed in `cmd` (`TestStatus*`) because manager port 45353 was already in use (`bind: address already in use`), a collision with another run on the host rather than a code failure. The one allowed re-run passed.
- [x] Shutdown closed the database before waiting for the tracked remove-on-failure deletes, so a delete still running at that point removed the job from the queue but failed to delete it from the live bucket, and a restarted manager recovered it buried again. Raised by Copilot's summary on #616; no worse than before the fix above, but cheap to close.
  - Red: `TestStopWaitsForARemoveOnFailureDelete` now holds the delete until shutdown has run `stopDeletes`, then releases it and checks the stopped manager's live bucket (opened read-only). Before the fix, `Expected: false, Actual: true`: the job was still there.
  - Fixed in `jobqueue/server.go`. The delete goroutines are also counted on a new `deletesWG` (still on `s.wg` too). `closeServerCommsAndDB` calls the new `waitForDeletes` right after `stopDeletes` and before `db.close`. The wait is bounded by `ServerShutdownWaitTime` and logs a warning if it gives up. A delete still running then stays on `s.wg`, so the queue still outlives it.
  - Checked for teardown and deadlock. At that point the queue exists (it's destroyed after `wg.Wait`) and the database is open. The casters are closed, but `Send` is then a no-op, and the subscription sweep has its own `subsClosed` guard. Shutdown holds no lock while it waits, and the delete takes only the queue, db, `rpl`, dep-group and `psgmutex` read locks. A `scheduleRunnersAsync` it triggers runs after `scheduler.Cleanup`, as it could before; it isn't waited for here, only on `s.wg`.
  - After: the targeted tests pass with and without `-race`.
