- [x] I'm getting the following failure. These tests are gated because they can fail if the LSF cluster is too busy to get jobs running, for example, but I don't think that's the issue here. Investigate and make this and any other tests more reliable when LSF is not contended. Ensure the spirit of what the test was trying to prove remains, but you can completely reimplement the test if needed. Follow bugfix skill principles (but just fixing tests doesn't mean writing new tests per TDD).
  - Moved the LSF integration and queue-selection tests into `jobqueue/scheduler/scheduler_lsf_test.go` so cluster-specific coverage is isolated from the main scheduler test file.
  - Replaced fixed sleeps in the LSF integration tests with bounded polling for job start/finish marker files, preserving the original scheduling behaviours without depending on exact cluster timing.
  - Made the process liveness helper wait for the submitted PID/host file longer under LSF and poll after `kill -9` so transient process-state cleanup does not fail the test.
  - No new regression tests were added because this change makes existing integration tests reliable rather than changing production behaviour.
