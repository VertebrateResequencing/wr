# Bugfixes 260829-2

Renamed from `260829-1.md` during the rebase onto develop after #559 merged:
#559 had taken that filename for its own checklist, exactly as #556 had taken
`260828-1.md` before it. Develop's file is restored untouched; this is #558's.

PR #558 review-comment resolution batch (branch `fix-cwd-matters-cleanup-deletes-parent`). The PR's own work is recorded in `.docs/bugfixes/260828-3.md`.

- [x] Copilot review thread on `jobqueue/utils.go:833` (PR #558): "Typo in the doc comment: "it's" should be the possessive "its"." Valid — `rmEmptyDirs`'s doc comment reads "deletes leafDir and it's parent directories". The typo predates this branch, but the branch rewrote that comment (LAYER 2 changed "stopping if it reaches baseDir (leaving that undeleted)" to "stopping before it reaches baseDir (leaving that, and everything above it, undeleted)") and carried it along, so it is in the diff and in scope.
  - Source: PR #558, thread `PRRT_kwDOAKD33M6dW-fb`, comment `3885454105`, review SHA `e707ec5`.
  - Red command: `grep -n "it's parent directories" jobqueue/utils.go` — exits 0 (match found) while the defect is present, non-zero once fixed. A textual defect in a doc comment has no behavioural surface, so per testing-principles no new test is appropriate; the gates are `make test` and `make lint`.
  - Red evidence (at e707ec5): `833:// rmEmptyDirs deletes leafDir and it's parent directories if they are empty,`
  - Fix: `jobqueue/utils.go:833` `it's` -> `its` (one word; the next line's "It's ok if leafDir doesn't exist" is a correct "it is" and was left alone). No test added: per testing-principles a doc-comment typo has no behavioural surface. Gates: `make test` `361 passed / 9 skipped / 2m52s` PASSED, `make lint` `0 issues.`
  - Out of scope, noticed while checking whether the defect was systemic: the same possessive error exists at `jobqueue/utils.go:1190` ("the behaviour of it's jitter") and `jobqueue/job.go:480` ("with docker, it's name will be our Key()"). Neither line is in this PR's diff, so fixing them here would be churn in an already-large PR. `jobqueue/behaviours.go:272` and `jobqueue/client.go:969` also match the grep but are correct uses of "it is".

- [x] Copilot review thread on `jobqueue/behaviours_bench_test.go:59` (PR #558): "`b.N` is an `int`, so `for i := range b.N` does not compile." **Invalid** — ranging over an integer has been valid since Go 1.22, `b.N` is an `int`, and this module sets `go 1.26.3`. Verified on the head rather than by inspection: `go vet ./jobqueue/` clean, and `go test -bench=BenchmarkJobCleanup -benchtime=2x` ran it (`BenchmarkJobCleanup-4  2  264351 ns/op`). Replied with that evidence and resolved; no code change.

- [x] Copilot suppressed comment on `jobqueue/utils.go:892` (PR #558, review 5056905598 of head 0c877ab): `rmCheckedEmptyDirs` detects the "directory is not empty" case with `strings.Contains(err.Error(), "directory not empty")`. Valid, and the code already admits it — the line above reads `// *** not sure where the "directory not empty" string comes from; probably not cross platform!`. Matching on a message rather than the errno means a system whose text differs turns an expected, benign "stop walking here" into a returned error. Consequence is a spurious `errNotBelowBaseDir`-style failure surfacing in `Execute`'s "Behaviour problems" stderr or a server-side `clog.Warn`, not data loss — the deletion simply stops. In scope because this branch restructured that function (LAYERs 2, 5 and 8 moved it from `rmEmptyDirs` into `rmCheckedEmptyDirs` and changed its signature), so it is in the PR diff.
  - Source: PR #558, review `5056905598`, suppressed comment (no thread to resolve).
  - Red command: none available on this platform — Go's syscall error strings are stable and English, so the current code is correct on Linux and the defect is a portability/robustness one with no behavioural red here. Per testing-principles that means no new behavioural test is invented for the errno change itself; the existing `TestRmEmptyDirs` "stop at first non-empty parent" case pins the behaviour that must not change, and a new assertion should pin that detection is by errno rather than by message.
  - Fix: new `errIsDirNotEmpty(err)` in jobqueue/utils.go using `errors.Is(err, syscall.ENOTEMPTY) || errors.Is(err, syscall.EEXIST)` (Linux reports ENOTEMPTY; POSIX allows EEXIST), replacing the substring match and deleting the `***` comment. `syscall` was already imported elsewhere in the package (client.go, server.go, memory_attribution.go) and `errors.Is(err, syscall.<ERRNO>)` is the repo's existing idiom (cmd/manager.go:945, jobqueue/scheduler/scheduler_test.go:323, network/port/port_test.go:71). The predicate is a named helper rather than an inline expression so the test asserts the same code production uses, not a copy of it.
  - Test: a Convey case in `TestRmEmptyDirs` — a non-empty leaf survives and no error is returned — with a nested assertion that the real OS error satisfies `errIsDirNotEmpty`. It has teeth: narrowing the predicate to EEXIST alone fails it with `Expected: nil / Actual: 'remove .../unique: directory not empty'`, confirming Linux returns ENOTEMPTY. What it does NOT prove, stated honestly: nothing about darwin/windows/BSD errnos, which no test here can exercise, and nothing about errno-vs-message on Linux, where both are correct — it is a tripwire against a future platform change.
  - Portability checked directly: `GOOS=darwin go build ./jobqueue/` passes; `GOOS=windows` fails **identically before and after** the change (go-fuse needs `syscall.Stat_t`/`Iovec`/`Flock_t`/`O_DIRECTORY`, and jobqueue/scheduler/local.go:1157 uses `Setpgid`), so it is pre-existing and was not touched. The new expression itself was compiled for windows, darwin, linux and freebsd in a scratch module — both constants exist in `syscall` on all four.
  - Gates: `make test` `361 passed / 9 skipped / 2m51s` PASSED (count unchanged; the new case is a Convey inside `TestRmEmptyDirs`, whose assertions went 51 -> 57), `make lint` `0 issues.`, `BenchmarkJobCleanup` 63 allocs/op unchanged with ns/op inside noise.


- [x] Copilot suppressed comment on `jobqueue/job.go:203` (PR #558, same review): `mergeBehaviours` silently drops behaviours when a trigger has multiple entries. **Valid, and already independently found and recorded** during LAYER 9 as item 14 of `.docs/bugfixes/260828-3.md`, with the same measurement (`--on_exit '[{"run":"x"},{"cleanup":true}]'` keeps only the last). It is a pre-existing `wr mod` bug with nothing to do with `--cwd_matters`, so it is being fixed on the split-out branch (`fix-shutdown-hang-and-flakes`, PR #557) rather than growing this one. No change here; explained in a PR comment.

- [x] Copilot suppressed comment on `jobqueue/behaviours.go:359` (PR #558, review 5056981386 of head 3d2f80c): `removeActualCwd`'s `keepDirs` branch calls `removeAllExcept`, which begins with `os.ReadDir(actualCwd)` and so fails if the workspace has already been deleted. Valid, and **independently found twice**: the LAYER 8 review's 29-case probe hit it too, logging the already-deleted-workspace + relative-`Mount` row as `open .../cwd: no such file or directory` and noting it was identical at HEAD. The sibling `removeAllManaged` branch is already idempotent, because `os.RemoveAll` returns nil for a missing path — only the relative-mount branch is not.
  - Why it matters more than it looks: `Behaviour.cleanup` can run MORE THAN ONCE for the same job — runner-side at client.go:1681/1998/2089, and again server-side at server.go:4588 via `killLostJobAndTriggerBehaviours`. So a lost job whose runner already cleaned up gets a second cleanup that returns a spurious error and stops before `rmCheckedEmptyDirs`, leaving the empty parents un-tidied. That is exactly the lost-job path the original incident travelled.
  - Consequence is a spurious "Behaviour problems" line or `clog.Warn` plus leaked empty dirs, never extra deletion.
  - In scope: this branch changed `removeActualCwd`'s signature (LAYER 8 made it take `provenDirs`).
  - Source: PR #558, review `5056981386`, suppressed comment (no thread to resolve).
  - Red command: trigger `Behaviour{When: OnExit, Do: Cleanup}` twice on a job with a relative `MountConfig.Mount`; the second call must return nil and tidy the empty parents, not error.
  - Red evidence (at 3d2f80c, new Convey case in `TestBehaviourCleanupSafety`): second `Trigger` returned `open .../wr_cwd/hash/unique/cwd: no such file or directory`, and the empty parents leaked — `stat .../wr_cwd/hash: <nil>`, `stat .../wr_cwd: <nil>`.
  - Fix: the tolerance went at the two directory reads that start the mounts branch, act-then-tolerate as `rmEmptyDirs` does, rather than at the `removeActualCwd` call site: `removeAllExcept` (jobqueue/utils.go) now does its own top-level `os.ReadDir` and returns nil for `os.IsNotExist`, and `removeWorkSpaceExtras` (jobqueue/behaviours.go) does the same for the workspace. Fixing only `removeAllExcept`, as the comment asked, was proven insufficient: the second cleanup then failed one line later at `open .../wr_cwd/hash/unique: no such file or directory`, because `Behaviour.cleanup` is not idempotent until both reads tolerate the missing workspace. The recursive `removeWithExceptions` keeps its strict read (a new `removeEntriesWithExceptions` holds the shared loop), so only "the top-level dir we were asked to clean out has gone" is tolerated, never a dir that vanished mid-walk. The why lives once, on `cleanupWorkSpace`.
  - Deliberately still surfacing, checked by running `removeAllExcept` against each: `permission denied` on an unreadable dir (`isNotExist=false`), `not a directory` when the path is a file (`isNotExist=false`), and every error from the recursion. A missing *keep* dir returns nil, as it always did — keep dirs are only ever skipped when listed, never read.
  - Test: a Convey in `TestBehaviourCleanupSafety` alongside the mounts cases, reaching the state through production code rather than by hand: cleanup empties the workspace but keeps the relative `Mount` dir, `Job.Unmount`'s `rmEmptyDirs` then deletes the emptied workspace and walks up until a sibling job's dir stops it, that sibling finishes, and the server's second cleanup must return nil and remove the two empty parents up to Cwd. Tidying is asserted before the error, per this branch's ordering rule.
  - Gates: `make test` `361 passed / 9 skipped / 2m51s` PASSED (count unchanged; the new case is a Convey inside `TestBehaviourCleanupSafety`, whose assertions went 108 -> 122), `make lint` `0 issues.`, `BenchmarkJobCleanup` 63 allocs/op with 154.4-156.3k ns/op against a 154.8-156.7k ns/op baseline at 3d2f80c (the mounts branch this touches is not on the benchmark's path).

- [x] Performance regression on this branch: `Job.rmEmptyMountDirs` (jobqueue/job.go) resolved the whole workspace before looking at whether the Job had any mount points to tidy, so every mountless Job paid for the proof on its way out. `Job.Unmount` calls it, and `Client.Execute` calls `job.Unmount()` unconditionally at jobqueue/client.go:2100 at the end of EVERY run, so the cost landed on the exit path of the overwhelming majority of Jobs — all of them at LSF scale. Develop's version did zero filesystem work for such a Job: its whole body was `for _, mc := range j.MountConfigs`, a loop that never ran. This branch's version instead called `resolveWorkSpace()` first — `absJobDir` on Cwd and ActualCwd, `createdCwdRel` (which hashes the Job's `Key()`), `os.OpenRoot` on Cwd, an `os.Lstat` per path component of the ~6-level path below it (`componentsAreRealDirs`), and a final `cwdRoot.Lstat` of the working directory — and only then iterated `ws.keep.mountPoints`, which is empty for a Job with no mounts.
  - How it was found: a user-reported flaky test on an NFS checkout prompted a comparison of this branch against develop, and the diff of this function showed a resolution added to a path that develop left free. Nothing about the result changed, so no existing test could have caught it.
  - `mountPoints()`/`keptDirs` for a mountless Job, established by reading jobqueue/workspace.go: `keptDirs.mountPoints` is appended to only inside `for _, mount := range p.mountPoints()`, and `workSpacePaths.mountPoints()` returns exactly one resolved point per `MountConfig` (`make([]string, 0, len(p.mounts))`, one `append` per `mc`). `p.mounts` is the snapshot of `j.MountConfigs`. So no MountConfigs means no mount points, and `jobWorkSpace.rmEmptyMountDirs` — whose only loop is over `ws.keep.mountPoints` — has nothing whatever to do. The short-circuit can therefore key off `len(j.MountConfigs)`. (`keptDirs`' other fields are also all filled from the same two loops over the mounts, so none of them can be set either; only `workSpaceEntries` is even allocated.)
  - Red command: `CGO_ENABLED=1 go test -tags netgo --count 1 ./jobqueue/ -run TestJobUnmountTidyUpCostsNothingWithoutMounts`. The behaviour is unchanged, so the test measures the work: a new test-only seam `workSpaceResolveHook` in jobqueue/workspace.go, called at the top of `jobWorkSpaceSnapshot.resolveWorkSpace` (nil in production, exactly like the existing `cleanupProvenHook`/`runResolvedHook`/`runProvenHook`), lets the test count resolutions. It asserts both sides: a mountless Job's `Unmount` makes 0 resolutions, and a Job with a `Mount` still makes 1 and still deletes the mount dir and walks up to Cwd.
  - Red evidence (at 649614b, mountless half): `Line 385: Expected: 0 / Actual: 1 (Should equal)!` — `9 total assertions`, `--- FAIL: TestJobUnmountTidyUpCostsNothingWithoutMounts (0.00s)`. The with-mounts half was already green, as it must be.
  - Fix: an early `if !j.hasMounts() { return nil }` in `Job.rmEmptyMountDirs`, mirroring `Behaviours.Trigger`'s existing "answered before the snapshot, not after" guard for the same reason on the same exit path. New `Job.hasMounts()` reads `len(j.MountConfigs)` under the Job's `RLock` — the same lock `workSpaceSnapshot` takes, and released before anything walks the filesystem, per DEVELOPERS.md rule 1 — rather than inlining a `defer j.RUnlock()` that would be held across the resolution. Error semantics are untouched: a Job with mounts still swallows a resolution failure and returns nil, and the proven path for such a Job is byte-for-byte as it was.
  - Files touched: `jobqueue/job.go` (the guard and `hasMounts`), `jobqueue/workspace.go` (the `workSpaceResolveHook` seam), `jobqueue/behaviours_test.go` (the test, beside `TestJobUnmountEmptyDirTidyUp`).
  - Mutations, each re-verified to compile with `go vet ./jobqueue/` before believing its verdict: remove the guard (`_ = j.hasMounts()`) — vet clean, mountless half RED (`Line 385: Expected: 0 / Actual: 1`); short-circuit always (`if j.hasMounts() || true`) — vet clean, with-mounts half RED (`Line 399: Expected: 1 / Actual: 0`). Both reverted, test green.
  - Gates: `go build ./...` and `go vet ./jobqueue/` clean, `make test` `392 passed · 9 skipped · 3m15s` PASSED, `make race` `392 passed · 9 skipped · 4m12s` PASSED, `make lint` `0 issues.` (392 rather than the branch's 391 baseline is the one test added).

- [x] Reviewer claim on PR #558's keep-set classification (**data loss through a live mount, one directory spelled two ways, round 12**): "Mount points that resolve *into* the workspace via symlink spellings may not be recognized as being inside `workSpace`. In that case `keep.protect()` won't mark the workspace entry as protected, and `removeWorkSpaceEntries()` can `RemoveAll` the mounted directory while the mount is live, potentially recursing into the remote filesystem." **Valid.** LAYER 58 taught `dirIsAtOrAbove` - the "a mount at or above the workspace means delete nothing at all" test - to ask the filesystem when the two strings disagree, but left the two tests that decide what is PROTECTED purely lexical: `keptDirs.protect` -> `protectInActualCwd` and `entryLeadingTo` both used `relBelowDir` (`filepath.Abs` + `filepath.Rel`), and so did `protectCaches`' `rel == "."` test for `muxfysNamesWorkSpaceEntry`. One classification understood symlink spellings and three did not.
  - Verified by construction, not by inspection: the deletion really happens. With the classification assertions removed so the sweep could be observed, a Job whose absolute `MountConfig.Mount` is a symlink resolving into its own workspace had the mount's contents deleted in both shapes - `stat .../jobqueue_cwd/9/a/1/08ce.../cwd/mnt/remote.txt: no such file or directory` for a mount inside the working directory, and `stat .../jobqueue_cwd/f/c/6/1e57.../shared/remote.txt: no such file or directory` for one beside it. Cleanup runs BEFORE `Job.Unmount` (client.go), so those reads and `RemoveAll`s go through a live mount into the user's remote objects.
  - **Reachability: NOT reachable by any configuration; reachable only by a process deliberately racing the runner.** Settled by reading rather than asserted:
    - The workspace is `<Cwd>/<AppName>_cwd/k0/k1/k2/<key[3:]><digits>`, and every component except the digits is predictable to whoever submits the Job: `Job.Key()` is built from `Cmd`, `MountConfigs.Key()` and (only when `CwdMatters`) `Cwd`, all of which the submitter chose. The digits come from `os.MkdirTemp` inside `mkHashedDir`, on the runner, at run time.
    - So the `Mount` STRING cannot name anything inside the workspace, exactly as `TestBehaviourCleanupSafety`'s existing comment says. A symlink escapes that circularity - the string stays fixed, only the TARGET names the workspace - but the target still has to be written after `mkHashedDir` has run.
    - It has to be written before muxfys resolves the mount point, because `mount(2)` resolves symlinks: where the mount PHYSICALLY lands is settled then, and a symlink created afterwards moves only the classification, not the mount. `Client.Execute` mounts at client.go:1660 and starts the Cmd at client.go:1748, so the Job's own Cmd is too late.
    - Nothing publishes `ActualCwd` in that window either: the touch loop that carries the live snapshot starts before the mount, but its first tick is `ClientTouchInterval` (15s) later, while `job.Mount` is called immediately.
    - `MountConfig.Mount` is stored verbatim from `--mount_json` or the REST API (`mountParseJSON`, `serverREST.go`), with no normalisation, and `--mounts` never sets it at all. `wr mod` cannot change it mid-run: cmd/mod.go modifies only "incomplete, non-running" jobs.
    - What is left is a race: a process with write access to the Job's Cwd watches the fully predictable `<Cwd>/<AppName>_cwd/k0/k1/k2` for the leaf to appear, then creates the symlink at the `Mount` path it chose at submit time, before muxfys resolves it. The window is everything between `resolveWorkingDir` (client.go:1549) and muxfys' path resolution, which includes `buildRemoteConfigs`' S3 accessor construction and, on the `fusermount exited with code 256` path, a 3-second retry. No legitimate configuration reaches it.
  - Fixed anyway, for LAYER 58's reason and the brief's: the classification that licenses deletion must not have one test that understands symlinks and three that do not, because the next change to this code will not know which is which. Widening the keep set can only PREVENT deletion, at worst leaving an emptied dir behind.
  - Red command: `CGO_ENABLED=1 go test -tags netgo --count 1 ./jobqueue/ -run TestCleanupKeepsSymlinkSpelledMounts`
  - Red evidence (at 9871a99): `Line 469: Expected: []string{"mnt"} / Actual: []string(nil)`, `Line 489: Expected: true / Actual: false`, `Line 516: Expected: true / Actual: false`, `Line 557: Expected: []string{"mnt"} / Actual: []string(nil)`.
  - Fix: `relBelowDirResolved` (jobqueue/workspace.go) is LAYER 58's fallback given a name and a rel - lexical first, then `relBelowDir(resolvedDir(dir), resolvedDir(path))` - and `dirIsAtOrAbove` is now one line over it, so there is one mechanism rather than two. `protectInActualCwd`, `entryLeadingTo` and `protectCaches`' `muxfysNamesWorkSpaceEntry` test all call it, which is every place that classifies a mount or cache for PROTECTION.
  - **Why a resolved answer is safe here, when `realDirBelow` deliberately has no such fallback**: what these three keep is the REL, not an absolute path. Resolving a path does not change which directory it names, so the components of a rel taken from the resolved comparison are entries of the unresolved dir itself, and `EvalSymlinks` leaves none of them a symlink - which is exactly what `removeAllExcept` and `removeWorkSpaceEntries` name to their handles. An absolute resolved path would not be safe that way, being named in a different tree from the handles; nothing here keeps one.
  - **`keep.mountPoints` is deliberately left lexical**, and now says so in the code. It licenses `Job.Unmount`'s upward walk of empty dirs rather than protecting anything, and `rmEmptyDirsIn` -> `realDirBelow` requires a path inside the open Cwd with no symlink among its components. A symlink-spelled mount is not such a path, and its resolved twin is an absolute path in the other tree, so feeding it either would trade a leaked empty dir for a deletion licence. The consumers cannot disagree about what is PROTECTED, which is what the trap was about.
  - Cost, measured rather than assumed with a temporary counter on `resolvedDir`: a Job with one relative `Mount` goes from 2 `EvalSymlinks` walks per cleanup to 4. The extra pair is the default `CacheBase`, which IS the workspace and so fails `protectInActualCwd`'s lexical test as `".."` and now retries resolved. A Job with no mounts still pays 0, because both of `keptDirs`' loops are over its (empty) MountConfigs - `TestJobUnmountTidyUpCostsNothingWithoutMounts` still pins that.
  - Test: `TestCleanupKeepsSymlinkSpelledMounts` (jobqueue/workspace_test.go), four cases, each asserting the classification the production resolution makes (a new `jobKeptDirs` helper reads `ws.keep` from `resolveWorkSpace`) AND then the sweep, since the reachability verdict means the scenario itself is a fixture rather than a run: a mount inside the working directory and a mount beside it, both spelled through a symlink with the Job's Cwd real; a `CacheBase` symlink resolving to the workspace, so the `.muxfys*` dir muxfys names for itself survives; and the two spellings the other way round, with the JOB's Cwd the symlinked spelling and the mount's symlink naming the real path.
  - Mutations, 8, each re-verified to compile with `go vet ./jobqueue/` before its verdict was counted: the fallback removed entirely (M1); `protectInActualCwd` lexical again (M2); `entryLeadingTo` lexical again (M3); the `muxfysNamesWorkSpaceEntry` test lexical again (M4); resolving only the dir side (M5) or only the path side (M6). **6 of 6 RED.** M5 and M6 are the M13a/M13b lesson from round 11 restated: the fourth case, with the Job's Cwd symlinked and the mount named by the real path, is what makes the dir side load-bearing - without it M6 was GREEN.
  - Two mutations are GREEN and both are correct to be: `resolvedDir` returning `""` instead of falling back to `dir` (round 11's M15) is not this test's guard and is still RED against `TestBehaviourCleanupSafety` (`Line 1748`), checked; and swapping the order so the resolved comparison is tried FIRST is a pure cost regression with no behavioural difference, which no test can see.
  - Gates: `go build ./...` and `go vet ./jobqueue/` clean, `make test` `393 passed · 9 skipped · 3m20s` PASSED, `make race` `393 passed · 9 skipped · 4m0s` PASSED, `make lint` `0 issues.` (393 rather than 392 is the one test added).
  - Out of scope, found while trying to benchmark this: `BenchmarkJobCleanup` is broken at 9871a99, before this change. `benchCleanupJob` builds its workspace with `mkHashedDir(cwd, fmt.Sprintf("bench.cleanup.%d", i))` but the Job it returns has no `Cmd`, so `relIsJobCreatedCwd` refuses the path against the Job's real `Key()`: `actual cwd is not a working directory wr created: .../jobqueue_cwd/b/e/n/ch.cleanup.0<digits>/cwd`. Verified identical with jobqueue/workspace.go and jobqueue/workspace_test.go checked out at HEAD. Not touched here.

- [x] Reviewer claim on `jobqueue/behaviours_bench_test.go` (**`BenchmarkJobCleanup` measures nothing**): `benchCleanupJob` builds the workspace with `mkHashedDir` using a `tohash` that is NOT the Job's `Key()`, so workspace validation (`createdCwdRel` -> `relIsJobCreatedCwd`) reconstructs the expected path from the Job's own key, refuses the path, and `Behaviour.Trigger(Cleanup)` fails with `errNotACreatedCwd`. **Valid**, and already recorded as an out-of-scope observation on the previous item; fixed here.
  - **More severe than "measures nothing"**: because the benchmark's loop does `if err := behaviour.Trigger(...); err != nil { b.Fatal(err) }`, the broken fixture does not silently report the cost of a refusal — it makes the benchmark FAIL outright. Measured at `da95ad0`: `--- FAIL: BenchmarkJobCleanup` / `behaviours_bench_test.go:64: actual cwd is not a working directory wr created: /tmp/BenchmarkJobCleanup4104218654/001/jobqueue_cwd/b/e/n/ch.cleanup.04056359778/cwd`. So `make bench` has been red on this benchmark, and the earlier items' quoted `BenchmarkJobCleanup` figures (`63 allocs/op`, `154-156k ns/op`) cannot have come from this fixture in its current form.
  - Red command: `CGO_ENABLED=1 go test -tags netgo --count 1 ./jobqueue/ -run TestBenchCleanupJobIsCleanable`
  - Red evidence (at `da95ad0`, with the new test but the old fixture): `Line 116: Expected: nil / Actual: 'actual cwd is not a working directory wr created: /tmp/TestBenchCleanupJobIsCleanable399821278/001/jobqueue_cwd/b/e/n/ch.cleanup.01306676413/cwd'`, `1 total assertion`, `--- FAIL: TestBenchCleanupJobIsCleanable (0.00s)`. The refused path shows the defect directly: `b/e/n/ch.cleanup.0<digits>` is `calculateHashedDir`'s split of the string `bench.cleanup.0`, not of the Job's key.
  - Fix: `benchCleanupJob` now builds the Job FIRST and hashes `job.Key()`, then sets `job.ActualCwd` to what `mkHashedDir` produced and returns that same Job. The Job also gains the `Cmd` it was missing (`echo bench.cleanup.<i>`) — `Key()` is built from `Cmd`, `Cwd` (only when `CwdMatters`), `MountConfigs.Key()` and the container image, so every contributing field is set before the key is taken.
  - Test: `TestBenchCleanupJobIsCleanable` in `jobqueue/behaviours_bench_test.go`, beside the benchmark whose fixture it guards — the same file-level convention `db_bench_test.go` already follows with `TestArchiveJobsConcurrentlyDoesNotDeadlockOnError`. It calls the REAL `benchCleanupJob`, not a copy, so it cannot drift from what the benchmark runs; that required widening the helper's parameter from `*testing.B` to `testing.TB` (named `tb`, per the `thelper` linter). It asserts both halves the reviewer named: `Trigger` returns nil, and the working directory is gone afterwards. A benchmark cannot be its own red command — `make test` never runs one, so a `b.Fatal` there is not a guard.
  - Files touched: `jobqueue/behaviours_bench_test.go` only.
  - Mutations, each re-verified to compile with `go vet ./jobqueue/` before its verdict was counted:
    - M1, the required one: fixture reverted to `mkHashedDir(cwd, fmt.Sprintf("bench.cleanup.%d", i))` — vet clean, **RED** (`Line 126: Expected: nil / Actual: 'actual cwd is not a working directory wr created: .../jobqueue_cwd/b/e/n/ch.cleanup.02041306502/cwd'`). Reverted, green.
    - M2: the `Cmd` dropped, keeping `job.Key()` as the tohash — first attempt did NOT compile (`vet: "fmt" imported and not used`), which per the brief is a zero-assertion non-verdict, so it was rewritten to compile (vet clean) and is **GREEN**. Stated honestly: the `Cmd` is not what makes cleanup succeed — hashing `job.Key()` is — and no test can guard the `Cmd`, because an empty `Cmd` still yields a self-consistent key. What the `Cmd` buys is what the benchmark MEASURES: distinct keys give each iteration its own `k0/k1/k2` chain, so cleanup's upward tidy walk deletes the realistic number of levels instead of stopping at a parent shared by every iteration. Measured at `-benchtime 200x -benchmem`: constant `Cmd` `190615 ns/op / 6629 B/op / 135 allocs/op` vs per-iteration `Cmd` `217028 ns/op / 6662 B/op / 135 allocs/op`, ie. the shared-parent fixture understates the cost by ~12%.
  - Benchmark now executes the cleanup path: `go test -tags netgo -run '^$' -bench BenchmarkJobCleanup -benchtime 10x ./jobqueue/` -> `BenchmarkJobCleanup-4  10  205521 ns/op`, `PASS` (was `--- FAIL` before).
  - Gates: `go build ./...` and `go vet ./jobqueue/` clean, `make test` `394 passed · 9 skipped · 3m10s` PASSED, `make race` `394 passed · 9 skipped · 3m57s` PASSED, `make lint` `0 issues.` (394 rather than the 393 baseline is the one test added; the first `make lint` caught `thelper: parameter testing.TB should have name tb`, and both `make test` and `make race` were re-run after that rename rather than quoting pre-rename numbers).
