- [x] The bug is not that the openstack tests ran, it's that they ran, failed, and took so long to do so.
  - Updated the live OpenStack scheduler test helpers so timed-out scenarios cancel the shared live-test context and report scheduler/OpenStack state, preventing later contexts from burning additional 120s/150s waits after the first serious timeout.
  - Changed OpenStack CLI counting helpers to execute shell pipelines in their own process group, so context timeouts kill child `openstack`/`nova` processes instead of leaving them holding output pipes open.
  - Added focused regression coverage for live-wait cancellation diagnostics and shell pipeline timeout cleanup.
- [x] the tests should actually pass! So fix the reason for the test failures.
  - Verified the sourced live scheduler path with `source ~/.openstack.rc; go test -tags netgo --count 1 ./jobqueue/scheduler -run "^TestOpenstack$" -timeout 32m -v`, which passed in 1021s with 133 assertions.
  - Verified focused scheduler regressions and `make lint`; confirmed the passing live run left no `wr-testing-sb10-*` server, keypair, or security group resources behind.
