- [x] make test takes a long time and fails in scheduler_openstack OpenStack scheduler tests, with assertions such as "Run a job on a specific flavor" and "Override the default os image and ram" reporting Expected true, Actual false.
  - Added explicit `WR_TEST_LIVE_OPENSTACK=1` opt-in for wr-testsuite live OpenStack lanes; sourced `OS_*` / `OS_OS_*` variables alone now keep the normal non-live plan.
  - Normal non-live lanes override sourced OpenStack `OS_*` variables to empty so package tests behave like an unsourced shell, while explicit live lanes still inherit the OpenStack environment.
  - Documented the opt-in in the Makefile test knobs.
  - Verification: focused internal tests failed before the scrub/opt-in fix and pass after; `go test -tags netgo --count 1 ./internal/testsuite`, `make lint`, and `source ~/.openstack.rc; timeout 5m make test` all pass.
