- [x] Consolidate the two broadcast projections so every job-state transition fans out through a single emission chokepoint instead of each transition site remembering to update both by hand. Today the web UI status counts (`statusState.applyTransition`) and the per-job subscription updates behind `wr add --sync` and the web UI details push (`enqueueSubscriptionUpdate`) are emitted as separate, manually-paired calls in three places (the queue change callback, the TTR callback, and the serverCLI touch/recover path), so a future transition path can silently forget one and let the counts drift or a `--sync`/details subscriber miss an update. Route all transition sites through one chokepoint that performs both projections. Behaviour must be identical (keep the two delivery mechanisms separate; preserve the count-vs-subscription asymmetry where counts fire on every transition but subscription updates are gated by `subscriptionUpdateState` + per-subscriber filtering). Recommended as the worthwhile reuse in the broadcast follow-up discussion.
  - Added a single emission chokepoint `Server.emitJobTransition` (new `jobqueue/jobtransition.go`): it applies the per-RepGroup count contributions (`statusState.applyTransition`) then runs the per-job subscription fan-out, strictly sequentially, introducing no new lock.
  - Routed all three transition sites through it — the queue change callback, the TTR reclaim callback (running→lost), and the serverCLI touch/recover path (lost→running) — so the counts and per-job subscription updates can no longer be emitted independently or forgotten on a new path.
  - Behaviour preserved exactly: counts still update on every transition; per-job subscription updates stay gated by `subscriptionUpdateState` + per-subscriber filtering; the change callback's running-but-`Lost`→`lost→to` sub-tally, the TTR `lostUpdate`/defer/kill-confirm logic, and the serverCLI live-snapshot update are unchanged. No wire/protocol change; the two delivery mechanisms remain separate. `wr add --sync` behaviour unchanged.
  - Added a behavioural invariant test (`jobqueue/statusstate_invariant_test.go`) that drives every transition path — including TTR reclaim and touch/recover, which bypass the change callback — and asserts the absolute counts equal a ground-truth recompute (live queue + completed DB; the UI-only `deleted` accumulator is excluded symmetrically) AND that subscribers receive the per-job update for subscription-relevant transitions. The reviewer independently confirmed the test fails if either fan-out is removed.
  - Re-pointed the existing `TestSubscriptionStateChangeEvents` structural guard to track the relocated per-job loop while keeping its behavioural guarantee (one `ToStatus` per job, reused for the subscription, never written to the browser). Lock order `queue.mutex → job → statusState.mu` preserved (`statusState.mu` stays a strict leaf); `-race` clean.
  - Verified by implementor and reviewer subagents: `make lint` clean, `make test` pass, focused `-race` pass, and `make browser-test` pass (all status fixtures green).
