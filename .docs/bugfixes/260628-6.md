# Bugfix 260628-6

- [x] Use the bugfix skill workflow to make this change: have `wr cloud deploy` go down the "To debug further..." path when hitting "could not talk to wr manager" errors.

## Notes

- Added an interactive manager-connect failure handler that reuses the SSH debug prompt before cleanup and teardown.
- Preserved deploy SSH forwarder cleanup after the debug pause so failed deploys do not leak port-forwarding processes.
- Added focused regression coverage for the prompt/cleanup ordering.

## Verification

- Implementor: focused `./cmd` tests, `gofmt`, `cleanorder`, `golangci-lint run ./cmd`, `git diff --check`.
- Reviewer: PASS with focused `./cmd` tests, `gofmt -l`, and `golangci-lint run ./cmd`.
- Main: `env CGO_ENABLED=1 GOCACHE=/tmp/wr-go-cache-main GOMODCACHE=/tmp/wr-go-mod-cache-main GOTMPDIR=/tmp go test -tags netgo --count 1 ./cmd`.
