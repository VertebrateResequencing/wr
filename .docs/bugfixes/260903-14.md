# Bugfixes 260903-14

Branch `fix-same-key-workspace-identity`: a job's `on_exit cleanup` can delete
another live run's FUSE-mounted remote data, and its un-uploaded writable-mount
output, silently, returning `nil`.

- [x] `Job.Key()` treats as one job configurations whose live mounts and
  un-uploaded output are in different places, so the keep set a finished run
  computes from its own `MountConfigs` does not name what a live run of the same
  key has on disk - and the finished run's `ActualCwd` can name the live run's
  workspace byte for byte.

  Fixed by giving a workspace the identity of the run that created it and
  refusing to delete a workspace that is not this run's; see below. Files
  touched: `jobqueue/utils.go` (mint, record, and the one removal entitled to
  take it), `jobqueue/workspace.go` (`proveWSToken` in `prove()`, `keptEntry`),
  `jobqueue/job.go` (`Job.ActualCwdToken`, `setActualCwd`, `clearActualCwd`),
  `jobqueue/client.go` (`JobEndState.CwdToken`, `resolveWorkingDir`,
  `startRequestJob`, `executeLiveState`), `jobqueue/serverCLI.go` and
  `jobqueue/serverWebI.go` (the report and clear sites),
  `jobqueue/workspace_run_identity_test.go` (new), eight existing test files
  whose fixtures now report the pair, `go.mod`/`go.sum` (go-fuse promoted to a
  direct dependency for the real-mount rows), and
  `.docs/cwd_matters_cleanup/readme.md`.

  Red command (four rows, four distinct keep-set rules):

      unset $(compgen -v | grep '^OS_')
      go test ./jobqueue/ -run 'TestCleanupOfAnotherRunsWorkSpace|TestWorkSpaceRunRecord' \
        -count=1 -timeout 10m

  The four rows were measured before the fix under the name the harness they came
  from gave them, `TestProbeMountConfigsOneKeyCovers`; they are the same four
  `Convey` rows, reshaped into `jobqueue/workspace_run_identity_test.go` as
  `TestCleanupOfAnotherRunsWorkSpace`. Measured on the branch point
  (`a1a1c861`), exit 1, 0.02s:

  ```text
  * jobqueue/behaviours_test.go
  Line 1740:
  Expected: nil
  Actual:   'stat /tmp/TestProbeMountConfigsOneKeyCovers1971616592/002/REMOTE_DATA:
            no such file or directory'
  Line 1740:
  Expected: nil
  Actual:   'stat .../003/jobqueue_cwd/d/c/1/92569f8c4d691f0b547cef5f3bbcb2276510700/
            s3.example.com/bucket/REMOTE_DATA: no such file or directory'
  Line 1740:
  Expected: nil
  Actual:   'stat .../004/jobqueue_cwd/d/c/1/92569f8c4d691f0b547cef5f3bbcb472509131/
            tmp/REMOTE_DATA: no such file or directory'
  Line 1740:
  Expected: nil
  Actual:   'stat .../005/jobqueue_cwd/d/c/1/92569f8c4d691f0b547cef5f3bbcb2341595071/
            .muxfys_cache123/REMOTE_DATA: no such file or directory'

  --- FAIL: TestProbeMountConfigsOneKeyCovers (0.02s)
  ```

  The first row's path is the backing directory of a real go-fuse loopback
  mount: the object was unlinked THROUGH the live mount, which is the user's
  remote data. The other three are a writable mount's output waiting for
  `Unmount` to upload it. `cleanErr` and `tmpErr` were both `nil`, and the mount
  point survived, so nothing looked wrong.

## The chain, verified at file:line

1. `MountConfigs.Key()` (jobqueue/mount.go:248) substitutes `defaultMountDir`
   (`"mnt"`, mount.go:41) for an empty `MountConfig.Mount` (mount.go:255-257).
2. `Job.mountBaseDirs` (jobqueue/job.go:852-868) sets `defaultMount = cwd` - the
   working directory itself - for a job wr created a working directory for, and
   `resolveMountPoint` (job.go:127-137) returns that `defaultMount` unchanged for
   an empty `Mount`. So `wr add --mounts cw:bucket/path`, which leaves `Mount`
   empty (`mountParseSimple`, cmd/mount.go:451-461), mounts ON the working
   directory, while `--mount_json '[{"Mount":"mnt",...}]'` mounts at
   `<cwd>/mnt` - and the two are one job by key.
3. `MountConfig.writeTargetsKey` (mount.go:294-306) reads only `Target.Profile`
   and `Target.Path`. Nothing that decides where a cache lands is in the key:
   not `MountConfig.CacheBase`, not `Target.CacheDir`, not `Target.Cache`, not
   `Target.Write`.

`os.MkdirTemp`'s digits (utils.go:729) are not pinned to a run, so the workspace
leaf name one manager's run finished with is free to be handed to another
manager's run of the same key later - dedup is per-manager, so two managers
sharing a filesystem can each hold a job with that key but independently spelled
mounts. The stale run's `ActualCwd` then names the live run's workspace byte for
byte, `createdCwdRel` (workspace.go:236-251) is satisfied because the path really
is one this key builds, and the sweep reads through the live mount:
`os.Root.RemoveAll` is bounded by `RESOLVE_BENEATH`, which does not imply
`RESOLVE_NO_XDEV`.

The four rows hit four distinct keep-set rules: `keptDirs.wholeActualCwd`
(workspace.go:759), `wholeWorkSpace` (workspace.go:729), `workSpaceEntries` via
the separate `removeTmpDir` deletion (workspace.go:929), and
`muxfysNamesWorkSpaceEntry` (workspace.go:717).

The `"" -> "mnt"` normalisation dates to `8eead7ad` (2017-06-12, "Job mounts are
now part of the unique key"); `902eccf0` only renamed the literal to a const. It
predates the keep set by years and is only now load-bearing.

## Not fixed in `Job.Key()`

Two reasons, both checked:

- The key IS the persisted database key. `db.go` uses `job.Key()` as the BoltDB
  key in `bucketJobsLive` and `bucketJobsComplete` (db.go:1440, 2233, 2434) and
  as the value of every lookup bucket (db.go:316, 2773). Changing the algorithm
  orphans every existing record and changes dedup for jobs already in flight.
- Semantically the key is arguably right: two jobs with the same command and cwd
  but different cache locations are the same unit of work for scheduling and
  dedup. The defect is that cleanup assumes key equality implies on-disk layout
  equality.

## The fix: a workspace carries the identity of the run that made it

`Job.Key()` is left alone. What changes is that cleanup no longer assumes key
equality implies on-disk layout equality: it asks the workspace WHICH RUN made
it, and refuses a workspace that answers with another run.

- `mkCwdAndTmp` (jobqueue/utils.go) mints a random v4 UUID
  (`github.com/gofrs/uuid/v5`, the repo's direct uuid dependency and
  `Job.ReservedBy`'s type), writes it into the workspace as `.run_token`
  (`createdWSTokenName`) with `O_CREATE|O_EXCL`, and returns it;
  `mkHashedDir` passes it on.
- It travels PAIRED with `ActualCwd`, so neither can be stored without the
  other: `Job.ActualCwdToken` (exported, so binc persists it by name and a job
  recovered from the database keeps it), `setActualCwd(cwd, token)` and the new
  `clearActualCwd()`, `JobEndState.CwdToken`, `executeLiveState`, the `Started`
  request (`startRequestJob`) and `copyJobForClient`.
- `workSpacePaths.proveWSToken`, called from `prove()` (jobqueue/workspace.go),
  reads the record back through the handle already open on the Job's `Cwd`, at
  the workspace's already-proven relative path, and refuses with
  `errNotThisRunsWorkSpace` unless the run reporting the path is the run
  recorded. Absent allows; present-and-unmatched refuses, whether the reporter
  offers a different identity or none.
- The record outlives every deletion decision: `jobWorkSpace.keptEntry` claims
  it unconditionally, so no sweep takes it, and the ONE removal entitled to,
  `removeDirHoldingOnlyWSToken` (utils.go), is reached only from
  `dirChain.removeLeaf`/`removeParent` after a `Remove` has already failed with
  `ENOTEMPTY`, and only when the record is the sole remaining entry. It cannot
  take record and directory atomically - rmdir will not take a directory the
  record is still in, and no syscall removes a directory with its last entry -
  so it unlinks the record, and PUTS IT BACK if the directory then refuses to
  go, which is what happens when the run that owns it has created something in
  it since. What that guarantees is not atomicity but that no call of it leaves
  a LIVE workspace with no record. A directory another process removed while
  the record was down is not a failure at all - the same interleaving
  `removeLeaf` has always treated as ordinary. What stays returned is a restore
  that finds another run's record already at the name (nothing is left
  unprotected; only wr's ability to say whose workspace it was is lost) and a
  write that fails for any other reason (which does leave the workspace
  unrecorded for the rest of its life); the two are reported as different
  errors.

### Why not the alternatives

- **Not in `Job.Key()`**: see above. It is the persisted db key.
- **Not more entropy in the `os.MkdirTemp` leaf name** (a UUID instead of ~9
  digits, so a stale `ActualCwd` could never name a live workspace). It makes a
  collision improbable rather than DETECTABLE, and it cannot be proven by a
  test: the collision it prevents can no longer be arranged the way production
  would produce it, so a test could only assert the naming scheme, which
  testing-principles forbids.
- **Not the `.hold` file** that `mkHeldDir` already drops (utils.go). It lives
  in the hashed PARENT, not the workspace, and `removeHoldFile` deletes it via
  `defer` before `mkHashedDir` returns, so it is gone before any cleanup could
  read it.

## Answers to the four questions the brief asked

**Where the token comes from, and why it cannot collide.** A random v4 UUID per
call to `mkCwdAndTmp`, so per RUN rather than per job: 122 bits from
`crypto/rand`, and the values only ever have to be distinct among the
workspaces one filesystem holds at one time. NOT host+pid: `Job.Pid` becomes
the COMMAND's pid at `Started` (`applyJobStart`), so the runner's own pid does
not survive on the manager's side, and pids recycle. Nor the manager's existing
`runToken` (`Job.runID`, job.go): it is minted per manager, is zero for a run
recovered from the database, and two managers sharing a filesystem - the bug's
own premise - would both mint 1.

**Backwards compatibility.** Absent fails OPEN, present-and-unmatched fails
CLOSED. Absent has to be allowed or every workspace made before the upgrade
would leak for ever; present proves a token-aware wr created the workspace, so
a deleter with no identity of its own cannot be shown to be its run. The two
are distinguished, deliberately: `ENOENT` is absence and is allowed, while an
EMPTY (or non-regular) record is present and matches nothing, so it is refused.
The mixed-version cases are consistent in both directions - an old runner
writes no record AND sends no token, and a new runner's field is ignored by an
old manager. `reliable2_dbcompat_test.go`, which decodes a golden BoltDB, still
passes.

**Whether `prove()` is the right place.** Yes. It is the single point all four
consumers pass through - `cleanupWorkSpace`, the separate `removeTmpDir`,
`Job.rmEmptyMountDirs` and `resolveRunDir` - so one check covers all three
deletion paths, and it also stops a `run` behaviour executing the user's
command inside another run's live workspace, which is the same class of bug.
`cleanup()`'s `wholeWorkSpace` early return happens after `prove()`, so the
check cannot be skipped.

**The unstarted and lost-job paths.** Both reached, neither a hole.
`cleanupUnstartedWorkSpace` (client.go) snapshots the runner's own `*Job`, on
which `resolveWorkingDir` set both fields. `triggerLostRunBehaviours`
(server.go) acts on `pinnedBehaviours`, whose `workSpaceSnapshotLocked()` now
copies the token with the path, so a pin taken at `markJobLost` carries the
identity of the run it belongs to; the manager learns both from the `Started`
request and from every live touch. Mutating the `Started` report to drop the
token reddens `TestLostJobOnWeblessManagerCleansItsWorkSpace`, which is that
path measured rather than argued.

## The review cycle

The first implementation was reviewed and FAILED on one blocking defect, then
fixed and reviewed again.

`keptEntry` did not claim the record, so `removeWorkSpaceEntries` deleted it on
any sweep - including the PARTIAL sweep that keeps the workspace because a live
mount is in it. `Client.Execute` fires behaviours (client.go:2213) BEFORE
`job.Unmount()` (client.go:2218), so for the very configuration in the incident
(`--mounts cw:bucket/path --on_exit cleanup`, whose mount point IS the working
directory) the live run's own cleanup destroyed its own protection while its
mount was up, and the original data loss was reachable again. Measured through
the production paths:

```text
PROBE record after the live run's own sweep: err=stat .../.run_token: no such file or directory
PROBE stale cleanup err=<nil> tmp err=<nil>

* jobqueue/behaviours_test.go
Line 1740:
Expected: nil
Actual:   'stat /tmp/TestF1LiveRunSweepsItsOwnRecord1623835536/002/REMOTE_DATA: no such file or directory'
```

`/002/REMOTE_DATA` is the go-fuse backing directory outside the Job's `Cwd`: the
user's remote object, unlinked through the live mount, silently. That probe is
now the fifth Convey of `TestCleanupOfAnotherRunsWorkSpace`.

## Verified by test

    unset $(compgen -v | grep '^OS_')
    go test ./jobqueue/ -count=1 -timeout 10m -run \
      'TestCleanupOfAnotherRunsWorkSpace|TestWorkSpaceRunRecord|TestWorkSpaceRecordAfterUnmount'

- `jobqueue/workspace_run_identity_test.go`,
  `TestCleanupOfAnotherRunsWorkSpace`: the four key-blind pairs, each hitting a
  different keep-set rule, plus the live-run's-own-cleanup row. Every row
  asserts the survivors FIRST and the refusal's class second, since Convey
  halts a leaf at its first failure and a deletion is the failure that matters.
  Row one and the fifth row raise a real go-fuse loopback mount, so what they
  measure is the user's remote data rather than a stand-in directory.
- `TestWorkSpaceRunRecord`: the guard's own arms - the recorded run sweeps the
  workspace record and all; a reporter with no identity is refused; a
  non-regular record is refused UNREAD; an empty record is not no record; an
  absent record is swept, as a pre-upgrade wr's has none.
- `TestWorkSpaceRecordAfterUnmount`: the reclamation half, through the real
  `Job.Unmount` - a mounting job keeps mount point and record through its own
  cleanup, and `Unmount` then reclaims the mount point, the workspace and the
  hashed base. Without it, the record-tolerance in `removeEmptyParents` is
  undefended.

## Mutation checks

`go vet ./jobqueue/ ./cmd/` was run on every mutated tree and was clean each
time, so no verdict here is a build failure masquerading as a dead guard. Each
was reverted and the suite confirmed green again. Four were measured
independently at orchestration level (marked O); the rest by the implementor or
reviewer, and every O one confirms what they reported.

- [x] **O1: the identity check neutralised** - `proveWSToken`'s refusal replaced
  with `if !present || true { return nil }`. Vet clean. **RED**: all four
  key-blind rows back to the original data loss at `behaviours_test.go:1740`
  (`stat .../REMOTE_DATA: no such file or directory`, the first being the
  go-fuse backing directory outside the Job's `Cwd`), plus three guard Conveys.
- [x] **O2: `keptEntry` stops claiming the record** - the defect the first
  review caught. Vet clean. **RED**: `stat .../007/REMOTE_DATA: no such file or
  directory` at `behaviours_test.go:1740`, and the record's own survival in
  `TestWorkSpaceRecordAfterUnmount`.
- [x] **O3: the parents' record-tolerance removed** - `removeParent`'s tail
  replaced with `return false`. Vet clean. **RED on
  `TestWorkSpaceRecordAfterUnmount` ONLY**, at `behaviours_test.go:1748`: the
  workspace and every hashed level above it leak. Nothing else reddens, which
  is why that test had to exist.
- [x] **O4: the restore dropped** - `return false, restoreWSToken(...)` replaced
  with `_ = record; return false, nil`. Vet clean. **RED**: `stat
  .../.run_token: no such file or directory` at `behaviours_test.go:1740` in
  `TestWorkSpaceRecordThroughItsOwnRemoval` - the live workspace left with no
  record, which is the state the second review found the comments wrongly
  promised could not exist. The `_ = record` is load-bearing to the MUTATION:
  without it the tree does not compile (`declared and not used: record`), vet
  fails, no test runs, and the zero failures would masquerade as a dead guard.
- [x] **O5: the gone-directory tolerance removed** -
  `if err := parentRoot.Remove(name); err == nil || os.IsNotExist(err)` cut
  back to `err == nil`. Vet clean. **RED** at
  `workspace_run_identity_test.go:620`: `Expected: nil / Actual: '... was left
  with no record of the run using it: openat .run_token: no such file or
  directory'` - the false failure that fails a job whose Cmd succeeded.
- [x] **O6: `restoreWSToken`'s `O_EXCL` replaced with `O_TRUNC`** (that line
  alone). Vet clean. **RED** at `workspace_run_identity_test.go:653`:
  `Expected: "6f1c0b1e-...-000000000001" / Actual: "1f16955a-..."` - wr writing
  its own stale record over one written by someone else in the window. Before
  the test for it existed this mutation left the whole suite GREEN, so the
  guard was live but undefended.
- [x] **M5: the mint made constant**, so two runs share an identity. Vet clean.
  **RED**, and narrowly: every leaf halts at
  `So(liveToken, ShouldNotEqual, stale.ActualCwdToken)` before a cleanup runs,
  so what it measures is that the mint is unique per RUN, not that the
  deletions return. Recorded because the readme first claimed otherwise.
- [x] **M6: `proveWSToken` accepts any record that is present.** Vet clean.
  **RED**: all five rows at `behaviours_test.go:1740`, each a distinct survival
  failure (`/002/REMOTE_DATA`, `s3.example.com/bucket`, `tmp`,
  `.muxfys_cache123`, `/007/REMOTE_DATA`), plus two guard Conveys. This is the
  mutation that puts every deletion back, and it proves the rows measure the
  deletion rather than the error class.
- [x] **M7: the `Started` request stops reporting the token.** Vet clean.
  **RED**: `TestLostJobOnWeblessManagerCleansItsWorkSpace` at
  `behaviours_test.go:1748` - the manager's only source of the token for a run
  that never touches.
- [x] **M8: `setActualCwd` stores the path but not the token.** Vet clean.
  **RED**: `job_test.go` and `TestLostJobBehavioursActOnTheLostRun`.
- [x] **M9: the leaf's record-tolerance removed.** Vet clean. **RED**: the swept
  workspace leaks, in `TestWorkSpaceRunRecord` and in the pre-existing
  `TestClientExecuteUnstartedRunWorkSpace`.
- [x] **M10: `takeSoleWSToken`'s sole-entry restriction weakened**
  (`len(entries) != 1` -> `== 0`). Vet clean. **RED**: `/007/REMOTE_DATA`
  deleted through the live mount again.
- [x] **M11: the non-regular-file refusal dropped** from `recordedWSToken`. Vet
  clean. **RED**: "a record that is not a regular file is refused rather than
  read" - a relative symlink to a file holding the DELETER's identity is
  followed and matched.
- [x] **M12: `wsTokenMaxBytes` refusal dropped** from `takeSoleWSToken`. Vet
  clean. **RED**: the oversized-record row - wr would otherwise put back a
  truncated copy of a file the Job's own Cmd wrote.
- [x] **M13 (GREEN, and a finding rather than a gap):** `takeSoleWSToken`'s
  `entries[0].Name() != createdWSTokenName` check dropped. Vet clean, suite
  GREEN. It is redundant by construction - the read and the unlink name that
  entry themselves and neither can succeed unless it is there - so no test can
  redden it. Kept as explicit intent, with the comment now saying so, rather
  than left for a later reader to mistake for missing coverage.

## Gates

Measured on the committed tree with all `OS_*` unset. `go build ./...`,
`go vet ./cmd/ ./jobqueue/`, `go mod tidy -diff`, `gofmt -l jobqueue/ cmd/` and
`cleanorder -min-diff` on each edited `.go` file: all clean, run at
orchestration level.

The three full suites were run by the implementing and reviewing agents on this
tree before the box was reserved for central gating, and are reported as their
measurements rather than mine:

- `make test`: `430 passed · 9 skipped · 29 packages · 3m50s`, PASSED.
- `make race`: `430 passed · 9 skipped · 29 packages · 5m45s`, PASSED, no
  `DATA RACE`.
- `golangci-lint run jobqueue/...`: 23 issues, none naming a line this change
  added, `jobqueue/utils.go` and `jobqueue/workspace.go` at zero.

430 is the branch point's 426 plus this branch's four new test functions;
`git diff -U0 -- '*_test.go' | grep -E "^[+-]func Test"` is empty, so no
existing file gained or lost one.

Wider targeted runs at orchestration level after the last change, all green:
`TestWorkSpace|TestBehaviour|TestLostJob|TestCleanupOfAnotherRunsWorkSpace|
TestClientExecuteUnstartedRunWorkSpace|TestJobSetActualCwd|
TestRelIsJobCreatedCwd|TestRmEmptyDirsIn` - `ok ... 23.396s`.


## Discovered while fixing this, and NOT fixed here

- [ ] `dirChain.removeParent` stops the upward walk on a parent another process
  has already removed, leaking every level above it. `jobqueue/utils.go`:
  `if !errIsDirNotEmpty(err) { return false, nil }` treats `ENOENT` as "will
  not go", which is the opposite of `removeLeaf`'s `err == nil ||
  os.IsNotExist(err) -> gone` six lines up. Predates this branch: at
  `a1a1c861`, `removeEmptyParents` was `if c.roots[i].Remove(parents[i]) != nil
  { return }`, so any error including `ENOENT` stopped the walk. It leaks
  rather than deletes, which is why it is recorded rather than fixed here, but
  it is the first thing a reader will notice beside the tolerance this branch
  added to the leaf.

- [ ] `make lint` does not report `0 issues.` on this branch point. With the
  `golangci-lint` version the Makefile pins (v2.12.2, Makefile:95), `make lint`
  exits 2 with **29 issues** at `a1a1c861` itself, measured in a throwaway
  worktree of that commit after `golangci-lint cache clean`, and 29 on this
  branch. The two issue lists are identical apart from two line-number shifts in
  files this change touches (`jobqueue/client.go:1174` -> `1182`,
  `jobqueue/job.go:1198` -> `1206`), so this change adds none. The 29 are
  `funlen: 9, gocognit: 2, gocritic: 1, gocyclo: 3, govet: 3, nestif: 4,
  staticcheck: 2, unparam: 1, wsl_v5: 4`, spread over files this branch has no
  business touching. Clearing the shared cache fixes the symptom of issues
  naming OTHER checkouts under `/home/ubuntu/`, but not the count. Recorded
  rather than fixed: it is repository-wide lint debt, it predates the branch,
  and every file it names is one a concurrent branch may be editing.

- [ ] A full `make test`/`make race` run leaves untracked `jobqueue/jobqueue_cwd/`
  debris in the repository. `jobqueue_test.go`'s "You can modify the cwd of a
  job" gives a Job the package directory as its `Cwd` (via `os.Getwd()`) and
  then `wr mod --cwd`s it away, which is the documented "Leaks, the affordable
  half" residual - so wr creates a workspace inside its own source tree and
  nothing ever sweeps it. Predates this branch; this branch's run record is one
  more file inside that debris.

## Verified against the ORIGINAL reproduction, not only against our own tests

The probe round that found this bug encoded it as four deliberately-red rows
(`probeKeyBlindRows` / `TestProbeMountConfigsOneKeyCovers`, preserved on the
`probe-round8-cases` branch). This fix was written with its own tests rather
than by adopting those rows, so the fix had not been checked against the shape
that actually found the bug. It has been now.

Dropping that file onto this branch (with the three signature updates it needs
for `pin.trigger()`, `mkCwdAndTmp` and `mkHashedDir`, none of which change what
it asserts) and running it gives, for all four rows:

        Line 1668:
        Expected: nil
        Actual:   'workspace was created by another run of the job: <ws> records
                   a different run than the one reporting it'

        60 total assertions
        --- FAIL: TestProbeMountConfigsOneKeyCovers

Line 1668 is `So(cleanErr, ShouldBeNil)`. Every assertion BEFORE it is a
survival assertion - the planted cache files, the FUSE-mounted remote and its
object, and the user's own `precious`/`cwd` - and all of them passed. So the
deletion those rows were written to demonstrate no longer happens: the data
survives and cleanup returns `errNotThisRunsWorkSpace` instead of silently
destroying it.

**Consequence for the probe corpus:** those four rows now assert the OLD
behaviour and must be updated when they land, from "cleanup succeeds and the
remote data is gone" to "cleanup refuses with `errNotThisRunsWorkSpace` and
everything survives" - the corpus's normal shape for a refused attack. Until
then they are red for the right reason, and they are red on this branch rather
than on `develop`.

## A second bug, found by the rebase: the run record turns off five of #577's tests

- [x] The run record this branch writes into the workspace makes that directory
  permanently non-empty, and #577's and #575's mount-boundary tests raise a real
  FUSE mount over the workspace itself. `fusermount` 2.9.9 refuses a non-empty
  mount point without the `nonempty` option, so five `Convey` scopes that guard a
  real data-loss bug - a job's cleanup sweeping into a mount rooted at the very
  directory it is clearing - now SKIP instead of running. A skipped safety test is
  worse than a failing one, because nothing draws attention to it.

  Two full-suite figures reported by the user rather than measured here, since
  `make test` was run once and only on the fixed tree: `origin/develop`
  (`44be38e3`) gives `444 passed · 9 skipped` with no FUSE skip line, and this
  branch at `b07c3dc5` gives `448 passed · 14 skipped` with a new summary line
  `- this host refused an unprivileged FUSE mount x5`. What was measured here is
  the five skips themselves, by the red command below.

  Red command (exits 1 while any scope skips, 0 when all five run):

      cd /home/ubuntu/wr_fix_key
      unset $(compgen -v | grep '^OS_')
      go test ./jobqueue/ -count=1 -timeout 10m -v \
        -run 'TestCleanupCrossesNoMountBoundaryAtTheWorkSpace|TestJobTmpDirRemovalCrossesNoMountBoundary' \
        > /tmp/red.log 2>&1
      ! grep -q 'refused an unprivileged FUSE mount' /tmp/red.log

  Exit 1, five matches. What fusermount itself says, verbatim from a wider run
  that also drove the three mount-boundary tests which do NOT skip - Convey's
  totals are cumulative across a run, so the count below is the running total at
  that point, not this scope's own; the red command's own two-test run reaches 41:

  ```text
    Given a Job whose Cmd raised a live mount over the workspace wr gave it ✔✔✔✔✔✔✔
  /usr/bin/fusermount: mountpoint is not empty
  /usr/bin/fusermount: if you are sure this is safe, use the 'nonempty' mount option
      workspace_test.go:228: this host refused a loopback FUSE mount at
        /tmp/TestCleanupCrossesNoMountBoundaryAtTheWorkSpace3811377557/001/jobqueue_cwd/0/0/c/ed1d13...

      this host refused an unprivileged FUSE mount ⚠

  50 total assertions (one or more sections skipped)
  ```

  Fixed in `jobqueue/workspace_test.go` only; no production code changed. The
  five scopes are the 2x2 loop of `TestCleanupCrossesNoMountBoundaryAtTheWorkSpace`
  (workspace_test.go:229 before the change) and the single scope of
  `TestJobTmpDirRemovalCrossesNoMountBoundary` (:284). Both raise their mount over
  the WORKSPACE. The other three mount-boundary tests never skipped:
  `AtTheWorkingDir` (:114) and `TestCleanupCrossesNoMountBoundary` (:1690) mount
  over or inside `cwd`, which `mkCwdAndTmp` leaves empty, and
  `OverAPopulatedWorkingDir` (:163) already passes `nonempty`.

### The production shape, which is what decided the test change

The record does NOT narrow the exposure #577 defends against, so no scope became
unreachable and none was deleted or reduced. `mkHashedDir` ends
`return mkCwdAndTmp(dir)` (utils.go:739) with `dir` the `os.MkdirTemp` leaf, ie.
the workspace; `mkCwdAndTmp` creates `<workspace>/cwd` (utils.go:898) and
`<workspace>/tmp` (utils.go:903) in it; and `Client.resolveWorkingDir` sets
`cmd.Dir = actualCwd` (client.go:1556), which is `<workspace>/cwd`. So by the
time a Job's own `Cmd` can run `sshfs remote:/data ..`, the workspace already
held `cwd` and `tmp`. It has been non-empty in production since long before this
branch; the record makes it three entries instead of two.

What the record actually broke was the FIXTURE. The tests emptied the workspace
with `So(os.RemoveAll(actualCwd), ShouldBeNil)` / `So(os.RemoveAll(tmpDir),
ShouldBeNil)` purely to get past `fusermount` 2's emptiness check, and that
worked only while `cwd` and `tmp` were the sole entries. The record cannot be
removed without making the fixture unlike production - its location was chosen
so no sweep takes it (`keptEntry`, workspace.go:1137) - so emptying the
workspace is no longer possible, and it was never production-like to begin with.

`nonempty` is verified on this host rather than assumed: #577's own
`TestCleanupCrossesNoMountBoundaryOverAPopulatedWorkingDir` passes with it at
workspace_test.go:162, and what `fusermount` 2.9.9 printed in the red run names
the option itself. So the earlier claim that this box refuses `nonempty` is
wrong; the previous agent's report that it accepts it is right.

### What changed

At both sites: `"nonempty"` passed to `mountLoopback`, the two `os.RemoveAll`
fixture lines and their now-false comment dropped, a comment put in their place
saying why the workspace is never empty and that raising the mount needs the
option, the skip message switched to the non-empty variant already used at
:163, and the two now-unused returns of `realWorkSpace` discarded.

No assertion about wr's behaviour changed: the inner `Convey` bodies
(workspace_test.go:233-241 and :290-295) are byte-identical to #577's and
#575's, and no `soPathsExist`, `soPathsGone`, `So(err, ...)` or
`So(buff.String(), ...)` line appears on either side of the diff. The two
deleted `So(os.RemoveAll(...), ShouldBeNil)` lines asserted that the test's own
fixture teardown succeeded, not anything about wr. Assertions in these two tests
went from 41 (five scopes skipped) to 52: 10 fixture assertions
removed, 21 behavioural ones gained that had been executing zero times.

### Mutation checks

`go vet ./cmd/ ./jobqueue/` was clean on every mutated tree before its verdict
was read, and each was reverted with `git --no-pager diff -- jobqueue/utils.go`
empty afterwards. Run at orchestration level (O) on the final tree.

- [x] **O7: #577's guard neutralised** - `sweptDir.sweepable` (utils.go:1701)
  cut to `_ = crossesMountBoundary(d.above, info); return info, true, nil`, the
  single choke point all three sweeps pass through. Vet clean. **RED**, all five
  scopes, at `behaviours_test.go:1843` (`soPathsExist`): `stat
  .../002/REMOTE_OBJECT: no such file or directory` and `.../004/REMOTE_OBJECT`
  (the loopback BACKING file, outside the Job's `Cwd` - the user's remote
  object), `.../006/cwd/remote_output.txt` and `.../008/cwd/remote_output.txt`,
  and `.../002/tmp/remote_output.txt` for the TMPDIR scope. Five distinct
  deletions through the live mount.
- [x] **O8: the same mutation on the UN-FIXED tree** - guard defeated,
  `workspace_test.go` reverted to `b07c3dc5`. Vet clean. **GREEN**: `ok
  github.com/VertebrateResequencing/wr/jobqueue 0.029s`, five skips. This is the
  measurement that makes the case: with the tests skipping, destroying the guard
  changes nothing anyone would see.
- [x] **O9: `crossesMountBoundary` itself** (utils.go:1991) made to `return
  false`, a different site with the same intent. Vet clean. **RED**, the same
  five deletions.
- [x] **O10 (discarded, not a verdict):** `removeAllGuarded` made to ignore
  `sweepable`'s `ok`. Vet clean, tests RED - but by nil-pointer panic, because
  the refusal path returns a nil `os.FileInfo`. It proves only that the scopes
  reach the sweep, so it is recorded rather than counted.

The mutations also settle a question the fixture raises: if this branch's own
`proveWSToken` refused, cleanup would return early, delete nothing, and O7 would
have stayed GREEN. It went RED, so the sweep is genuinely reached. The reason is
that `recordedWSToken` looks the record up beneath the workspace handle
(workspace.go:757), the workspace is now a mount root, so the lookup lands in
the loopback backing dir where no record exists - `ENOENT`, absent, fails open
(workspace.go:723-725). The real record survives, shadowed by the mount. That is
exactly what production does when a Job's `Cmd` mounts over its workspace.

### Gates

`go build ./...`, `go vet ./cmd/ ./jobqueue/` and `gofmt -l jobqueue/ cmd/`: all
clean on the committed tree, run at orchestration level. `make race` and
`make lint` not run - gated centrally.

`make test` on the fixed tree, run once: `448 passed · 9 skipped · 29 packages ·
4m0s`, PASSED. The whole log contains ZERO occurrences of "fuse", case
insensitive, so no FUSE skip line survives. The nine skips are the same nine
`origin/develop` reports - three `WR_TEST_MODULES`, the memory test, the speed
test, two OpenStack, the S3 path and LSF - and 448 is develop's 444 plus this
branch's four new test functions.
