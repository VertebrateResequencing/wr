- [x] Docker package fails to build after Moby SDK migration because `Client.Ping` now requires `client.PingOptions`.
  - Migrated Docker API usage to `github.com/moby/moby/api` and `github.com/moby/moby/client`, including the new options/result wrapper calls.
  - Updated the Docker test ping call to pass `client.PingOptions{}` so `TestDocker` builds against the new SDK.
- [x] Docker-related focused gates must force the `docker` group so `container/TestRunReal` exercises the daemon instead of failing with socket permission denied.
  - Confirmed no code change was needed; the gate must run under `sg docker` in this environment so CLI/API Docker tests can reach `/var/run/docker.sock`.
  - Verified the group-forced focused Docker gates and the nested jobqueue Docker-monitoring scenario pass without Docker-related skips.
