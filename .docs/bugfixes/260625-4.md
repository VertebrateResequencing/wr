- [x] `make test` failing in jq_default / TestJobqueueWithMounts at jobqueue/jobqueue_test.go line 8848: `listen tcp 0.0.0.0:46407: bind: address already in use`.
  - Isolated the mounts test config like the rest of the jobqueue tests, so it no longer reuses the configured development manager port or manager directory inside an orchestrated lane.
- [x] `make race` failing in medium_a / TestJobqueueMedium at jobqueue/jobqueue_test.go line 3737: expected JobState delayed, got ready, with a warning `kill requested externally`.
  - Kept the temporary-failure delay behaviour under test, but stopped depending on exact wall-clock sleeps around the delay boundary. The test now verifies the delayed state and then waits until the job is externally visible as ready before reserving it.
- [x] `make race` failing in jqA1 / TestJobqueueModules at jobqueue/jobqueue_test.go line 5783: module command ran but final state update hit `jarchive(...): bad job (not in queue or correct sub-queue)`, plus disconnect object closed.
  - Relaxed the module test's item TTR/touch timings so the server does not reclaim the job while the runner is still finishing and archiving under race/load.
- [x] Local `make race` exposed a limiter test flake: the concurrent SetLimit/Increment/Decrement test expected exactly 125 successes and 75 failures.
  - Preserved the concurrency behaviour being checked while allowing valid schedules where more goroutines observe the old limit before the limit-raising goroutine runs.
- [x] Validated caller-provided `WR_TEST_PORT_BASE` values before running lanes.
  - A manual base now has to be usable for the planned lanes and stay below the OS ephemeral port range, matching the automatic port-base selection rules.
