# Bugfixes 260819-1 — reliable4 batch 1 (items A and B)

Source: `.docs/reliable4/next-steps-260819.md`, sections "ITEM A" and "ITEM B".
That doc is the authoritative brief: it carries the proven mechanism, the
candidate fixes, the binding constraints, the TDD shape and the scale-gate
requirement for each item. Prior batch: `.docs/bugfixes/260818-1.md`.

Quality gates for every bug: `make lint` (0 issues), `make test` and `make race`
with all `OS_*` vars unset (baseline 393 passed / 9 skipped / 29 packages, 0 data
races), plus a `developers/wrdev.sh` scale mode that is **proven to FAIL pre-fix**
from a pristine worktree at the pre-fix commit.

- [x] Bug A — a release that happens before the job reported a start never decrements `UntilBuried`, so any pre-start release retries forever and ignores `--retries`. `releaseJobSnapshot` (`jobqueue/server.go:4956`) only sets `bury` when `!job.StartTime.IsZero()`, and `finalizeReleasedJob` (`jobqueue/server.go:5017`) only decrements `UntilBuried` under the same gate; server-side `StartTime` is set only by `applyJobStart` (`jobqueue/serverCLI.go:908`), which needs a real pid+host, and `resetJobForReservation` re-zeroes it on every reservation. So a job that never starts burns a runner slot per iteration forever, invisible except in runner logs (measured: `untilBuried=3` after 4 releases; a scale run sat at `delayed: 20, buried: 0` for 90s while exec attempts climbed 20 -> 47). `0d22eda` closed this only for the three permanent errnos it buries; the transient pre-exec path still loops unboundedly. The fix must NOT make a healthy-but-slow-starting job burn its retries — prove that with a test.
  - Root cause confirmed as written: `releaseJobSnapshot`/`finalizeReleasedJob`
    both gated the bury decision and the `UntilBuried--` on `!job.StartTime.IsZero()`,
    and server-side `StartTime` is only ever set by `applyJobStart` (real pid+host),
    so a job that never reported a start retried forever.
  - Fix: the **client now says explicitly whether it attempted the command**.
    New ADDITIVE wire field `clientRequest.Attempted` (ugorji Binc, no `toarray`,
    appended last, following the `Host`/`SchedulerID`/`Pid` precedent — old client
    sends nothing, decodes to `false`, keeps today's behaviour; new client against
    an old server is likewise a no-op). New unexported `Client.releaseAfterAttempt`,
    used by `reportStartFailure`'s transient branch and `applyFinalState`'s release
    branch; the public `Client.Release` stays `attempted:false`.
  - Pure candidate 1 from the handoff doc ("decrement on any owner-reported
    release") was **measured wrong**: `cmd/runner.go:252/269/292` release jobs they
    never tried to run ("not enough time to run", env-var failures), which would
    have started burning retries. Candidate 2 (a separate pre-start counter) was
    rejected: new per-job state with a reset lifecycle to get wrong, refilled
    silently by a manager restart, and it gives the operator a second ceiling they
    never asked for (`--retries N` means N, not N+N).
  - Server predicate `releaseSpendsARetry(job, rep) = rep.attempted ||
    !job.StartTime.IsZero()` is evaluated **exactly once**, in `releaseJobSnapshot`,
    and carried to `finalizeReleasedJob` on `releaseReport.spendsRetry`. The
    six-arg `releaseJob`/`finalizeReleasedJob` signatures became a `releaseReport`
    struct. Manager-initiated paths (`confirmOrReleaseLostJob`, `killJob`) use
    `lostJobReleaseReport()` (attempted=false) so they keep the StartTime-only
    rule — which is what DEVELOPERS.md rule 4 actually protects, and is why a
    healthy-but-slow-starting job cannot burn its retries.
  - Review cycle 1 caught: two doc comments made false by the change (`Release`'s
    exported retry contract, and `reportStartFailure`'s now-untrue "those retries
    are unbounded" rationale — swept in `developers/wrdev.sh` too); an unprotected
    guarantee (the `Client.finishRelease` mirror survived reverting with the whole
    `jobqueue` package green, 477s); and a **uint8 underflow** reachable because the
    predicate was evaluated twice against mutable `StartTime` under two separate
    lock acquisitions (`[2 1 0 255 254 253]`) — i.e. a silent restoration of
    near-unbounded retries. Fixed by the single evaluation above plus a clamped
    decrement.
  - Review cycle 2 caught a regression **introduced by that clamp**: `UntilBuried =
    Retries + 1` is uint8, and `--retries 255` is documented and accepted
    (`cmd/add.go`/`cmd/mod.go` "[0-255]", `restModifyUint8Max`), so it seeded a
    **live job at 0**. The clamp then blocked the decrement while
    `finalizeReleasedJob` still set `State = JobStateBuried` (`0 <= 0`), leaving the
    item in `delay` forever — measured `[0 0 0 0 0 0 0 0]` vs base's terminating
    `[255 254 253 ...]`. Fixed at source with `initialUntilBuried` (saturates at
    `MaxUint8`) at all three seed sites (`prepareInputJobs`, `kickJobs`,
    `resetJobStatusFields`; a repo grep confirms there are no others, and
    `JobModifier.applyScheduling` sets `Retries` without re-seeding).
  - `releaseJobSnapshot`'s bury test now **mirrors `finalizeReleasedJob`'s
    arithmetic** (`remaining := UntilBuried; if spendsRetry && remaining > 0
    { remaining-- }; bury := forceBury || remaining == 0`) rather than the
    reviewer-suggested literal `UntilBuried <= 1`, which was **measured
    insufficient** (mutation M2': it fixes only the spendsRetry branch and leaves
    the same item-state/job-state divergence on a manager release of a 0-budget
    job). Verified arithmetically identical to base for all ordinary `UntilBuried
    >= 1` jobs, so no off-by-one in the normal retry count.
  - Files: `jobqueue/server.go`, `jobqueue/serverCLI.go`, `jobqueue/client.go`,
    `jobqueue/job.go`, `jobqueue/serverWebI.go`, NEW
    `jobqueue/reliable4_prestart_retries_test.go`,
    `jobqueue/reliable4_execfail_test.go` (ETXTBSY negative control strengthened,
    not loosened: "not buried on attempt 1" is still asserted, now with an exact
    count and an in-band `Kick`-and-complete transience proof),
    `jobqueue/reliable4_busyexit_test.go` (call-site signature only),
    `developers/wrdev.sh`.
  - RED/GREEN (`TestReliable4PreStartReleaseRetries`, verified by the reviewer from
    a restored pre-fix tree, not taken on faith): transient start failure
    `reserves=6 untilBuriedSeq=[3 3 3 3 3 3] state=delayed` -> `reserves=3
    seq=[2 1 0] state=buried item=bury`. ETXTBSY: `reserves=4, delayed,
    untilBuried=3` -> `reserves=3, buried, untilBuried=0`. Negative controls
    (manager-release, owner-handback, slow-start) unchanged at `[3 3 3 3 3 3]`.
  - Mutation-tested, every guarantee pinned to one named assertion (9 mutations
    across the three cycles), including: reverting the clamp -> `Expected 0,
    Actual 255`; reverting `initialUntilBuried` -> `Expected 255, Actual 0`;
    reverting the `finishRelease` mirror -> client `[3 2 1]` vs server `[2 1 0]`;
    `lostJobReleaseReport` -> `attempted:true` -> the slow-start control fails.
    Dropping the `!job.StartTime.IsZero()` half fails 5 pre-existing tests.
  - Scale gate: NEW `developers/wrdev.sh transient-start-retries` (real binary,
    `-s local`, ENOEXEC via a bogus `newgrp` on PATH — the shell itself can't be
    used, the local scheduler launches runners with it). **Proven to FAIL pre-fix
    from a pristine worktree at `1feb6f5`**: `buried=0/20 execAttempts=151` and
    still climbing (20->40->60->91->151) at the 300s cap, `EXIT=1` propagated;
    post-fix `buried=20/20 execAttempts=60` (exactly `20*(2+1)`). Its
    `FAIL (NOT MEASURED)` branch was fired deliberately to prove it can fail.
  - No wrdev gate for the `--retries 255` edge: 255 attempts against a release
    backoff capping at `ClientReleaseDelayMax` 1800s is ~125h of wall clock per
    job. Stated rather than added, per the doc's "say so explicitly rather than
    adding one that cannot fail"; the defect is deterministic and pinned twice in
    the fast suite.
  - Regression gates re-run green each cycle: `exec-impossible-retries 20 120 2`
    (Bug 5 / `0d22eda`, buried 20/20, 20 attempts), `idle-backlog-cpu 50000 25`
    (640-770 ms per 25s), `backlog-rescan-check 2000 50000` (`scanWork=2000`).
  - Gates: `make lint` 0 issues; `make test` **394 passed / 9 skipped / 29
    packages**; `make race` 394 passed / 8 skipped, **0 data races**. (394 = 393
    baseline + 1 new test. The 8-vs-9 skip under `race` is a netgo/cgo
    `user.LookupGroupId` difference present on the pristine tree too.) Every test
    failure seen across the three cycles was A/B-cleared against a pristine
    worktree or a load-matched re-run, never accepted on attribution alone.
- [x] Bug B — `wr status -i <substr> -z -l 1` still decodes the entire archived history before applying the limit, so the 12.1 GB heap excursion is still reachable from a status command (prod history is ~2.15M complete jobs). `limitJobs` (`jobqueue/server.go:5696`) runs *after* `getDBJobsByRepGroup` (`jobqueue/server.go:5506`) -> `retrieveCompleteJobsByRepGroup` (`jobqueue/db.go:2315`) has called `decodeArchivedJob` per record for every matching repgroup. Push `Limit`/`Offset` down into `retrieveCompleteJobsByRepGroup` and its RTK cursor. Watch the multi-repgroup `-z` loop (a limit spans repgroups) and `filterAndGroupJobs`, which may legitimately discard records after decode. Do NOT change what `wr status` returns.
  - A correct pushdown **was** possible, but not the shape the handoff doc assumed.
    `limitJobs`' limit is **per group** (`groupJobsByCharacteristics` keys on
    `state.exitcode.failreason`; `addJobToGroup` keeps the first `Offset+Limit` of
    each group and folds every further member into the last kept one's `Similar`),
    so the pushdown needs a per-group budget **plus an exact count of what it did
    not decode**. `completeJobsBudget` is created once in `getJobsByRepGroup` and
    spent across the `getRepGroupsList` loop, so the limit spans repgroups: `-z`
    over 20 repgroups with `--limit 1` costs **1** decode, not 20.
  - Ordering by `(StartTime, EndTime)` needed a cheap way to read those fields
    without materialising 130 KB records, so `archivedJobFacets` declares a
    **subset** of `Job`'s field names and the Binc codec (map-keyed by field name,
    no `toarray`) structurally skips the rest. Measured by the reviewer on this
    host: **2,119 ns / 104 B / 2 allocs** vs **56,218 ns / 142,036 B / 9 allocs**
    per record. `oldestArchivedJobs` keeps the oldest `limit` per group in
    O(limit) memory; only those are fully decoded.
  - `filterAndGroupJobs`' three filters are handled by decidability, **not** by
    guessing what archived records contain: `State` is applied in the DB pre-pass
    (`archivedJobGrouper`, same `normalizeJobState`+`matchesStateFilter`);
    `FailReason`/`ExitCode` likewise, since both fields are already facets;
    `WaitingForDepGroups` **refuses** the pushdown, because its input is a
    variable-length `[]string` the facets deliberately do not carry and nothing
    clears it on archive.
  - Review cycle 1 found the `FailReason` refusal's original rationale **inverted**
    ("archived jobs never match" was being used as a reason to refuse, when it is
    why a pushdown there is trivially correct) and, more importantly, that the only
    caller which sets that filter is the **web status page's failed-jobs
    drill-down** (`sendJobDetails`, `serverWebI.go:616`) — so the same 12.1 GB-class
    excursion was still reachable from the web UI on prod's ~2.15M history. Now
    fixed: that drill-down costs **2 decodes instead of 207** on the test history,
    and the rule is stated as "a filter stops the pushdown only when the pre-pass
    cannot DECIDE it from the facets".
  - Review cycle 1 also found the load-bearing guarantee **unprotected**: the codec
    has `ErrorIfNoField` false, so a facet field-name mismatch is silent, and
    renaming `StartTime` returned **different jobs with the whole suite green** —
    it survived only because the seeding gave every record `EndTime = StartTime+1s`,
    so a zeroed `StartTime` fell through to an `EndTime` tie-break that reproduced
    the right order. Fixed two ways: seeded durations now make start-order the exact
    reverse of end-order (the rename now fails 10 behavioural assertions), and NEW
    `TestReliable4StatusLimitFacets` adds a reflect walk (every facet names a `Job`
    field of identical type, plus a field-count assertion so a 7th cannot be added
    unpinned) **and** a round-trip through the real `db.ch` handle — which catches a
    future `toarray`/`StructToArray`/`codec:` tag change that a name check
    structurally cannot see. The reviewer confirmed both guards are independently
    earned: renaming `Lost` fails ONLY the facets test; a `codec:"ExitcodeX"` tag is
    invisible to the name check but caught by the round-trip.
  - Files: `jobqueue/db.go` (**purely additive, 298 added / 0 removed**),
    `jobqueue/server.go`, NEW `jobqueue/reliable4_status_limit_test.go`,
    `developers/wrdev.sh`. `getCompleteJobsByRepGroup` and
    `retrieveCompleteJobsByRepGroup` are unchanged, so the subscription catch-up
    seed and the other `serverWebI.go` callers are untouched. **No existing test
    file was modified** (`git diff --name-only 785a151 -- '*_test.go'` is empty).
  - RED/GREEN, `db.archivedDecodes` (the inert counter from `5c75a15`), same query:
    `--limit 1` over 5,000 archived **5,000 -> 1**; `--offset 3` -> 4;
    `--limit 2 --offset 1` -> 3; `-z --limit 1` over 2 repgroups **8,000 -> 1**;
    `-z --limit 5001` -> 5,001; web drill-down **207 -> 2**; unlimited 5,000 ->
    5,000 and `WaitingForDepGroups` 5,000 -> 5,000 (deliberate guards against
    over-fixing).
  - Results proven identical, not asserted: on a pristine worktree at `785a151`
    with only the new test file added, the suite reports the **same assertion
    counts** with **only the decode-count assertions failing** — every assertion
    about which jobs, in what order, with what `Similar`, passes on both trees
    (each block asserts the answer *before* the cost, because GoConvey halts a
    block at its first failure). The reviewer then built its **own** independent
    sweep against the pre-fix `getJobsByRepGroup` transcribed from `785a151`:
    **2,192 shapes, 0 mismatches**, with a negative control (grouper key ignoring
    the exit code) producing 185 mismatches to prove the sweep can fail. It also
    drove `sendJobDetails` end-to-end over an `httptest` websocket in the three
    shapes `repgroup-handler.js` actually sends.
  - Mutation-tested; each guarantee pinned to a named assertion: `Similar` folding,
    cross-repgroup budget spend, oldest-first ordering, `Offset` handling, the
    `WaitingForDepGroups` refusal, the state filter, the per-group selector, and
    RepGroup stamping. Three mutations initially SURVIVED (state filter, single
    global selector, RepGroup stamping), which is why
    `TestReliable4StatusLimitMixedHistory` exists — it seeds a non-zero exit code,
    a buried state and a stored RepGroup from a later re-run, shapes today's
    `markJobComplete` cannot produce and so must not be assumed away.
  - Scale gate: `developers/wrdev.sh control-rpc-history` extended with a timed
    `wr status -i <substr> -z --limit 1 -o details` having its **own** VmHWM
    baseline, taken after a `-o counts -z` warm-up and before the pre-existing
    reference scan (the warm-up is load-bearing: bbolt mmap makes pages resident
    regardless of decoding, and from a cold baseline the two trees are only 757 vs
    1391 MB apart). **Proven to FAIL pre-fix from a pristine worktree at `785a151`**
    — 664-677 MB peak-RSS growth, ~2,100 ms, `EXIT=1` — and PASS post-fix at
    **11-27 MB, ~950 ms**, `similar=199999` identical both sides. Deliberately does
    NOT repeat `5c75a15`'s mistake of sampling the baseline after the reference
    scan; its `FAIL (NOT MEASURED)` branches were fired for real.
  - **Honest limit:** only *decoding* is bounded. The request is still O(history) in
    cursor + record I/O, because `Similar` needs an exact count and the ordering
    needs each record's times; a start-time index would be the startup history scan
    DEVELOPERS.md rule 6 forbids. ~1.0 s per 200,000 records warm, so prod's ~2.15M
    is roughly 10 s of CPU instead of 12 minutes and 12.1 GB.
  - **The handoff doc's own trigger was wrong:** in `wr status`, `-l` is
    `--cmdline`, not `--limit`; `--limit` has no shorthand and **defaults to 1**.
    So the 12.1 GB excursion needed no flag at all — plain
    `wr status -i <substr> -z` was enough. Confirmed at `cmd/status.go:702/716`.
  - Residual, deliberately out of scope and recorded rather than hidden:
    `WaitingForDepGroups` + `IncludeComplete` still decodes unbounded (reachable via
    `/rest/v1/jobs/<rg>?waiting_deps=true&limit=N` and any `handleGetByRepGroup`
    caller setting the flag; the CLI's `GetIncompleteWaitingForDepGroups` is NOT
    affected, it never reads the DB); `wr status -o plain` and any `--limit 0` keep
    the unbounded decode because `cmd/status.go:194` zeroes the limit for ungrouped
    output formats.
  - Gates: `make lint` 0 issues; `make test` **397 passed / 9 skipped / 29
    packages**; `make race` 397 passed / 8 skipped, **0 data races** (397 = 394 +
    2 new tests + the facets test). `control-rpc-history`, `idle-backlog-cpu`
    (680-880 ms per 25 s) and `backlog-rescan-check` (`scanWork=2000`) all green.
    The one `make race` failure seen was A/B-cleared: full `make race` PASSED on a
    pristine `785a151` and on a re-run of the fixed tree at matched load.
  - Process note: the cycle-2 reviewer accidentally ran `git checkout --` on
    `jobqueue/db.go` and `jobqueue/server.go` mid-review, destroying the
    uncommitted work, then reconstructed both from the diff it had captured and
    verified them md5-identical to the pre-damage hashes. Independently re-verified
    before commit: `git diff --numstat 785a151` reproduces `298/0`, `225/30`,
    `97/12` exactly, no scratch files remain, no `*_test.go` tracked file changed,
    the batch's tests pass, `make lint` is 0 issues and `go vet` is clean.
