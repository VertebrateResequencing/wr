- [ ] `make test` flakes (first run, 8-core server) on `TestJobqueueRunnerScheduling` (runner_scheduling_test.go:310, lane `runner_scheduling_a`): "add some jobs with fractional CPU requirements ... the jobs get automatically run" — `So(ranClean, ShouldEqual, count+1)` got `count` (16) vs `count+1` (17). ROOT CAUSE (structural over-assertion, not a transient): `count = maxCPU*2` (16 on 8 cores), 16 jobs each `Cores: 0.5` (so all 16 fit in 8 cores). `runnertmpdir` holds the `runner` exe (copyCompiledSelf) plus one `"ok"` marker per RUNNER that finishes its reserve loop cleanly — the test runner (jobqueue_test.go:9109-9115) creates exactly one `os.CreateTemp(runnermodetmpdir, "ok")` if `clean`. But a runner's loop (9073-9104) reserves+runs MULTIPLE jobs sequentially, so the number of runners (hence `"ok"` files) is `1..count`, NOT exactly `count`. On the 8-core box first run, one runner served two of the short `sleep 1` jobs (it finished one and reserved another before a fresh runner spawned) -> 15 ok + exe = 16. `== count+1` wrongly assumes one runner per job; the same test only requires `simultaneous > maxCPU` (line 279), already acknowledging imperfect simultaneity, and `ran == count` (line 298) already proves every job ran. FIX (preserve intent, remove flake): relax line 310 to tolerate runner reuse — assert the runner exe is present and at least one clean `"ok"` marker (and at most `count`), not exactly `count+1`. Audit the runner-tmpdir `"ok"`-file count family (runner_lifecycle_test.go:600/659, the other runner_scheduling Conveys) for the same brittle assumption and fix any whose job profile allows runner reuse; leave any where exactly `count` runners is genuinely guaranteed (verify).
  - Fix: added a shared name-based helper `assertCleanRunnerMarkers(runnerTmpDir, maxRunners)` (runner_lifecycle_test.go) — asserts the `runner` exe is present, zero `"fail"`-prefixed markers (no unclean exits), and clean `"ok"`-prefixed markers in `[1, maxRunners]`. This tolerates runner reuse (fewer runners than jobs) while still proving runners ran and exited cleanly; `ran == count` already proves every job ran, so a real job failure (which writes no marker and no output) is still caught by that earlier check.
  - `runner_scheduling_test.go:307` (the reported flake): replaced `ranClean == count+1` with `assertCleanRunnerMarkers(runnertmpdir, count)` — these 0.5-core jobs can have up to `count` simultaneous runners, so `count` is the right ceiling; `simultaneous > maxCPU` still proves fractional-CPU parallelism.
  - `runner_lifecycle_test.go:603` (`TestJobqueueRunnerAutomaticExecution`, same brittle class): replaced `ranClean == maxCPU+1` with `assertCleanRunnerMarkers(fixture.runnerTmpDir, fixture.maxCPU)`. Used `maxCPU` (not `count=maxCPU*2`) as the ceiling deliberately: 1-core jobs are scheduler-capped at `maxCPU` concurrent runners, so capping the clean-marker count at `maxCPU` preserves this Convey's "without over-spawning" intent (a wasteful over-spawn > maxCPU still fails) while tolerating reuse.
  - Audited the runner-tmpdir marker family: left `runner_lifecycle_test.go:659` (`TestJobqueueRunnerFailureRetry`, already a robust name-based `"fail"`-count range) and the other Conveys (only `.started`/`.done` existence markers, or no marker-count assertion) unchanged.
  - Verified by implementor + reviewer (reviewer flagged the over-spawn bound, addressed by the `maxCPU` ceiling above): `go build`/`go vet`/`make lint` clean; `TestJobqueueRunnerScheduling` passes 3/3; `TestJobqueueRunnerAutomaticExecution`/`TestJobqueueRunnerFailureRetry`/`LostJobs`/`KillRequests` pass. Test-side only; intent preserved.
