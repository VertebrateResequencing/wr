- [x] I want to run the Openstack tests. I have docker installed. The test suite should not fail. Why is it failing? If the test is running on a spawned openstack node and assuming the spawned node has docker installed because this node has docker installed, that's a bad test that needs fixing. It should check if the spawned node has docker.
  - Added a runner-side Docker probe in `TestJobqueueWithOpenStack` before the negative "when docker is not installed" scenario, so the test only asserts no-Docker behavior when the spawned OpenStack runner actually lacks Docker.
  - Kept the Docker-positive OpenStack scenario intact, and added focused helper coverage for interpreting the probe result.
  - Verified with lint, focused helper tests, and the live focused `TestJobqueueWithOpenStack` gate.
