- [x] After lots of changes (esp. openstack-related dependency updates) since master, the openstack tests are failing: `could not upload exe [/usr/bin/echo]: permission denied (try putting the exe in /tmp?)`.
  - Fixed the OpenStack executable check in `jobqueue/scheduler/openstack.go`: non-path command tokens are now resolved on the remote host with `command -v` before falling back to the local absolute path, remote `test -x`, and upload.
  - Added regression coverage in `jobqueue/scheduler/openstack_test.go` for remote PATH resolution avoiding upload, absolute path checks, and missing remote executables preserving upload behaviour.
  - Cleaned stale `wr-testing-sb10*` OpenStack test resources while preserving the current node's `sb10-agent-key`/`sb10-agent-ssh`; focused live deathrow OpenStack verification passed after temporary `FocusConvey` narrowing was reverted.
