- [x] With no db or a brand new one, this version constantly hits the 10s timeout. The manager does eventuall start. Use the bugfix skill to fix the speed or client connectivity regression.
  - Local clean-start check under `.tmp/agent` used a truly absent manager dir/token: `WR_MANAGERDIR=/home/ubuntu/wr/.tmp/agent/no-managedir-abs.LFxZtL/manager WR_MANAGERPORT=32217 WR_MANAGERWEB=32218 /home/ubuntu/wr/.tmp/agent/wr-cycle2 manager start --deployment development --scheduler local --timeout 2`. On this filesystem it did not reproduce, returning 0 in 0.272s and creating a new DB, token, certs, log and address file; `manager status` reported started.
  - Found a deterministic parent readiness bug that explains the user-observed no-token timeout on filesystems with coarse mtimes: the child can create the manager dir and token after `preStart`, but the token mtime may still not compare `After(preStart)`. The parent then refuses to attempt any socket ping and times out even though the manager later serves normally.
  - Added a manager startup regression that starts with an absent manager dir/token, creates a new token whose mtime is truncated to coarse filesystem resolution, and proves startup connects before the ordinary timeout without reporting DB-upgrade progress. The regression fails against the old mtime-only gate.
  - Fixed startup waiting to treat only a non-empty token as enough to attempt connection; absent and empty token files keep the connection gate closed, the connect-time ping remains the actual readiness check, and legitimate active DB-upgrade status remains the only path that extends the initial startup timeout.
