# Bugfix checklist 260727-4 — bound the confirm-dead SSH storm (Layer 3)

- [x] markJobLost spawns `go confirmOrReleaseLostJob` per lost job with no concurrency bound, so under mass false-lost (a freeze firing TTR on thousands of jobs at once) thousands of concurrent confirmJobDead -> scheduler.ProcessNotRunningOnHost SSH sessions fire (the ~852-goroutine SSH storm), a resource storm on FDs/network/CPU and the single mercury SSH auth path.

  Fixed per design .docs/reliable4/layer3-confirmdead-bound.md (Layer 3).
  - jobqueue/server.go: per-Server counting semaphore `confirmDeadLimiter chan struct{}` sized N (default 16 via new `ServerConfirmDeadConcurrency` + `ServerTimings.ConfirmDeadConcurrency`; `withDefaults` maps <=0 -> 16 so `Serve` never builds a self-deadlocking 0-cap channel). Acquired/released (deferred) INSIDE `confirmJobDead`, after the `jobPID==0` early return, around both `ProcessNotRunningOnHost` calls — so it bounds BOTH the initial path (markJobLost -> go confirmOrReleaseLostJob -> confirmJobDeadAndKill -> confirmJobDead) AND the retry path (go confirmJobDeadAndKillAfterRetryTime -> confirmJobDeadAndKill -> confirmJobDead). Per-Server field, not a global (gochecknoglobals-safe).
  - jobqueue/scheduler/mock.go: test seam (RunCmdFunc + mockHost + getHost wiring); default (nil,false) behaviour preserved when RunCmdFunc is nil.
  - jobqueue/reliable4_confirmdead_bound_test.go (new, untagged): TestReliable4ConfirmDeadConcurrencyBound drives the real confirmOrReleaseLostJob path for M=100 lost jobs with bound N=8; mock records peak in-flight ProcessNotRunningOnHost. RED without the semaphore (peak=100, unbounded); GREEN with it (peak<=8), deterministic under -race.
  - Preserved (only CONCURRENCY bounded, not the verdict): confirmJobDead's both-pid liveness (a live/actively-reporting runner is never condemned) and the kill/release-after-confirm behaviour; a delayed confirmation still eventually kills a genuinely-dead job. NO prior-behaviour change (no boundary flips).
  - Safe: the acquire only ever blocks in `go`-spawned goroutines (never an RPC handler / queue mutex / db lock / ticker), no lock held at acquire, deferred release on every path, holders free within checkTimeout (~15s) — no deadlock (impl + independent reviewer both verified). Minor accepted note: the acquire is not shutdown-aware, so Server.Stop may wait up to ~checkTimeout for a backlog to drain slots (bounded, not a hang).
  - Gates: make test 364 passed / 9 skipped; make lint 0 issues; go vet clean; go build -tags reliability_repro clean. -race batch (TestLostDetection*, LostRunnerBackstop, RunnerPidLiveness, Reliable2Lost*, Reliable3Recovery*, Layer-1 BackupCoordination, Layer-2 BusyExitStates, the new bound test) all green. TestSubscriptionReconnectResync 5/5 (pre-existing rare env flake on the shared farm node, not fix-induced). /bugfix implementor + independent reviewer both PASS.
