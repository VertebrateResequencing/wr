- [x] 1: get rid of whatever it is doing during startup prior to the token getting created; it should be ~nothing, ie. restore consistent quick manager starts.
  - Context: Post database upgrade, starting a manager can still take 6mins on a large database, but the `wr manager start` client is timing out and not waiting for whatever is slowly happening. This is new since `v0.36.5` and most likely related to the database-upgrade code.
  - Root cause: the post-`v0.36.5` absolute-status startup path enumerated every historical RepGroup, opened a Bolt transaction per group, and decoded every archived job before persisting the readiness token. Completed-only database history therefore dominated manager startup.
  - Fix: remove the global completed-history scan and lazily seed only RepGroups becoming live, serialized before queue callbacks in one count-only Bolt view with no job decoding or global RepGroup enumeration. Raw archived-key counts integrate with the archived-rerun transition accounting, including recovery of live reruns after restart. Missing status buckets now return named, wrapped database errors instead of panicking.
  - Verification: the unchanged path scaled 10.6-12x from 25,000 to 250,000 completed histories, while the fix stayed flat at roughly 15.47-16.19ms and verified returned and persisted readiness tokens. Direct, partial, dependency, multi-RepGroup, rearchive, live-restart, WebSocket, upgrade-readiness, missing-bucket, repeated, and race coverage passed. Independent review passed; `make lint` reported 0 issues and `make test` passed 310 tests with 9 environment-dependent skips.
- [x] 2: if we are ever stuck waiting on something to complete before the manager starts, provide feedback on the CLI and don't time out.
  - Context: Post database upgrade, starting a manager can still take 6mins on a large database, but the `wr manager start` client is timing out and not waiting for whatever is slowly happening. This is new since `v0.36.5` and most likely related to the database-upgrade code.
  - Root cause: the daemon-start parent retained a fixed token/connect deadline and extended it only while a recognized database-upgrade sidecar was active. Any legitimate slow startup before the token or after that sidecar timed out even while the exact spawned manager process remained alive.
  - Fix: monitor and reap the exact go-daemon child process, keep waiting while that child is alive, and fail promptly with PID and exit-status context if it exits. Detailed matching-PID database phases remain visible; otherwise generic elapsed startup feedback begins at `--timeout` and repeats every 30 seconds. Readiness still requires a successful manager connection, so stale tokens or mismatched sidecars cannot extend or complete startup.
  - Verification: real subprocess tests cover waiting beyond the old deadline through readiness, repeated feedback, quick silent startup, stale tokens, matching and mismatched status, and prompt exit-status failure. Legacy upgrade, cloud, logging, configuration, repeated, and race coverage passed. Independent review passed; `make lint` reported 0 issues and `make test` passed 311 tests with 9 environment-dependent skips.
- [x] Rerunning an archived job leaves a stale completed contribution in absolute status counts while also counting the resurrected job as ready.
  - Discovered during clean review of item 1: after archive, re-adding the same job with `ignoreComplete=false` produces status `{complete:1, ready:1}` instead of ground truth `{ready:1}`; concurrent first-seed and dependency-resurrection paths may have the same race.
  - Root cause: making an archived key live suppresses its completed database row, but the queue callback recorded only `new -> live`; keys submitted under multiple RepGroups also retain a persisted reverse lookup in every historical group, so a single payload RepGroup cannot describe all completed contributions.
  - Fix: derive the full deduplicated historical RepGroup set from the persisted reverse index, consume it exactly once on the actual `new -> live` callback, apply same- and cross-group status transitions atomically, and refresh/restore every historical completion contribution when the job is archived again.
  - Verification: direct and dependency resurrection, duplicate and partial adds, concurrent first seeding, distinct jobs, A-to-B-to-C rearchive/restart history, fresh/reconnected WebSocket seeds, and database-ground-truth comparisons pass repeatedly and under the race detector. Independent review passed; `make lint` reported 0 issues and `make test` passed 308 tests with 9 environment-dependent skips.
