- [ ] `make test` on another machine (LSF cluster head node where I don't have root) is failing. Explain the errors and fix obvious issues with bugfix skill principles (but just fixing tests doesn't mean writing new tests per TDD). Also, if all those warnings are expected for that test, can they be silenced from the terminal?
  - Deferred module execution changes pending cluster-side diagnosis: the tests are meaningful because they verify `Job.Modules` and `JobModifier.SetModules` load environment modules before the job command runs.
  - Made the different-group test choose a supplementary group that resolves by name, skipping only that environmental scenario if none exists.
  - Kept disjoint per-lane test port ranges while skipping ports already bound by other processes.
  - Made `make test`/`make race` print only failing lane logs when failures can be identified, with an all-log fallback.
  - No new tests were added because these changes preserve existing supported behavior and only make environment assumptions/log reporting more robust.
