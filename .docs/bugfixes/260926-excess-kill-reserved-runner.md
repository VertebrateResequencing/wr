- [x] wr's LSF excess-runner kill can bkill an element that has just reserved a job, which breaks DEVELOPERS.md rule 5. killExcessCmds copies the reserved set (snapshotReserved(), jobqueue/scheduler/lsf.go:~2267) BEFORE running bjobs (~2271, parseBjobs at ~2306, which takes a second or more at scale), then bkills the collected PEND ids (~2273-2275, killElements at ~973) without re-checking. The manager records a reservation (s.scheduler.Reserved, serverCLI.go:~1025-1027) only after handing the job to the runner. So an element seen as PEND can dispatch, reserve a job and then be bkilled.
  - Symptoms: if the runner is inside Execute, the job is released as "runner received a signal to stop" and delayed (report-storm-lsf peaked at 339 delayed on 2026-09-26, and 159 on develop). If it's between reserve and Execute, there's no handler yet and the default SIGINT kills the runner, so the job goes lost (limit-drain had 25, 1, 14 and 22 lost across two sweeps). The LSF elements died with EXIT 130 / TERM_OWNER about 2s after starting. Pre-existing: seen on develop. Logs: /nfs/hgi/wr/sb10-bigdb/sweep-260925/ and sweep-260926/.
  - Red command: `go test ./jobqueue/scheduler/ -run TestExcessKillSparesElementReservedMidCycle -count=1`. The test, jobqueue/scheduler/excess_kill_reserved_race_test.go (untracked when the red was taken; now committed as the regression test), uses fake bjobs/bkill exes; the fake bjobs waits until the test has reserved "4242[1]" (via s.reserved at red time; claimForReserve since the fix). It fails 10/10 with `Expected the container ([]string) NOT to contain: '4242[1]' (but it did)!`
  - Recommended fix (claim under lock): after parseBjobs, under reservedMu, drop collected ids that are now reserved and mark the rest doomed before killElements. A new scheduler method, for example ClaimForReserve(schedulerID) bool, is a no-op true for other schedulers; under the same lock it refuses a doomed element and otherwise marks it reserved. The server's reserve handler calls it before reserveItem and gives the runner no job if refused.
  - Fixed with the claim-under-lock approach. In `jobqueue/scheduler/lsf.go`, `killExcessCmds` still collects from a reserved snapshot, then calls the new `doomUnreserved` once `parseBjobs` returns. Under `reservedMu` it spares any collected element that claimed a reservation while bjobs ran (adding it back to the returned count), records the rest in a new `doomedElements` set against the scan's job prefix, and only then calls `killElements`. A complete scan of a prefix forgets that prefix's doomed elements it no longer saw, and `pruneReserved` drops gone doomed elements with the reserved ones.
  - `Scheduler.Reserved` and `scheduleri.reserved` are replaced by `Scheduler.ClaimForReserve` / `claimForReserve(schedulerID) bool`. LSF refuses a doomed element and otherwise marks it reserved, under the same lock. Local (and so openstack) returns true. The mock returns true unless a test sets the new `ConfigMock.ClaimForReserveFunc`.
  - `handleReserve` (`jobqueue/serverCLI.go`) claims after `skipReserve` and before `reserveItem`, and returns no job if refused, so the runner exits as if nothing were ready. Nothing is reserved or counted before the claim, so a refusal leaves no limit group slot or count taken. The post-hand-off `Reserved` call is gone, since the claim already marked the element. An element that claimed but got no job stays marked: its runner is alive and about to exit, killing it gains nothing, and every later reserve claims again under the lock, so leaving it marked can't hand a doomed element a job. Old runners that send no SchedulerID skip the claim, as before.
  - A doomed element stays doomed until bjobs stops reporting it, even if a later cycle no longer wants it killed: a bkill already issued may still land, so letting it take a job then would reopen the race. At worst such an element's runner is refused and exits, and wr submits a replacement.
  - Red before: `Expected the container ([]string) NOT to contain: '4242[1]' (but it did)!` at `excess_kill_reserved_race_test.go:105`. After: `TestExcessKillSparesElementReservedMidCycle` passes and also asserts the spared element still counts (`count == 1`).
  - New tests: `TestExcessKillRefusesClaimFromDoomedElement` (same file) has `killExcessCmds` doom an excess element and asserts its claim is refused while the wanted element's is allowed, and that the doomed entry is forgotten once bjobs stops listing it. `TestReserveRefusedForDoomedElement` (`jobqueue/excess_kill_claim_test.go`) uses the mock's claim hook: the refused runner gets no job, the next runner gets it despite a `:1` limit group, and a runner sending no SchedulerID isn't asked. With the server's claim disabled it fails at line 107 (`Expected: nil`). `TestLSFReservedElements` now also covers `claimForReserve`, `doomUnreserved`'s prefix and completeness rules, `pruneReserved` of doomed ids, and `ClaimForReserve` on a non-LSF scheduler.
  - Gates: `make lint` 0 issues, `make test` pass (706 passed), `CGO_ENABLED=1 make race` pass (706 passed), all on the first run.
  - Follow-up after review (two non-blocking notes, for LSF scale):
    - A doomed element whose bkill failed or was deferred stayed PEND and still counted toward later cycles' `maxAllowed`, though it could never take a job. `killCollector.consider` now takes a snapshot of the scan prefix's doomed ids and never counts one; a non-RUN doomed element is collected for killing again, and `dueForKill`'s back-off still decides whether bkill is re-issued. New case in `TestExcessKillRefusesClaimFromDoomedElement`: with `4242[2]` doomed and still PEND, a next cycle allowing 2 keeps `4242[1]` and `4242[3]` (count 2) and doesn't bkill `4242[3]`. Without the `consider` change it fails at line 147: `Expected the container ([]string) NOT to contain: '4242[3]' (but it did)!`
    - `doomedElements` is now a `doomedSet` (`jobqueue/scheduler/lsf_doomed.go`) keyed by prefix, with an id-to-prefix index. Forgetting after a scan walks only that prefix's entries, and `claimForReserve` stays an O(1) lookup. `TestLSFReservedElements` now also covers moving an id doomed again under another prefix.
    - Rebased onto origin/develop 46e867a first.
    - Gates: `make lint` 0 issues; targeted tests pass with and without `-race`; `make test` pass (714 passed) and `CGO_ENABLED=1 make race` pass (714 passed), both on the first run.
