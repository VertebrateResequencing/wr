- [x] Properly implement the bsub-stderr surfacing in jobqueue/scheduler/lsf.go submitToQueue (currently a "QUICK HACK (uncommitted, reliable4 prod trial)" from commit 52e77c4). It IS beneficial with no notable downside — on a failed bsub it surfaces bsub's stderr (the real LSF rejection reason: pending-job threshold, restricted queue, etc.) instead of the bare "exit status 255", which is what was needed to diagnose post-restart runner-scheduling failures. Implement it properly: replace the "QUICK HACK" comment with a clear permanent doc comment, wrap the 132-char return line to <=120 (the repo lll limit; this is the only whole-repo `make lint` failure), and add a dedicated test (there is none yet). Reach clean all-green (`make lint` AND `make test` both pass).
  - jobqueue/scheduler/lsf.go: replaced the QUICK-HACK comment with a permanent doc comment; extracted helper `bsubStderr(err error) string` using `errors.As` (removes the `//nolint:errorlint`); wrapped the failure `return Error{...}` to <=120 cols. bsub-failure error message text/verbs unchanged (`failed to run %s %s: %s (bsub stderr: %q)`); success path unchanged. Added `"errors"` import.
  - jobqueue/scheduler/scheduler_lsf_test.go: added `TestLSFSubmitToQueueStderr` — fakes bsub via a temp script (writes a known message to stderr, exits 255) and asserts submitToQueue's error carries that stderr with the `(bsub stderr: ...)` framing. Fails-before (bare "exit status 255"), passes-after; needs no real LSF.
  - jobqueue/reliable4_backup_stall_test.go: comment-only tidy — the removed WR_EXP_* knobs no longer exist; the paced backup copy is now the default.
  - Gates: `make lint` 0 issues (fully green; the last pre-existing lll is cleared); `make test` 356 passed.
